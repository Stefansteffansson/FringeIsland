# Features — Platform Domain Services

Feature specifications for the seven Platform Domain services (DS-1 World Model, DS-2 Narrative, DS-3 Journeys, DS-4 Content, DS-5 Communication, DS-6 Discovery, DS-7 Intelligence). Each feature uses the Shape Up pitch format with BDD stories embedded as Given/When/Then acceptance criteria.

**Feature ID prefix:** `PD` (e.g., `FEAT-PD001-world-model.md`)
**Template:** `../../../templates/feature-spec.md`

Most domain services are not yet built. See `../../../planning/waves/FERD-CAPABILITY-MAP.md` for current implementation status across the ecosystem.

## Feature index

| ID | Title | Service | Wave | Maturity | Equipment |
|----|-------|---------|------|----------|-----------|
| [FEAT-PD001](./FEAT-PD001-personal-journal-primitive.md) | Personal Journal primitive — private own-subject entry store with write/read/erasure/export contracts (IDN-5's platform half) | DS-7 Intelligence | Ferd | 6-done | none |
| [FEAT-PD002](./FEAT-PD002-journey-catalogue-and-enrolment-contracts.md) | Journey catalogue & enrolment contracts — catalog/detail/my-enrolments reads with the viewer block, self- + group-enrolment with withdraw, the group enrolment-summary read (the G-A seam), enrolment-write narrowing (JRN-1/2/3/4's platform half) | DS-3 Journeys | Ferd | 6-done | none |
| [FEAT-PD003](./FEAT-PD003-journey-step-substrate-and-progress-contracts.md) | Journey step substrate & per-traveller progress contracts — ADR-U044 realized: steps become rows, step-kind + content-family registries, step-instances as the progress grain, the legacy JSONB steps migrated, catalog/detail re-pointed, player-boot/engagement/completion contracts, the Q1 withdraw revisit (JRN-6..10 + 18's platform half) | DS-3 Journeys | Ferd | 6-done | none |
| [FEAT-PD004](./FEAT-PD004-journey-completion-timing-review-contracts.md) | Journey completion, timing & review-read contracts — server-side completion detection on the transition edge (idempotent, `completed_at` once; solo flip, via-group traveller-grain), the V3 durable `journey_completed` row, derived timing semantics, additive `completion`/`timing` payload blocks, the enter/complete guard loosening (JRN-11/12/13's platform half) | DS-3 Journeys | Ferd | 6-done | none |
| [FEAT-PD005](./FEAT-PD005-group-progress-sharing-frozen-contracts.md) | Group journey progress, sharing consent & frozen-walk contracts — the consent-gated Steward/Guide progress read (roster + marks + honest-basis aggregate; never comparative), the append-only progress-sharing consent write, additive `freeze`/`progress_sharing` player blocks, and the bridge-`_14` freeze-cascade re-verification (JRN-14/16/17's platform half) | DS-3 Journeys | Ferd | 6-done | none |
| [FEAT-PD006](./FEAT-PD006-onboarding-designation-and-arrival-contracts.md) | Onboarding designation & first-arrival contracts — the `is_onboarding_designated` flag + single-designation index, `enroll_self_in_journey` replaced in place to admit a Mist iff the journey is the designated onboarding journey, the `get_onboarding_status()` first-arrival read, the seeded placeholder onboarding journey (native ADR-U044 steps + the ADR-U046 takeaway seed), and the JRN-5 carry-over + ADR-U031 ephemerality proofs (Mist enrolment & transcendence continuity — JRN-5/15's platform half) | DS-3 Journeys | Ferd | 6-done | none |
| [FEAT-PD007](./FEAT-PD007-step-response-capture-contracts.md) | Step-response capture & review-substance contracts — the `response` payload on the lived record + the `captures_response` registry column, the `save_step_response` write verb (optional-always, orthogonal to completion; frozen/withdrawn refused), additive player-read keys (responses + `journey.takeaway` — the J-E seed finally served), the private-only wall pinned (progress consent never covers content), and the `get_own_step_instances_export()` walks-export read discharging the FEAT-H010 flag (ADR-U046's platform half — deepens JRN-9's lived record and JRN-13's review; no new JRN row) | DS-3 Journeys | Ferd | 6-done | none |
| [FEAT-PD008](./FEAT-PD008-conversation-and-message-contracts.md) | Conversation & message contracts — the conversation-model redesign (participants junction + `conversation_kinds` registry, `dm`/`group` seeded per CB-7; `direct_messages` → `messages`), the eight conversation/message contracts (inbox, detail with departed-sender display, send, DM get-or-create, group create/list/join/leave, mark-read), write-narrowing to the contracts, the conformance-gate DS-5 extension, and the ADR-U039 publication disposition (COM-1/2/3/4/15's platform half — the first DS-5 feature spec) | DS-5 Communication | Ferd | 6-done | none |
| [FEAT-PD009](./FEAT-PD009-forum-and-attribution-contracts.md) | Forum & attribution contracts — the group-forum contracts as the only door (paged forum read, role-gated post/reply with the flat-threading trigger speaking through, idempotent soft-delete moderation), the COM-14 attribution ladder ("Former member" per CB-9 / 'Unknown' fallback; ADR-U021 display law, applied to forum + the amended conversation detail), the ADR-U047 relocation of `admin_hard_delete_user`'s forum crossing into `ds5_lifecycle_user_hard_deleted`, write-narrowing, and `forum_posts` + `enforce_flat_threading` joining the conformance gate in lockstep (COM-5/6a/6b/7/14's platform half) | DS-5 Communication | Ferd | 6-done | none |
| [FEAT-PD010](./FEAT-PD010-realtime-hint-emission.md) | Realtime hint emission — the ADR-U039 live-delivery layer: server-originated content-free hints on private channels (`account:<auth_uid>:conversations` for messages + the badge, `group:<group_id>:forum` for forum activity) emitted by triggers on every write path, receipt gated by RLS on `realtime.messages` (§8 Q7 — policy, never a filter), no client-send door, non-fatal WARNING-logged emission; conventions shaped for the A-NTF bell as third tenant (COM-10/11's platform half) | DS-5 Communication | Ferd | 6-done | none |
| [FEAT-PD011](./FEAT-PD011-announcements-window-and-reports-contracts.md) | Announcements, windowed own-edits & content-report contracts — the ADR-U049 realization: the durable `announcements` home (one table, scope CHECK-bound — ADR-U028 by construction) with two scope-separated send contracts + same-gate retract, send-time fan-out of per-recipient V3 delivery rows (ADR-U048 obligation-fulfilment; the A-NTF bell/outward seam) under read-time visibility (late joiners see standing announcements), the CB-3 windowed `edit_own_forum_post`/`delete_own_forum_post` return (15 min; DMs stay immutable; self-delete tombstones ride the existing C-C hint), and the `content_reports` durable store (snapshot-at-report, idempotent resubmit, the ADM-10 queue seam), both tables + contracts joining the conformance gate in lockstep (COM-8/9/12/13's platform half) | DS-5 Communication | Ferd | 6-done | none |
| [FEAT-PD012](./FEAT-PD012-lifecycle-dispositions-and-export-contracts.md) | Lifecycle dispositions & own-communication export contracts — D2 executed as preserve-and-seal (`ds5_lifecycle_group_closed` called in-transaction from `close_group`/`delete_group` per ADR-U047; group-kind conversations sealed via `sealed_at`, forum record untouched), the `communication` section of the GDPR export (`get_own_messages_export()`: own messages, participations, forum posts incl. soft-deleted, reports-submitted incl. snapshot) with the CB-6 right-of-access posture (suspended members export; the walks-export 42501 asymmetry repaired at source, PC008 §155 closed), and the CB-1 Mist-exclusion proof (verify-and-record; DS-5 §8 Q2 resolved-for-Ferd) — the C-E dues (the D2 + export halves of the lifecycle capability rows) | DS-5 Communication | Ferd | 5-in-cycle | none |

**FEAT-PD001** is the first Domain-tier feature spec, opening **Cycle D** (private Journal) of the [Phase-3 Identity-completion plan](../../../planning/hub-v2/phase-3-identity-completion-plan.md). The Journal primitive was routed to DS-7 Intelligence at the Cycle D decomposition (2026-07-03; PC-2's Step 3 Q1 carry — see `../intelligence.md` §L3 and its Sources-status amendment). It pairs with the Hub surface [FEAT-H011](../../../products/hub/features/FEAT-H011-private-journal.md), API-first (ADR-U009/U038). **FEAT-PD002** (6-done, built Cycle J-A 2026-07-07) is the **first DS-3 Journeys feature spec**, opening **Cycle J-A** of the [Journeys completion plan](../../../planning/hub-v2/phase-3-journeys-completion-plan.md); it pairs with the Hub surface [FEAT-H019](../../../products/hub/features/FEAT-H019-journey-catalogue-and-enrolment.md) and carries the Groups-area seam fill (`get_group_enrollment_summary` — a DS-3 read composed at the Hub BFF, per the one-way rule) plus the [FEAT-PC016](../../core/features/FEAT-PC016-pending-nominations-read-contract.md) rider on its schema-gate migration. **FEAT-PD003** (6-done, built Cycle J-B 2026-07-07) realizes [ADR-U044](../../../architecture/decisions/ADR-U044-journey-step-model.md) — the step substrate (rows + registries + step-instances), the mechanical migration of the legacy JSONB steps, the same-migration re-point of the catalog/detail contracts, the player contracts (`get_player_state`, `enter_journey_step`, `complete_journey_step`), and the carried FEAT-PD002 Q1 withdraw revisit; it pairs with the Hub player [FEAT-H020](../../../products/hub/features/FEAT-H020-journey-player.md). **FEAT-PD004** (6-done, built Cycle J-C 2026-07-08; gate nodded "yes to all", PR #134) closed **Cycle J-C (Completion & review)**'s platform half — completion detection inside `complete_journey_step` on the transition edge (row-locked, idempotent, `completed_at` stamps once; the solo party flips to `completed`, via-group completion stays traveller-grain per invariants 4 + 8), the V3 durable `journey_completed` notification row, derived per-step/total timing semantics, and additive `completion`/`timing` payload blocks; no new tables/columns/RLS — function re-issues only, including the labelled loosening of the enter/complete status guard to admit `completed`. It pairs with the Hub surface [FEAT-H021](../../../products/hub/features/FEAT-H021-journey-completion-and-review.md). **FEAT-PD005** (6-done, built Cycle J-D 2026-07-08; gate nodded, PR #143 + the separately-nodded rider) closed **Cycle J-D (Group progress & frozen mode)**'s platform half — the area's hardest privacy design shipped demonstrated: the consent-gated group-progress read (invariants 4 + 8 enforced in the contract — sharing is per-enrolment opt-in via the append-only `consent_records`; aggregates derive from sharing members only with an honest basis; no timing, no ordering), the catalog's existing `view_group_progress`/`view_others_progress` grain (no seeding — discovered live-wired at build), the additive `freeze`/`progress_sharing` blocks on `get_player_state`, Q9 lived-record read standing for frozen walks, and the PC013/PC014 freeze cascades re-verified red-first (the bridge-`_14` commission discharged; Q8 closed-vs-archived recorded working-as-intended). It pairs with the Hub surface [FEAT-H022](../../../products/hub/features/FEAT-H022-frozen-mode-and-group-progress.md). **FEAT-PD006** (6-done, built Cycle J-E 2026-07-10; gate nodded, PR #163) opens — and closed — **Cycle J-E (The onboarding arc)**'s platform half — realizing [ADR-U045](../../../architecture/decisions/ADR-U045-onboarding-journey.md) + Amendment 1: onboarding designation as data (a boolean flag with a single-designation partial unique index), the in-place replacement of `enroll_self_in_journey` to admit a Mist iff the journey is the designated onboarding journey (the FIM-only gate holds everywhere else), the `get_onboarding_status()` first-arrival read (Mist-callable, enrolment-absence is the signal), the seeded placeholder onboarding journey (native ADR-U044 steps + the [ADR-U046](../../../architecture/decisions/ADR-U046-step-response-capture.md) takeaway seed via `journeys.takeaway` + per-step `content`), and the JRN-5 transcendence carry-over + ADR-U031 ephemerality as proofs over existing machinery (no new mechanism). It pairs with the Hub surface [FEAT-H023](../../../products/hub/features/FEAT-H023-onboarding-arrival-and-carry-over.md). **FEAT-PD007** (6-done, built Cycle J-F 2026-07-18; gate nodded "ok merge", PR #174; migrations `20260718090000/0100/0200` applied + repaired) closed **Cycle J-F (Step-response capture & review substance)** — the last Journeys cycle before the J-O3 area gate — realizing [ADR-U046](../../../architecture/decisions/ADR-U046-step-response-capture.md): the response payload lands on the per-traveller step-instance (the ADR-U044 §4 lived record realized — traveller-own scoping, transcendence carry-over, and ADR-U031 forgetting come free), the `captures_response` registry column names the Ask-family as data, the dedicated `save_step_response` verb keeps capture orthogonal to completion (optional-always, invariant 3; the enter/complete status guard family extends — frozen/withdrawn refuse, completed admits), `get_player_state` gains byte-additive keys (responses, `captures_response`, `journey.takeaway`), response content stays private-only in Ferd (pinned: the group-progress read never carries it), and `get_own_step_instances_export()` discharges the FEAT-H010 step-instances flag before the first response is ever stored. It pairs with the Hub surface [FEAT-H024](../../../products/hub/features/FEAT-H024-ask-capture-and-review-substance.md). **FEAT-PD008** (6-done, built Cycle C-A 2026-07-20; gates nodded, PRs #203+#204; migrations 20260719230500+20260720003000 applied) is the **first DS-5 Communication feature spec**, opening **Cycle C-A** of the [Communication completion plan](../../../planning/hub-v2/phase-3-communication-completion-plan.md) — the conversation-model redesign (CB-7: group conversations in scope; participants junction + open `conversation_kinds` registry; per-participant read-state; `direct_messages` renamed `messages`), the conversation/message contracts as the only write door, the conformance gate extended to police DS-5's tables, and the ADR-U039 disposition of the legacy `postgres_changes` publications; it pairs with the Hub surface [FEAT-H025](../../../products/hub/features/FEAT-H025-messages-dm-and-group-conversations.md) and carries the area's first schema-gate migration.
