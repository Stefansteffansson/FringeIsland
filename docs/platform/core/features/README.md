# Features — Platform Core

Feature specifications for Platform Core (Infrastructure, Identity, Organisation, Governance). Each feature uses the Shape Up pitch format with BDD stories embedded as Given/When/Then acceptance criteria.

**Feature ID prefix:** `PC` (e.g., `FEAT-PC001-auth-service.md`)
**Template:** `../../../templates/feature-spec.md`

Retroactive specs (maturity `6-done`) are being written first to capture already-shipped Platform Core functionality. See `../../../planning/waves/FERD-CAPABILITY-MAP.md` for current implementation status.

## Feature index

| ID | Title | Owner area | Wave | Maturity |
|----|-------|-----------|------|----------|
| [FEAT-PC001](./FEAT-PC001-mist-anonymous-substrate.md) | Mist anonymous-identity substrate (arrival) — the platform half of IDN-1 | Identity (PC-2) | Ferd | 6-done |
| [FEAT-PC002](./FEAT-PC002-mist-transcendence-reaper-consent.md) | Mist ephemerality reaper + atomic transcendence + consent substrate — the platform half of IDN-2 | Identity (PC-2) | Ferd | 6-done |
| [FEAT-PC003](./FEAT-PC003-self-service-profile.md) | Self-service profile — own-row read + identity-scope update contract — the platform half of IDN-4 | Identity (PC-2) | Ferd | 6-done |
| [FEAT-PC004](./FEAT-PC004-account-state-read.md) | Account-state read — the caller's own lifecycle state (active / suspended / decommissioned) — the platform half of IDN-9 | Identity (PC-2) | Ferd | 6-done |
| [FEAT-PC005](./FEAT-PC005-self-service-account-reactivation.md) | Self-service account reactivation — owner-gated, audited, origin-gated paused→active transition — the platform half of IDN-12 (un-parked + built C-F) | Identity (PC-2) | Ferd | 6-done |
| [FEAT-PC006](./FEAT-PC006-member-consent-read.md) | Member consent read — own effective consent decisions + history, over the granular-consent substrate (decision column + purpose catalog) — the platform half of IDN-6 | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC007](./FEAT-PC007-consent-decision-write.md) | Consent decision write — own-subject, append-only, withdrawability-gated grant/withdraw — the platform half of IDN-7 (consent half) | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC008](./FEAT-PC008-member-data-export.md) | Member data export — own-subject synchronous assembly of complete personal data into one versioned document + a durable export-event record — the platform half of IDN-8 | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC009](./FEAT-PC009-session-inventory-and-revocation.md) | Session inventory & targeted revocation — own-subject per-device session list + revoke-one over `auth.sessions`, with the server-originated instant-sign-out hint (ADR-U039 first tenant) — the platform half of IDN-11 | Identity (PC-2) | Ferd | 6-done |
| [FEAT-PC010](./FEAT-PC010-group-creation-and-settings-contracts.md) | Group creation & settings contracts — atomic create-engagement-group bootstrap, the group-detail read with viewer capability flags, permission-gated settings/visibility updates, direct-write narrowing on `groups`, and the system-group seeding repair — the platform half of Groups Cycle G-A | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC011](./FEAT-PC011-group-role-and-permission-contracts.md) | Group role & permission contracts — role-fabric read with capability flags, template/custom role definition with definition-time anti-escalation, `can_assign_role()`-walled assignment/removal, and the published `get_user_permissions()` effective read — the platform half of Groups Cycle G-B | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC012](./FEAT-PC012-group-invitation-and-joining-contracts.md) | Group invitation & joining contracts — scoped member search (the D3 / DS-6 re-home seam), FIM invitation, durable email invitation with signup auto-claim (dispatch = the D4 / V3 seam), the Steward pending read/cancel, the invitee's accept/decline, and the FEAT-PC002 erasure-gap closure — the platform half of Groups Cycle G-C | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC013](./FEAT-PC013-group-membership-lifecycle-contracts.md) | Group membership lifecycle contracts — pause/activate over the seeded keys, removal with its composed cascade, `leave_group` replaced in place as the honest regular exit (sole-Steward/last-member refused until G-E), the `membership_status` detail amendment, and the member-exit DELETE-policy narrowing — the platform half of Groups Cycle G-D | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC014](./FEAT-PC014-leadership-transfer-and-closure-contracts.md) | Leadership transfer, closure & deletion contracts — `nominate_steward` replaced-in-place + hardened, `respond_to_stewardship_nomination`, the `hand_stewardship_to_deusex` last resort (ADR-U019), `close_group` (last-member), `delete_group` (Steward, soft-terminal), the sprint3 anon-grant + caller-data-dispatch hole closed, and the raw `groups_delete` policy dropped — the platform half of Groups Cycle G-E | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC015](./FEAT-PC015-group-of-groups-membership-and-acting-contracts.md) | Group-of-groups membership & acting contracts — the `act_as_group` key (ADR-U041), `invite_group` + `search_invitable_groups`, the wielded `respond_to_group_invitation` / `leave_group_as_group`, the acting-context reads, nominee eligibility hardened to persons, and the honest member payloads (`member_group_type`, `non_system_member_count`) — the platform half of Groups Cycle G-F | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC016](./FEAT-PC016-pending-nominations-read-contract.md) | Pending-nominations read contract — `get_my_pending_nominations()` mirroring `get_my_invitations()`; `leadership.ts` becomes a thin consumer (closes the 2026-07-06 audit LOW finding; rides the FEAT-PD002 schema-gate migration) | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC017](./FEAT-PC017-account-lifecycle-self-service.md) | Account lifecycle self-service — the `deactivation_origin` split (ADR-U050), `pause_own_account()`, `delete_own_account()` (terminal walk + F-2 erasure split), and retirement of `admin_exit_user_from_platform` — the platform half of IDN-10 | Identity (PC-2) | Ferd | 6-done |
| [FEAT-PC018](./FEAT-PC018-telemetry-event-store-and-statistics.md) | Telemetry event store & platform statistics — the deny-all `telemetry_events` sink, the never-raises recorder, the 90-day pg_cron prune, and the admin-gated computed-on-read statistics document (ADR-U052; the V4 sink + the ADM-1 read) — the platform half of Hub FEAT-H034, Cycle ADM-A | Infrastructure (PC-1) | Ferd | 6-done |
| [FEAT-PC019](./FEAT-PC019-durable-auth-event-audit-binding.md) | Durable auth-event audit binding — `record_auth_event()`, the SECURITY DEFINER audit-write primitive for the four member-auth moments (sign-up, sign-in, transcendence, farewell; the AC-6/AC3-O6 discharge), additive over the untouched `admin_audit_log` — the platform half of Hub FEAT-H034 STORY-3, Cycle ADM-A | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC020](./FEAT-PC020-group-administration-contracts.md) | Group administration contracts — cross-platform enumeration incl. the `deusex_stewarded` caretaker filter (AC3-O8/RW-05 discharge), detail, the first `groups.status='suspended'` producers, and stewardship reassignment out of caretakership composing the PC-3 role fabric — the platform half of Hub FEAT-H035, Cycle ADM-B | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC021](./FEAT-PC021-member-administration-contracts.md) | Member administration contracts — the platform-scope member read family (`admin_get_users`/`admin_get_user_detail` — greenfield, walk-verified), the audited-and-typed re-issue of the four legacy write contracts (closing the sanction-family audit gap + the untyped-refusal gap), `admin_exit_user_from_platform` re-derived as the ADM-6 full exit against `delete_own_account`'s walk (deliberately no erasure legs), `admin_remove_member_from_group` (ADM-18), and the first ADM-12 contracts (`admin_grant_platform_admin`/`admin_revoke_platform_admin` over what was RLS-only substrate) — the platform half of Hub FEAT-H036, Cycle ADM-C; two schema gates | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC022](./FEAT-PC022-moderation-and-audit-read-contracts.md) | Moderation and audit-read contracts — the ADM-10/11 moderation family over the C-D report store (queue/detail reads with live author resolution, per-report resolve writing `moderation.report_resolved`, the `report_resolved` registered kind through the registry + N-D dispatcher — NTF-6's moderation-decision leg closes), the ADM-16 keyset audit read, the audit client-write door closed, and the AB-4 export split executed (ADR-U052 §6) — the platform half of Hub FEAT-H037, Cycle ADM-D; one schema gate + the ADR-U047 ownership-split rider | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC023](./FEAT-PC023-group-suspension-enforcement-contracts.md) | Group availability enforcement contracts — the two-mode hold, **Active / Resting / Suspended** (RB-6/RB-7 amendment + naming settle): the `resting` status value, the `rest_group` permission seed (Steward template + admin auto-grant; toggle + act-during-rest in one key), mode-aware `assert_group_writable()` across ~28 member-facing write doors (resting = member freeze with the `rest_group` exemption + exits open; suspended = no one below the admin plane, exits included, content quarantined at contracts AND RLS), `rest_group()`/`wake_group()` + audited admin wrappers + `admin_suspend_group` amended, the `leave_group` amendment (the member-trap sprung), the legacy four-table RLS write-door closure, and the additive `get_member_groups.status` — the platform half of Hub FEAT-H038, Cycle HYG-A; the schema gate closed + applied 2026-08-03 | Organisation (PC-3) | Ferd | 6-done |
| [FEAT-PC024](./FEAT-PC024-bounded-member-enumeration-contract.md) | Bounded member enumeration — the keyset + server-search re-issue of `admin_get_users` (composite `(display_name, id)` cursor, cap 200/default 50, `p_search` over name/email, the keyed `{users, next_cursor, generated_at}` return; the parked paging decision of `20260801180000` reopened by the W-5 measurement per RB-3) — the platform half of Hub FEAT-H039, Cycle ADM-E; one schema gate (`20260803210000`, applied 2026-08-03) | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC025](./FEAT-PC025-role-template-editing-and-walk-rider-contracts.md) | Role-template editing contracts + the walk riders — ADM-17's platform half per RB-4/RB-5 (versioned templates with a default pointer over the write-sealed PC-3 substrate: `role_template_versions` + backfill, clone-don't-edit seeds, apply-as-repoint materialising onto the live rows the untouched instantiation physics read, the protected-permission guard extending the last-DeusEx instinct, audit rows carrying old-set → new-set diffs) + the three ADM-E walk-rider re-issues (WA-2 `admin_get_audit_log` symmetric target resolution, WA-3 `admin_hard_delete_user` consent-erasure leg, WA-4 `admin_force_logout` per-session `session_revoked` hints) — the platform half of Hub FEAT-H040, Cycle ADM-F; one schema gate | Governance (PC-4) | Ferd | 6-done |
| [FEAT-PC026](./FEAT-PC026-suspended-group-admin-access-contracts.md) | Suspended-group admin access contracts — WF-2's platform half per the settled G-board (suspended-only, purpose-bound): the sight arms where the gates actually block (suspended-scoped admin disjuncts on `get_group_announcements` / `get_group_conversations` / `get_conversation_detail` group-kind-only, the `is_conversation_participant` re-issue closing the conversations/messages RLS family + realtime, the `get_group_forum` Tier-1 pass pinned as law), the `admin_get_group_detail` members-`email` payload re-issue (the W-4 echo law), and the audited in-place act (`admin_moderate_group_forum_post`, P0001-refusing off suspension; `admin_remove_member_from_group` composed from PC021, gate-proven on a held group) — the platform half of Hub FEAT-H041, Cycle ADM-G; one schema gate | Governance (PC-4) | Ferd | 5-in-cycle |

FEAT-PC001 is the first forward-looking Platform Core feature spec — the substrate provider consumed by Hub [FEAT-H003](../../../products/hub/features/FEAT-H003-mist-identity-on-arrival.md) (IDN-1). FEAT-PC002 continues it with the departure + transcendence slice (§9 stages 3-4), consumed by Hub [FEAT-H004](../../../products/hub/features/FEAT-H004-mist-transcendence-and-farewell.md) (IDN-2); it is gated on [ADR-U033](../../../architecture/decisions/ADR-U033-mist-ephemerality-reaper.md) + [ADR-U034](../../../architecture/decisions/ADR-U034-consent-record-substrate.md). FEAT-PC003 realises the PC-2 §L3 profile-update capability (own-row read + identity-scope update over `public.users`), consumed by Hub [FEAT-H005](../../../products/hub/features/FEAT-H005-member-profile-and-sign-out.md) (IDN-4). **FEAT-PC004 + FEAT-PC005** are the Cycle A account-lifecycle pair (Phase-3 Identity completion, [plan](../../../planning/hub-v2/phase-3-identity-completion-plan.md)): PC004 is the member-facing read of own account state — a `SECURITY DEFINER` read that bypasses the `is_active` visibility filter for the caller's own row (the platform half of IDN-9, consumed by Hub [FEAT-H006](../../../products/hub/features/FEAT-H006-render-account-state.md)); PC005 is the owner-gated, audited self-service reactivation transition (paused→active, decommissioned terminal; the platform half of IDN-12, consumed by Hub [FEAT-H007](../../../products/hub/features/FEAT-H007-self-service-account-reactivation.md)). PC004 is now `6-done` (IDN-9, built in Cycle A — the off-but-not-closed state realised as `suspended`, an admin hold); PC005 was parked at Cycle A on the self-pause + origin-field pair and **un-parked at Cycle C-F (2026-07-21)**, gaining the origin gate (only member-origin pauses flip back; see the [account-lifecycle decision record](../../../planning/hub-v2/account-lifecycle-states-decision.md)); **FEAT-PC017** (C-F, IDN-10) is the sibling that provides both unblockers — the `deactivation_origin` column (ADR-U050), self-pause, and the terminal self-delete that retires `admin_exit_user_from_platform`. Each carries net-new substrate (a `SECURITY DEFINER` function + an additive account route) landing through the schema gate at build. **FEAT-PC006 + FEAT-PC007** are the Cycle B consent/GDPR pair and the **first PC-4 Governance** feature specs: PC006 lands the granular-consent substrate (a `decision` column on the PC-2-owned `consent_records` + a seeded `consent_purposes` catalog) plus the own-subject `get_own_consent_state()` read (consumed by Hub [FEAT-H008](../../../products/hub/features/FEAT-H008-render-consent-state.md), IDN-6); PC007 adds the withdrawability-gated `record_consent_decision()` grant/withdraw write (consumed by Hub [FEAT-H009](../../../products/hub/features/FEAT-H009-update-consent-decisions.md), IDN-7 consent half). Both extend the PC-2-owned `consent_records` (ADR-U034 amendment, schema gate at build); the consent-state capability they realise is attributed to PC-4 by `hub/SPECIFICATION.md` §L3:366 and now enumerated in PC-4's own §L3 (consent row added 2026-06-29), and IDN-7's sharing-controls half is split to a later slice (G-34). **FEAT-PC008** is the **Cycle C** data-export/GDPR slice — a synchronous own-subject `get_own_data_export()` that assembles the caller's Core-owned data (profile/account, consent history, memberships) into one versioned `jsonb` document + a durable export-event record, consumed by Hub [FEAT-H010](../../../products/hub/features/FEAT-H010-download-my-data.md) (IDN-8); the data-export capability is now enumerated in PC-4's §L3 (added 2026-06-30, **G-35 narrowed to its feature-flag remainder**), and Domain-owned data (enrolments, …) plus the Journal are forward-seam sections. Net-new RPC → schema gate at build. **FEAT-PC009** is the **Cycle E** sessions slice and closes the G-29-routed PC-2 reciprocation gap (`hub/SPECIFICATION.md` §L3 IDN-11): own-subject `get_own_sessions()` + `revoke_own_session()` over the `auth.sessions` substrate (`admin_force_logout` is the mechanics precedent; feasibility gate passed 2026-07-03, [plan](../../../planning/hub-v2/phase-3-identity-completion-plan.md) row E), plus the server-originated `session_revoked` hint on a private channel — the first tenant of the [ADR-U039](../../../architecture/decisions/ADR-U039-realtime-socket-doctrine.md) socket doctrine, consumed by Hub [FEAT-H012](../../../products/hub/features/FEAT-H012-per-device-sessions.md). New SECURITY DEFINER functions + `realtime.messages` policies → schema gate at build. **FEAT-PC010** is the **first PC-3 Organisation** feature spec, opening the Groups area (**Cycle G-A**, [Groups completion plan](../../../planning/hub-v2/phase-3-groups-completion-plan.md)): `create_engagement_group()` (atomic group + role-instances + creator membership + Steward binding, per the ADR-U016 composed invariant), `get_group_detail()` (visibility-honest read with the viewer `can_manage_settings` capability flag), `update_group_settings()` (permission-gated partial update; the two visibility toggles independent) — plus the ADR-U038 direct-write narrowing on `public.groups` and the idempotent `FringeIsland Members`/`DeusEx` seeding repair (the substrate-audit C3-1 fresh-DB concern, PC-3 slice), consumed by Hub [FEAT-H013](../../../products/hub/features/FEAT-H013-group-creation-and-stewardship.md). New SECURITY DEFINER functions + write-narrowing + seeds → schema gate at build. **FEAT-PC011** continues PC-3 with the **Cycle G-B** role & permission contracts: `get_group_roles()` (fabric + capability flags + the catalog riding the payload), `create_group_role()` / `update_group_role()` / `set_group_role_permission()` / `delete_group_role()` (GRP-6 — template instantiation + custom definition with **definition-time anti-escalation**; custom-and-unheld deletion only), `assign_member_role()` / `remove_member_role()` (GRP-7 — through the existing **`can_assign_role()` anti-escalation** and the last-Steward/last-DeusEx invariants), and GRP-8 served by the **existing published `get_user_permissions()`** (no new function; group-as-actor contexts deliberately routed to G-F/G-29); `get_group_detail`'s members payload extends additively (`member_group_id`, `roles[]`); consumed by Hub [FEAT-H014](../../../products/hub/features/FEAT-H014-group-roles-and-permissions.md). New SECURITY DEFINER functions + TRUNCATE revokes → schema gate at build; **no new table, no policy changes** (the role tables' RLS is already substantively correct and stays as defense-in-depth). **FEAT-PC012** continues PC-3 with the **Cycle G-C** invitation & joining contracts (MEM-1/2/3): `search_invitable_members()` (name-partial + exact-email, cap 8, `invite_members`-gated — the **D3 / DS-6 re-home seam**, deliberately not a PC-3 §L3 capability), `invite_member()` / `invite_by_email()` (durable pending row + the existing `handle_new_user` signup auto-claim; **no dispatch** — the D4 / V3 seam; existing-FIM emails convert server-side, Open Q2), `get_group_invitations()` + the two cancels (Steward tending, predicate-based expiry rendered honestly), `get_my_invitations()` + `accept_group_invitation()` / `decline_group_invitation()` (the invitee's answer over the existing `invited→active` RLS path and role-auto-bind trigger), plus the **FEAT-PC002 erasure-cascade extension** over `pending_email_invitations` (Art. 17 gap found at decomposition), consumed by Hub [FEAT-H015](../../../products/hub/features/FEAT-H015-group-invitations-and-joining.md). New SECURITY DEFINER functions + the erasure amendment + TRUNCATE revokes → schema gate at build; **no new table, no policy changes** (the invite/accept/decline RLS is already substantively correct and stays as defense-in-depth). **FEAT-PC013** continues PC-3 with the **Cycle G-D** membership lifecycle contracts (MEM-4/5/6): `pause_member()` / `activate_member()` (over the **already-seeded** `pause_members`/`activate_members` keys; permission darkness is `has_permission()`'s existing `status='active'` filter — oracled B-RBAC; roles rows preserved for reactivation; last-active-Steward guard closes the trigger walls' status-flip blind spot), `remove_member()` (the cascade the RLS DELETE path never had — enrolment freeze `'removed_from_group'` + role cleanup + membership delete; paused targets removable), `leave_group()` **replaced in place** (regular exit with house no-leak refusals; the sole-Steward and last-member scenarios **refused honestly until G-E** re-lands MEM-7/MEM-8 from the legacy oracle; MEM-6's DS-5 attribution disposition tagged `pending-DS-5`, D2), the additive `membership_status` amendment to `get_group_detail` (paused rows for management-permission viewers, Open Q3), and the **two member-exit DELETE policies dropped** (`memberships_delete_leave`/`memberships_delete_remove` — the raw paths bypassed freeze, role cleanup, and the Steward guards; G-A narrowing precedent, Open Q2), consumed by Hub [FEAT-H016](../../../products/hub/features/FEAT-H016-group-membership-lifecycle.md). New SECURITY DEFINER functions + one replacement-in-place + the read amendment + policy drops → schema gate at build; **no new table, no trigger changes**. **FEAT-PC014** closes PC-3's Groups arc with the **Cycle G-E** leadership-transfer, closure & deletion contracts (MEM-7/MEM-8/GRP-9): `nominate_steward` **replaced in place** and hardened (template-aware + active-membership Steward resolution — closing the v2-Steward-name `'Member'`-class bug the sprint3 body carried and the paused-Steward blind spot — plus the sprint3 **anon/PUBLIC grant + caller-data-dispatch security hole** confirmed live on dev), `respond_to_stewardship_nomination()` (accept→succeed + nominator departs / decline→next-or-DeusEx), `hand_stewardship_to_deusex()` (the ADR-U019 last resort), `close_group()` (MEM-8 last-member → `status='closed'`) and `delete_group()` (GRP-9 Steward → soft-terminal `status='archived'`, Open Q5) each with owned-non-public-journey reassignment to DeusEx + DS-4/DS-5 tagged layers, the generic `handle_notification_action` + `_handle_stewardship_nomination_action` **dropped** (Open Q2 default) and the raw `groups_delete` policy dropped (Open Q4), consumed by Hub [FEAT-H017](../../../products/hub/features/FEAT-H017-leadership-transfer-and-closure.md). New + replaced SECURITY DEFINER functions + the dispatch-surface drop + the policy drop + grant hardening → schema gate passed (PR #80, Open Q1–Q5 + four build findings nodded); **no new table, no trigger changes**. **FEAT-PC015** (4-ready, decomposed Cycle G-F 2026-07-06) re-opens PC-3 with the group-of-groups membership & acting contracts (MEM-10 depth-1, governed end-to-end by [ADR-U041](../../../architecture/decisions/ADR-U041-group-representation-by-permission.md)): the `act_as_group` catalog key + Steward-template seed (wielding as ordinary RBAC), `invite_group()` / `search_invitable_groups()` (admission mirrored on PC012's proven `invited→active` path — engagement targets only, self/cycle/duplicate refused), `respond_to_group_invitation()` / `leave_group_as_group()` (the wielded answer and exit — the ADR's two-step walk enforced in-contract, the audit-trace column as Open Q4), `get_acting_contexts()` / `get_group_memberships_of()` (the selector + belongs-to reads), `nominate_steward` eligibility hardened to persons (closing the post-fallback DeusEx pick-list hole, FEAT-H017's routed rider), and the additive honest payloads (`member_group_type`, `non_system_member_count`), consumed by Hub [FEAT-H018](../../../products/hub/features/FEAT-H018-group-of-groups-and-acting-as-a-group.md). New SECURITY DEFINER functions + one replacement-in-place + seeds + one additive column (Open Q4) → schema gate at build; **no new table, no trigger changes, no policy changes**. **FEAT-PC016** (6-done, built Cycle J-A 2026-07-07) is the small nominee-window read — `get_my_pending_nominations()` on the exact `get_my_invitations()` pattern (FIM-only, own-recipient, **server-clock** expiry), closing the 2026-07-06 compliance audit's LOW finding (the pending-nomination derivation sole-homed in `hub/lib/groups/leadership.ts` with client-clock date math); it rides the [FEAT-PD002](../../domain/features/FEAT-PD002-journey-catalogue-and-enrolment-contracts.md) schema-gate migration as a rider, and the Hub side thins to a consumer inside Cycle J-A. Retroactive `6-done` specs for already-shipped Platform Core functionality may follow.
