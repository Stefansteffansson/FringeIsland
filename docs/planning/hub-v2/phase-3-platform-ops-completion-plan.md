# Phase 3 — Platform-Ops (A-ADM) completion plan

**Status:** v15 (2026-08-03) — **CYCLE ADM-E BUILT + CLOSED: [FEAT-PC024](../../platform/core/features/FEAT-PC024-bounded-member-enumeration-contract.md) + [FEAT-H039](../../products/hub/features/FEAT-H039-bulk-member-actions-and-bounded-list.md) both `6-done`.** The one schema gate (`20260803210000`, PR #399) merged on Stefan's approval and applied same-day (management-API path; history repaired); red 20/52 at head → **52/52 post-apply**, the B1a page-walk proving union-equals-census against the live ~1,900-user population. The surface landed in two tranches around the gate (the HYG-A pattern): tranche 1 pre-apply (PR #400 — the shape-tolerant shim making the apply order-independent, the ConfirmModal ReactNode widening, the W-4 email echo on all nine member ceremonies; 11 red at head) and tranche 2 post-apply (the bounded list — pager + debounced server search + As-of/Refresh per RB-8, page-scoped selection, the three bulk routes serially looping the singles with per-row verbatim outcomes and per-member audit rows; 19 red at head). **The route-policy gate caught the thin-delegate route shape at first contact** — restructured to per-file `getUser()` + a shared post-auth handler, zero exception entries. Gates at close: unit 1221/1221 · lint 0 · `next build` green · E2E journey 4/4 (designed-refusal partial success verbatim; per-member audit containment proofs) · full E2E sweep at close. **Next: ADM-F** (ADM-17 role-template editor, RB-4/RB-5) per the settled sequence ADM-E → ADM-F → ADM-G → N-E → AB-6. Prior status (v14, 2026-08-03): **THE WF SLOTTING BOARD SETTLED + THE ADM-E BUILD NODDED (Stefan: "go with recommended" — all four rows WS-1..WS-4; the decomposition PR #398 nodded, build started):** WF-2 → its own cycle **ADM-G** after ADM-F, scope **suspended groups only** (WS-1/WS-2); WF-1 → its own small notifications cycle **N-E** after ADM-G, carrying the polish rider (WS-3/WS-4); the sequence is **ADM-E → ADM-F → ADM-G → N-E → AB-6**, everything ahead of the FULL audit. The AB-6 precondition from the walk findings (WF-2 at least slotted) is satisfied. ADM-E's schema gate stays held for its own NAMED approval per the standing rule. Prior status (v13, 2026-08-03): **CYCLE ADM-E DECOMPOSED to 4-ready + THE WF SLOTTING BOARD OPENED:** paired [FEAT-PC024](../../platform/core/features/FEAT-PC024-bounded-member-enumeration-contract.md) ↔ [FEAT-H039](../../products/hub/features/FEAT-H039-bulk-member-actions-and-bounded-list.md), authored with the contract walk run against live substrate signatures + the Hub tree at HEAD (findings recorded in both §Problem sections, each file:line-anchored): the parked keyset decision is written in `20260801180000`'s own header ("until a measurement asks") and **W-5 is that measurement** (RB-3 executed); `admin_force_logout` is already array-shaped but writes **one audit row per batch** (`20260801190000:432-434`) — the BFF loops it one-id-per-call so per-member rows fire by construction (RB-2's mechanics, the batch shape deliberately unused); the last-DeusEx floor is **trigger-enforced with whole-call rollback** (confirming why grant/revoke stay out of the bulk subset); the audit read's keyset cursor is a bare timestamp that doesn't fit a name-ordered list — the re-issue takes a composite `(display_name, id)` cursor with the same cap expression; `ConfirmModal.message: string` widens additively to ReactNode for the bulk listing; W-4 verified live (no ceremony echoes email today) and RB-8 verified (As-of/Refresh absent from the list). Tasks TASK-ADME-01/02; **one schema gate** (the read re-issue — held with red evidence + apply commands for named approval per the standing rule). Opening hygiene: lint + `next build` green on main, nothing owed. **The decomposition PR is HELD (platform/core carve-out); build starts on the nod. The HYG-A walk directives WF-1/WF-2 await Stefan's slotting — §Decision board — WF slotting below ([walk findings](./2026-08-03-hyga-walk-findings.md)).** Prior status (v12, 2026-08-03): **HOLD-STATE VOCABULARY SETTLED + THE REST PERMISSION (Stefan's calls at the naming board):** the Social Gathering family — **Active / Resting / Suspended** (vetted against shipped vocabulary: `active`/`suspended` are the live values and `suspended` keeps its account-plane gravity; `resting` is the one new value, in the village's voice; the draft `offline` never ships — the hard semantics land on `suspended`, which retroactively makes the walk's W-3 instinct right). **Resting is permission-driven:** one key `rest_group` (toggle + act-during-rest — the toggle dominates the exemption, so one key; a future `act_while_resting` split stays open) seeded to the Steward template and reaching platform admins via the live auto-grant; **symmetric** (`rest_group()`/`wake_group()`, member-plane, telemetry-mirrored) with thin audited `admin_rest_group`/`admin_wake_group` wrappers; **every transition touching `suspended` stays admin-only** (no steward path out of the hard state; no direct suspended→resting). Steward control lives on group settings (capability-flagged); verb "rest", never "put to rest"; `resting` (placed) stated distinct from `paused` (chosen). PC023/H038 renamed and reworked in place on held PR #388. Prior status (v11, 2026-08-03): **THE SUSPENSION MODEL GOES TWO-MODE (Stefan's directive at the HYG-A decomposition review; RB-6/RB-7 amendment recorded at the board below):** `suspended` = the visible hold (group + content readable by members, member writes refuse typed, steward-permission holders fully exempt, exits open) · `offline` = the hard hold (findable + labeled "Off-line" — copy provisional — content inaccessible, **no actions below the admin plane, stewards and exits included**). **PC023 + H038 REWORKED in place on held PR #388:** the `offline` status value (check-constraint change), the steward-exemption permission seed (`act_during_suspension`, named default), mode-aware `assert_group_writable()`, off-line read minimalization (read-door enumeration run against live signatures), and the admin availability ceremonies (`admin_take_group_offline` new; suspend/reactivate amended for the three-state moves — the "no admin-contract changes" no-go dies with the model). Build still starts on the nod; the schema gate stays held for named approval. Prior status (v10, 2026-08-03): **RE-SCOPE BOARD SETTLED (Stefan: "go with recommended" — all eight rows RB-1..RB-8; defaults stand) + CYCLE HYG-A DECOMPOSED to 4-ready:** paired [FEAT-PC023](../../platform/core/features/FEAT-PC023-group-suspension-enforcement-contracts.md) ↔ [FEAT-H038](../../products/hub/features/FEAT-H038-suspension-integrity-and-state-honesty.md), authored with the RB-6 contract walk run against live substrate signatures (cumulative-forward, 99 migrations + live-DB policy verification). **Walk findings recorded (PC023 §Problem, each disk-anchored):** only 8 of ~30 member-facing group-context write doors check group status (a status *mention* is a visibility gate that short-circuits for members, not a guard); `leave_group` TRAPS members of suspended groups today (`20260719190205:300-302` — amended per the RB-6 "leaving stays allowed" ruling); the four legacy membership/role tables carry 13 live `authenticated` write policies + grants (verified live 2026-08-03 — closed at the gate, the ADM-D client-write-door precedent, bootstrap/invite paths in the Q1 post-apply set); `get_member_groups` lacks `status` (additive key; the list chip). The matrix derives to per-door calls under the rule **growth-and-activity frozen, reduction-and-exit open** (withdraw/decline/cancel/pause/leave/report stay open — `submit_content_report` is the safety valve; `moderate_forum_post` frozen, Steward included per the ruling). Tasks TASK-HYGA-01/02; one schema gate (held with red evidence + apply commands for named approval); sequence per RB-1: HYG-A → ADM-E → ADM-F, all ahead of AB-6. **The decomposition PR is HELD (platform/core carve-out); build starts on the nod.** Prior status (v9, 2026-08-03): **THE ADM-7 + ADM-17 DECOMPOSITION BOARD OPENED** (§Decision board — ADM-7 + ADM-17 below). The area gate is **CLOSED 2026-08-03** ([gate record](./2026-08-02-platform-ops-area-gate.md) §Gate verdict); the board is the first post-gate work item ([bridge](../sessions/2026-08-03_01_-_A-ADM-GATE-CLOSED-WALK-COMPLETE-PHASE-3-BUILD-DONE.md)). One board covers both re-scoped rows (the gate-close call: they share the admin plane); it absorbs **Stefan's fix directive** on the stall/cache family (walk findings W-5/W-7/W-8/W-9/W-10 — committed fix work), the **W-3 suspended-refusal-matrix decision**, and the recorded **ADM-17 design skeleton**. Every row awaits Stefan's call; settlement is followed by decomposition to 4-ready paired specs per cycle, ahead of AB-6. Prior status (v8, 2026-08-02): **CYCLE ADM-D BUILT + CLOSED: [FEAT-PC022](../../platform/core/features/FEAT-PC022-moderation-and-audit-read-contracts.md) + [FEAT-H037](../../products/hub/features/FEAT-H037-moderation-and-audit-view.md) both `6-done` — A-ADM's build scope is COMPLETE; the area gate is now due.** Two named-approval schema gates (#376 → `20260802120000`, red 27/29 pre-apply with the two greens exactly the labelled invariants; #377 → `20260802170000`, the rider). **The platform conformance gates caught two real things at first post-apply contact** — the trigger-function EXECUTE leak and five core-to-domain edges (ADR-U047 rule 3) — fixed by the COR-A relocation pattern: sealed DS-5 `ds5_moderation_*` primitives under thin PC-4 `admin_*` wrappers, signatures byte-identical. Post-rider: gate suite 29/29, admin domain 101/101, platform conformance 23/23, account 83/83 (the AB-4 export adaptations live at `schema_version` 2), notifications 106/106, communication 107/107. H037 red-first at the unit tier (4 suites red → admin components 85/85; full unit 1147/1147; route-policy + outer-ring zero exceptions; `next build` green; E2E 6/6 labelled test-after, leak delta 0 — plus one environment catch for the watch-list: a dev server predating heavy parallel jest/build load held a poisoned compile-worker pool, "Jest worker exceptions", restart fixed). AB-4 executed per its own manifest instruction; NTF-6's moderation-decision leg closed; the audit client-write door dropped; sanction-communication kinds → Eid (DB-4). TASK-ADMD-01/02 done. **Next: the A-ADM area gate** — the §Exit checklist is canonical (perf pass incl. the three new routes · Stefan's live walk · W12+GC-14 roll-up · the B-ADMIN oracle disposition · the deferred-five presentation · AB-6 FULL audit · the two process questions). Prior status (v7, 2026-08-01): **CYCLE ADM-D BOARD SETTLED + DECOMPOSED to 4-ready** (Stefan: "go with recommended" — all eight rows DB-1..DB-8; see §Decision board — ADM-D below). Paired [FEAT-PC022](../../platform/core/features/FEAT-PC022-moderation-and-audit-read-contracts.md) ↔ [FEAT-H037](../../products/hub/features/FEAT-H037-moderation-and-audit-view.md) authored with the contract walk run against **live substrate signatures** (the standing practice; cumulative-forward): **six findings recorded** (PC022 §Problem, each file:line-anchored) — the C-D store is resolution-blind (no resolution columns, no write door), submit discards the author (live re-resolution needed for escalation), the audit log's **client INSERT policy is still open** (the ADR-U038 direct-caller hole — dropped in the gate), the audit SELECT policy predates the typed-gate re-issue (PG17 ceiling class — re-issued), the ADM-16 read needs keyset paging from birth (unbounded growth — the row-cap lesson applied at design time), and the manifest's AB-4 entry instructs its own rewrite here. One schema gate (DB-1); the DB-4 ruling resolves CB-1's activation clause — **sanction-communication kinds → Eid** (dated pointers added to both ADM-C specs). The `report_resolved` kind flows the registry FK + the N-D BEFORE-INSERT suppression dispatcher + the N-C hint **by construction**; NTF-6's moderation-decision leg closes. Tasks TASK-ADMD-01/02; no opening hygiene owed (DB-8). **The decomposition PR is HELD** (platform/core carve-out); build starts on the nod, the schema-gate PR held separately with red evidence + apply commands for NAMED approval per the standing rule. Prior status (v6, 2026-08-01): **CYCLE ADM-C BUILT + CLOSED ([bridge](../sessions/2026-08-01_04_-_ADM-C-BUILT-AND-CLOSED.md)): [FEAT-PC021](../../platform/core/features/FEAT-PC021-member-administration-contracts.md) + [FEAT-H036](../../products/hub/features/FEAT-H036-member-administration-view.md) both `6-done`.** Ten contracts across three named-approval schema gates (#368 → `20260801170000`; #369 → `20260801180000`, the jsonb-array row-cap amendment at first contact; #372 → `20260801190000`). Gate-2 red 26/28 → green 28/28 post-apply (admin domain 72/72, account 83/83, platform conformance 23/23); the sibling-assertion sweep caught its **fourth** instance pre-gate — the C-F S8a cell pinning the retired exit function's non-existence, adapted in the gate PR instead of biting at apply. H036 red-first at the unit tier (3 suites red pre-implementation → admin components 55/55; full unit 1117/1117; route-policy + outer-ring gates zero exception entries; `next build` green; E2E journey 6/6 labelled test-after, leak delta 0; PR #373). TASK-ADMC-01/02 done; TASK-DOC-008 (the second changelog backfill) closed post-cycle — the platform-core register now reads continuously PC001 → ADM-C with no register note. **Next: ADM-D (moderation + audit surfaces) — deliberately NOT started (Stefan's stop-point instruction, 2026-08-01)**; the area-gate owed list stands unchanged (ADR-U043 perf pass incl. `/admin/members` + detail · Stefan's live walk · the W12 + GC-14 per-RPC roll-up · the deferred-five presentation · the AB-6 FULL audit · the E2E-at-schema-close process question · the 398-BFF-telemetry-sites criteria). Prior status (v5, 2026-08-01): **CYCLE ADM-B BUILT + CLOSED ([bridge](../sessions/2026-08-01_02_-_ADM-B-BUILT-AND-CLOSED.md)); ADM-C BOARD SETTLED + DECOMPOSED to 4-ready.** Board CB-1..CB-5 settled same day (Stefan: "go with recommended, ADM-6 full exit") — see §Decision board — ADM-C below. Paired [FEAT-PC021](../../platform/core/features/FEAT-PC021-member-administration-contracts.md) ↔ [FEAT-H036](../../products/hub/features/FEAT-H036-member-administration-view.md) authored with the payload walk run against **live contract signatures** (the ADM-B picker lesson executed as standing practice); **five substrate findings recorded** (PC021 §Problem, each file:line-anchored): the admin member read family is greenfield (zero live or archive precedent), the two sanction contracts write **no** audit rows, the legacy write family refuses **untyped** (breaking the admin plane's 42501→404 existence-hiding), no admin platform-exit exists (the walk survives only inside self-only `delete_own_account`), and ADM-12 is RLS-only below the API — so CB-2's "surface work only over existing mutations" premise narrowed: gate 2 carries four re-issues + four new contracts (ten functions total across the two gates). Tasks TASK-ADMC-00/01/02; opening hygiene block = TASK-ADMC-00 (the two main-branch lint errors) + TASK-DOC-007 (the changelog backfill — **re-ID'd from TASK-DOC-005**, which collided with the anatomy-refresh task; 006 is a retired ID). **The decomposition PR is HELD** (platform/core carve-out); build starts on the nod, schema-gate PRs held separately per the standing rule. Prior status (v4, 2026-08-01): **CYCLE ADM-A BUILT + CLOSED; CYCLE ADM-B DECOMPOSED to 4-ready.** ADM-A closed 2026-07-31: FEAT-PC018/PC019/H034 all `6-done` (PRs #352–#358; migrations 20260731180000/190000 applied on named approvals; unit 1068/1068, integration 65 suites 761/761 post-apply, `next build` green, admin E2E 3/3); [ADR-U052](../../architecture/decisions/ADR-U052-telemetry-sink-and-analytics-posture.md) **Accepted** (Stefan, #359); AC3-O6/O7 + GC-13/O5 annotated CLOSED; V4 spec reconciled, [perf ledger](../reference/PERF-MEASUREMENT-LEDGER.md) seeded, TASK-OBS-01 closed. The gates caught three real things at first contact (outer-ring: the telemetry browser-leak shape; the consent append-only trigger: a fixture shortcut; the sweeps: latent COR-C fallout — 7 unit + 4 E2E adapted labelled, 4 E2E adjudications filed as [TASK-DBT-02](../backlog/tasks/TASK-DBT-02-cor-c-e2e-fallout-four-specs.md)). **ADM-B decomposed 2026-08-01** (the AB-5 ADM-8-early ruling): paired [FEAT-PC020](../../platform/core/features/FEAT-PC020-group-administration-contracts.md) ↔ [FEAT-H035](../../products/hub/features/FEAT-H035-group-administration-view.md); opening hygiene block = TASK-INT-05 + TASK-DBT-02 (Stefan: "plan together"); substrate verified at decomposition — `groups.status` admits `'suspended'` with zero producers (ADM-9's slot exists, unwritable today), reassign's canonical direction is OUT of caretakership (RW-05). Tasks TASK-ADMB-01/02. **The decomposition PR is HELD** (platform/core carve-out); build starts on the nod. Prior status (v3, 2026-07-31): **CYCLE ADM-A DECOMPOSED to 4-ready:** paired specs [FEAT-PC018](../../platform/core/features/FEAT-PC018-telemetry-event-store-and-statistics.md) (PC-1 — its **first** feature: telemetry sink + statistics read) + [FEAT-PC019](../../platform/core/features/FEAT-PC019-durable-auth-event-audit-binding.md) (PC-4 — the auth-audit primitive) ↔ [FEAT-H034](../../products/hub/features/FEAT-H034-admin-dashboard-and-durable-audit-wiring.md) (Hub — gated `/admin` dashboard + the four-caller audit wiring); [ADR-U052](../../architecture/decisions/ADR-U052-telemetry-sink-and-analytics-posture.md) drafted (Proposed, rides the ADM-A schema gate, carries AB-1a..d + AB-4); PC-1's §L3 gains the two attributed-but-unenumerated rows via dated reconciliation notes (the G-35 pattern) and its first §L4 section; tasks TASK-ADMA-01..05 (payload walk recorded in both paired specs; TASK-ADMA-01's manifest split runs before any new function lands). **The decomposition PR is HELD for Stefan's review** (ADR + `platform/core/` carve-outs); build starts on the nod, schema-gate PRs held separately per the standing rule. Prior status (v2, same day): **BOARD SETTLED (Stefan: "go with recommended" — all eleven rows AB-1a..AB-8 as recommended), with one standing rider: the five capabilities AB-8 defers must be restated plainly at area close as "what this run did not do"** — see the deferred register below; the exit checklist carries the line. Cycle ADM-A decomposition may begin. Prior status (v1, same day): board OPEN, kickoff sweep complete, plan held for review. A-ADM is the sixth and last Phase-3 area (Identity, Groups, Journeys, Communication, Notifications all closed). It opens the day COR-C closed, on the just-repaired ADR-U050 admin contracts, and **every new table/function/trigger/surface it ships lands under the COR-C gate lattice from the first commit** (classification completeness, trigger licenses, export completeness, token gate, jest-axe, pinned vertical set, outer-ring closure).
**Provenance:** area-open kickoff sweep 2026-07-31 — bridge [`2026-07-31_01`](../sessions/2026-07-31_01_-_COR-C-EXECUTED-FIVE-PRS-HELD-FOUR-MERGED.md), [COR-C execution ledger](../reference/ANATOMY-CONFORMANCE-AUDIT-3.md) (register + observations), Hub spec §L3 ADM rows (`docs/products/hub/SPECIFICATION.md:328-344`), `supabase/ownership.manifest.json` (incl. the W2 export classifications), live migration set, [`behaviour-inventory.md`](./behaviour-inventory.md) B-ADMIN spine, TASK-OBS-01 (discharged by ADR-U052 + FEAT-PC018; swept at the A-ADM close — see the [tasks README](../backlog/tasks/README.md) sweep log), TASK-INT-05 (done — the E2E leak instrument + teardown helpers shipped; swept at the A-ADM close, see the [tasks README](../backlog/tasks/README.md) sweep log), [re-walk RW-05](./2026-07-30-antf-rewalk-findings.md). Load-bearing facts disk-verified at the cited lines.

---

## Where this picks up

COR-C closed 2026-07-31 (all five held PRs merged on named approval; three migrations applied; full integration green; ledger annotated). A-ADM inherits, settled-by-canon (recorded, not asked):

- **ADR-U050 admin contracts are repaired and producer-proven.** COR-C W1 re-issued `admin_update_user_status` (writes `deactivation_origin='admin'` on hold / `NULL` on release, `FOR UPDATE`) and `admin_decommission_user`; the GC-10 producer-driven suite is green; the member-escape is structurally impossible. The console builds on these exact contracts.
- **ADR-U028 routing lock (recorded at the §L3 lock):** content moderation + self-service exit stay woven in-place; the audit-log viewer (ADM-13/16), feature flags (ADM-15), and economy management route to **the Console**, whose surface status (own entity vs Hub-shell bundle) is a deferred U025/U028 decision. Board AB-7 decides the Ferd shape *without* reopening the entity question.
- **The COR-C gate lattice is in force.** New tables must classify `memberData` at birth (W2 invariant — unclassified fails red); new functions must register in the manifest (GC-1 completeness); new triggers need a license (GC-8); surfaces pass token (GC-12) + jest-axe gates; the `vertical:*` table set is pinned (GC-3); the outer-ring closure covers transitive value-imports (GC-7).
- **GC-14 wording fix:** this area's per-RPC gate rows carry a **composition column** — each contract verified *composed into its declared consumers*, not just internally gated (the wording that let AC3-3 get a "VERIFIED" stamp).
- **Carried by name to this board:** TASK-OBS-01 (AB-1) · AC3-O6 audit seam (AB-2) · GC-13/AC3-O5 manifest split (AB-3) · the `admin_audit_log` W2 exemption's "revisit at A-ADM area open" (AB-4) · ADM-8-early sequencing, RW-05 + AC3-O8 (AB-5) · the bridge addendum's audit cadence (AB-6).
- **A-NTF hand-off verified:** the content-report **store** is live (`content_reports`, `20260720200000:126` + `submit_content_report`); the moderation surface (ADM-10/11) is this area's, and NTF-6's moderation-decision-communication seam closes against ADM-11 here.

## Corrections and notes to carried-forward premises (canonical-wins)

1. **The wave map does not bind the ADM rows.** `FERD-CAPABILITY-MAP.md` contains zero `ADM-*` rows (checked 2026-07-31). The Ferd cut across the 18 §L3 rows is genuinely the board's to make (AB-8) — nothing upstream forces build-all.
2. **Naming drift: "A-OPS".** `hub/lib/audit/audit.ts:7,28` and the AC3-O6 register row call this area A-OPS; the canonical area code everywhere else is **A-ADM**. Standardize to A-ADM when AB-2 discharges the TODO (two files, zero canon impact).
3. **The audit seam has FOUR callers, not the "sign-in" the AC-6-era prose suggests:** `signup` (pre-session, null actor), `audit` (sign-in), `transcend` (Mist→FIM consent moment), `farewell` (Mist explicit erase) — three GDPR-relevant. AB-2's recorder must accept a null actor.
4. **`admin_exit_user_from_platform` no longer exists — CORRECTED at ADM-A decomposition (2026-07-31).** This note originally said it ships from sprint4 (`20260228144747`) and needs re-derivation; disk truth is stronger: FEAT-PC017 (C-F) **DROPPED** the function outright — its three membership scenarios live on inside `delete_own_account()`, and PC017's own record homes "any future admin-initiated full exit Console affordance" to A-ADM (COR-A F-3). ADM-6 is therefore a **fresh contract** built against the live MEM-5/7/8 routing (the NB-1 lesson held: re-derive against what exists — what exists turned out to be nothing).
5. **ADM-9 / ADM-17 substrate marked VERIFY-at-decomposition.** No group-suspension or role-template-CRUD contract was positively identified in the sweep; the rows below say VERIFY rather than claim.

## Substrate audit at kickoff (verified 2026-07-31, migrations + manifest + oracle inventory)

Admin substrate at HEAD: `admin_audit_log` (PC-4, append-only, DeusEx-only read — B-ADMIN-007 semantics), `admin_update_user_status` + `admin_decommission_user` (COR-C W1 re-issues), `admin_hard_delete_user`, `admin_send_notification` (oracle ported at A-NTF), `admin_force_logout` (v1-era, array-signature, never re-issued), `content_reports` + report contracts (C-D), PC-3 role/permission substrate incl. last-DeusEx floor. *(v5 correction, 2026-08-01: this paragraph originally listed `admin_exit_user_from_platform` — stale; the function was DROPped at C-F (`20260721161500:611`), post-dating COR-A's re-issue. Note 4 below was right; the kickoff list was not.)* **No statistics primitive** (the only `get_platform_*` functions are announcement reads). **No feature-flag or platform-policy substrate.** **No contract enumerates DeusEx-stewarded groups** (AC3-O8, 87-migration sweep).

| Cap | Capability (compressed) | Substrate | Oracle | Cycle (proposed) |
|---|---|---|---|---|
| ADM-1 | Admin dashboard + platform statistics | **NONE for stats** (PC-1 primitive is genuinely new); dashboard page new | PARTIAL (B-ADMIN-002 stat labels) | ADM-A |
| ADM-2 | Member search/filter/list at platform scope | HAVE reads via PC-3; DS-6 ranking not yet consumed (§L2 list) | STRONG (B-ADMIN-002/013 — filters, selection model) | ADM-C |
| ADM-3 | Activate / suspend / decommission | **HAVE** (W1-repaired contracts; producer suite green in v2) | STRONG (B-ADMIN-008/010 + v2 producer suite) | ADM-C |
| ADM-4 | Hard-delete + sentinel reassignment | HAVE (`admin_hard_delete_user`; U047 reassignment hooks) | STRONG (B-ADMIN-009) | ADM-C |
| ADM-5 | Force-logout active sessions | HAVE (`admin_force_logout`) | STRONG (B-ADMIN-019) | ADM-C |
| ADM-6 | Platform-wide sweep through exit paths | **BUILD FRESH** (the v1 function was DROPPED at C-F — note 4; new contract against the live MEM-5/7/8 routing) | STRONG contract spine (B-ADMIN-007 platform-exit) | ADM-C |
| ADM-7 | Bulk-action safe subset | Singles are the substrate; selection-model oracle is component-tier | PARTIAL (B-ADMIN-013) | **defer (AB-8)** → **RE-SCOPED INTO FERD at the gate close (2026-08-02, Stefan)** |
| ADM-8 | Group administration view (cross-platform) | **NONE** (AC3-O8: no enumerating contract) · **precondition TASK-INT-05** (DeusEx system group is 39/42 test detritus) | PARTIAL (B-ADMIN-012/016/017/018 visibility rules) | ADM-B |
| ADM-9 | Suspend / reassign / reactivate group | **VERIFY at decomposition** (no contract identified) | PARTIAL | ADM-B |
| ADM-10 | Moderation queue render | HAVE store (C-D `content_reports`); triage reads new | SILENT (store is v2-born; C-D contract tests cover submit/window) | ADM-D |
| ADM-11 | Triage + resolve + resolution communication | NONE (resolve contracts new; DS-5 kind new — dispatcher covers it by construction) | SILENT | ADM-D |
| ADM-12 | Platform Administrator membership mgmt | HAVE (DeusEx add/remove + last-admin floor) | STRONG (B-ADMIN-003/005) | ADM-C |
| ADM-13 | Auto-grant verification view | PC-3 trigger exists; surface publication unreciprocated (G-29) · Console-routed | PARTIAL (B-ADMIN-004/006) | **defer (AB-8)** |
| ADM-14 | Platform policy (versioned, reversible) | NONE | SILENT | **defer (AB-8)** |
| ADM-15 | Feature flags | NONE — and **zero flag-reading code exists in v2** · Console-routed | SILENT | **defer (AB-8)** |
| ADM-16 | Platform-scope audit log surface | HAVE table (append-only; DeusEx-only read) · shape per AB-4 · Console-routed (AB-7) | STRONG for the table (B-ADMIN-007); surface new | ADM-D |
| ADM-17 | Role templates + permission catalogue CRUD | Data HAVE (PC-3 seed); CRUD contracts **VERIFY** | PARTIAL | **defer (AB-8)** → **RE-SCOPED INTO FERD at the gate close (2026-08-02, Stefan)** |
| ADM-18 | Targeted removal at platform scope via exit paths | Routes exist (MEM-5/7/8); override contract new | STRONG spine (B-ADMIN-016/017/018) | ADM-C |

## Vertical obligations A-ADM must fulfil

- **V1 Administration** — this area *is* V1's platform-scope realization; every admin action audit-logged (append-only holds — B-ADMIN-007 is the oracle).
- **V2 Privacy** — the AB-4 right-of-access ruling executed (manifest exemption rewritten, invariant updated in the same PR); every new store classified at birth; telemetry actor-linkage dispositioned against ADR-U034 (AB-1); erasure cascades over any new actor-linked store (Mist rule: a Mist's telemetry/audit-linked rows must not survive its erase — prove it, the NB-8 lesson).
- **V3 Notifications** — ADM-11's resolution communication ships as a **registered kind** through the registry + dispatcher (no bespoke write path can exist — the FK forbids it).
- **V4 Observability** — the area **ends the emits-well-stores-nowhere state** (AB-1) and de-scaffolds the V4 spec (G-03); every new admin action emits content-free telemetry.
- **Transactions** — still absent, still cited (AC3-O9); re-verify at the wave boundary, nothing here.

## The cycle sequence (proposed — foundation-first, paired-platform-first)

- **ADM-A — Observability foundation + audit spine (the platform cycle).** TASK-OBS-01's outputs: the telemetry sink (AB-1, ADR candidate), the PC-4 auth-event recorder discharging `recordAuditEntry`'s TODO for all four callers (AB-2), the PC-1 statistics primitive + minimal ADM-1 dashboard, the ADR-U043 durable measurement ledger, G-03 V4 de-scaffolding. **Rider:** the AB-3 manifest split executed *before* the area's first new function lands, so every A-ADM function is born classified.
- **ADM-B — Group administration (the AB-5 early ruling).** In-cycle precondition: TASK-INT-05 fixture cleanup (leak fixed red-first; the 39 detritus rows retired; live DB verified). Then the DeusEx-stewarded enumeration contract (the AC3-O8 gap), ADM-8 list/detail, ADM-9 ops (substrate VERIFY at decomposition). Closes RW-05: a handed-over group becomes visible to the only party who can act on it.
- **ADM-C — Member administration console.** ADM-2 search/filter/list, ADM-3/4/5 surfaces over the repaired contracts, ADM-6 re-derived through MEM-5/7/8, ADM-12, ADM-18. The v1 oracle's admin-suite spine (B-ADMIN-002/003/005/008/009/010/013/019) ports/adapts here. **DECOMPOSED 2026-08-01** to paired FEAT-PC021 ↔ FEAT-H036 (4-ready) under the settled CB board; the walk narrowed the "surfaces over the repaired contracts" premise — the repaired contracts exist but are audit-silent and refusal-untyped, ADM-12 has no contracts, and the reads are greenfield (the five findings, PC021 §Problem).
- **ADM-D — Moderation + audit surfaces.** ADM-10 queue, ADM-11 triage/resolve + the DS-5 resolution kind (NTF-6 seam closes), ADM-16 per the AB-4 shape, under the AB-7 console shell; the `admin_audit_log` manifest entry rewritten in this cycle's PR. **DECOMPOSED 2026-08-01** to paired FEAT-PC022 ↔ FEAT-H037 under the settled DB board (§Decision board — ADM-D); **BUILT + CLOSED 2026-08-02** (both `6-done`; gates #376/#377; the ADR-U047 ownership-split rider).

**Area gate absorbs the standing dues:** ADR-U043 measurement pass + Stefan's live walk; W12 per-RPC rows **with the composition column** (GC-14); the B-ADMIN oracle dispositioned row-by-row; the AB-4/AB-8 records verified executed; the post-area FULL audit per AB-6.

## Design sessions

This one (the board below). One ADR candidate is near-certain: **telemetry sink & analytics posture** (AB-1 — sink ownership, retention, consent posture; the audit right-of-access ruling AB-4 can ride the same ADR or stay a recorded W2-register amendment). AB-3 needs no ADR — the anatomy already states the split; GC-13 just makes it mechanical.

## Decision board — SETTLED 2026-07-31 (Stefan: all rows as recommended) — presented whole, recommendations marked

| # | Question | Recommendation | Default if unaddressed |
|---|---|---|---|
| **AB-1a** | Telemetry sink: durable store vs log-drain vs hybrid | **Postgres-native durable event store, PC-1-owned** (`telemetry_events`), written by a fire-and-forget SECURITY DEFINER recorder (emit failure never fails the action — the C-C hint discipline); content-free props (existing `[telemetry]` discipline); no external vendor in Ferd (pre-launch, zero consumers, no DPA surface); hybrid/drain re-openable at Eid | durable store |
| **AB-1b** | Retention + what ADM-1 aggregates | **Raw events 90-day retention** with a scheduled prune; **ADM-1 computes on read** — live domain counts (members/groups/journeys — cheap at this scale) + event-derived activity trends; **no pre-aggregation until a measured need** (ADR-U042 posture); cardinality bounded by the registered event-name set | 90d + on-read |
| **AB-1c** | V2/consent posture for actor-linked telemetry | **Operational telemetry + audit under documented legitimate interest**, actor-linked via personal-group id, content-free; **erasure cascades** (FK/U047 — a Mist's rows die with its erase, proven); export: exemption-with-citation mirroring AB-4's narrowed shape (bounded retention, ops purpose). "Analytics/optimization" registered as a **named ADR-U034 purpose now, collection deferred** until a consumer exists — don't collect what nothing reads | LI + cascade + cited exemption |
| **AB-1d** | ADR-U043 measurement continuity | **A docs-side durable ledger** (`docs/planning/reference/PERF-MEASUREMENT-LEDGER.md`), appended at every gate pass — measurements are planning artifacts, not member data; no DB table | docs ledger |
| **AB-2** | `recordAuditEntry` durable home (AC3-O6) — four callers, three GDPR-relevant | **PC-4 SECURITY DEFINER `record_auth_event()` writing `admin_audit_log`** (the TODO's own direction; the table exists and is the PC-4 audit substrate); null-actor accepted (signup is pre-session); awaited-but-non-fatal (log + telemetry mirror on failure, never fail the auth action); append-only preserved (B-ADMIN-007); telemetry mirror stays; A-OPS naming fixed in the same PR. **Schema gate.** Distinct from AB-1's store: audit trail ≠ telemetry | PC-4 recorder |
| **AB-3** | GC-13/AC3-O5 — manifest PC-1..PC-4 split at function granularity | **Full four-way split** of the ~100-function CORE declaration, as the ADM-A rider (manifest + gate only, no schema gate): unclassified-fails-red, plus a pinned mechanical rule `admin_* → PC-4` (the anatomy's admin-holds sentence made testable). A-ADM is exactly where the split becomes load-bearing, and GC-1's completeness gate already enumerates the population — the labels are the only missing bit. *Fallback if judged too heavy:* split the PC-4 boundary only, leave PC-1/2/3 flat with a dated narrowing | full split |
| **AB-4** | `admin_audit_log` Art. 15 shape (COR-C W2 revisit) | **Split by row direction:** rows where the member is the **actor** (incl. their own `data_export` entries) gain an export representation in the composite; rows where the member is the **target of third-party admin action** stay exempt under a **narrowed** citation (moderation/security integrity + the admin actor's own third-party identity in the row; admin actions already visible by effect — e.g. suspension — need no log disclosure). Executed in ADM-D with ADM-16; manifest entry + invariant updated in the same PR | split shape |
| **AB-5** | ADM-8 sequencing | **Ratify ADM-8-early** — cycle ADM-B, immediately after the foundation cycle, ahead of members/moderation. RW-05 (handed-over groups invisible to the only actor who can act) + AC3-O8 (no enumerating contract exists — platform work either way) argue it; **TASK-INT-05 is an in-cycle precondition** (never build the view against 39/42 detritus) | ADM-8-early |
| **AB-6** | Audit cadence (bridge addendum, PROPOSED) | **Ratify as proposed:** gates are the always-on audit; bounded delta-audits of un-gated corners after big builds; **one more FULL audit after A-ADM closes, before Phase-4 cutover**; then full audits only at structural inflection points; standing rule — every audit converts findings into gates, and periodic judgment passes audit the gates themselves | ratify |
| **AB-7** | Console-routed rows (U028): Ferd shape | **Build Console-routed surfaces that make the Ferd cut (ADM-16) inside the Hub shell under a distinct admin route group** — the Hub-shell bundle is one of U028's two named options, so nothing is pre-empted; the Console-as-entity decision stays deferred (U025/U028), recorded | Hub-shell group |
| **AB-8** | The Ferd cut across the 18 ADM rows (no wave-map binding — note 1) | **In:** ADM-1..6, 8, 9, 10, 11, 12, 16, 18 (thirteen rows — the operational floor: observe, administer members and groups, moderate, audit). **Defer with dated records naming activation:** ADM-7 (bulk — after singles prove; Eid), ADM-13 (G-29 unreciprocated + Console-routed; when G-29 closes), ADM-14 (no substrate, no consumer), ADM-15 (zero flag-reading code exists — a flag manager with no flags is scaffold), ADM-17 (catalogue is stable Ferd seed; DeusEx CRUD is high-risk surface; Eid) | recommended cut |

## Deferred register — what this run deliberately does NOT build (Stefan's standing ask, 2026-07-31: restate this plainly at area close)

Five of the 18 ADM capabilities are out of scope for this run (AB-8, settled as recommended). Each carries a dated deferral naming its activation point; at area close this register is presented to Stefan verbatim-in-plain-language and each row converts into an Eid backlog entry (or gets re-scoped) per his call. **EXECUTED at the gate close (2026-08-02): all five rows CALLED — 13/14/15 as proposed; ADM-7 and ADM-17 RE-SCOPED INTO FERD (post-walk, one shared decomposition board; the [gate record](./2026-08-02-platform-ops-area-gate.md) §The deferred five is canonical — the table below is the 2026-07-31 state, kept for the record).**

| Row | Capability (plain terms) | Why deferred (2026-07-31) | When to revisit |
|---|---|---|---|
| ADM-7 | **Bulk actions** — act on many members at once (select twenty, suspend all) | Convenience layered on the single-member operations; prove the singles in practice first | Eid, once the ADM-C singles have run in anger |
| ADM-13 | **Auto-grant verification screen** — verify that new permissions auto-attached to the admin role correctly | Depends on a PC-3 surface publication that does not exist yet (G-29 unreciprocated); Console-routed | When G-29 closes; ships under the AB-7 shell |
| ADM-14 | **Platform policy settings** — versioned, reversible platform-wide configuration | No substrate and **no consumer** — nothing in the platform reads any policy today | When the first real policy consumer appears |
| ADM-15 | **Feature flags** — create, toggle, scope flags | **Zero flag-reading code exists in v2** — a flag manager with no wires attached; Console-routed | First rollout/launch need — Phase-4 cutover planning at the earliest |
| ADM-17 | **Role-template & permission-catalogue editing** — DeusEx-level CRUD on the role/permission building blocks | The catalogue is stable seed data in Ferd; an editing UI is high-risk (mistakes ripple platform-wide) with no current payoff | Eid |

## Decision board — ADM-C — SETTLED 2026-08-01 (Stefan: "go with recommended, ADM-6 full exit") — presented whole, recommendations marked

| # | Question | Ruling (as recommended) |
|---|---|---|
| **CB-1** | The queued V3 question: do suspend/reassign (group, from ADM-B) and the new member ops notify affected members? | **No new notification kinds this cycle — dated V3 deferral in both specs.** Everything is visible-by-effect on shipped surfaces (GRP-5 badge, IDN-9 render; the reassigned steward already gets `notify_role_assigned` free), and the registered-kind machinery for admin→member communication is ADM-D's mandated seam (DS-5 resolution kind, NTF-6) — a parallel sanction-communication kind one cycle early would duplicate it. Recorded exception to the tier's "state change without trigger is incomplete" sentence; activation named: the ADM-D kind family, or Eid. |
| **CB-2** | Cycle shape | **One cycle, paired FEAT-PC021 ↔ FEAT-H036; two serial schema gates** (gate 1 the member read family, gate 2 the operations family). The walk subsequently narrowed the "existing mutations need no schema work" premise — the gates stand, gate 2 grew (four re-issues + four new contracts; findings recorded, adjudicate at the decomposition nod). |
| **CB-3** | ADM-6 / ADM-18 shape — and does admin platform-exit end the account? | **Two distinct contracts, one family**, composing the MEM-5/7/8 classification (walls never reimplemented); **ADM-6 = full exit** — memberships sweep + account to terminal state (decommission), minus the self-service consent moment — and, per the derivation at decomposition, minus the F-2 erasure legs and profile scrub: erasure stays member-initiated or hard-delete's. |
| **CB-4** | The admin-action audit slices (governance §L4) | **Ride inside PC021** — every mutation writes `admin_audit_log` per-contract, dotted namespace extended (`member.*`, `platform_admin.*`); no new table; the §L4 note marks the write slices specced, the ADM-16 read surface stays ADM-D's. |
| **CB-5** | Opening hygiene block | **Both in the opener:** TASK-ADMC-00 (the two main-branch lint errors — `AdminDashboard.tsx:50`, `AccountMenu.tsx:35`, files this cycle touches anyway) + TASK-DOC-007 (the changelog backfill, re-ID'd from the colliding TASK-DOC-005). |

**Defaulted at the board (recorded, Stefan raised no objection):** DS-6 ranking stays unconsumed in Ferd (ADM-2 ships plain filters + client-side search; recorded available-not-required) · console vocabulary follows IDN-9's shipped terms (suspended = admin hold; "deactivated" stays retired) · ADM-12 grant/revoke lives on the member detail page, not a separate admins page · force-logout surfaces single-target over the array-signature primitive (the array stays unexposed — ADM-7 territory). **Console-consolidation note (Stefan's question at the settle):** all admin tasks stay in the one `/admin` home — the AB-7 shape already guarantees it; ADR-U028's woven rows (content reporting, self-service exit) are member actions, their admin halves land under `/admin`.

## Decision board — ADM-D — SETTLED 2026-08-01 (Stefan: "go with recommended") — presented whole, recommendations marked

| # | Question | Ruling (as recommended) |
|---|---|---|
| **DB-1** | Cycle shape + gates | **One cycle, paired FEAT-PC022 ↔ FEAT-H037, ONE schema gate** (the ALTER, the moderation family, the kind + producer trigger, the audit family, the AB-4 composite/manifest/invariant work). Held with red evidence + apply commands for named approval. |
| **DB-2** | Resolve model on `content_reports` | **In-place ALTER**: `resolved_by_group_id` (SET NULL) / `resolved_at` / `resolution_kind` / `resolution_note`; `status` stays the queue axis (`open`→`resolved`), outcome in `resolution_kind` (`actioned`/`dismissed`, open TEXT contract-validated); **per-report resolution** (target-grouping is the queue's render concern); no separate decisions table. |
| **DB-3** | The DS-5 kind: name, recipient, producer | Kind **`report_resolved`**, category `platform`, notifying **the reporter** (closure — NTF-6's moderation-decision leg closes here); the content owner is NOT notified (visible-by-effect, the CB-1 logic). Producer = **AFTER UPDATE trigger** on the status flip (the `notify_*` precedent, GC-8 license); payload content-free (`report_id`, `target_kind`, `resolution_kind` — no note, no admin identity). Preference suppression honored (the N-D dispatcher's word is final — no override). |
| **DB-4** | CB-1 activation: sanction-communication kinds now or Eid? | **Eid.** ADM-D ships only the resolution kind; suspend/hold comms stay visible-by-effect (IDN-9, GRP-5); no new evidence since CB-1. Both ADM-C deferral notes gained the dated pointer at this decomposition. |
| **DB-5** | ADM-10/16 read shape: RLS-direct vs contracts | **SECURITY DEFINER read contracts** (`admin_get_content_reports` + detail with live author resolution; `admin_get_audit_log` keyset-paginated with the open-namespace prefix filter) — PC020/PC021 read-family consistency, display shaping, existence-hiding. The RLS policies remain the substrate license. |
| **DB-6** | AB-4 execution vehicle | **Everything in the schema-gate PR**: the composite gains the `audit_trail` own-actor section (the split is structural — `actor_group_id = self`), the manifest entry rewritten, the W2 invariant updated — per AB-4's own text and ADR-U052 §6. |
| **DB-7** | Console shell | `/admin/moderation` (queue + detail rendering `content_snapshot` — load-bearing, content drifts) + `/admin/audit`, two dashboard cards, the H034-036 shell idiom. |
| **DB-8** | Opening hygiene | **None owed by name** — the opener lint/build check on main only; block only if it finds something. |

**Defaulted at the board (recorded, Stefan raised no objection):** task IDs TASK-ADMD-01/02 · the audit namespace extends with `moderation.*` · every mutation writes `admin_audit_log` (the CB-4 precedent) · new-function ownership follows the `admin_* → PC-4` pin with the DS-5 store-crossing recorded (the store stays DS-5's; `notify_report_resolved` registers with its `notify_*` siblings) · Mist-rule proofs ride the gate tests (reporter CASCADE covers the new columns; erased resolver anonymises via SET NULL). **Substrate hardening found at the walk, folded into the gate (recorded):** DROP `audit_log_insert_admin` (the ADR-U038 direct-caller hole — pattern (c)'s client-authored audit rows; the seven legacy sites are frozen oracle code) · re-issue `audit_log_select_admin` on `is_platform_admin()` (the PG17 ceiling class the gate-2 function re-issue already fixed at the function tier).

## Decision board — ADM-7 + ADM-17 (the Ferd re-scope) — SETTLED 2026-08-03 (Stefan: "go with recommended" — all eight rows RB-1..RB-8; defaults stand) — presented whole, recommendations marked

One board covering both re-scoped rows (the gate-close call, 2026-08-02: they share the admin plane and both benefit from what the walk surfaced — [gate record](./2026-08-02-platform-ops-area-gate.md) §The deferred five). Inputs absorbed: **Stefan's fix directive** on the stall/cache family ([walk findings](./2026-08-02-admin-live-walk-findings.md) W-5/W-7/W-8/W-9/W-10 — committed fix work, not observations), the **W-3 suspended-refusal-matrix decision** (the enforcement home is the member-facing walls, never the admin contract), and the **ADM-17 design skeleton** recorded at the gate. The area retro placed W-5 as ADM-7's selection substrate and routed the member-side four (W-7/8/9/10) to this board for placement. Settlement here is followed by decomposition to 4-ready paired specs per cycle (spec → gate → build → measure), all ahead of AB-6.

| # | Question | Recommendation | Default if unaddressed |
|---|---|---|---|
| **RB-1** | Cycle shape + sequence | **Three small cycles, in order:** (1) **HYG-A** — member-side suspension integrity (the RB-6 matrix enforcement + W-7/8/9/10) runs *first*, because bulk member-suspend (ADM-7) multiplies exactly the member-side suspension gaps the walk found; (2) **ADM-E** — ADM-7 bulk actions + the W-5 bounded members list (one paired FEAT-PC ↔ FEAT-H); (3) **ADM-F** — ADM-17 role-template editor (paired specs). All three ahead of AB-6, so the FULL audit covers everything the re-scope ships | as recommended |
| **RB-2** | ADM-7: the safe action subset + bulk mechanics | **Bulk = suspend / reactivate / force-logout only** (the reversible, non-destructive family). Excluded, stay singles: decommission, hard-delete, platform-exit, remove-from-group, grant/revoke platform admin (destructive, irreversible, or high-privilege). **Mechanics: the BFF loops the proven single contracts** with per-row outcome reporting — partial success is honest and reported per member, per-member audit rows fire by construction, and the singles' guards (last-DeusEx floor, typed refusals) hold unchanged; no set-based mutation primitives, no mutation schema gate. Members only in Ferd (the §L3 row is member-scoped; groups stay singles) | as recommended |
| **RB-3** | W-5 absorption: the bounded members list (ADM-7's selection substrate) | **Keyset pagination at the contract** (`admin_get_users` re-issue — the ADM-16 keyset precedent) + **server-side search/filter behind a debounced input**; selection is explicit and current-page only (no cross-page select-all in Ferd — the safe-subset instinct applied to selection). Discharges the B-PERF "pagination when a measurement asks" trigger — the walk was the measurement. The read re-issue is ADM-E's one schema gate | as recommended |
| **RB-4** | ADM-17: ratify the recorded design skeleton | **Ratify as recorded:** atoms code-owned — the permission *catalogue* stays pinned to the code manifest (the B-RBAC exact-count pins re-shape to "catalogue equals manifest", they don't disappear) and only role *templates* get an editor · seeded templates immutable, clone-don't-edit (the seed stays a known-good rollback anchor) · edits create versions with a default pointer, rollback = repoint · diff preview before apply (affected-principals blast radius) · self-lockout guards extend the `prevent_last_deusex_role_removal` instinct to a protected-permission set · audit rows carry the old-set → new-set diff, not just the event | ratify |
| **RB-5** | ADM-17: the propagation question (the skeleton's one open design point) | **Snapshot-now with a migration path** (the recorded low-regret default on an empty platform): template edits shape *future* group instantiations only; already-instantiated `group_roles` keep their snapshot; live by-reference propagation deferred with a dated record (re-openable at Eid when a real fleet exists) | snapshot-now |
| **RB-6** | The W-3 suspended-refusal matrix (define here; enforce member-side) | **A suspended group freezes its member-facing write plane for everyone below the admin plane, Steward included:** group-context writes refuse typed — forum posts/replies, announcements, group conversations, journey enrolment/progress within the group, invitations/joins/role changes (the finding's own axes; the decomposition contract walk enumerates the concrete write doors against live substrate). **Reads stay honest** (content + the suspended chip remain visible). **Leaving stays allowed** (exit paths are never frozen — suspension is a hold on activity, not imprisonment). Enforcement lives in the substrate behind the member-facing walls (RLS/contract status checks, ADR-U038 — never the admin contract, never BFF-only); refusals surface typed (the W-8 pattern) | as recommended |
| **RB-7** | The member-side fix family: placement + shapes | **One small cycle (HYG-A) carries all five:** the RB-6 matrix enforcement (its schema gate) · **W-7** in-session account-state revalidation (on soft-nav cadence + any 4xx write refusal re-checks the wall; the notification half stays the recorded Eid deferral, DB-4) · **W-8** typed refusals threaded to the surface (the house rule made real at the profile-save specimen) · **W-9** `hub.adminEntry` keyed by user id + cleared on auth-state change (the retro's rule: any client cache keyed by nothing is keyed by the wrong thing) · **W-10** the suspended wall's exit made explicit ("Sign out to use another account") | as recommended |
| **RB-8** | The cheap riders from the walk | **Both in:** **W-4** — grant/revoke (and sibling member-ceremony) confirms echo the unique identifier (email), killing the doppelganger mis-grant class · the members list gains the dashboard's "As of + Refresh" affordance in the RB-3 rework (the recorded parity note) | both in |

**Defaulted at the board (recorded; they stand unless Stefan objects):** cycle IDs HYG-A / ADM-E / ADM-F, task IDs to match · ADM-17 builds without ADM-13 (its §L3 ADM-13 dependency is the verification-view adjacency; the audit log + RB-4 diff-audit rows cover verification in Ferd; the G-29 activation record is untouched) · the ADM-17 substrate VERIFY-at-decomposition stands (kickoff note 5 — the contract walk runs against live PC-3 signatures, cumulative-forward) · new functions follow the `admin_* → PC-4` pin over PC-3 substrate (the ADM-D sealed-primitive/wrapper pattern if a core-to-domain crossing appears) · every mutation writes `admin_audit_log` (the CB-4 precedent; bulk writes per-member rows by construction under RB-2's loop mechanics) · ADR-U043 measurement pass at each cycle's gate (standing; the Amendment-2 dual signal applies) · schema-gate PRs held with red evidence + apply commands for named approval (the standing rule) · the Eid piles are untouched (W-11/W-12 moderation observations; sanction-communication kinds per DB-4) · COR-C lattice from first commit for anything new (classification at birth, manifest registration, trigger licenses, token + axe).

**RB-6/RB-7 AMENDMENT — the two-mode suspension model (Stefan, 2026-08-03, at the HYG-A decomposition review; supersedes RB-6's single-state matrix):** suspension splits into two admin-set hold states. **(1) `suspended` — the visible hold:** the group and its content stay visible and readable to its members; member writes refuse typed; **holders of the steward-exemption permission act fully** (the exemption keys on a permission seeded to the Steward role template, never on the role name — the products-tier rule; key name `act_during_suspension` is a named default); reduction-and-exit stays open (leave / pause / decline / cancel / withdraw). **(2) `offline` — the hard hold:** the group remains findable and labeled **"Off-line"** (label copy provisional — Stefan: "for now") wherever groups normally render, *but that's it* — content is inaccessible below the admin plane and **no one acts, stewards and exits included** (memberships stay intact; no member-facing explanation of the reason — Eid, with the sanction-communication family). Admin ceremonies move groups between the three states (active ↔ suspended ↔ offline), every transition audited. W-3's walk reproduction re-reads under the model: the steward's +84 s forum post would be *legal under the soft hold* (the exemption) and *refused under the hard hold* — the defect was the absence of a model, not the steward acting. PC023/H038 reworked in place on the held PR #388.

**NAMING + PERMISSION SETTLE (Stefan, 2026-08-03, the vocabulary board):** the states are **Active / Resting / Suspended** (the Social Gathering family; the amendment's "suspended/offline" working names retire — soft hold = `resting`, hard hold = `suspended`, `offline` never ships). Rejected columns for the record: Community Center (Open collides with joinability + the report queue; Recess/Intermission is two words in a foreign register) and Cozy Venue (Live collides with realtime; "Closed for Maintenance" collides fatally with the terminal `closed`; Quarantined lost to the already-shipped gravity of Suspended). **Resting is permission-driven:** one key **`rest_group`** covering toggle + act-during-rest, seeded to the Steward template + auto-granted to platform admins; symmetric rest/wake; suspended-touching transitions admin-only. The walk-era finding stands corrected into the model: W-3's group was in the hard state, so the steward's post was a true violation.

## Decision board — WF slotting (the HYG-A walk directives) — SETTLED 2026-08-03 (Stefan: "go with recommended" — all four rows WS-1..WS-4) — presented whole, recommendations marked

The two directives from Stefan's HYG-A walk ([findings + verdict](./2026-08-03-hyga-walk-findings.md)) await slotting, per their own routing notes ("Stefan slots it at the next planning step" / "Slotting is Stefan's call"). The findings doc's standing constraint: **AB-6 must not run before WF-2 is at least slotted** (it changes the admin plane's claimed capability set). If the recommendations stand, the sequence becomes: ADM-E → ADM-F → **ADM-G** (WF-2) → **N-E** (WF-1) → AB-6.

| # | Question | Recommendation | Default if unaddressed |
|---|---|---|---|
| **WS-1** | WF-2 (admins SHALL have full access to suspended groups): where does it build? | **Its own cycle ADM-G, after ADM-F, ahead of AB-6** — a PC023 amendment (the admin visibility arm in `get_group_detail` + admin arms across the read-quarantine family, contract AND RLS layers, door-by-door per the findings' substrate verification; schema gate) paired with the Hub surface half (the design choice — is-admin affordance arm on the member-plane surface vs a dedicated admin content view under `/admin/groups/[id]` — settles at spec time). Folding it into ADM-E/ADM-F would graft a schema-gate amendment onto cycles whose boards are already settled; building it ahead of AB-6 keeps the FULL audit covering everything the re-scope ships (the RB-1 logic) | ADM-G, after ADM-F |
| **WS-2** | WF-2 scope: the admin arm for suspended groups only, or all groups? | **Suspended-only (option a — the findings' recorded recommendation):** the stated mandate (rectify wrongdoing) operates under the hard hold, and (a) preserves "private and absent look identical" everywhere else — an admin gains sight of exactly the groups the admin plane has already acted on; (b) needs its own privacy decision. Purpose-bound and audited either way | suspended-only |
| **WS-3** | WF-1 (group invitations answer in the bell, like nominations): where does it build? | **Its own small paired cycle in the notifications family (cycle N-E, PD + H), after ADM-G, ahead of AB-6** — `invitation_received` gains `action_data` + a typed response path composing the existing accept/decline contracts (the PD014 dispatch pattern, N-B convergence for answered-elsewhere/cancelled), the bell renders Accept/Decline via the H031 renderer pattern; MyInvitations stays (two doors, one truth). A rider inside ADM-E would mix families and stretch a deliberately small cycle | N-E, after ADM-G |
| **WS-4** | The WF-1 polish note (a bell notice landing on `/groups` anchors/highlights the invitation card) | **Rides N-E** (same territory, one E2E covers both) — pull it earlier into any cycle only if it bothers a walk before then | rides N-E |

## Exit checklist — the Platform-Ops area gate (planted now)

- [ ] All 18 ADM rows `6-done` or explicitly dispositioned per AB-8 (deferred rows carry dated deferrals naming their activation point).
- [ ] The emits-well-stores-nowhere state is ended: sink live per AB-1, TASK-OBS-01 closed, G-03 de-scaffolded (V4 §§3-6 are an obligation inventory), ADR nodded if raised.
- [ ] `recordAuditEntry`'s TODO discharged for **all four** callers per AB-2; no console-only audit path remains; A-OPS naming corrected.
- [ ] AB-3 executed: the manifest function split live (or the recorded narrow fallback), unclassified-fails-red, `admin_* → PC-4` pinned; every A-ADM-born function classified at birth.
- [ ] AB-4 executed: `admin_audit_log` manifest entry rewritten (representation for own-actor rows, narrowed exemption for target rows); export-completeness invariant updated in the same PR; composite exports the new section.
- [ ] TASK-INT-05 closed **before ADM-8 ships**: fixture leak fixed red-first; DeusEx system-group membership verified clean on the live DB.
- [ ] ADR-U028 honoured: Console-routed rows shipped per AB-7 with the entity question recorded still-deferred; woven rows woven.
- [ ] ADM-11's resolution communication is a registered kind through the registry + dispatcher (no bespoke path — structurally verified).
- [ ] Every new table/function/trigger under the COR-C lattice from first commit: `memberData` classified at CREATE, manifest-registered, trigger-licensed, token + axe green, pinned vertical set untouched (or amended by test-with-reason).
- [ ] Mist-rule proof for every new actor-linked store (telemetry, audit): a Mist's rows do not survive its erase (the NB-8 lesson — prove, don't assume).
- [ ] W12 per-RPC verification **with the composition column** (GC-14): each contract verified composed into its declared consumers, not just internally gated.
- [ ] Oracle dispositioned row-by-row: the B-ADMIN-001..019 spine ported/adapted/superseded with citations (the A-NTF pattern — using a function is not testing it).
- [ ] ADR-U043 measurement pass (cold + warm, tail rule) + Stefan's live walk, both before the area retro; numbers appended to the AB-1d ledger.
- [ ] CHANGELOGs updated (root + `hub/` + platform-core as applicable — check both before calling an entry missing).
- [ ] **Cycle N-E (WF-1) executed before AB-6** (the settled WS-board sequence): FEAT-PD017 + FEAT-H042 `6-done` — invitations answer in the bell, the all-doors convergence closes the verified cancelled-invitation hole, the WS-4 landing focus ships; one E2E covers both (WS-4's own line). *(Row added 2026-08-05 at the N-E decomposition — the checklist predated the WF slotting and carried no N-E row.)*
- [ ] The post-area **FULL anatomy audit** (AB-6) run before Phase-4 cutover; findings converted to gates.
- [x] **The deferred register presented to Stefan at area close, in plain language** (ADM-7/13/14/15/17 — "what this run did not do") — **EXECUTED 2026-08-02: 13/14/15 as proposed; ADM-7 and ADM-17 re-scoped INTO Ferd** (gate record §The deferred five).

## After Platform-Ops

Phase 3 ends here. What A-ADM hands forward: the Phase-4 cutover inherits a fully-audited surface (the AB-6 FULL audit is the cutover's entry condition); Eid inherits the dated deferrals (ADM-13/14/15 per their triggers — **ADM-7 and ADM-17 were re-scoped INTO Ferd at the gate close, 2026-08-02** — plus email-channel ADR, digest/aggregation, i18n activation per TASK-I18N-01, TASK-DBT-01 test-tier `tsc` debt, and the moderation-family W-11/W-12 observations); the Console entity question (U025/U028) stays open for its own decomposition.
