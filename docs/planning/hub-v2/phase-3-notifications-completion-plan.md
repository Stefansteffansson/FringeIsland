# Phase 3 — Notifications (A-NTF) completion plan

**Status:** v6 (2026-07-28) — **THE AREA IS CLOSED. The gate passed.** All six independently-owed gate items discharged and the task sweep executed — full record in the [area gate](./2026-07-27-notifications-area-gate.md#closure-summary-2026-07-28). **The gate closed on its stated conditions, which are met. Three exit-checklist lines below remain unticked and are named rather than assumed** (see the note under the checklist) — they were not gate-closing conditions, and ticking them on the strength of "the cycle probably did it" is the locally-honest-checkbox failure this area's own retro warned about. Three of the six turned out to be **wrong rather than merely undone**: NB-8's premise was refuted by its own proof (Mists *did* hold durable rows, readable and unsilenceable), W-09 was filed against the wrong scope (it missed `stewardship_nomination` — 802 rows, the largest ask population, with the identical defect in a category the finding never named), and the 937 ms warm ceiling had a documented cause that measurement contradicted (the parked fan-out lever is now **refuted**, not deferred). Migrations `20260727120000` (anon-EXECUTE repair), `20260727180000` (Mist posture + asks split) and `20260728060000` / `20260728080000` (retiring 12 512 bootstrap self-rows and 36 961 unreachable orphan rows — `public.notifications` went 65 654 → 16 923, every survivor provably reachable). **Next: A-ADM (Platform-Ops), the sixth and last Phase-3 area**, where [`TASK-OBS-01`](../backlog/tasks/TASK-OBS-01-telemetry-sink-and-analytics-posture.md) lands at area open. Prior status (v5, 2026-07-26) — **THE AREA'S BUILD IS COMPLETE. All four cycles built and closed: N-A, N-B, N-C, N-D.** Cycle N-D (the last) shipped NTF-10 — per-category × per-channel preferences plus the shared suppression dispatcher — as [FEAT-PD016](../../platform/domain/features/FEAT-PD016-notification-preference-contracts-and-shared-suppression-dispatcher.md) ↔ [FEAT-H033](../../products/hub/features/FEAT-H033-notification-preferences-and-operator-nudge-console.md), both `6-done` (migration `20260726120000`; PRs #294/#295/#296). **The preference-home tension recorded below is SETTLED**: it turned out to be a **four-way** disagreement, not the three that was filed — `communication.md` contradicted *itself*, claiming DS-5 tables at `:44` while naming PC-2 at `:126`. Ruled **DS-5 for both the table and the contract**; consent unmoved; all four lines amended in the same pass ([full record](./2026-07-26-ntf-n-d-preference-home-adjudication-and-board.md)). N-D also closed N-C's three deferrals and cleared `TASK-INT-02`, so the suite carries **no known reds** (integration 690/690, unit 982/982, E2E 89/89). **What remains for the area is the GATE, not the build** — see the area-gate items in the closing session bridge: the owed ADR-U043 measurements (N-C's `/groups` before/after and N-D's new page), Stefan's live walk, the ADR-U039 acceptance question, U049 §8 Q1 adapter ownership, the NB-8 Mist-posture proof, the email-deferral recording, the DS-5 spec advance, W12 per-RPC verification, and the missing N-A/N-B CHANGELOG entries. Prior status (v4, 2026-07-24) — **Cycle N-A BUILT + CLOSED; Cycle N-B DECOMPOSED to 4-ready**: [FEAT-PD014](../../platform/domain/features/FEAT-PD014-actionable-notification-dispatch-and-acting-fanout.md) ↔ [FEAT-H031](../../products/hub/features/FEAT-H031-notification-typed-actions.md), captured by [ADR-U051](../../architecture/decisions/ADR-U051-actionable-notification-typed-response-framework.md) (Proposed, rides the N-B schema gate). **The N-B scope-lock's premise that the group-of-groups acting-invitation was already an actionable notification was refuted by disk verification** (no `acting_invitation` kind; the PD013 FK forbids one; `respond_to_group_invitation` is membership-keyed and notification-blind; the cited `pc015:608` is actually a `nominate_steward` insert). **Stefan re-decided (2026-07-24): build the acting-invitation as a real actionable notification** — fanned out at invite-time to the invited group's `act_as_group` permission-holders (ADR-U041), first-answer-wins, "answered by [name]" recorded durably on the notification rows (ADR-U051 Option A), on a data-driven/extensible response engine. Prior status (v3, 2026-07-23) — **Cycle N-A DECOMPOSED to 4-ready**: [FEAT-PD013](../../platform/domain/features/FEAT-PD013-notification-routing-contracts-and-category-registry.md) (registries + read/serve contracts + export section) ↔ [FEAT-H030](../../products/hub/features/FEAT-H030-notification-bell-and-inbox.md) (bell + dropdown + inbox over BFF routes); payload walk done at decomposition (recorded in H030; `action_data`/`action_taken_at` deliberately excluded from the N-A payload — N-B extends). Both feature-inventory summaries + READMEs updated in the same commit. Tasks TASK-NA-01..05. Next: red-first build, held at the schema gate. **Recorded for N-D decomposition: the preference-home three-way tension** — V3 §6 says preferences live in PC-2, Hub §L3 NTF-10 says PC-4, the DS-5 spec §Entities says DS-5 tables; adjudicate there, not before. Prior status (v2, same day): **board SETTLED** (presented whole; NB-1..6 + NB-8 adopted as recommended; **NB-7 overridden by Stefan — drop the legacy realtime publication in N-C**, see the board row). Prior status (v1, same day): board OPEN, kickoff sweep complete. A-NTF is the fifth Phase-3 area (Identity, Groups, Journeys, Communication all closed). It starts on the corrected Internal-API pattern (ADR-U047/U048) and inherits the C-C real-time conventions (U039).
**Provenance:** four-scout terrain sweep 2026-07-23 (platform/DS-5+ADRs · substrate · surface/oracle · seams-and-dues-and-V3), load-bearing facts disk-verified; two cumulative-forward corrections folded in (see "Corrections" below). Canonical-wins flags noted inline.

---

## Where this picks up

Communication (A-COM) closed 2026-07-22 (gate PASS with riders dispositioned — bridge `../sessions/2026-07-22_03_-_A-COM-RETRO-COMMITTED-AREA-FULLY-CLOSED-A-NTF-NEXT.md`). A-NTF inherits, settled-by-canon (recorded, not asked):

- **ADR-U048 — delivery/routing split.** `public.notifications` + its delivery mechanics are the **Notifications-vertical delivery substrate**; any tier writing a delivery row is obligation-fulfilment, **not** a boundary crossing (ADR-U048:37). DS-5 owns only the **routing layer above** — routing rules, recipient preferences, digest/aggregation, channel fan-out (ADR-U048:38-39). `notifications` stays **out** of `DS_TABLES` by design.
- **ADR-U039 — real-time doctrine.** v2 is **ping-then-fetch over private broadcast channels, hint-never-authority, durable-row-first** (ADR-U039:22-26,31). The bell channel `account:<auth_uid>:notifications` is already reserved forward-looking (`docs/products/hub/SPECIFICATION.md:38,:99`). NTF-2 (bell) and NTF-9 (reconnect) build on the C-C conventions — no fresh doctrine needed.
- **ADR-U049 — announcements.** Durable DS-5 `announcements` home (built in C-D) + **per-recipient V3 delivery rows fanned out at send**; the bell badges off delivery rows; recipients resolved once at send-time (ADR-U049:41,43,47). The **outbound channel adapter that consumes delivery rows and never re-resolves recipients is seamed to A-NTF** (ADR-U049:16,43; DS-5 §8 **Q1** — `docs/platform/domain/communication.md:97`).
- **ADR-U040 — no platform email to strangers (Accepted 2026-07-05, Option C).** Supersedes MEM-2 "invite by email"; the outward path is a **FIM-shared referral link**, not a platform-sent email. **Whether the platform ever sends email at all is explicitly a separate, later, own-ADR decision — Option C does not require it.** → the "D4 email due" the A-COM plan forward-pointed here is **superseded, not inherited** (see Corrections).
- **The conformance gate** already auto-allows `/^ds\d+_lifecycle_/` and holds `notifications` out of `DS_TABLES`. When the DS-5 notification-routing contract layer lands, `DS_OWNED_ALLOWLIST` gains those functions; `notifications` itself stays out (U048).
- **W12 + ADR-U043** — per-RPC gate verification and the cold/warm performance pass are in the area-gate DoD from this area onward.

## Corrections to carried-forward premises (canonical-wins)

1. **"D4 = MEM-2 outbound email dispatch comes due at A-NTF"** (A-COM plan `phase-3-communication-completion-plan.md:115`, Groups plan seam) — **SUPERSEDED by ADR-U040.** `invite_by_email` is retired; email dispatch is not required in Ferd and needs its own ADR. A-NTF ships **in-app channel only**; email is a recorded forward-deferral, not a build. (Board NB-2.)
2. **"DS-5 §8 Q7 covers the notifications realtime publication"** (A-COM plan phrasing) — **false.** DS-5 §8 holds only Q1-Q6 (`communication.md:95-102`); the announcement-adapter seam is **§8 Q1**, and the legacy-publication disposition is already decided in **ADR-U039:31** (serves the legacy app until Phase-4 cutover), not an open DS-5 question. (Board NB-7.)
3. **"~7 trigger-generated notification types"** (FERD capability map row 72) — **stale.** ~19 distinct `type` strings exist today across triggers/RPCs (rebuild + sprint2/3/4 + pc013/pc014 + pd002/pd004 + c-d). The category-catalog registry (NB-4) must fold all of them.
4. **The generic `handle_notification_action` dispatcher was DROPPED** in pc014 (`20260705072252_feat_pc014_leadership_transfer_closure_contracts.sql:947`), replaced by the **dedicated** `respond_to_stewardship_nomination`. v2's live pattern is **dedicated-handler-per-action**, not a generic dispatcher — NTF-6 must be re-derived against it (NB-1).

## Substrate audit at kickoff (verified 2026-07-23, migrations + seeds, cumulative-forward)

`public.notifications` born whole in the D15 rebuild (`20260222000000_rebuild_universal_group_pattern.sql:214-225`: `id`, `recipient_group_id`, `type`, `title`, `body`, `payload` jsonb, `group_id`, `is_read`, `read_at`, `created_at`); one post-rebuild ALTER (`20260228125730_sprint3_smart_notifications.sql:14-32`: +`action_type`, `action_data`, `action_taken`, `action_taken_at`, `expires_at`, a `notifications_action_consistency` CHECK, a pending-actions partial index). RLS `select/update/delete _own` keyed on `get_current_personal_group_id()` (rebuild:1829-1841). Base trigger fns re-defined by the COR-A inversion migration.

| Cap | Capability (compressed) | Substrate | Oracle | Cycle |
|---|---|---|---|---|
| NTF-1 | Receive/render passive notifications | HAVE (table + ~19 passive types) | STRONG (B-COMM-001, B-NOTIF-001, B-ADMIN-011) | N-A |
| NTF-2 | Bell + unread count | HAVE (`is_read` + unread partial index; **no count RPC**) | STRONG (B-COMM-003; bell render untested) | N-A |
| NTF-3 | Inbox / history surface | HAVE data (RLS select + index; **no fetch RPC; v1 had only a 15-item bell dropdown, no history page**) | PARTIAL | N-A |
| NTF-7 | Per-notification read state | HAVE (`is_read`/`read_at` + update RLS) | STRONG (B-COMM-002/003) | N-A |
| NTF-4 | Smart (actionable) notifications | HAVE (sprint3 action cols + CHECK + pending index) | STRONG (B-NOTIF-001) | N-B |
| NTF-5 | Typed-action UI (Accept/Decline, +types) | HAVE backend; **UI new** (v1 rendered inline in the bell dropdown, no component test) | PARTIAL | N-B |
| NTF-6 | Submit response + server-side dispatch | **REBUILD** (generic handler DROPPED; dedicated per-action handlers exist — NB-1) | STRONG contract (B-NOTIF-003) | N-B |
| NTF-8 | Lazy-resolve expired smart notifs on view | PARTIAL (`expires_at` + reject-on-action only; **no sweep / mark-expired / on-view mutation**) | PARTIAL | N-B |
| NTF-9 | Reconcile missed on reconnect | PARTIAL (realtime channel present; **no server reconciliation primitive**; v1 never websocket-tested) | SILENT | N-C |
| NTF-10 | Per-category / per-channel preferences | **NONE** (no prefs table/column/seed anywhere) | SILENT | N-D |

**Genuinely-new design (no substrate):** the **category-catalog registry** (replace free-text `type`; NB-4), **NTF-10 preferences** (table + enforcement), the **NTF-8 lazy-expiry sweep/mark**, the **NTF-9 reconnect reconciliation** primitive, the **NTF-6 dispatch re-derivation**, and explicit **unread-count / inbox-fetch RPCs** (today RLS-select only).

**Oracle: STRONG for the passive + smart-contract spine, SILENT on the two design-not-port capabilities.** B-COMM-001..003 + B-NOTIF-001/003 + B-ADMIN-011 live in `hub-legacy/tests/integration/communication/` (`notifications.test.ts`, `smart-notifications.test.ts`, `admin-notification-send.test.ts`) and port the spine (trigger-driven passive delivery, own-only RLS, read-state + accurate unread count, smart-schema consistency, action-handler rejection of other's/passive/already-actioned/expired). **Two silences, design-not-port:** reconnect reconciliation (NTF-9 — v1 leaned on a `postgres_changes` subscription U039 retires; inventory L148 "real-time push: NONE") and per-category/channel preferences (NTF-10 — no table/test/UI anywhere).

## V3 obligations A-NTF must fulfil (canonical: `docs/verticals/notifications/SPECIFICATION.md §6`)

- **Shared dispatcher** — a central point that accepts triggers from every layer and applies preference / consent / suppression **centrally** (vertical levies, DS-5 routes, PC-1 transports). Scope-in-Ferd is NB-5.
- **Category catalog** — every trigger declares a category from a **data-driven registry (not a sealed enum)**, each carrying a lawful basis (transactional vs consent-required). Today's gap: `notifications.type` is free TEXT, no CHECK — "preferences cannot suppress by category yet" (V3 §3). NB-4.
- **Per-category / per-channel preferences** — **DS-5-owned (table *and* get/set contract), adjudicated 2026-07-26 at the N-D decomposition (board row ND-1)**; surfaces read, never own (→ NTF-10). *Superseded reading: this line previously said "in Core (PC-2/PC-4) … external dep PC-4 per `SPECIFICATION.md:366`". A preference row FK-enforces against DS-5's `notification_categories`, so a Core home would point that FK Core → Domain, which `docs/platform/CLAUDE.md:38` forbids. **Consent is unmoved** — PC-2 substrate / PC-4 contract, ADR-U034 + G-35. Realized in [FEAT-PD016](../../platform/domain/features/FEAT-PD016-notification-preference-contracts-and-shared-suppression-dispatcher.md); full record in [`2026-07-26-ntf-n-d-preference-home-adjudication-and-board.md`](./2026-07-26-ntf-n-d-preference-home-adjudication-and-board.md).*
- **Mist rule** — no email, no durable state; in-app in-session only. NB-8.
- **Delivery outcomes = V4 (Observability) events; no silent drop.** External payloads content-minimised.
- **Channels** — in-app (inbox/bell/badge/realtime) now; email (transactional + digest) **deferred per ADR-U040** (NB-2). CLAUDE.md is a stub (no rules beyond the verticals tier).

## The cycle sequence (foundation-first, paired-platform-first — mirrors A-COM)

> **N-A BUILT + CLOSED 2026-07-23** — FEAT-PD013 + FEAT-H030 both `6-done`. Schema gate #271 nodded; PD013 16/16 integration green; H030 24 unit + a real-invitation E2E (bell→inbox, read-state survives reload) green; full unit suite 915/915; `next build` green. Fleet due discharged: two pre-existing A-COM tombstone-copy assertions realigned (found-not-caused, commit `00c0010`); ES256 + a profile fleet-flake fenced (pass in isolation). The category registry folded all ~19 kinds into 6 categories; `notifications.type` FK-enforced; v1 UPDATE/DELETE policies dropped. Bridge: `../sessions/2026-07-23_02_-_A-NTF-N-A-BUILT-PASSIVE-NOTIFICATIONS-LIVE.md`.

- **N-A — Passive delivery core + category registry (the area's walking skeleton).** NTF-1/2/3/7. Realize the DS-5 **notification-routing contracts** (list / unread-count / mark-read / mark-all-read) over the delivery substrate; build the **category-catalog registry** (NB-4) folding the ~19 existing type strings, each tagged transactional/consent-required. Paired **FEAT-PD### (DS-5 notification contracts) ↔ FEAT-H### (notification surface: bell + unread badge + inbox/history page)**. Fetch-based (no socket work yet, per A-COM's C-A/C-B precedent). Riders: conformance-gate `DS_OWNED_ALLOWLIST` += notification RPCs (`notifications` stays out of `DS_TABLES`); DS-5 spec begins its notification-routing realisation.
- **N-B — Smart (actionable) notifications + dispatch + expiry.** NTF-4/5/6/8. Typed-action UI (Accept/Decline + acknowledge + extensible action registry, no sealed enum); **NTF-6 re-derived per NB-1** — the action submit records `action_taken` and thin-dispatches to the **already-existing dedicated handlers** (`accept_group_invitation`/`decline_group_invitation` — pc012:522,562; `respond_to_stewardship_nomination` — pc014/cor-a; `submit_content_report` — c-d:600); NTF-8 lazy-expiry-on-view (mark expired at read-time, hide action buttons). Paired **[FEAT-PD014](../../platform/domain/features/FEAT-PD014-actionable-notification-dispatch-and-acting-fanout.md) ↔ [FEAT-H031](../../products/hub/features/FEAT-H031-notification-typed-actions.md)** (decomposed to 4-ready 2026-07-24; framework in [ADR-U051](../../architecture/decisions/ADR-U051-actionable-notification-typed-response-framework.md)).
  - **N-B — BUILT + CLOSED 2026-07-24.** Both halves `6-done`: FEAT-PD014 merged at the schema gate (PR #276, migration applied + log repaired; 13/13 integration, conformance 12/12) and FEAT-H031 merged as PR #277 (940/940 unit, `next build` green, E2E green). [ADR-U051](../../architecture/decisions/ADR-U051-actionable-notification-typed-response-framework.md) **Accepted** at this close. E2E: the new `notification-actions.spec.ts` (two-holder acting fan-out → first answer wins → co-leader reads "Answered by [name]"), plus `group-of-groups.spec.ts` and `leadership-transfer.spec.ts` adapted to the fold (**both** nomination journeys moved to the bell — the second was an unflagged red found by sweeping for the retired testids). **Two acceptance criteria were found unmet at the close and fixed red-first:** STORY-1 AC3 (a failed dispatch rolled back but swallowed the reason) and STORY-2 AC1 (the "Respond by <date>" window was lost with the retired panel). **Carried follow-up:** the `/api/me/overview` bundle still computes and ships a `nominations` slice that no surface consumes now that `PendingNominations` is deleted — dead weight on the `/groups` first paint, scoped out of the N-B diff.
  - **N-B SCOPE — LOCKED 2026-07-24, then CORRECTED + EXPANDED 2026-07-24 (disk verification + Stefan re-decision); DECOMPOSED to 4-ready:**
    1. **Schema-gate touch:** extend `get_own_notifications` to carry `action_data` (N-A withheld `action_data`/`action_taken_at`; the typed-action UI needs `action_data` — for acting, the `membership_id` to dispatch; for the render, the group context). Verified: `action_type`/`action_taken`/`expires_at` are **already** in the N-A payload — only `action_data` is added; `action_taken_at` stays withheld.
    2. **CORRECTED — the acting-invitation was NOT an actionable notification.** The lock assumed `stewardship_nomination` **and** the acting-invitation were both `action_type`-bearing; disk verification refuted the acting half — there is **no `acting_invitation` kind** (the PD013 FK forbids one), `respond_to_group_invitation(p_membership_id, p_accept)` is membership-keyed and never touches `notifications`, and the cited `pc015:608` is actually inside `nominate_steward` (a *nomination* insert). **Stefan re-decided (2026-07-24): build the acting-invitation as a real actionable notification** — a new `acting_invitation` kind fanned out at invite-time (extending the invited-insert delivery trigger) to the invited group's `act_as_group` **permission-holders** (ADR-U041; never the "Steward" role name), one notification per holder; **first-answer-wins** convergence records the outcome + the resolver's display name **durably on the notification rows** (ADR-U051 Option A — survives the decline that deletes the membership), so co-leaders see "Answered by [name]". `stewardship_nomination` stays single-recipient (its existing `respond_to_stewardship_nomination` + `/api/notifications/[id]/nomination-response` route reused). NTF-5 renders response buttons **data-driven from `action_type`** (extensible beyond accept/decline — Stefan's ask), each behind `ConfirmModal`; NTF-6 thin-dispatches to the dedicated handlers (nomination route reused; a new `/api/notifications/[id]/acting-response` → the new DS-5 `respond_to_acting_invitation` wrapping the untouched Core handler), **no generic dispatcher** (NB-1). Framework captured in **[ADR-U051](../../architecture/decisions/ADR-U051-actionable-notification-typed-response-framework.md)**.
    3. **Close the D8 re-home seam:** **retire the bespoke `PendingNominations` section** above `/groups` (its component + route **docstrings** name A-NTF as the re-home target — *not* the rendered `<h2>` "You are nominated to lead", corrected) — the inbox/bell becomes its home. **Fold the acting-invitation response affordance** off `GroupMembershipsPanel` (read-only invited status remains).
    4. **Out of scope (left bespoke):** group **invitations** stay on the `my-invitations` panel (`/groups`) — `invitation_received` is a *passive* notification, richer in-context, never flagged for re-homing. N-B does not make personal invitations actionable-in-bell.
    5. Reconcile: acting in the inbox and the surviving read-only `GroupMembershipsPanel` status must stay consistent on re-fetch (no realtime until N-C — refresh-based).
- **N-C — Real-time hint + reconnect reconciliation + announcement in-app adapter.** NTF-9. Ping-then-fetch over the private `account:<auth_uid>:notifications` broadcast per U039 (bell goes live); reconnect reconciliation (re-fetch unread on visibility/reconnect — a server primitive the SILENT oracle needs fresh tests for); render the **announcement `announcement`-type delivery rows** in the bell/inbox (the U049 §8 Q1 in-app adapter — NB-3). Channel taxonomy fixed at decomposition inside U039 rails. Rider: **drop `notifications` from `supabase_realtime`** in this cycle's migration set (NB-7 override — replace-then-remove; the publication ends this cycle empty). **Rider carried from N-B (Stefan, 2026-07-24): retire the orphaned `nominations` slice from the `/api/me/overview` bundle** — N-B deleted `PendingNominations`, its only consumer, so the bundle still computes `get_my_pending_nominations` on every `/groups` first paint and ships a slice nothing reads (`fetchMyNominations` has no production caller left). Drop the slice from the bundle route + `overview-client` + their tests, and decide in the same pass whether `/api/me/nominations` and `fetchMyNominations` retire with it. Dead weight on a first-paint read — the ADR-U042/U043 posture wants it gone.
  - **N-C SCOPE — CORRECTED + DECOMPOSED to 4-ready 2026-07-25.** A live-DB substrate audit ([`2026-07-25-ntf-n-c-substrate-audit.md`](./2026-07-25-ntf-n-c-substrate-audit.md)) found **three of the four planned build items already realized**, so this cycle is materially smaller than the bullet above assumes. Paired [FEAT-PD015](../../platform/domain/features/FEAT-PD015-notification-realtime-hint-and-reconnect-reconciliation.md) ↔ [FEAT-H032](../../products/hub/features/FEAT-H032-live-notification-bell-and-reconnect-reconciliation.md). Four corrections to the premises above:
    1. **The realtime substrate is already built, on both sides.** A-COM C-C shipped `ds5_emit_hint` (the one thin non-fatal `realtime.send(private => TRUE)` helper, verified deployed) plus three `realtime.messages` receive policies; and `hub/lib/realtime/manager.ts` (FEAT-H027) states in its own docstring that *"the notification bell joins at A-NTF by calling `registerTenant`, with **no manager edit**"* — including the `onStatus(status, rejoin)` hook reconnect reconciliation needs. N-C therefore builds **no** emit helper and **no** client channel abstraction: one receive policy (in the `20260704075549` initplan-wrapped form, not the original PC009 shape), one emit site, one tenant registration.
    2. **NB-3 is already realized end-to-end — nothing to build.** Verified live: `announcement` is a registered kind under category `platform`; the C-D rider `20260720203000` populated `body` (NOT NULL — 5,501 of 5,501 rows non-null) and its own header says *"the delivery row is what the A-NTF bell will render… exactly like `journey_completed` rows"*; `get_own_notifications` joins `notification_kinds` and returns the category; `NotificationItem` renders kind-agnostically with a `?? Bell` fallback. **Downgraded from a build item to verify-and-record** (the NB-8 shape). The §8 Q1 adapter-ownership answer is still owed.
    3. **"A server primitive the SILENT oracle needs fresh tests for" — there is no new primitive.** `get_own_unread_notification_count()` and `get_own_notifications(...)` already suffice; reconciliation is a *client* behaviour re-reading them at the right moments (reconnect, visibility, focus), which is exactly ADR-U039:25. What is genuinely missing is the **tests** — FEAT-PD015 STORY-4 and FEAT-H032 STORY-2 own them.
    4. **The emit site was the cycle's real open question, and it is not where the plan assumed.** There is **no trigger on `public.notifications`** (verified live); the ~38 `INSERT INTO public.notifications` sites span 11 migrations, with delivery triggers living on *source* tables. Settled: **one `AFTER INSERT` trigger on `public.notifications`**, so every writer — legacy and future — is caught by construction.
  - **N-C DECISIONS — SETTLED 2026-07-25 (Stefan).** **The platform-wide-announcement nudge is an operator toggle, defaulting off** — shipped as data (`ds5_config`, following the established `pc2_config` key/value pattern) seeded `realtime_hint_platform_announcements = 'false'`. Community-scoped announcements nudge normally; only the send-to-everybody path is suppressed, because it is the sole path whose cost scales with **headcount** rather than activity, and nobody waits on a platform announcement. This discharges the ADR-U039:46 fan-out budget against a **measured** number: the largest single observed announcement send fanned out to **857 recipients** (community-scoped), against a reachable FIM population of **1,274**. The toggle's **admin UI is N-D's**, alongside the preferences surface and the shared dispatcher that already owns suppression (NB-5) — N-C ships the setting and the lookup, no admin surface, and **no** general per-category nudge switch (that is N-D's too). Fail-quiet on an unreadable policy, so a misconfiguration cannot produce a headcount-sized burst.
  - **N-C — two questions put to Stefan and left unanswered; conservative defaults taken, both reversible.** (a) **The nominations rider cuts to the slice, the adoption in `overview-client`, and the now-unreachable `client.ts` trio** (`adoptMyNominationsRead` / `fetchMyNominations` / `requestMyNominations` + the `adoptedNominations` state) — confirmed dead, `fetchMyNominations` has **zero callers** and its deliberate `guarded.catch(() => {})` is why it failed silently. **`/api/me/nominations` is left intact**: it is FEAT-H017-owned (MEM-7 STORY-2) and ADR-U042 guardrail 3 keeps standalone routes canonical, so retiring it here would edit another feature's spec from outside this cycle's ownership — filed as a follow-up against FEAT-H017 instead. (b) **ADR-U039 is left untouched** though it is still `Status: Proposed` after three realizations and its §31 "serves the legacy app… until Phase-4 cutover" rationale is voided by NB-7; ADR edits are a fuller-auto carve-out, so N-C only *records* the supersession and the promotion to Accepted waits for an explicit call — naturally at the area gate, which already carries the NB-7 disposition.
- **N-D — Preferences + shared dispatcher (the genuinely-new cycle). BUILT + CLOSED 2026-07-26.** NTF-10 (per-category/per-channel preferences, **DS-5-persisted** per the ND-1 adjudication — *not* "via PC-4/Core" as this line originally read; surface reads) + the **shared dispatcher** applying category+channel suppression centrally (NB-5 scope), realized as one `BEFORE INSERT` trigger on `public.notifications` catching all ~38 writers by construction. Depends on the N-A category registry. **Correction to this line's original dependency claim:** it named "IDN-7's preference-persistence pattern", which does not exist — IDN-7's realized half is an append-only *consent ledger*, and its preference-shaped half is **G-34, unbuilt**. What N-D reused is IDN-7's **contract idiom** (own-subject SECURITY DEFINER get/set, typed refusals, own-rows-only RLS, private BFF route), never its storage. [FEAT-PD016](../../platform/domain/features/FEAT-PD016-notification-preference-contracts-and-shared-suppression-dispatcher.md) ↔ [FEAT-H033](../../products/hub/features/FEAT-H033-notification-preferences-and-operator-nudge-console.md), both `6-done`; migration `20260726120000`; PRs #294 (decomposition) / #295 (build, schema gate) / #296 (close). Also closed N-C's three deferrals: the nudge-toggle admin UI **with a live cost line**, the general per-category nudge switch, and the shared-topic optimisation — **deferred to Eid because its saving is currently zero** (the platform nudge defaults off, so the expensive path is not being paid).

**Area gate absorbs the standing dues** (no separate cycle): legacy realtime-publication disposition (NB-7), the email-deferral recording (NB-2), the DS-5 spec advance (notification-routing realised; digest/feeds recorded still-forward), the V3 vertical reconcile, W12 per-RPC verification, and the ADR-U043 + live-walk pass.

**DS-5 realisation stays partial by design:** A-NTF realises the **notification-routing sliver** (list/mark-read/preferences/announcement in-app adapter). Digest/aggregation and outbound channel adapters (email/push) stay **forward** (Eid+); the DS-5 feature-inventory summary records the split honestly.

## Design sessions

One likely: **notification category-catalog + preference/dispatcher model** (before N-A decomposition; ADR candidate). It firms the data-driven category registry (lawful-basis per category), the preference shape (per-category × per-channel), and the dispatcher's Ferd scope. The NTF-6 dispatch model (NB-1) can fold into the same session. Real-time needs no session — U039 is the doctrine; taxonomy is decomposition-time.

## Decision board — SETTLED 2026-07-23 (NB-1..6/NB-8 as recommended; NB-7 overridden)

Settled by canon, recorded not asked: U048 delivery/routing split · U039 realtime doctrine (ping-then-fetch, `notifications` out of `DS_TABLES`) · U049 announcements durable-home + delivery-row fan-out · U040 no-platform-email-to-strangers · W12 + ADR-U043 gates in force.

| # | Question | Recommendation | Default if unaddressed |
|---|---|---|---|
| NB-1 | NTF-6 dispatch model — generic dispatcher (rebuild the dropped `handle_notification_action`) vs dedicated-handler-per-action | **Thin-dispatch to the existing dedicated handlers.** The generic handler was deliberately DROPPED (pc014:947); all three NTF-6 targets already exist as dedicated RPCs; matches ADR-U047's per-action direction. The action UI calls the existing RPC (`accept_group_invitation` / `respond_to_stewardship_nomination` / `submit_content_report`) and the notification row records `action_taken`. `action_type`+`action_data` name which handler | thin-dispatch |
| NB-2 | Email channel in Ferd | **In-app only; email explicitly deferred (record D4 supersession).** Per ADR-U040 email dispatch is not required and needs its own ADR; no email substrate exists (`lib/email/send.ts` is a `console.log` sim, no vendor dep). Build nothing email-shaped; record the deferral in the V3/DS-5 specs | in-app only |
| NB-3 | Announcement outbound adapter scope (U049 §8 Q1) | **Build the in-app path only** — render `announcement`-type delivery rows in the bell/inbox (rows already fan out from C-D; bell already badges off delivery rows). External-channel (email/push) adapters deferred with email (NB-2). Settle adapter-ownership (DS-5 code vs vertical/PC-1 plumbing) at N-C decomposition | in-app only |
| NB-4 | Category-catalog registry | **Build in N-A** — a data-driven category registry (no sealed enum) folding the ~19 existing `type` strings, each tagged transactional/consent-required; add the CHECK/FK so preferences can suppress by category. V3-mandated; unblocks NTF-10 | build in N-A |
| NB-5 | Shared-dispatcher scope in Ferd | **Minimal** — category+channel preference suppression only (what NTF-10 needs). Quiet-hours, frequency-caps, and digest are **not** in NTF-1..10; forward to Eid+ and record the seam. Full dispatcher is over-build for Ferd | minimal |
| NB-6 | Digest / aggregation | **Forward (Eid+)** — DS-5 owns it per U048 but it is absent from the Hub §L3 NTF rows; record the split in the DS-5 summary | forward |
| NB-7 | Legacy `notifications` realtime publication membership | ~~Hold to Phase-4 per ADR-U039:31~~ **OVERRIDDEN (Stefan, 2026-07-23): drop now, in N-C.** No one runs v1 (pre-launch, oracle-only); the oracle is not websocket-tested (inventory L148 — zero test breakage); deferring plants a forgettable due. This *early-executes* U039:31's own Phase-4 disposition — the "serves the legacy app" rationale is void with no v1 users; doctrine untouched, no ADR amendment needed. The DROP rides N-C's migration set (replace-then-remove: v2 bell live over broadcast in the same gate), emptying `supabase_realtime` | — settled |
| NB-8 | Mist notification posture | **Verify-and-record** (as A-COM's CB-1) — Mists get in-app in-session notifications only, no durable rows, no email (V3 Mist rule). Adversarial proof that the delivery/recipient path structurally excludes Mist durable rows + spec recording, not a scrub build | FIM-durable only |

## Exit checklist — the Notifications area gate (planted now)

- [x] All 10 NTF rows `6-done` or explicitly dispositioned on the board.
- [x] Category-catalog registry live; all ~19 legacy `type` strings folded and categorised (transactional vs consent-required); free-text `type` constrained.
- [x] NTF-6 wired to the dedicated handlers (adversarial direct-call tests per handler); no generic dispatcher reintroduced unless NB-1 is overridden. **Confirmed 2026-07-28 — it was a location miss, and NB-1's "three" was itself wrong.** No generic dispatcher exists: `handle_notification_action` survives only in historical migrations, dropped on purpose at `20260705072252` (FEAT-PC014). What shipped is **two in-row dedicated handlers** — `stewardship_nomination` → `respond_to_stewardship_nomination` and `acting_invitation` → `respond_to_acting_invitation` (`hub/lib/notifications/client.ts:102-105`, `DISPATCH_SEGMENTS`) — plus **one out-of-row answering surface**, `invitation_received` → `/groups` (`ANSWER_PATHS`, `:126-128`, the W-04 fix; a kind answerable in the row needs no entry). NB-1's third named target, `submit_content_report`, is not a notification response at all and has no actionable kind — the board named it before the build found the shape. Adversarial direct-call tests exist for every one, in the suite that owns each handler rather than in `actionable-notifications.test.ts`: nomination at `stewardship-succession.test.ts:629-681` (another's row P0002, expired, already-answered, stale) plus `:733-786` (anon EXECUTE, hand-crafted `action_data`); acting at `actionable-notifications.test.ts:352-390`; invitation at `invitation-contracts.test.ts:868-924`; report at `window-and-report-contracts.test.ts:486-519`.
- [x] Announcement `announcement`-type delivery rows render in the bell/inbox (U049 §8 Q1 in-app adapter); external channel deferral recorded. **Found already realized at the N-C audit (2026-07-25) — this is a verify-and-record item, not a build item** (registered kind under category `platform`; C-D rider populated `body`; the surface renders kind-agnostically — all verified on the live DB). What remains owed here is the **§8 Q1 adapter-ownership answer** (DS-5 code vs vertical/PC-1 plumbing) and the recorded external-channel deferral, not code.
- [x] NTF-10 preferences live and enforced by the shared dispatcher (category+channel suppression); **persisted at DS-5** (the ND-1 adjudication — this line originally read "via PC-4/Core"); surface reads only. **Done at N-D (2026-07-26):** `notification_preferences` + `notification_channels` + `ds5_may_deliver()` + one `BEFORE INSERT` dispatcher on `public.notifications`; `member_suppressible` carries suppressibility on its own axis because all six categories are `transactional` and reading that as "lawfully compelled" would have made every switch inoperable. The email channel is **stored but non-delivering**, so the preference binds when email ships without the surface promising it now.
- [x] Email deferral recorded in V3 + DS-5 specs (ADR-U040); no email substrate shipped. **Done 2026-07-27:** recorded in [V3 §3](../../verticals/notifications/SPECIFICATION.md) and [DS-5 §3](../../platform/domain/communication.md) — in-app is the only delivering channel, `email` is registered but stored-not-delivering (so a preference binds the day delivery arrives), and dispatch needs its own ADR first. **"No email substrate shipped" verified literally:** `lib/email/send.ts` and its one open-coded caller `app/api/invitations/send-email` exist **only in `hub-legacy/`**, the frozen v1 oracle awaiting Phase-4 deletion — Hub v2 has no `lib/email/`, no send-route and no email vendor dependency. Both specs described that seam without saying where it lived; they now say.
- [x] Legacy `notifications` realtime publication DROPPED (NB-7 override, executed in N-C); `supabase_realtime` publication verified empty on the live DB.
- [x] Conformance gate updated: DS-5 notification RPCs registered; `notifications` stays out of `DS_TABLES` (U048). **Confirmed 2026-07-28 — and the line named a structure that had already been retired.** `DS_OWNED_ALLOWLIST` no longer exists in code: COR-B W1 replaced the flat allowlist with per-service resolution from `supabase/ownership.manifest.json` (`functionOwner()` / `dsTables()`), and the conformance test records the supersession itself at `internal-api-conformance.test.ts:33-36` — *"the history those arrays carried now lives in the manifest's `note` fields."* The notification RPCs are **explicitly registered in the manifest's `DS-5` block** (`ownership.manifest.json:90-143`): `get_own_notifications`, `get_own_unread_notification_count`, `mark_notification_read`, `mark_all_notifications_read`, `get_own_notifications_export`, `respond_to_acting_invitation`, `notify_notification_hint`, plus the five preference contracts. **Not riding the `/^ds\d+_lifecycle_/` auto-allow** — that was the open half of the question, and it is answered. `notifications` stays `vertical:notifications` and out of `dsTables()` per U048. Platform suite 15/15 green at the check.
- [x] DS-5 spec reconciled: notification-routing realised; digest/aggregation + outbound-channel adapters recorded still-forward; feature-inventory summary shows the partial-realisation split; §8 Q1 seam dispositioned. **§8 Q1 RESOLVED 2026-07-27 (board GB-2):** DS-5 owns channel adapters as contracts below the Platform API, PC-1 owns transport substrate — the cold lean promoted to law. Two facts settled it: the opposing lean's anchor (`app/api/invitations/send-email`) lives **only in `hub-legacy/`**, the frozen v1 oracle, so it was never part of the app being built; and **ADR-U038** forbids a BFF route being the sole home of a rule, ruling that shape out as precedent regardless. Recorded **resolved-in-principle, unrealized** — nothing outward ships in Ferd (NB-2/ADR-U040), so the shape is fixed without building to it.
- [x] Oracle ported: B-COMM-001..003 + B-NOTIF-001/003 + B-ADMIN-011 spine ported (adapted to v2 contracts, labelled); the two silences (NTF-9 reconnect, NTF-10 preferences) covered by fresh tests. **Closed 2026-07-28 — this was the real gap, discharged in [`oracle-spine-port.test.ts`](../../../hub/tests/integration/notifications/oracle-spine-port.test.ts) (11 tests, green; notifications integration 89 → 100).** It splits three ways, recorded rather than assumed. **B-NOTIF-001 — PORTED:** the `notifications_action_consistency` CHECK (migration `20260228125730:23-27`) says a *passive* row may not carry an answer — a row with no question on it cannot record having been answered. It was live on the table and asserted **nowhere** in v2. Both limbs are now proved (`action_taken` **and** `action_taken_at`, the second of which `action_taken` alone never reaches), together with the smart-side permission and a structural assertion that the constraint exists and is not `NOT VALID`. Its one v1 sibling — that a recipient can read the smart columns — is already covered at the contract door (`actionable-notifications.test.ts:264`) and is not duplicated. **B-ADMIN-011 — PORTED:** ADR-U049:49 keeps it explicitly valid ("B-ADMIN-011 stays a valid oracle for it"). v2 already *used* `admin_send_notification` — as an unmodified-writer fixture proving the N-D dispatcher catches writers it does not know about (`preference-and-dispatcher-contracts.test.ts:61-68`) — but never asserted the RPC's own contract. **Using a function is not testing it.** Now asserted: fan-out one row per target addressed to each target's personal group, count 0 on an empty list, registered kind + title + body + unread, and a non-admin refused with no row written (PAIRED with the fan-out, which is what makes the refusal non-vacuous). **B-NOTIF-003 — SUPERSEDED, ruled not ported:** its subject is the generic `handle_notification_action`, dropped on purpose at `20260705072252` and confirmed dropped by NB-1's thin-dispatch decision. Re-porting it would mean testing a function that must not exist. Its six-rejection matrix survives per dedicated handler and is cross-referenced row-by-row in the port file's header (another's row, already-actioned, expired, stale; *passive* is unreachable by construction — a passive kind has no `DISPATCH_SEGMENTS` entry, so no route exists to call; *invalid action value* has no referent, the boolean body admitting none). **Characterization, not red-first** — there is no migration in this change, so a red would have been a defect found, not a build pending. That is the point of porting an oracle late: it can only tell you something you did not already know.
- [x] Mist posture (NB-8) proved and recorded. **RUN 2026-07-27 — the premise was REFUTED, so this cannot be ticked as written** ([full proof](./2026-07-27-antf-nb8-mist-posture-proof.md)). Mists do **not** structurally lack durable notification rows: every Mist holds one `role_assigned` row from its own personal-group bootstrap, can read / mark-read / export it, and is refused only at the preference doors (`28000`) — so it can see a notification it has no way to silence. The realtime hint fires for Mists too, contradicting `FEAT-PD015:59` and the trigger's own comment. **Not a leak** (the row is about the Mist itself and CASCADEs away on erase) and **not Mist-specific** (1516 of 1548 FIMs carry the identical row). V3's own risk register predicted this at `:57` ("Mist durability leak … detected by ephemerality verification on Mist-linked rows") — this was that verification. **DISPOSITIONED AND FIXED** ([board GB-1](./2026-07-27-antf-gate-decision-board.md), Stefan chose *both* guards; migration `20260727180000` applied and verified live 2026-07-28). The rule is now true **by construction**, not by hope: the dispatcher refuses any row whose recipient is `is_temporary` (catching all ~38 writers and every future one), and `notify_role_assigned` skips self-assignment, which also removes the same meaningless row from 1516 FIMs. Ticked on a proof that ran, failed, was fixed, and now passes — integration notifications 89/89, platform 15/15 (the anon-EXECUTE invariant intact), groups 198/198.
- [x] W12 per-RPC verification for every RPC shipped; sole-home-in-BFF and core-referencing-domain remain automatic fails.
- [x] ADR-U043 measurement pass (cold >=20-min idle x3 + warm, tail rule) + Stefan's live walk — both before the area retro.

**The three lines that were left unticked, 2026-07-28 — now CLOSED.** They were named as verified-as-open rather than overlooked, and clearing them found what naming them was for. **Two of the three were mis-worded rather than undone**, which is the same pattern the gate session found in three of its six owed items: the conformance line named `DS_OWNED_ALLOWLIST`, a constant COR-B had already retired, and the NTF-6 line named "three dedicated handlers", a count that never matched what the build shipped. Only one — the oracle spine — was a real gap, and it was the one the gate session flagged as having positive evidence. Ticking any of them on "the cycle probably did it" would have buried two wrong sentences and one missing test file. The record as closed:

- **NTF-6 wired to the dedicated handlers, with adversarial direct-call tests per handler.** **CLOSED — the suspicion was right and the line was wrong.** It was a location miss (the tests live with the handlers they exercise, not in `actionable-notifications.test.ts`), *and* NB-1's "three" never matched the build: two handlers answer in the row, one kind answers on a surface, and the third name on the board — `submit_content_report` — is not a notification response at all. See the ticked line above for the per-handler citations.
- **Conformance gate: `DS_OWNED_ALLOWLIST` += DS-5 notification RPCs.** **CLOSED — the entries could not be located because the structure holding them no longer exists.** COR-B W1 replaced the flat allowlist with `supabase/ownership.manifest.json`. The RPCs are explicitly registered there under `DS-5`, so the answer to "as written or via the auto-allow?" is **neither**: registered, in the manifest that superseded the allowlist. See the ticked line above.
- **Oracle spine ported.** **CLOSED — the real gap, and it was real.** B-NOTIF-001's consistency constraint and B-ADMIN-011's whole contract were genuinely untested in v2; both are now ported in `oracle-spine-port.test.ts`. B-NOTIF-003 is ruled superseded with its rejection matrix mapped to the handlers that inherited it. See the ticked line above.

None was a gate-closing condition, and none is open now. **The exit checklist is fully ticked.** A-ADM inherits nothing from this list — but it inherits the prior that produced it: a checklist line can be wrong about the world as easily as the world can be behind the line, so re-check by reading the thing itself, not by re-reading the sentence.

## After Notifications

**Platform-Ops (A-ADM)** is the last Phase-3 area. A-NTF hands it the content-report **store** (COM-13) already live — the **moderation-decision surface / triage-resolve** (ADM-11) is A-ADM's, and NTF-6's moderation-decision-communication seam closes against it there. The email-channel ADR (deferred here per NB-2) and digest/aggregation (NB-6) are Eid+ forward work, not A-ADM blockers.
