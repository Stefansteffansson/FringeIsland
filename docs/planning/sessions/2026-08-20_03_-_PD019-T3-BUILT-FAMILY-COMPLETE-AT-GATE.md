# Session bridge — 2026-08-20 (third entry): tranche 3 built — the acting family's platform side is complete, held at its last gate

**Continuation of `2026-08-20_02`** (conversations walk green). Stefan pulled tranche 3 same-day; it went walk → firm ACs → red build → gate in one pass, in-session (the venue question answered: the family's context was all live here).

## The pull — walk findings dissolved the board (no forks)

The family sweep (the T2R lesson applied: `pg_proc LIKE '%announcement%'`, never a hand-list) found six contracts; three are tranche scope (the platform pair is the DeusEx plane, `get_platform_announcement_reach` an admin instrument). The applied bodies settled everything: **retraction is already a role power** (`send_announcements` in scope retracts any community announcement — the feared "unretractable group announcement" fork doesn't exist); the **platform arm refuses wielding structurally** (NULL scope fails limb 2a); and the wielded send must carry **dual actor exclusion** (one act, two identities) plus **`sent_by_group_id = A`** in the FIM-visible payload (the current `v_me` would leak the person behind the hat).

## The build (PR #567 — HELD at the schema gate; merging takes FEAT-PD019 to 6-done)

**Migration `20260820150000`**: three DROP + CREATE re-issues under the shared gate; the personal paths byte-identical (the dual-exclusion predicate collapses to today's when `v_actor = v_me`). **The PD020 interplay proven by cell**: a wielded announcement's engagement-group recipient rows expand to answerers' personal rows with zero group-addressed residue (cell A2), and guard G2 pins the personal path's shipped expansion as the bar.

**Evidence:** red-first **7 red / 2 labelled guards → 9/9**; slices **communication + platform + notifications, 26 suites 313/313**; lint 0 errors; applied **and** recorded in the migration log. The PR carries maturity `6-done` + both L4 rows + the root changelog.

## The family, at its last gate

Platform side complete: **forum (T1, #551) · conversations (T2 #556 + T2R #562) · announcements (T3, #567 at gate)** — ten contracts wielded under one helper, every posture ruled and recorded. Hub halves shipped and live-walked for forum (H046) and conversations (H047); **deferred, stated plainly:** the announcements Hub affordances (a future H-spec — the wielded composer on the group board), the wielded-edit posture (editable by no one, v1), hint silence (topic-channel rider recorded), TASK-EDT-01, TASK-DBT-03, wave assignment for the whole family.

## Open items for the next session

1. **PR #567 waits for the named approval** — the one design point for eyes is the dual actor exclusion in the fan-out. Merging closes FEAT-PD019 at `6-done`.
2. After the gate: the natural pulls are the **announcements Hub half** (completes the surface story), **TASK-EDT-01**, or the **wave-assignment sweep**.
3. doc-health-check: deliberately skipped — no cross-cutting changes; consider running it once the family closes (a natural cycle boundary).
