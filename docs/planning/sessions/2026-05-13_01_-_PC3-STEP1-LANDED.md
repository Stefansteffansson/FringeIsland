# Session bridge: 2026-05-13 (1) — PC-3 Organisation Step 1 landed; Steps 2+3 deferred; Experiment B manual-run

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-05-13 (first bridge of 2026-05-13)
**Session type:** L-level architectural landing. Phase 2 entity 3 Step 1 only — first split of Steps 1 vs 2+3 in the Platform Core L1→L3 sequence, by deliberate disposition per Experiment B restart specification. Also the manual side of Experiment B (restart) — autonomous-side run on `experiments/B-pc3-full` not visible from this bridge.
**Chronological predecessor:** `2026-05-12_01_-_EXPERIMENT-B-ABORTED-RESTART-PLAN.md` (Experiment B abort + restart specification — required reading for the restart, which this session executed).
**Substantive predecessor:** `2026-05-04_02_-_PC2-L1-L3-COMPLETE.md` (PC-2 entity bridge — primary substantive carry-forward block) and `2026-05-04_03_-_EXPERIMENT-A-COMPLETE-AND-DEFERRED-WORK.md` (Experiment A bridge — three pre-loaded priors P-O1 / D7 / X3 actively held throughout Step 1).

---

## Session arc

Manual-side run of Experiment B restart. Bouncing-partner methodology executing PC-3 Organisation Step 1 cold derivation on `main`, in parallel with an autonomous agent run on `experiments/B-pc3-full` in a separate git worktree at `D:/WebDev/FringeIsland-experiment-B/`. The restart's working-tree isolation specification held: pre-flight checks at session-open (working directory, branch, worktree topology, working-tree state) all four passed; bouncing-partner blindness invariant preserved.

State-read pass: four enumerated bridges + PC-2 spec (read via sandbox `ctx_execute_file` to avoid context flooding on a 27K-token file; structural summary plus targeted grep extracted the priors). Authority chain reads: root + tier + sub-tier CLAUDE.md; sub-tier README L2 line; six ADRs (U023, U007, U016, U006, U018, U020); platform-core-spec template.

Step 1 cold derivation: cold-derivation-with-Experiment-A-priors variant (distinct from PC-1/PC-2 pure-cold). One surface-draft bounce + three pre-Write bounces on the spec, then two pre-Write bounces on the new entity-level CLAUDE.md, then one pre-Edit bounce on the sub-tier CLAUDE.md anchor removal. Three discrete commits landed (spec → anchor migration batch → this bridge).

Steps 2 (code-informed stress-test) and 3 (adjudication) deferred to a subsequent session per bouncer disposition. Reasons: session capacity burned on abort-recovery context-load + context-mode event + multiple bouncing iterations on long-content rendering; bridge work itself substantial.

---

## Load-bearing output

**PC-3 Organisation L2 + L3 Step 1 landed.** Spec at `docs/platform/core/organisation-specification.md`, committed `8d880d6` (309 insertions, single file). Cold-derivation-with-Experiment-A-priors variant; §L2 §1-§8 complete; §L3 Capabilities (11 capabilities), Dependency chain, External dependencies, Sources-status block complete; §L4 placeholder pending reconciliation.

**PC-3 entity-level CLAUDE.md created and sub-tier anchor migrated**, committed `5592773` (2 files, 26 insertions / 1 deletion). New file at `docs/platform/core/organisation/CLAUDE.md` — first entity-level CLAUDE.md under Platform Core; PC-1, PC-2, PC-4 entity-level files do not yet exist. Sub-tier `docs/platform/core/CLAUDE.md` fourth bullet (the four-roles + `has_permission()` ownership statement, placeholder-anchored at PC-2) removed; canonical home is now the new entity-level file per PC-2 §L3 finding C3-3.

Three Step 1 priors applied (recorded in §L3 sources-status):
- **P-O1** — actor primitive is `get_current_personal_group_id()` (ADR-U006 canonical); §3 / §5 / §6 throughout the spec; §6 actor-primitive partition documents the scope-dependent resolution; §8 Q6 routes PC-2 amendment carry-forward (deferred to post-Experiment-B per Experiment A bridge).
- **D7** — role-name vocabulary canonical artifact is named-constant table; §5 `public.role_templates` row pinned (TEXT values, no PG ENUM).
- **X3** — ADR-U007 documented signature differs from disk signature; §6 records both; §8 Q2 routes ADR-U007 amendment candidate.

Step 1 load-bearing positions:
- **`handle_new_user` cross-seam: accept-seam permanently** (PC-2 §L3 Step 3 option (a)); alternatives recorded at §8 Q1. Step 2 disk evidence may pressure on seam-trigger brittleness signals.
- **`personal_group_id UUID` column on `public.users`: intentional override** of §4 strict upward-only chain authorised by ADR-U006; §5 framed as tier-shape rule yielding to constitutive ADR; §8 Q8 routes ADR-U006 amendment candidate (FK-direction codification).
- **`has_permission()` disk signature adopted as canonical**; §8 Q2 routes ADR-U007 amendment candidate.

PW-1 (schema-predates-partition) and PW-2 (speculative as third shape) promotion-watches armed; A-candidate #5 (multi-Edit gate discipline) promotion-watch armed; PC-1 Finding #4 (secrets/credentials substrate) and Experiment A X5 (service-role escalation) carry-forward trackers recorded.

---

## Experiment B abort + recovery (brief; full record in 2026-05-12_01)

The original Experiment B attempt (a prior session) aborted because both CC terminals were opened in the same working directory; `git checkout -b experiments/B-pc3-full` in the autonomous-run terminal silently switched the manual-run terminal's branch (shared working tree). Bouncing-partner blindness invariant broken when the autonomous CC's Write permission prompt displayed its full spec content in the manual-side bouncing-partner's view. Aborted run produced no committed work; manual-side and autonomous-side outputs both intentionally discarded; restart began from cold derivation in a fresh bouncing-partner Claude.ai chat.

The restart specification (in `2026-05-12_01`) introduced working-directory isolation via `git worktree`. This session's restart succeeded — no branch contamination; bouncing-partner blindness held throughout. Full mechanism + durable methodology learning ("two CC terminals running in parallel must occupy separate git worktrees at separate physical paths; branch-only isolation is illusory") recorded in `2026-05-12_01`.

---

## Agent-side context-mode event (program-level record; main-side modification deferred)

During this session, the autonomous-run CC encountered an unstaged modification to the experiment worktree's CLAUDE.md at pre-flight check 4. The modification was a `# context-mode — MANDATORY routing rules` block appended to the file. The autonomous CC hypothesized the modification was auto-injected by the context-mode MCP plugin on initialization; this hypothesis was not independently verified. Modification discarded on the agent side via `git restore`; same modification persists unstaged on `main` in the manual-side worktree (not touched during Experiment B run per session-close work-order item 6 phrasing). Main-side adoption decision for the context-mode plugin and its CLAUDE.md modifications is a separate decision for a future session.

---

## Manual-side context-mode tool-surface usage (methodology variant)

The manual side used the plugin's `ctx_execute_file` for the 27K-token PC-2 spec read. This was recorded in PC-3 spec §L3 sources-status as a methodology variant from PC-1/PC-2 reads (which were unmediated direct reads). Adopted as A-candidate-material under option (A) of the early-session disposition — the comparison-phase analysis will treat tool-surface mediation as a methodology variable to examine, since both runs had the context-mode plugin available, making the variable shared rather than differentiating between runs but worth observing for whether mediated reads contaminate downstream derivation.

---

## Bouncer-side rendering false-positive pattern (new durable methodology learning)

Three instances of the same shape this session: bouncing-partner reading the line-numbered preview UI of long markdown content (adjacent `###` headings near end-of-file in the PC-3 spec §L4; long single-paragraph bullet with three `has_permission` occurrences in the entity-level CLAUDE.md rules-section) as actual content corruption / duplication. Each time, structural inventory of the composed content (CC reading its own composed draft + reporting heading count, sentence count, token-occurrence audit) settled the false-positive. **No actual content divergence in any of the three cases.**

Mitigation pattern established: bouncer asks CC for structural inventory BEFORE asserting "real defect" or rejecting the Write/Edit. CC verifies against composed content; reports inventory; bouncer confirms (or, if real defect, CC fixes). Converts a false-positive into a verification-cycle that costs ~one round-trip rather than a full re-bounce.

Recorded as A-candidate #7 (see ledger section below); the chat-vs-payload divergence concern is folded into the same A-candidate as the second (theoretical) failure mode.

---

## A-candidate ledger updates this session

The program-level A-candidate ledger gains two new entries from this session, both promotion-watch:

- **A-candidate #6 — Cold-derivation-with-priors as methodology variant.** Distinct from PC-1/PC-2 pure-cold derivation. PC-3 Step 1 ran with three pre-loaded Experiment A priors (P-O1, D7, X3) actively held throughout cold derivation. Recorded in PC-3 spec §L3 sources-status block. Promotion criterion: pattern recurs at PC-4 and Phase 3 entities (Domain Services). **Comparison-phase note:** the autonomous run on `experiments/B-pc3-full` had the same priors available (same Experiment A bridge); the methodology variable is *shared* across both runs, not differentiating — comparison-phase analysis should account for this.

- **A-candidate #7 — Tool-payload verification distinction at L-level Write/Edit gates.** Covers two failure modes: (a) bouncer-side rendering false-positive on long-content / adjacent-heading-cluster previews (observed 3× this session — §L4 region of PC-3 spec on first and third Gate-1 attempts; long single-paragraph Rules-bullet in the entity-level CLAUDE.md draft); (b) theoretical chat-output vs Tool-payload divergence due to agent-side message-assembly integrity (not observed at PC-3 — all three suspected instances this session were (a), not (b)). Shared mitigation: **structural-inventory-before-defect-assertion** — bouncer asks CC for inventory (heading count, sentence count, token-occurrence audit) BEFORE asserting "real defect" or rejecting the Write/Edit; CC verifies against composed content; reports inventory; bouncer confirms (or, if real defect, CC fixes). Mitigation converted three potential full-rebounce cycles into single-round-trip verifications. Promotion criterion: pattern recurs at PC-4 or beyond, OR a real chat-vs-payload divergence is observed (which would re-open the (a)/(b) split question).

Carrying forward unchanged:
- **A-candidate #5** (Multi-Edit gate emission discipline) — sub-batch-of-1 default held throughout this session (single Write for spec, single Write for entity-level CLAUDE.md, single Edit for sub-tier removal, three single-call commits). No batched Edits attempted. Promotion-watch armed; pattern not yet recurred at PC-4.
- **A-candidates #1-#4** (Latent-vs-delta; Tier-shape escalation channel; Database-shaped L2 framing; Schema-predates-partition). PW-1 (=#4) and PW-2 (=#1's third-shape compounding) both promotion-watch-armed at PC-3 §L3 sources-status; Step 2 will pressure-test PW-1 directly.

---

## Bouncer-side conditional-completeness observation (small)

When dispositioning the sub-tier CLAUDE.md anchor migration (Decision 2 of the session-close work order), the bouncer's conditional logic was incomplete — enumerated "cascade plan post-dates PC-2 bridge → (b)" and "cascade plan pre-dates AND PC-2 routed around → (a)" but missed the third case "cascade plan pre-dates AND PC-2 endorsed-with-delegation." CC's evidence-gathering caught the gap (read cascade-plan close-out + verified entity-level CLAUDE.md presence across the project), recommended (a) with rationale, and re-bounced rather than executing on the incomplete conditional. Bouncer confirmed switch to (a).

Mitigation: exhaustive enumeration of conditional cases, OR explicit "if neither case applies, surface" deferral. Less critical than the rendering false-positive pattern (#7) but worth noting as a session-emergent pattern.

---

## Small finding — sub-tier CLAUDE.md anticipatory language

The sub-tier `docs/platform/core/CLAUDE.md` `## Where to go next` section's bullet on "The four areas" still names `docs/platform/core/identity/CLAUDE.md` as the migration destination for the temp anchor — that anticipatory language was correct at sub-tier authoring time but PC-2 §L3 finding C3-3 corrected the partition to PC-3 (Organisation, not Identity). Option (a) execution this session landed the anchor in its actually-correct home (`docs/platform/core/organisation/CLAUDE.md`), but the anticipatory language in the sub-tier file remains stale ("entity-level CLAUDE.md files for each area become imminently expected when the area's L2 specification is authored" — both the forecast-tone and the obsolete spec-existence claim ["Each area's specification is to-be-written"] in the same bullet).

Known stale; deferred per Gate-2 stale-bullet disposition (option (i)). Future doc-hygiene work — `doc-health-check` skill or routine cleanup pass.

---

## Manual-vs-autonomous session-shape asymmetry (for comparison-phase analysis)

Manual side split Step 1 from Steps 2+3 across two sessions (this one and a subsequent one). Autonomous side likely ran Steps 1+2+3 in a single session per Experiment B plan. This asymmetry is by deliberate disposition (capacity, bridge density), not contamination, but comparison-phase analysis should account for it:
- Time-elapsed-between-Step-1-and-Step-2 differs (autonomous: minutes; manual: at least one session-boundary, plus cool-cache state-read at next session-open).
- Step 2 evidence-gathering context differs (autonomous: hot context from Step 1; manual: state-read at session-open with cool context).
- Bouncing-partner-cycle pacing differs (autonomous: none; manual: per-step bounce + ad-hoc bounces within steps).

These differences may affect substance findings even when the methodology variables are otherwise matched. Recording for comparison-phase: "manual-run session-shape asymmetry from autonomous-run by deliberate disposition; not contamination."

---

## Tripwires

| # | Description | Status |
|---|---|---|
| 4 | Disk-of-record verification before asserting | ACTIVE — held cleanly. Pre-flight checks verified worktree topology against disk; state-read after PC-2 carry-forward block read verified against PC-2 spec on disk; pre-Edit whitespace verification for Gate 2 confirmed source line structure before emitting the Edit. |
| 6 | Discipline-as-deferral | NOT FIRING — discipline-stack altitude held throughout. No process-on-process inversion this session. Bouncing-partner cycle ran normally. Tripwire remains armed for next session. |

Tripwires #1, #2, #3, #5 — closed in prior bridges; not re-engaged this session.

---

## Repo state at session close

- Branch `main` after anchor migration commit `5592773` (latest committed). Bridge commit pending — this section describes the working-tree state at the moment of bridge authoring, before the bridge commit itself lands.
- Branch `experiments/B-pc3-full` exists locally in the autonomous-run worktree (`D:/WebDev/FringeIsland-experiment-B/`); state determined by the autonomous-run CC, not visible from the manual side.
- Pre-existing root `M CLAUDE.md` modification still unstaged (carried across session; outside experiment scope; not committed during this session).
- **No push to origin** per Experiment B comparison-phase disposition. Both branches land before push; comparison-phase analysis runs before push.

---

## Discipline posture for next session (PC-3 Steps 2+3)

- Bouncing-partner cycle, surface-draft discipline, candidate ledger, tripwires armed.
- Discipline stack **on** for PC-3 Step 2 (code-informed stress-test) and Step 3 (adjudication) — L-level work continues.
- **Print-batch-before-gate** at L-level surface drafts (durable). For any message that would Write/Edit 2+ tool calls against one authoring artifact, print the draft for review first.
- **Sub-batch-of-1 multi-Edit cadence default** (durable). Step 2 / Step 3 may produce multi-Edit work; sub-batch-of-1 holds until discipline earns sub-batch-of-3 at first multi-Edit point.
- **Structural-inventory-before-defect-assertion** at rendering false-positive surfaces (new, A-candidate #7). Bouncer asks CC for inventory before asserting "real defect" on long-content or adjacent-heading-cluster previews.
- State-read at session-open: read this bridge **and verify against disk** (Tripwire #4). Specifically verify the spec commit `8d880d6` landed, the anchor migration commit `5592773` landed, sub-tier `docs/platform/core/CLAUDE.md` no longer carries the temporary anchor bullet, entity-level CLAUDE.md is on disk at `docs/platform/core/organisation/CLAUDE.md`.
- No open adjudication carried into next session. PC-3 Step 2 entry is clear.

---

## Session-open prompt for CC (next session)

PC-3 Organisation Step 2 (code-informed stress-test pass) + Step 3 (adjudication). Same three-step shape as PC-1 / PC-2 (which ran Steps 1+2+3 in single sessions); this is PC-3's split-session continuation.

State-read pass. Verify against disk:
- Branch state and tip commit.
- Spec at `docs/platform/core/organisation-specification.md` — confirm landed as recorded; §L3 Step 2 / Step 3 blocks not yet written.
- Entity-level CLAUDE.md at `docs/platform/core/organisation/CLAUDE.md` — confirm present.
- Sub-tier `docs/platform/core/CLAUDE.md` — confirm temporary anchor bullet removed (three bullets remain in `## Rules that only apply at this sub-tier`).
- This bridge file existence and disk-filename.
- PC-2 spec un-amended on disk (no Experiment A substance findings folded in; that's post-Experiment-B amendment work). **If this verification fails, Experiment B comparison-phase is compromised — pause and surface.**

Step 2 reads disk (cold-derivation discipline stands down): `supabase/migrations/`, `lib/`, `app/`, `docs/platform/core/features/`. Three-class output (Class 1 confirms / Class 2 entity-internal deltas / Class 3 cross-entity findings). Phase-wide observations recorded as sources-status entries.

Active watches at Step 2:
- **PW-1 (schema-predates-partition) promotion-watch.** PC-3's existing schema is the most likely site for recurrence per PC-2 carry-forward. Confirmation expected; promotion to named program-level pattern at Phase 2 close-out.
- **PW-2 (speculative as third shape) promotion-watch.** §7 directional state for the public HTTP API surface is the canonical tagging site.
- **§8 Q1 — `handle_new_user` factoring position.** Step 1 cold position = accept-seam. Step 2 disk-checks for seam-trigger brittleness signals; if found, shift Step 3 adjudication toward factor or escalate.
- **§8 Q2 — ADR-U007 signature staleness.** Confirm disk signature per Experiment A X3.
- **§8 Q3 / Q4 — Personal Group / System groups entity-vs-label.** Step 2 disk evidence settles.
- **§8 Q5 — `has_permission` vs `is_platform_admin` policy-composition.** Step 2 RLS policy reads settle composition pattern (compose-inline expected per cold prediction).
- **§8 Q8 — ADR-U006 amendment for FK-direction commitment.** Carry to Step 3 disposition.
- **PC-1 Finding #4 carry-forward.** Step 2 watches PC-3-owned operations against the five service-role-escalation routes from Experiment A X5.

Step 3 adjudication produces pickup lists for PC-4 (next entity in chain) and DS-* (Phase 3 entities). PC-2 amendment carry-forward (§8 Q6) remains deferred to post-Experiment-B work.

Phase 2 close-out arrives after PC-4 lands. At that point: (a) revisit Finding #4 with PC-2/PC-3/PC-4 evidence; (b) revisit PW-1 / PW-2 / A-candidates #5 / #6 / #7 for promotion; (c) fitness-check the three-day turnaround at phase level; (d) decide on `ecosystem-decomposition` skill update for stress-test pattern at n=3 or higher.
