# Session bridge: 2026-05-04 (1) — PC-1 Infrastructure L1→L3 derivation complete; stress-test pattern promoted at n=2

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-05-04 (first bridge of 2026-05-04)
**Session type:** L-level architectural commit. Phase 2 entity 1. PC-1 Infrastructure L1→L3 derivation; three-step code-informed stress-test pattern executed; adjudication producing pickup lists for downstream entities. First execution against the discipline-stack-altitude-aware disposition adopted mid-session.

**Chronological predecessor:** `2026-05-03_05_-_REFRAME-A-AS-PARALLEL-ARTIFACT.md` (committed `98d39bc`).
**Substantive predecessor:** same — that bridge made A-as-parallel-artifact load-bearing and named C-B (G-27 three-way fittability scoping) as the next adjudication-shaped surface.

---

## Session arc

The session opened with state-read confirming disk-state, then progressively re-engaged with C-B → frame-for-the-gate → is-the-gate-still-a-gate → relationship-between-doc-and-phase. **Stefan called the inversion mid-session: process-on-process. Cumulative progress on Platform L1→L3 = zero.**

The reset corrected the discipline-stack altitude (see *Way-of-working corrections* below). After the reset, four decisions taken with confidence levels explicit:

1. **Entity 1 = PC-1 Infrastructure** — high. Four of five surfaced criteria converge (dependency direction, blast radius, code-informed stress-test material, active-consumer pressure). Goal-line directness pulls toward PC-2 but loses against move-and-correct: PC-2 still gets done, second, against stable substrate.
2. **Phase 2 = Platform Core L1→L3 only.** Domain Services L1→L3 becomes its own phase. — high. Dependency rule (Domain → Core via Internal API) makes Core-first structurally necessary; "twelve-entity sequential traversal" framing in the prior bridge predates the three-day fitness function being explicit and now reads as too coarse.
3. **Order within Core = PC-1 → PC-2 → PC-3 → PC-4.** — medium-high. Strict dependency cascade. Within-Core ordering is more inferred than disk-confirmed; hold PC-2/PC-3 lightly.
4. **Program shape renumbered** — high. New sequence: Phase 2 (Platform Core) → Phase 3 (Platform Domain) → Phase 4 (Verticals) → Phase 5 (Hub L4-L5).

Phase 2 then opened directly. Three-step stress-test pattern executed cleanly through the rest of the session.

---

## Load-bearing output

**PC-1 Infrastructure L1→L3 derivation complete.** Spec at `docs/platform/core/infrastructure-specification.md`. Three steps executed cleanly:

- **Step 1 (cold derivation, 2026-05-03)** — L1+L2 cold-derived from `docs/platform/CLAUDE.md`, `docs/platform/core/README.md`, ADR-U023, and the platform-core-spec template. Eight capabilities derived. No code, no migrations, no existing FEAT-PC* read.
- **Step 2 (code-informed stress-test pass, 2026-05-04)** — Run against `supabase/migrations/` (19 files; 38 `CREATE TABLE`; 64 `ENABLE ROW LEVEL SECURITY`; many `SECURITY DEFINER`; **zero `CREATE EXTENSION`**) plus `lib/`, `app/api/` top-level. Three-class output: 5 confirms / 3 entity-internal deltas / 5 cross-entity findings.
- **Step 3 (adjudication, 2026-05-04)** — §8 Q1 resolved, Q2 partially resolved, Q3 sharpened-to-confirmed, Q5 sharpened (Q4 separately revised through Adjustment 1). Pickup lists issued for PC-2 / PC-3 / PC-4 / DS-5. Finding #4 escalated outside pickup-list channels.

**Stress-test pattern promoted to standing methodology at n=2.** The pattern held across substantively different entities (Hub B.2 / 2026-04-30 vs PC-1 / 2026-05-04) with substantively different findings shapes (no lateral-routing pressure at Hub B.2; four lateral findings plus one tier-shape finding here). Update to `ecosystem-decomposition` skill is downstream task — not session-close.

---

## Three A-candidates travelling with n=2 promotion

Three methodology-refinement signals surfaced during this instance. Captured here per A-as-parallel-artifact disposition; A authors when refinements compound, not from this bridge.

1. **Latent-vs-delta distinction.** Class 2 (entity-internal delta) currently collapses two shapes: "wrong-shaped on disk" (Database-extension management — granularity actually wrong) and "not yet exercised on disk" (Object storage substrate, Feature-flag substrate — L2 names them, code hasn't built them). Three-class output should distinguish these. Either a fourth class or explicit sub-categorization wants exploring.
2. **Tier-shape escalation channel (separate from G-29).** G-29 handles lateral routing of cross-entity findings. Finding #4 (secrets/credentials substrate is app-tier, not database-tier) surfaced a finding shape with no clean lateral destination — escalates instead to tier-shape question. Distinct from G-29; do not conflate. Methodology wants a named channel for tier-shape findings.
3. **Database-shaped L2 framing assumption.** L2 inventory conventions may carry an implicit substrate-at-database-tier assumption that breaks when actual substrate is broader. Worth examining when authoring PC-2 / PC-3 / PC-4 L2 strengthening, and when other tiers' L2 inventories are written. Q4 in PC-1's spec captures the entity-local form; A is the natural home for the cross-entity methodological generalization.

---

## Finding #4 program-level carry-forward

**Secrets/credentials substrate is app-tier, not database-tier.** Disk-grounded: zero `vault`/`secret`/`api_key` in migrations; `process.env`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` consumed across `lib/supabase/*`, `lib/admin/`, `app/api/*`, `app/dev/`, `app/error.tsx`. Substrate sits **above the database tier**.

**Structural difference (load-bearing — do not collapse):** no clean lateral destination across Platform Core / Domain Services / Surfaces. Not a routing question; a tier-shape question.

**Three-destination escalation path preserved without collapse:**
1. **Phase-2 close-out adjudication** — revisits with three more entities' L3 evidence (PC-2 may surface session-storage conventions; PC-4 may surface admin-tooling secrets).
2. **Candidate ADR** — three concrete option-shapes named: (a) PC-1 broadens to "platform substrate including runtime env"; (b) new platform-tier sibling area (PC-5 Runtime, or Core / Domain / **Runtime** as a third stability zone); (c) explicitly out-of-Platform-Core scope, named as Hub-product or Vercel-deployment responsibility with contract back to Core.
3. **Possible tier-shape revision** — ADR-U023 itself a candidate for revision. Most consequential outcome; named explicitly so it isn't pre-foreclosed by the lighter options (1) and (2).

**Why it lives in the bridge ledger, not just the PC-1 spec:** Phase-2 close-out is multiple sessions away (PC-2, PC-3, PC-4 each have their own L3 derivations). Bridge-ledger carry-forward ensures Finding #4 stays visible across the chain to close-out, independent of any single spec being read.

**Successor bridges should carry this forward** until Phase-2 close-out adjudicates or the candidate ADR lands.

---

## Pickup lists for next sessions

Issued from PC-1's spec §L3 *Step 3 — Adjudication outputs*. Each receiving entity's L3 author reads the relevant pickup list as part of their authority chain (alongside L1+L2+ADR-U023+template).

- **PC-2 Identity** (Phase 2, second entity, next session) — 1 item from Finding #5: claim ownership of session lifecycle and authenticated-context handoff. PC-1 retains role conventions; PC-2 owns session-lifecycle surface.
- **PC-3 Organisation** (Phase 2, third entity) — 2 items. Finding #1: resolve PC-3-vs-DS-5 boundary for canonical notification dispatch (joint with DS-5). Finding #2: confirm `has_permission()` ownership partition (PC-3 content; PC-1 shape convention).
- **PC-4 Governance** (Phase 2, fourth entity) — 1 main + 1 optional. Finding #3: claim ownership of `public.admin_audit_log` table, audit-content schema, and access policies; PC-1 retains trigger-as-primitive. Optional Finding #5 carry: service-role governance soft-edge.
- **DS-5 Communication** (Phase 3) — 1 item, joint with PC-3 (Finding #1).

---

## Way-of-working corrections this session (durable, A-candidate material)

Four corrections, all surfaced under real pressure during the process-on-process inversion and the reset that followed. Captured here; A authors the durable form when the pattern compounds across more sessions.

1. **Discipline-stack altitude-aware default.** Stack defaults **on** for architectural commits, L-level derivations, authority-chain work, cosmology integrity — anywhere errors compound. Stack defaults **off** for sub-question grinding where errors are cheap and self-correcting. Toolkit (surface-draft, tripwires, candidate ledger, verify-before-asserting, bouncing-partner cycle) doesn't change; the altitude at which it deploys does.
2. **Move-and-correct disposition.** Deploy quick, refine quick, correct in flight; first-time-right is not the goal. Wrong-shaped findings are signal, not failure. If cold derivation surfaces L1+L2 thinness, that's data about where L2 wants strengthening — captured in passing, not blocking.
3. **Three-day turnaround as fitness function for the ecosystem.** Lives at phase-level, not as sub-question gate. Don't benchmark every L3 capability against three-day-turnaround mid-derivation. Phase 2 closes when Core L1→L3 lands; that's when the fitness check fires.
4. **Bouncing-partner cycle demonstrated mid-flight correction.** The reframe was working before A was authored — discipline-as-deferral was caught and corrected mid-session via Stefan's call-out, then held cleanly through Steps 1/2/3. Worth A absorbing as evidence the pattern self-corrects under pressure when both sides stay alert to altitude.

---

## Tripwires

| # | Description | Status |
|---|---|---|
| 4 | Disk-of-record verification before asserting (carry-forward AND drafted-this-session content) | ACTIVE — held cleanly both directions throughout. State-read at session-open verified bridge claims against disk; Step 2 stress-test verified Step 1 cold-draft against disk; Step 3 pickup lists cite disk anchors per finding. |
| 6 | Discipline-as-deferral | ACTIVE — most-live early-session during C-B / frame-of-frame engagement; caught and corrected via Stefan's process-on-process call-out and the discipline-stack altitude reset. Held cleanly through Steps 1/2/3 once altitude was reset. |

Tripwires #1, #2, #3, #5 — closed in prior bridge; not re-engaged this session.

---

## Repo state at session close

- Branch `main`. Working tree clean post-commits.
- Origin/main caught up before session — no pre-existing unpushed commits.
- This session produces **two new commits**, logically separated:
  - Spec authoring commit — `infrastructure-specification.md` Steps 1+2+3 plus the two adjustment writes (frontmatter date update, derivation-note Step 2 / Step 3 narrative, §8 Q4 reframe with two-revision split, Sources-status block update, §L3 *Step 2 — code-informed stress-test findings*, §8 Q1/Q2/Q3/Q5 resolutions, §L3 *Step 3 — Adjudication outputs* with pickup lists).
  - Bridge commit — this file.
- Don't conflate: spec is authored artifact; bridge is process artifact.

---

## Discipline posture for next session

- Bouncing-partner cycle, surface-draft discipline, candidate ledger, tripwires armed.
- Discipline stack **on** for PC-2 L1→L3 derivation work itself (L-level commit). Stack stands down for sub-questions inside that derivation that don't compound.
- State-read at session-open: read this bridge **and verify against disk** (Tripwire #4). Specifically verify PC-1 spec landed as recorded, pickup item from Finding #5 still reads as cited.
- No open adjudication carried into next session. PC-2 entry is clear.

---

## Session-open prompt for CC (next session)

PC-2 Identity L1→L3 derivation. Same three-step shape as PC-1 (cold derivation → code-informed stress-test → adjudication).

State-read pass. Verify against disk:
- Branch state, tip commit
- This bridge file existence and disk-filename
- PC-1 spec at `docs/platform/core/infrastructure-specification.md` — confirm landed as recorded
- PC-2 pickup item from Finding #5 still reads as cited (`lib/supabase/{client,server,middleware}.ts` extant, finding text matches)

Authority chain for PC-2 cold derivation:
- L1: root `CLAUDE.md` + `docs/platform/CLAUDE.md`
- L2 inventory: `docs/platform/core/README.md` ("PC-2 Identity — Auth, profiles, sessions, Journal")
- ADR-U023 (Platform Core / Domain Services decomposition)
- Template: `docs/templates/platform-core-spec.md`
- **PC-1 spec pickup item** (Finding #5, Connection-role substrate) — read before deriving PC-2's contract surface and §6 auth-and-authz

Cold-derivation discipline: do **not** read `supabase/migrations/`, `lib/`, `app/`, or any existing `FEAT-PC*` during Step 1. Step 2 stress-test runs after cold draft lands.

Watch flags for PC-2:
- Database-shaped L2 framing assumption (A-candidate #3) — PC-2's L2 line ("Auth, profiles, sessions, Journal") may carry the same implicit assumption that bit PC-1's Q4. Surface as a finding if cold derivation produces app-tier substrate that L2 doesn't name.
- Latent-vs-delta distinction (A-candidate #1) — if PC-2 cold-derivation produces capabilities the L2 names but disk has not yet exercised, flag as latent rather than collapsing into delta.
- Finding #4 carry-forward — if PC-2 surfaces app-tier substrate adjacent to secrets/credentials, route to the same Phase-2 close-out / candidate ADR / tier-shape revision channel; do not re-derive a separate escalation path.

Phase-2 close-out arrives after PC-4 lands. At that point: (a) revisit Finding #4 with PC-2/PC-3/PC-4 evidence; (b) fitness-check the three-day turnaround at phase level; (c) decide on `ecosystem-decomposition` skill update for n=2 stress-test promotion.
