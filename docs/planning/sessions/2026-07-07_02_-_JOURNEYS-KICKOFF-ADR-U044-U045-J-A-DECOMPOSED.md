# Session bridge — Journeys area kickoff: Journey Designer recovered, ADR-U044/U045 locked, Cycle J-A decomposed to 4-ready

**Date:** 2026-07-07 (second session of the day; follows bridge `_01`'s pre-Journeys plan exactly — the Fable-5 design-first day).
**Session type:** Journeys area kickoff at full depth — four-scout terrain sweep → full-depth substrate audit → completion plan → **decision board settled in-session** → the two design sessions (the step-model session ADR-U008 mandated, + progress grain) → **ADR-U044 + ADR-U045 locked** → Cycle J-A decomposed (FEAT-PD002 ↔ FEAT-H019 + FEAT-PC016 rider, all `4-ready`).
**Status:** Everything merged; `main` clean and synced through **PR #113** (`22186b1`). **No migrations, no code — docs only**; dev DB unchanged at `20260706201500`. Dashboard refreshed (610 files). Build cycles deliberately deferred to later models inside the hardened gates, per `_01`.
**Participants:** Stefan (board settle; the J-S1 "dig deeper — we've dealt with this before" steer; the onboarding-journey canon; the merge nod) + Claude.

---

## What was done (in order)

- **[Completion plan](../hub-v2/phase-3-journeys-completion-plan.md)** (the kickoff's core deliverable, v2 = board settled): full-depth substrate audit (zero Hub surface; realised+seeded schema with no contract layer — the Groups shape; freeze semantics procedural in PC013/PC014; JRN-5 continuity-free via `finalise_transcendence`; oracle STRONG), all 18 A-JRN capabilities in cycles **J-A..J-E**, exit checklist planted. **First area under the ADR-U043 performance gate.**
- **The Journey Designer recovery — the kickoff's biggest find.** Stefan's steer ("we have some record of dealing with this before") led to [`2026-03-20-SESSION-01-journey-designer.md`](./2026-03-20-SESSION-01-journey-designer.md): the Present → Ask → Change universal step grammar, Journey → Node → Beat containment, the beat record with the six content families, four route types, step-instances-as-lived-records — stress-tested, status-tabled, and **never absorbed into the canon cores** (the DS-3 descent didn't consume it). Theory U is the documented real-world journey shape behind it.
- **[ADR-U044](../../architecture/decisions/ADR-U044-journey-step-model.md) — journey step model** (discharges the ADR-U008-mandated step-type session; resolves DS-3 §8 Q1): the Designer grammar becomes the schema's DNA — step rows (each a single-beat node), step-kind + content-family **registries** (ADR-U008 Tier-1 types as presets; the realized `{content, activity, assessment}` union maps mechanically and dies), and **per-traveller step-instances = the progress grain** (J-S2 folded in — two decisions, one structure). The session enters the authority chain permanently, **grammar only** — its pre-rename world vocabulary defers to the canon cores. Node/Beat grouping, Roads, pacing recorded as forward shape.
- **[ADR-U045](../../architecture/decisions/ADR-U045-onboarding-journey.md) — the onboarding journey** (Stefan's new canon, unifying board J-O1+J-O2; partially resolves DS-3 §8 Q2): **exactly one designated onboarding journey** (the former Journey Zero, designation as data); **Mist journey access scoped to it alone**; **auto-launch at first arrival for Mists AND new FIMs, opt-out honored** (rationale: sets the scene, jump-starts the FIM's relation with their Whisp and cord); progress **carries at transcendence** (realized continuity, proof-by-test); **forgotten via the existing ADR-U031 machinery** if the Mist drifts/bails — no new mechanics; **placeholder content seeded** until the first-experience (CQ-010) canon work. Consequence: **all 18 rows build in-area** (J-O4); J-E is the onboarding arc.
- **Board settled** (remainder): J-O3 measure-first binds (waterfall before the boundary bet); J-O5 **first-ever-cold budget < 2 s** (app share ≤ ~1.0 s over the auth-exchange vendor floor) — measured as its own scenario at the area gate; ADR-U043-addendum candidate when next touched.
- **Cycle J-A decomposed:** [FEAT-PD002](../../platform/domain/features/FEAT-PD002-journey-catalogue-and-enrolment-contracts.md) (the **first DS-3 feature spec** — six contracts over existing substrate, no new table, write-narrowing, Open Q1–Q5 for the gate) ↔ [FEAT-H019](../../products/hub/features/FEAT-H019-journey-catalogue-and-enrolment.md) (the **first Hub spec under the ADR-U043 budgets** — full B2/B3/B4/B5/B6 classification, data-boot per ADR-U042) + [FEAT-PC016](../../platform/core/features/FEAT-PC016-pending-nominations-read-contract.md) (`get_my_pending_nominations()` mirroring `get_my_invitations()`; closes the audit LOW finding; rides PD002's migration). L4 summaries + three indexes updated same-commit.
- **Anatomy correction at decomposition:** the GRP-4 enrolment summary is a **DS-3 read** (`get_group_enrollment_summary`) composed at the Hub BFF as a failure-isolated slice — *never* a `get_group_detail` field (a PC-3 function reading a DS-3 table would break the one-way rule, ADR-U023). The Groups plan's seam premise corrected in place.

## Doc-health

Not run — not a cycle boundary; the session's changes are additive docs with links verified at authoring. Next run rides the first Journeys cycle boundary / cooldown. The three `_01` soft flags carry unchanged.

## Next session — entry point

**Cycle J-A build** (later models, inside the gates): red-first from FEAT-PD002 — the schema-gate migration (six contracts + the PC016 rider + enrolment-write narrowing; **held at the gate per the carve-outs**, Open Q1–Q5 rule there) — then FEAT-H019 API-first; task files return (J-D3); route-policy conformance walks the five new routes automatically. **Before the bet is chosen: the J-O3 area-gate waterfall on production** (authenticated, cold + warm, ≥ 3 runs — needs Stefan's live walk) per the measure-first rule; candidates ready either way (P3b journey slice / P1-residual / P4). J-B is gated on the ADR-U044 schema (step rows + registries + step-instances; the 47 legacy seed steps migrate mechanically). J-E builds ADR-U045 (designation registry, placeholder seed, auto-launch both paths, carry-over + forgetting proofs).

## Standing / parked (deltas from retro §5; otherwise carried unchanged)

- **`get_my_pending_nominations()` contract:** SPEC'D (FEAT-PC016, `4-ready`) — moves from "kickoff batch" to "builds in J-A"; `leadership.ts` thins at that build.
- **First-ever-cold budget call:** DECIDED (J-O5, < 2 s) — the retro's Stefan-owned item closes as a decision; measurement lands at the area gate.
- **org-spec §5 staleness:** rides J-A as a docs item (already doc-health-routed at PC010).
- **G-36 / IDN-10 hooks:** unchanged (parked IDN-10 specs by next cooldown at latest); the enrolment-freeze disposition advances at **J-D** per the plan.
- Everything else carries: Stefan's two dashboard toggles, test hygiene, platform-core CHANGELOG disposition, E2E flake watch (`entry.spec` + `profile.spec`), `test:integration:rbac` cleanup, MEM-9 → Communication gate, IDN-12 + perf T2 parked, ADR-U040 referral rebuild unscoped, logo pick, launch checklist, the 3 doc-health soft flags.
