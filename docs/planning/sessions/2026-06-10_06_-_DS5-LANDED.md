# Session bridge: 2026-06-10 (6) — DS-5 Communication L1->L3 derivation complete

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-06-10 (sixth bridge of 2026-06-10)
**Session type:** Autonomous L1->L3 run, single-session (Steps 1+2+3). **Fifth instance of the autonomous L1->L3 template**, first on the `766e134` baseline (the DS-4-adjudicated revisions ran as template text for the first time). No FIRST DECISION (second consecutive; the first ratification surface was the Step 1 checkpoint). **Collapsed author-then-execute shape** (DS-3 precedent): the opener was authored and executed in the same session at Stefan's direction.
**Session opener:** [`openers/archive/ds5-communication-descent-opener.md`](./openers/archive/ds5-communication-descent-opener.md) (authored at `27044f0`, archived in this close batch).
**Chronological + substantive predecessor:** [`2026-06-10_05_-_DS4-LANDED.md`](./2026-06-10_05_-_DS4-LANDED.md).

---

## DS-5 Communication L1->L3 derivation completes at this commit batch

Spec at `docs/platform/domain/communication.md` (commit `2b469fc`): L2 identity + boundaries + six contract families + nine §7 service-level invariants + §8 eight questions (Q3 + Q7 resolved in-run); §L3 fourteen-capability inventory across six internal areas (Conversations & DM / Forums / Feeds & social surfaces / Notification routing / Attachments & content references / Lifecycle & retention) + dependency chain + external dependencies + Sources-status + Step 2 + Step 3 blocks. Entity CLAUDE.md at `communication/CLAUDE.md` (commit `a5da041`, registry row removed same-commit — the skill-file Edit was permitted; the DS-3 classifier denial did not recur). This bridge + STATUS.md update close the batch. **No push to origin** — Stefan dispositions push.

## Session arc

All five pre-flight checks passed; **both Check 4 named shapes fired and were dispositioned in-run** — the root `CLAUDE.md` context-mode re-injection (verified insertions-only, discarded per the ratified disposition) and concurrent `docs/novel/` activity (the parallel writer committed chapters 5-9 at `007b9a5` *between this session's own commits* — the named-intervener shape validated in real time). State-read consumed all twelve items + four disk verifications; three non-material remarks bundled into the Step 1 checkpoint (the DS-3 bridge carries zero literal DS-5 mentions — the DS-4 bridge is the complete carrier; **the platform tier CLAUDE.md carries the load-bearing notifications routing text** the opener cited only via ADR-U002; the roles-core Participant rename vs U021's "Former Member" display text, armed as §8 Q3). Step 1 cold derivation, checkpoint ratified in full (14/6 slate, both structural positions, three sibling confirmations, single-session shape), single Write held uncommitted. Step 2 ran as targeted sweeps with every zero dual-method verified: zero cold retractions, 7 Class 2 deltas, 3 Class 3 findings — the calibrated code-rich mixed profile arrived exactly. Step 3: deltas folded inline, the one sanctioned cross-entity amendment, L2-line revision, entity CLAUDE.md, this bridge.

## Ratified decisions (all by Stefan, 2026-06-10, this session)

1. **The attachment seam (DS-4 §8 Q7, the routed structural question):** forum/DM attachments are **DS-4 assets referenced opaquely by ID** — DS-5 owns the attach/detach act as message/post metadata, never the artifact; one content substrate holds; the opaque-reference direction pattern extends to **n=4**. Amendment landed in `content.md` §8 Q7 (`2b469fc`). Zero attachment substrate realized (dual-method) — forward-only.
2. **The notifications boundary (the second descent-owned structural question, surfaced at opener authoring):** the **vertical owns the obligation** (every layer emits its own triggers — ADR-U002), **DS-5 owns routing + in-platform delivery**, **products surface** (the platform tier CLAUDE.md carries this law verbatim). Step 2 found the exact shape on disk: PC-3-tier triggers + lifecycle functions emit, the `notifications` substrate routes, `NotificationBell` surfaces. Outward delivery is §8 Q1 (anchor: the open-coded `app/api/invitations/send-email` route).
3. **Three sibling re-checks confirmed without edits:** journeys.md (DS-5 consumes enrolment context; DS-3 holds no conversational state), world-model.md (DS-1 owns branch/cord state and the gate; DS-5 consumes the glance, never the reverse), narrative.md ("conversation or feeds (DS-5)").
4. **Step 1 candidate ratified as surfaced** (14 capabilities / 6 areas / 8 questions / 9 invariants); **Step 2 findings + Step 3 scope ratified** at the Step 2 gate.

## Forward-commitment classification

**Mixed profile — ~7 partial / 7 full-forward** (the DS-3 shape, as calibrated). Partial (realized substrate covers part of the capability): conversation lifecycle (pair-grain only), message exchange (Realtime live), participation/read state (inline columns), forum structure (flat threading), moderation (`forum_update_moderate`), notification routing & in-platform delivery (incl. sprint3 actionable notifications), lifecycle cascades (member-left/exit emissions). Full-forward: attribution display (ADR-U021's law unrealized — `'Unknown'` fallback), feed composition (zero feed substrate), journey-scoped social surfaces, cord-health glance, notification preferences (dual-pattern zero), attachment referencing, Shadow ephemerality.

## Pickup lists

**DS-6 Discovery (next entity; its opener must inherit):**
- The **feed-vs-recommendation boundary** (communication.md §8 Q4): anything beyond chronology + scope filters is DS-6's; re-check from the DS-6 side (sibling-provisional). The provisional consumer line (DS-6 may read forum/feed content for search indexing) re-checks too.
- The **DS-6 identity re-derivation watch** (DS-1 bridge) remains untouched and is DS-6's own descent question.
- DS-4's published-registry consumer line and the marketplace seam (DS-4 bridge) still route here.
- Opener-authoring notes: DS-6 is expected **near-zero-code** (no search/recommendation/marketplace substrate is known in the 19-table baseline — verify at authoring, don't inherit); enumerate binding ADRs by domain-noun grep (search/recommend/marketplace/discover/catalog/index) with citation-precision; **check tier-level CLAUDE.md files for L1-resident boundary text** (this run's platform-tier notifications find — see §13).

**DS-7 Intelligence:** communication.md §8 Q5 (Whisp-carried messages — cold lean: no, the branch is the FIM-FIM channel; resolve at the DS-7 descent); the Whisp-split ADR promotion still fires at DS-7 (PENDING.md); the provisional DS-7 consumer line (communication context for profile accumulation — privacy posture per its own descent).

**Notifications vertical (obligation-inventory, when it runs):** receives the ratified boundary (the vertical owns the obligation; DS-5 routes; products surface) + §8 Q1 (outward delivery shape — channel adapters over PC-1 substrate is the lean; the open-coded `send-email` route is the realized counter-anchor) + the realized trigger-emission inventory (~10 notification kinds across PC-3/PC-4 events).

**Privacy vertical:** the cord-health consent surface (glanceable/invited/self-first — world-model.md §6 names Privacy as its consent surface; DS-5's glance capability consumes it); the Shadow-communication posture (§8 Q2: Shadow access to DM/forums routes to PC-2/Privacy adjudication).

**Cross-tier-write channel (anchored at DS-3):** new anchors — PC-3-tier triggers (`notify_*` on `group_memberships`/`user_group_roles`/`groups`, rebuild L1379-1401) and lifecycle functions (sprint2 L232+, sprint4) INSERT INTO `public.notifications` directly; `audit_admin_message_send` (PC-4) fires on `direct_messages`. Recorded, not resolved (per the standing channel discipline).

**Hub / Gimbal (product tier):** the messaging/forum/notification surfaces are shell/feature work over DS-5's contract families at FEAT time; U021's display realization (and its "Former Member" vs "Former Participant" naming call) is FEAT-time work; equipment-keying stays feature-grain.

**Doc-health channel:** cascade additions this session: `communication/CLAUDE.md` created, registry row removed (`a5da041`); domain README DS-5 line revised + domain CLAUDE.md enumeration updated (`2b469fc`); no new assertion-bearing diagrams; no SVG edits.

## A-candidate ledger snapshot at DS-5 close

- **A#1, A#2, A#3, A#6, A#7** — carry forward as framings.
- **A#9 fired at its named site** (Supabase Realtime): the realized DS-5 contract surface is framework mechanisms — `postgres_changes` subscriptions, PostgREST, RPCs — with zero custom API routes (dual-method). Third confirming entity (PC-3, DS-4-classified-out, DS-5).
- **A#8 cumulative-forward** ran the lineage (drop_old_schema → rebuild → fixes → sprints) and surfaced the RC8 realtime-publication recovery mid-stream.
- **PW-1** attached: all four DS-5 tables born in the D15 rebuild — schema predates the partition.
- **PW-MARCH1: clean recovery** — the pre-D15 realtime-publication migration was re-created in the rebuild and made idempotent at RC8; the archived `remove_dm_notification_trigger` was deliberate retirement. (Contrast with the un-migrated `avatars` bucket at DS-4: here the infrastructure IS in migrations.)
- **PW-5:** the four DS-5 tables confirmed present; the full 19-count was not re-run this session (DS-4 re-verified it earlier the same day — accepted as current).
- **PW-T1:** partial-by-omission — `messaging.ts` covers Conversation/DirectMessage only; no forum/notification types in `lib/types/`.
- **Retraction-rate series:** PC-4 7/9; DS-1 0; DS-2 0; DS-3 0; DS-4 0; **DS-5 0 (with 7 Class 2 deltas)** — deltas-not-retractions holds at the second code-rich entity; the series settles at Phase 3 close-out.
- **#4 migration-name-as-shorthand — FIRST DECISIVE FIRING (n=5 opportunities):** `sprint3_smart_notifications.sql` carries the stewardship-nomination flow (~60% of body, `nominate_steward` RPC) — name-as-shorthand would mis-scope it. Rides with the firing recorded; landing adjudication at the next firing or Phase 3 close-out (see Template revision disposition).
- **Empty-result verification + ADR-enumeration-by-grep/citation-precision** — first run as template text: both held and fired productively (see §13).

## PW status at DS-5 close

PW-1: attached (all four tables in the rebuild). PW-MARCH1: clean recovery (its richest site yet — 16 archived files — produced lineage, one deliberate retirement, and a recovered incident, no asymmetric loss). PW-2: three §8 questions tagged speculative-third-shape (Q1 outward delivery, Q5 Whisp-carried, Q6 village presence); Q1 gained a realized anchor (the open-coded send-email route), the others defer to sibling descents — consistent with the DS-2 observation. PW-5: held (spot-verified; full count accepted from DS-4 same-day). PW-T1: asymmetric — types lag the realized schema by omission.

## Methodology data points (§13 capture)

**Fifth-instance framing — the fourteen cumulative template revisions held as template text.** The two newly-landed at `766e134` ran for the first time as template text and both fired productively: **empty-result verification** dual-method-confirmed four zeros (attachments, "Former Member", preferences, DS-5 API routes) and the method contrast was itself evidence each time; **ADR-enumeration-by-grep + citation-precision** at opener authoring excluded four false-positive ADRs with rationale (U001/U010/U022/U024), caught U028's hits as non-substantive ("dry-run thread #1" is a product-lock thread), and the read-the-ADR rule surfaced U021's exact display-law text that Step 2 then tested against code.

**Prompt 1 — worked well:** the two-structural-questions shape (the routed Q7 seam + the notifications boundary surfaced at authoring) gave Step 1 its spine, and both resolved cleanly at the first gate; the calibrated code-rich expectation with verified counts (10 live + 16 archived migration files, 14 surface files, contexts-not-hooks) made Step 2 sizing exact; the SETTLED block (profile-media, Whisp split) prevented re-litigation at two natural temptation points (avatars in `ConversationItem`; Whisp dialogue).
**Prompt 2 — redundant/confusing:** nothing misfired. The opener's Section 2 item 2 slightly over-promised (the DS-3 bridge as load-bearing for DS-5 — it carries zero DS-5 mentions; its value was the profile-shape precedent only). Routes to opener-authoring lifecycle, not template.
**Prompt 3 — wished was named:** **tier-level CLAUDE.md files as L1 derivation authority for boundary questions.** The platform tier file carried the decisive notifications-routing law ("the Communication service routes them") that no ADR states as directly; the opener found it only because the template's L1 line includes the tier file. Opener authors should grep tier/sub-tier CLAUDE.md files for the entity's domain nouns at authoring, the same way ADRs are swept (recommended as opener-authoring practice; template candidate if it recurs).
**Prompt 4 — new substance priors for DS-6:** the pickup block above (near-zero-code expectation to verify; the feed-vs-recommendation seam; the provisional consumer lines).
**Prompt 5 — new A/PW observations:** **#4 fired decisively for the first time** (n=5) — the firing shape is exactly what the watch predicted (a migration name describing a minority of its content); one decisive firing + four non-firings argues ride-with-recorded-firing rather than immediate landing. The collapsed author-then-execute shape ran clean a second time (DS-3 precedent) — calibration sweeps at authoring time keep Step 1 uncontaminated while making Step 2 expectations exact; the Check 4 novel-writer shape (b) was validated mid-session by an actual intervening novel commit.

## Template revision disposition

**NO REVISION LANDED; one candidate rides strengthened, one new candidate at n=1:**

1. **#4 migration-name-as-shorthand — first decisive firing recorded (n=5 opportunities, 1 firing).** Rides with the firing as evidence; landing adjudication at the next firing or the Phase 3 close-out retirement/promotion review (the firing converts the close-out question from retirement to promotion).
2. **Tier-CLAUDE-files-as-L1-boundary-authority (new, n=1):** sweep tier/sub-tier CLAUDE.md files for the entity's domain nouns at opener authoring, alongside the ADR grep. Rides opener-authoring practice; template candidate on recurrence.

## Carry-forward confirmations

CODE stayed a correction target (Step 2 read it as evidence; the cross-tier-write anchors route to the channel, not to in-session fixes). ASCII-only labels held. The 2026-04 Hub L3 working set was not used. OLDFEAT: not listed, not read. Sessions append-only. The Whisp split was consumed, never reopened; profile-media stayed settled. Zero PENDING.md dispositions and zero ADR amendments (as the opener expected — U021 is unrealized-not-contradicted; U002 is realized in shape). The avatars-bucket PC-1 routing was not re-raised (consumption evidence only, per the opener's scope boundary).

## Repo state at session close

Branch `main`. Commits this session: `27044f0` (opener) + `6a49bb4` (STATUS In flight) + `2b469fc` (spec + content.md Q7 amendment + README line + domain CLAUDE.md enumeration, 4 files) + `a5da041` (entity CLAUDE.md + registry row) + this bridge with the opener archived + STATUS.md close. Intervening novel commit `007b9a5` (parallel writer; per the named Check 4 shape). Tree otherwise clean (the context-mode injection was discarded at pre-flight per the ratified disposition). **No push to origin.**

---

*DS-5 Communication L1->L3 derivation completes at this commit batch.*
