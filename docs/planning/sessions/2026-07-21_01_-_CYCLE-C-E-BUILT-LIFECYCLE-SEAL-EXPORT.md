# Session bridge — 2026-07-21 (01): Cycle C-E built — endings seal, the record survives, the export is whole

**Session span:** resume ("continue where we left off") → the C-E board presented whole (D2 semantics asked with worked examples; Stefan: **Option A preserve-and-seal**, rest with recommendations) → design batch (FEAT-PD012 to 4-ready, TASK-CE-01..05; PR **#229** fuller-auto) → red suites (14 demonstrated red + 8 labelled greens) → the schema gate held (PR **#230**) → **"ok merge #230"** → apply + repair `20260721100000` → flip-green 22/22 (two labelled adaptations) → sweeps → E2E 2/2 + fleet 76/77 (after an environment-invalidated first attempt) → doc dues → close.
**Previous bridge:** `2026-07-20_05_-_CYCLE-C-D-BUILT-ANNOUNCEMENTS-WINDOW-REPORTS.md`.
**PR trail:** #229 (design batch: board settle recorded in the plan v5 + FEAT-PD012 4-ready + tasks; no ADR — decisions recorded in specs) · #230 (**schema gate** — migration `20260721100000`, nodded **"ok merge #230"**; applied + repaired) · the close PR (adaptations, E2E, doc dues, 6-done flips, CHANGELOG, this bridge).

## What shipped, as a member would tell it (the walkthrough)

When my group ends — closed by its Steward at the natural end, or deleted outright — the ending is real but nothing of mine is destroyed. The group's conversation goes quiet: it leaves my Messages inbox, nobody can speak into it again, no one new can join it — but if I open the record, my history is still there to read, because the seal ends *activity*, never my access to what was said. My DMs with the same people don't even notice; they were never the group's to end. What I wrote in the forum stays written — my own export still returns it, and any report that pointed at a post still points at something real. And my export is finally whole: one download now carries my messages, my forum posts (honest about the tombstoned one), where I participated (sealed and left conversations included), and the reports I filed — snapshot and all. If the platform has suspended me, my right to that download survives the suspension: an admin hold pauses my participation, never my right of access. And no Mist has ever spoken in any of these rooms — not as policy prose, but proven: every door refuses, and the tables carry the structural zero. Every sentence above is a tested behaviour (E2E 2/2 + the contract tier beneath).

## The build in facts

- **Design (no ADR — the board settled in-session, recorded in specs + plan v5):** D2 = **preserve-and-seal, both events identically** (Option A; the ghost-thread bug named and killed); CB-6 firmed (right-of-access; mechanism = ungated actor resolution in export contracts only, the universal resolver untouched); scrub posture = retain-under-legitimate-interest in Ferd, mechanics → A-ADM with the queue; reporter's-own-reports join the export, snapshot included; **CB-1 reframed verify-and-record** (`ds5_require_fim_actor()` already walls every write door — the ephemerality slot is a proof, not a build).
- **Platform (FEAT-PD012, `6-done`):** `conversations.sealed_at` (the only DDL) · `ds5_lifecycle_group_closed` (ds3 template; reason-validated; REVOKEd — W12 answers 42501; returns `{conversations_sealed}`) called in-transaction from `close_group`/`delete_group` after the ds3 calls (U047 Option A) · sealed semantics on the C-A contracts (inbox/group-list exclude; send/join refuse P0001 *behind* the 42501 walls; detail deliberately unchanged) · `get_own_messages_export()` (four sections; `[]`-stable; `target_group_id` omitted as third-party identity) into the composite's `communication` key · `get_own_step_instances_export` re-issued ungated (**PC008 §155 closed at source**) · conformance allowlist += 1 (handler auto-allowed by prefix). Migration assembled from each function's **latest on-disk body via anchored replacements** (fail-loud; all 8 edit sites verified) — a shape worth keeping.
- **Surface:** none — the area's first surface-neutral cycle held its decomposition. One type-only touch (`DataExportCommunication`), named in advance by the payload walk.
- **Red→green, honestly:** 14 red pre-apply (42703 / PGRST202+42883 / **the 42501 "no session actor" suspended composite — §155 demonstrated live** / un-sealed behavioural fails) + 8 labelled greens (3× CB-1 regression proofs, sanity, invariant-holds) → post-apply 20/22 → **22/22** with two labelled adaptations: join-on-sealed answers the membership wall (P0001-via-join structurally unreachable — `delete_group` deactivates memberships in-transaction; seal check kept as defense-in-depth) and the reason-probe asserts `runAdminSql`'s *thrown* 22023. **One sibling adaptation (J-B budgeted):** PD007's "actorless → 42501" probe used the *suspended* fixture — precisely the asymmetry C-E repaired; suspended now asserts success, a session-less client carries the 42501 (28/28 after).
- **Sweeps at close:** integration **50 suites / 582 tests** green (the `mist-transcendence` fence did not fire) · unit **115 / 837** · `next build` clean (0 type errors) · lint 0 errors (2 pre-existing warnings, not this cycle's files) · conformance green · E2E **2/2 C-E journeys** + **fleet 76/77** (the 1: profile.spec STORY-4, the standing fence, green 3/3 isolated; TASK-E2E-01 unchanged).

## Found at build (all run to ground)

1. **The journal-export false alarm (retracted before it landed):** grep suggested `get_own_journal_export` carried the same `is_active` gate; its body disproved it (already ungated, by its own comment). The `is_active` hits belong to the journal's live contracts. §155's walks-only claim stands. Verify-before-asserting did its job.
2. **The Next 16 dev-lock finding (banked for the retro):** `next dev` takes a single-instance lock at `hub/.next/dev/lock` — a second dev server from the same checkout refuses to start. Consequence discovered the hard way: the first fleet run rode the *other* live dev server on :3000 (HMR, in use) and returned **37 spurious failures** (19 bare 30-second timeouts across surfaces this cycle never touched, plus stale renders). Diagnosed as environment, not regression; re-run against an isolated **`next start` production server on :3001** (no dev lock) came back **76/77 in 3.4 min**. New coexistence rule candidate: E2E fleets run against `next start`, never against a shared dev server.
3. **The PD011 announcements contingency adjudicated:** under preserve-and-seal, membership-gated invisibility suffices — the handler touches no `announcements` row; recorded in both specs, asserted in the suite.

## Open at close

1. **C-F next (Identity Cycle F — IDN-10):** self-service exit/deletion. **Starts at L4 authoring** — the exit/deletion specs were never written (verified at area kickoff, CB-5); builds over the now-real DS-3 freeze + DS-5 disposition layers; retires `admin_exit_user_from_platform` (Console question stays A-ADM-homed, COR-A F-3). Last cycle before the A-COM area gate.
2. **Area-gate dues visible from here:** the exit checklist's D2 row is now done; still open at gate time — oracle-port verification (B-MSG/B-COMM spine), the ADR-U043 measurement pass + Stefan's live walk, the substrate-audit stale note (`notifications` column list), legacy `postgres_changes` publication disposition check, DS-5 spec status advance from `proposed` (§8 Q1 residue and Q6 route to A-NTF / FEAT-time as recorded).
3. **A-ADM seams grown:** the resolution-time snapshot scrub (with the queue + `status` transitions) joins ADM-10's inheritance.
4. **TASK-E2E-01** unchanged (profile.spec fresh-logins + shared-storageState consumer audit) — the fence held again this cycle.
5. Standing items carry (TASK-MIST-01, TASK-DOC-003/004, TASK-OBS-01 at A-ADM, Vercel scale-to-one, logo, launch checklist). **The :3001 `next start` server was stopped at close; the :3000 dev server was never mine and was left untouched.** Stefan's uncommitted KB edit (`2026-06-15_knowledge-base_whisp-and-universe-foundations.md`, +84 lines) remains on the working tree, deliberately uncommitted by this session.
