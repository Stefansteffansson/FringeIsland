# Session bridge: 2026-05-14 (2) — PC-3 Organisation Step 3 complete; L1→L3 derivation lands at entity 3 of 4

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-05-14 (second bridge of 2026-05-14)
**Session type:** L-level architectural commit close-out. Phase 2 entity 3 PC-3 Organisation Step 3 final commit batch (3 of 3 planned commits land in this session). **PC-3 Organisation L1→L3 derivation completes at this commit batch.**
**Position in PC-3 sequence:** Final of three PC-3 entity bridges. Predecessors `2026-05-13_01` (Step 1 landed), `2026-05-13_02` (Step 2 landed), `2026-05-14_01` (Step 3 split: spec + ADR-U006 landed; ADR-U007 + ADR-U018 + closing bridge deferred to this successor session).
**Chronological predecessor:** `2026-05-14_01_-_PC3-STEP3-SPEC-AND-ADR-U006-LANDED.md`.
**Substantive predecessors:** same chain as Step 3 split (Experiment B restart + Experiment A + PC-2 + PC-1 entity bridges).

---

## Session arc

Successor to split-bridge `2026-05-14_01`. This session executed Tasks #4 (ADR-U007 amendment), #5 (ADR-U018 amendment), and #6 (this bridge). Pre-flight state-read pass at session-open: seven session bridges in order per predecessor handover + disk verification per Tripwire #4. All checks pass clean — `edf72d3` ADR-U006 amendment confirmed at L4 + L27; ADR-U007 + ADR-U018 confirmed un-amended; PC-2 spec un-amended on disk (Experiment B integrity); worktree topology two-worktree-list as expected; OLDFEAT directory present and unread.

Three commits land this session: `3697732` (ADR-U007 amendment, Step 3 Q2) → `dd84a02` (ADR-U018 amendment, Step 3 Q9) → this bridge. All three follow append-only Option A per ADR-U023 + ADR-U006 precedent established at split-bridge session.

Sub-batch-of-1 multi-Edit cadence held throughout. Each ADR amendment ran the standard ritual: fresh-read current text → scope decision → draft → structural inventory + Tripwire #4 anchor uniqueness verification → print-batch-before-gate surface to bouncer → bouncer tightenings absorbed → Edit 1 (Date line) → Edit 2 (section insertion) → single commit with structured body. Bouncer tightenings absorbed cleanly per cycle: four tightenings on the ADR-U007 draft (signature derivation removed; Tier-2 framing dropped for split-by-context; call-site narrative trimmed; category-count speculation dropped) and two tightenings + line-anchor disposition on the ADR-U018 draft (state-attribute phrasing; fresh-grep for (c) anchors restoring shape-parity).

---

## PC-3 Organisation L1→L3 derivation completes at this commit batch

**Explicit closure statement per PC-1 / PC-2 entity-bridge precedent.** PC-3 Organisation is the third of four Platform Core entities in Phase 2; its L1→L3 derivation closes at the commit batch landing in the split-bridge session + this session (spec at `1ee9acc`; ADR-U006 at `edf72d3`; ADR-U007 at `3697732`; ADR-U018 at `dd84a02`; this bridge pending). PC-4 Governance — the fourth and final Platform Core entity — still to come. PC-3 implementation via FEAT-PC3-* feature specs + code is downstream and not in scope for Phase 2.

Phase 2 close-out arrives after PC-4 lands.

---

## Load-bearing output

**Three ADR amendments cluster lands the full PC-3 Step 3 Q-resolution scope.**

- **ADR-U006 amendment** (commit `edf72d3`, split-bridge session): Step 3 Q8 — three-component scope (FK direction codified + immutability commitment + supervised-bypass discipline). Disk anchors: rebuild L102, protect L5-L11, fix_rc7 L514 + L601. Two refinements inline.
- **ADR-U007 amendment** (commit `3697732`, this session): Step 3 Q2 — four-component scope (disk signature + Tier-1/split-by-context composition + server-side Tier-1 calling conventions + permission count baseline). Disk anchors: rebuild L419-L475, fix_rc7 L514+, spec §6 + commit `255219d`, lib/constants/permissions.ts + archive migration.
- **ADR-U018 amendment** (commit `dd84a02`, this session): Step 3 Q9 — three-distinction scope (typed group entities vs `group_type` discriminator + typing vs entity-state + typing vs growth-vocabulary). Framing: clarification-of-intent, not contraction. Disk anchors: rebuild L87 + L48 + L57, add_display_name_system L22, sprint1_foundation_schema L24.

All three amendments follow append-only Option A: original Decision / Why / Consequences (where present) sections untouched; new "Implementation commitments (PC-3 Step 3 amendment, 2026-05-14)" section inserted before Links; Date meta line gains 2026-05-14 amendment entry. Three amendment shape variants precedented at this session-pair: **THREE-COMPONENT** (U006), **FOUR-COMPONENT** (U007), **THREE-DISTINCTION** (U018). Append-only Option A is the durable shape; sub-variant naming acceptable when substance-aligned.

Spec at `docs/platform/core/organisation-specification.md` commit `1ee9acc` is the authoritative source for all three amendments; ADRs codify their PC-3-relevant implementation commitments with explicit provenance back to spec §L3 Step 3 Q-resolutions + §5/§6/§8 sub-sections.

---

## Pickup lists

### PC-4 Governance (next entity in chain)

- **Admin audit log ownership** (PC-1 Finding #3 at full disk-anchor specificity confirmed at PC-3 Step 2 C3-5): `public.admin_audit_log` table + audit-content schema + access policies route to PC-4; PC-1 retains trigger-as-primitive. The rebuild migration crosses PC-1/PC-3/PC-4/DS-* boundaries (18 tables in one 2,223-line artifact).
- **DeusEx vocabulary grandfathering containment** (PC-3 §L3 Step 3 Q9 resolution (b)): DeusEx vocabulary layer-bounded (schema-tier + admin-helper-tier; absent from API-route layer). PC-4 should §3-confirm DeusEx vocabulary stays at admin-helper-tier (no PC-4-tier propagation expected per Step 2 C3-4).
- **Service-role governance soft-edge** (optional carry per PC-1 Finding #5): if PC-4 surfaces admin-tooling secrets or service-role escalation patterns, route to Finding #4 channel.

### DS-* (Phase 3 entities)

- **PostgREST RPC reusability watch** (PC-3 §7 design rule): the §7 three-justification design rule for `app/api/*` routes is PC-3-authored but may generalize to DS-* entities. DS-* Step 1 derivations should stress-test their own contract-surface positions against the PC-3 design rule; if reusable, promote to platform-tier or sub-tier rule at Phase 2 close-out.
- **A-candidate #9 promotion-watch** (framework-provided contract mechanisms invisible to cold derivation): DS-* Step 1 cold derivations should explicitly check for framework-provided contract mechanisms before declaring contract-surface positions. Promotion criterion: pattern recurs at DS-* Step 1s.

### Deferred PC-1 amendment candidates

- **Finding #4 two-tier centralization** (Gap A substrate + Gap B auth-flow plumbing): PC-3 Step 2 sharpened the framing from "single centralization decision" to "two-tier substrate + auth-flow." Adjudicate at Phase 2 close-out after PC-4 evidence.
- **X5 anti-pattern scope** (5 service-role-using sites / 6 createClient instances): three permissions gate the X5 routes (`invite_members`, `enroll_group_in_journey`, `manage_all_groups`); PC-1 amendment candidate at Phase 2 close-out.

### Deferred PC-2 amendment candidates

- **Q6 PC-2 amendment carry-forward** (deferred to post-Experiment-B per Experiment A bridge): PC-3 §8 Q6 carries PC-2 display-name coupling (`nickname` / `display_preference` / `show_real_name` on PC-2 territory; sync trigger to PC-3 personal-group name) to the PC-2 amendment list.
- **Experiment A 10 findings** (deferred to post-Experiment-B per Experiment A bridge): the 7 substance findings + 1 ADR-maintenance finding + 2 phase-wide observations remain deferred until comparison-phase analysis closes.

### OLDFEAT reconciliation

- **Post-Experiment-B work item**: blindness invariant held throughout PC-3 derivation (manual side did not consume `docs/TMP/OLDFEAT/` content; only directory listings inspected); reconciliation deferred to a future session after comparison-phase analysis closes.

### DevOps-tier Phase 2 close-out

- **P1 four-mode fresh-DB deployability concern** (per PC-3 §L3 Step 2 C3-1): four failure modes documented at spec; pickup at Phase 2 close-out with PC-4 evidence + supabase-local-dev test recommendation.
- **P11 archeology cluster** (asymmetric recovery — 2 system groups + permissions registry + role_templates + role_template_permissions not recovered post-D15): pickup at Phase 2 close-out with restoration-migration adjudication.
- **P5 PG17 substrate concern** (PG17 RLS-vs-PLPGSQL substrate documented in migration 10 strategy comment): held watch; pickup at Phase 2 close-out.
- **P6 supervised-bypass SQL-side-only scope** (locked to SQL-side per PC-3 §6 + ADR-U006 amendment (c)): no app-tier bypass equivalent exists or is planned; documentation discipline carry-forward to PC-4 + DS-*.

### doc-health-check pickups

- **ADR-U007 stale signature preserved-for-provenance pattern**: Decision text at L16 still reads `has_permission(user_id, group_id, permission_name)`; Implementation commitments section (a) explicitly resolves to disk signature. Reader-friction risk: cross-references to the Decision text alone could mislead. Cite-and-resolve flag for future doc-hygiene pass; not blocking.
- **Sub-tier `docs/platform/core/CLAUDE.md` anticipatory language** (carry-forward from PC-3 Step 1 bridge): `## Where to go next` bullet contains stale forecast about entity-level CLAUDE.md files becoming imminently expected. Known stale; deferred per Gate-2 stale-bullet disposition.
- **PC-3 carry-forward propagation watch**: as PC-4 + DS-* entities land, their entity-level CLAUDE.md cascade integrity wants doc-health-check verification at cycle boundary or post-Phase-2 close-out.

---

## A-candidate ledger snapshot at PC-3 close

- **#1 Latent-vs-delta distinction** — promotion-watch (carry-forward from PC-1 + PC-2; PW-2 compounded at PC-3 Step 1 + retracted at Step 2).
- **#2 Tier-shape escalation channel** — promotion-watch (carry-forward).
- **#3 Database-shaped L2 framing assumption** — promotion-watch (carry-forward).
- **#4 Schema-predates-partition** — PW-1 disk-anchored at full specificity at PC-3 Step 2 (D15 monolithic rebuild commit `ce58227` consolidating 71 migrations into 2,223 lines, predating partition + ADR-U023 + ADR-U018). Program-level-pattern promotion fully earned and expected at Phase 2 close-out.
- **#5 Multi-Edit gate emission discipline** — **RATIFICATION CANDIDATE at PC-4 entry.** Sub-batch-of-1 cadence held throughout PC-3 Steps 1+2+3-split + this session (cumulative ~30+ Edits across the PC-3 entity work). PC-4 Step 1 + Step 2 will exercise the discipline on a fresh entity; that's the canonical ratification event (cross-entity-replication step required).
- **#6 Cold-derivation-with-priors as methodology variant** — promotion-watch armed for PC-4 Step 1 cold derivation; variant exercised cleanly at PC-3 Step 1.
- **#7 Tool-payload verification distinction — structural-inventory-before-defect-assertion** — held throughout this session; no rendering false-positives observed.
- **#8 Single-migration-snapshot vs cumulative-forward read order** — RATIFIED at PC-3 Step 2 (§8 Q5 retraction via Cluster B); carrying forward; named-program-pattern promotion-watch armed for PC-4 + DS-* Step 2 recurrence check.
- **#9 Framework-provided contract mechanisms invisible to cold derivation** — promotion-watch armed; PW-2 retraction (PostgREST RPC canonical PC-3 surface) confirmed at three convergent disk anchors at Step 2.

---

## Methodology data points (this session's contributions)

Recorded as bridge-prose observations; promotion to spec Sources-status block SS entries is a separate future spec-amendment-commit decision (no spec touched this session). Future spec amendments can cite "see PC-3 closing bridge methodology section" and assign canonical SS numbers at that time.

**Process-side enumeration-claim-scope near-miss (OLDFEAT head-truncation).** Initial state-read at session-open ran `ls docs/TMP/OLDFEAT/ | head -3` and reported "3 AR-* files visible" — verdict-scope under-specified from a head-truncated listing. Stefan caught it; corrected count was 18 entries (5 AR-* + 9 FR-* + 1 NF-* + 1 INDEX.md + 1 _archive subdirectory). This is the SS-17 sub-shape A failure mode applied to a process-side state-read command (not a substance verdict in committed text). **Durable mitigation:** listing commands in state-read contexts use explicit counts (`ls dir/ | wc -l` or full `ls dir/`), not `head`-truncated previews. Tripwire #4 sub-case extension.

**Commit-shape verify-before-asserting near-miss (Task #4 commit message draft).** Initial commit-message proposal for Task #4 framed ADR-U006's amendment commit (`edf72d3`) as "subject-only" based on `git log --oneline` output, without fresh-reading the full commit body. Actual `edf72d3` body carried a full structured layout (append-only shape statement + component scope + refinements + Tripwire #4 disk-anchor cite + Provenance + path-convention note + no-push line + Co-Authored-By trailer). Stefan caught the misframe; fresh-read of `edf72d3` body restored shape-parity for Task #4 commit. SS-17 sub-shape A pattern on the process side again (verdict-scope generalization from limited inspection). **Durable mitigation:** before claiming commit-message shape, fresh-read the full commit body with `git log -1 --format=%B <sha>`; `--oneline` output is structurally insufficient.

**Fresh-grep vs spec-citation disposition data point (Task #5 (c) line anchors).** For ADR-U018 (c) growth-vocabulary disk anchors, the initial draft used spec citation only ("CREATE TABLE definitions in `rebuild_universal_group_pattern.sql`; specific line anchors deferred to spec"). Bouncer dispositioned fresh-grep instead — single targeted grep yielded L48 + L57 in seconds, restoring shape-parity with (a) + (b)'s precision. **Methodology observation:** cost-of-grep ≪ cost-of-shape-asymmetry at ADR amendment time; small evidentiary tightenings are worth the marginal cycle when they restore shape-parity. "Spec-citation-only" is a legitimate disposition only when fresh-grep cost is genuinely prohibitive (multi-line CREATE FUNCTION verbatim cites, for instance, as in Task #4).

**Three ADR amendment shape variants precedented.** Append-only Option A is durable across three ADR amendments this session-pair (U006 = THREE-COMPONENT SCOPE; U007 = FOUR-COMPONENT SCOPE; U018 = THREE-DISTINCTION SCOPE). The shape variant naming reflects substance: components-of-implementation (U006/U007 add new commitments) vs distinctions-clarifying-scope (U018 codifies what was already implicit). U018 also introduced a "framing paragraph" between lead and SCOPE block, justified by the load-bearing "clarification-of-intent, not contraction" semantic. **Durable shape:** append-only Option A is the established ADR amendment shape; sub-variant naming + section count + optional framing paragraph acceptable when substance-aligned. Future ADR amendments inherit this discipline.

---

## Tripwires

| # | Description | Status |
|---|---|---|
| 4 | Disk-of-record verification before asserting | ACTIVE — held cleanly throughout. Two near-misses caught at the boundary (OLDFEAT head-truncation + commit-shape under-inspection); both corrected before any committed text was affected. Sub-case extensions documented in Methodology data points section. |
| 6 | Discipline-as-deferral | NOT FIRING — discipline-stack altitude held throughout. Bouncing-partner cycle ratified per cycle; sub-batch-of-1 cadence preserved at all gate points. Tripwire remains armed for PC-4 entry. |

Tripwires #1, #2, #3, #5 — closed in prior bridges; not re-engaged this session.

---

## Repo state at session close

- Branch `main`. Three commits land this session: `3697732` (U007), `dd84a02` (U018), and this bridge (pending). After this bridge lands, branch will be 13 commits ahead of `origin/main` (was 10 at session-open; +3 this session).
- Branch `experiments/B-pc3-full` exists locally in the autonomous-run worktree at `D:/WebDev/FringeIsland-experiment-B/`; state determined by autonomous-run CC; not visible from manual side per blindness invariant.
- Pre-existing root `CLAUDE.md` modification still unstaged (carried across sessions; pre-existing context-mode plugin injection per Step 1 + Step 2 + split-bridge; outside experiment scope; not committed this session).
- **No push to origin** per Experiment B comparison-phase disposition. Comparison-phase analysis runs before push.

---

## Discipline posture for PC-4 entry session

All durable disciplines from PC-1 / PC-2 / Experiment A / Experiment B restart / PC-3 Steps 1+2+3-pair remain in effect:

- Bouncing-partner cycle (manual-run side only)
- Surface-draft + candidate ledger + tripwires armed
- Sub-batch-of-1 multi-Edit cadence default (A#5 ratification candidate at PC-4 entry)
- Print-batch-before-gate at L-level surface drafts
- Tripwire #4 disk-of-record verification (with two new sub-shape extensions this session: head-truncation + commit-shape claim under-inspection)
- In-commit-consistency discipline (fix Edit-introduced inconsistencies in same commit)
- Forward-only correction discipline (prior commits carry their own provenance)
- Cross-section fresh-read before drafting Edits on previously-touched content
- Structural-inventory-before-defect-assertion (A#7)
- Enumeration-claim-scoping (durable from SS-16; extended this session with head-truncation sub-case)
- Verify-before-asserting on commit-shape claims (new from this session: fresh-read full commit body before claiming shape)
- Append-only Option A as durable ADR amendment shape (three-precedent across U006 + U007 + U018)
- OLDFEAT (`docs/TMP/OLDFEAT/`) blindness invariant — listing-only, no content reads — carried forward through PC-4 + Phase 2 close until comparison-phase work disposition
- Experimental artifacts on experiment branches; canonical specs on `main` via deliberate provenance-citing commits
- No push to origin per Experiment B comparison-phase disposition

---

## Comparison-phase carry-forward

- Manual-side PC-3 state captured at commits `8d880d6` + `5592773` + `02bc9df` + `5dca171` + `255219d` + `fff7ef1` + `147853e` + `1ee9acc` + `edf72d3` + `300daa1` + `3697732` + `dd84a02` + this bridge (13 commits across the PC-3 entity manual-run sequence).
- Autonomous-side state on `experiments/B-pc3-full` (worktree at `D:/WebDev/FringeIsland-experiment-B/`): not interpreted; remains for comparison-phase analysis (separate future session per Experiment B restart specification).
- **PC-2 spec un-amended on disk** through entire PC-3 derivation (single commit `0565d65`; Experiment A's 10 substance findings NOT folded in). Experiment B comparison-phase integrity preserved through PC-3 close — load-bearing.
- OLDFEAT (`docs/TMP/OLDFEAT/`) unread on manual side through entire PC-3 derivation; blindness invariant preserved.
- Comparison-phase scope per Experiment A bridge + Experiment B restart bridge.

---

## Session-open prompt for CC (PC-4 entry, embedded; operational artifact filename per prior session-opener convention, to be created at PC-4 entry session-open as a separate `chore(planning)` commit)

PC-4 Governance L1→L3 derivation. Same three-step shape as PC-1 / PC-2 / PC-3 (cold derivation → code-informed stress-test → adjudication). Phase 2 entity 4 of 4.

State-read pass. Verify against disk per Tripwire #4:
- Branch state and tip commit (should be this bridge or later).
- Spec at `docs/platform/core/organisation-specification.md` — confirm landed as recorded at commit `1ee9acc`; §L3 Step 3 + §3/§5/§6/§7/§8/§L4 + Sources-status SS-17/SS-18/SS-19/SS-20 amendments all in place.
- ADR-U006 amended (commit `edf72d3`); ADR-U007 amended (commit `3697732`); ADR-U018 amended (commit `dd84a02`).
- This bridge file existence and disk-filename.
- PC-2 spec un-amended on disk (commit `0565d65` single — Experiment B comparison-phase integrity check). **If this verification fails, comparison-phase is compromised — pause and surface.**
- OLDFEAT `docs/TMP/OLDFEAT/` directory present (18 entries: 5 AR-* + 9 FR-* + 1 NF-* + 1 INDEX.md + 1 _archive); listing only, no content reads.
- Worktree topology two-worktree-list (main + `experiments/B-pc3-full` at `D:/WebDev/FringeIsland-experiment-B/`).

Authority chain for PC-4 cold derivation:
- L1: root `CLAUDE.md` + `docs/platform/CLAUDE.md`
- Sub-tier: `docs/platform/core/CLAUDE.md`
- L2 inventory: `docs/platform/core/README.md` (PC-4 line)
- ADR-U023 (Platform Core / Domain Services decomposition)
- Template: `docs/templates/platform-core-spec.md`
- **PC-3 carry-forward block** (this bridge §Pickup lists → PC-4 Governance): admin_audit_log ownership, DeusEx grandfathering containment, service-role governance soft-edge

Cold-derivation discipline: do **not** read `supabase/migrations/`, `lib/`, `app/`, or any existing `FEAT-PC4-*` during Step 1. Step 2 stress-test runs after cold draft lands.

Watch flags for PC-4:
- **A#5 ratification confirmation watch**: sub-batch-of-1 multi-Edit cadence is the ratification candidate at PC-4 entry. PC-4 Step 1 + Step 2 + Step 3 should exercise and ratify; record at §L3 sources-status block whether the cadence holds across the fresh entity's full L1→L3 derivation.
- **PW-1 + A#8 promotion at Phase 2 close-out**: PW-1 (schema-predates-partition) and A#8 (single-migration-snapshot vs cumulative-forward) are promotion-ready; expected promotion to named program-level patterns at Phase 2 close-out (after PC-4 lands).
- **A#9 framework-provided contract mechanisms**: PC-4 Step 1 cold derivation should explicitly check for framework-provided contract mechanisms (e.g., PostgREST RPC for `admin_*` orchestration RPCs).
- **Finding #4 carry-forward**: if PC-4 surfaces app-tier substrate adjacent to secrets/credentials, route to the Finding #4 two-tier channel (Gap A substrate + Gap B auth-flow).
- **`handle_new_user` cross-seam check**: PC-2/PC-3 accept-seam stands; PC-4 should §6 / §5 confirm no new cross-seam triggers introduced by Governance entity (admin operations are SECURITY DEFINER RPCs, not triggers).

Phase-2 close-out arrives after PC-4 lands. At that point: (a) revisit P1 four-mode + P11 archeology cluster with PC-4 evidence; (b) revisit PW-1 / A#8 / A#5 / A#6 / A#7 / A#9 for promotion to named program-level patterns; (c) revisit Finding #4 two-tier centralization adjudication; (d) revisit ADR-U007 stale-signature doc-hygiene flag; (e) decide on `ecosystem-decomposition` skill update for stress-test pattern at n=4; (f) fitness-check the three-day turnaround at phase level.
