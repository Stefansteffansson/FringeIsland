# Session bridge — 2026-07-10_04 — Cycle J-E CLOSED: gate executed, deep-cold measured, and the boot-scope staleness bug found-and-fixed

**Session class:** the continuation of the `_03` build session (same conversation, autonomous with Stefan's gate nod). Hub v2 Phase 3 state after this session: **Journeys J-A..J-E all `6-done`; Cycle J-E CLOSED 2026-07-10** (completion plan v9). Next: the J-E retro at the J-E→J-F boundary, then **J-F** (ADR-U046 capture + review substance), then the **J-O3 area gate**.

## What happened

1. **The two flagged E2E failures were root-caused and fixed (PR #166, merged)** — answering Stefan's "why do we get this and why can't we fix it?". Mechanism: `OverviewBoot`'s `BOOT_PATHS` regex (`groups(?:\/|$)`) also matched group **detail** pages, so a full-load arrival there prefetched the bundle and left a groups slice adopted-but-unconsumed; after a leave/hand-over, the client-side `/groups` mount consumed that **pre-mutation** slice (consume-once) and painted the departed group with no revalidation ever firing. Not a race — a stale adopted read satisfying the mount's only read, red the full 15 s retry window, reproduced identically at main HEAD. Fixed red-first (the detail-page unit case + the two E2E arcs as the red): boot fires on the groups **home** exactly; arcs **10/10 green**, unit 675/675. **The same latent shape in H023's `ARRIVAL_PATHS` was fixed on the branch before its merge** (a deep-linked never-arrived FIM opening a shared group link would have been yanked into the welcome) — red-first, unit 693/693.
2. **The gate executed (Stefan's explicit "ok merge"):** PR #163 (PD006 schema gate) merged, #164 retargeted to `main` per the stacked-PR rule and merged, branches cleaned. Process note repeated from `_02`: the permission classifier refused both the generic "okay to continue" and "go on!" as gate approval — only the explicitly-named "ok merge" unlocked it; the classifier is the schema-gate protection working as designed.
3. **TASK-JE-07 deep-cold spot check RAN (ADR-U043 Amendment 1)** — first measurement of a J-cycle on the target Node/Fluid runtime, and it is clean: deploy `a1dd9b2` confirmed live 20:16:32Z; a fresh **arrived-once** measurement FIM signed in (last setup traffic 20:18:41Z); **23.3 min enforced zero traffic**; one authenticated walk of `/groups` at 20:41:58Z. The landing made **exactly one API request** (`GET /api/me/overview`) answered **599 ms server-side on the instance's first invocation** (`x-overview-timing` `n:1`; auth 185 ms; six concurrent slices 337–413 ms, the new `onboarding` slice 407 among them). **Zero standalone `/api/me/onboarding` calls** (the B1 rides-the-bundle claim verified deep-cold) and **no relaunch** (arrived-once honoured on production). Wall-to-network-idle 6.1 s (cold headless browser + full asset load — labelled as a different quantity than the §8 fan-out numbers). Measurement account erased via the full D15 chain.
4. **Cycle J-E CLOSED (PR #167, merged):** FEAT-PD006 + FEAT-H023 → `6-done` with §L4 inventories, both feature indexes, completion plan v9 (J-E CLOSED row + gate deltas recorded), CHANGELOG entries (the cycle + the #166 fix), TASK-JE-01..07 all `done`. Dashboard refreshed.
5. **doc-health-check RAN at the boundary (delegated agent, report-only): effectively clean.** Cycle J-E's deliverables are documentation-clean — inventories reconcile both directions (§8 journeys 5/5, hub 23/23), maturities honest, cascade holds, the edge→Node sweep verifies in code, no new drift. **One pre-existing backlog finding:** `docs/architecture/ARCHITECTURE_ANATOMY_V1.md` predates the ADR-U025/U029/cosmology reconciliations — still names "AI Mentor" (→ Whisp), "the game" as a product, and "The Other Side"; recommend reconcile-or-banner-as-frozen-baseline. Soft: pre-ADR-U026 "FringeIsland Studio" naming in future-wave studies; ADR-U029 absent from the graduation tracker (likely out of scope — one human glance). Full report goes into the J-E retro.

## Open items / next session (the J-E→J-F boundary)

1. **The J-E cycle retro** — inputs staged: the `_03` bridge (build decisions + plain-English walkthrough), this bridge (§1's staleness lesson: *an adopted slice is a read that already happened — never arm adoption on a page that won't consume it*; the classifier-as-gate process note), the doc-health report above, and the key numbers below. TASK-JE-01..07 sweep after the retro commits.
2. **ARCHITECTURE_ANATOMY_V1.md reconciliation** — the doc-health backlog item (reconcile to ADR-U025/U029/cosmology, or banner as frozen v1 baseline).
3. **J-F decomposition** (ADR-U046 — response capture + review substance; renders the takeaways J-E seeded; the richer review entry returns).
4. **J-O3 area gate riders logged:** the journey-detail Mist seam (fake-door avoidance kept it FIM-gated), the enrolled-catalogue nuance (pinned as current semantics), the L3 fan-out reduction candidate, Stefan's live walk + the formal budget/tail pass.
5. **Real onboarding content is CQ-010** (the placeholder's re-authoring hook is planted; keep step structure stable for in-flight walks).

## Key numbers (for the retro)

Gate-to-closed in one session: PRs #163/#164/#166/#167 merged (+ #161/#162/#165 earlier). Deep-cold: 23.3 min idle → 1 request, `n:1`, **599 ms server** (slices 337–413 concurrent), 0 standalone arrival calls, 0 relaunches. #166: 2 E2E arcs red at main HEAD → 10/10 green; unit 675/675 (main) / 693/693 (H023 branch). Cycle totals stand as in `_03` + the boot-scope fix (1 regex, 2 unit cases) + the close-out docs (12 files).
