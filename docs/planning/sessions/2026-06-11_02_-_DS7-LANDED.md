# Session bridge: 2026-06-11 (2) — DS-7 Intelligence L1->L3 derivation complete

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-06-11 (second bridge of 2026-06-11)
**Session type:** Autonomous L1->L3 run, single-session (Steps 1+2+3). **Seventh instance of the autonomous L1->L3 template**, baseline `766e134` (unchanged since DS-4). **NO FIRST DECISION** (third no-decision run; the DS-7 name was already dispositioned in place in PENDING.md). **THE WHISP-SPLIT ADR PROMOTION FIRED** — the first PENDING-to-ADR promotion of the descent series (ADR-U029). **Collapsed author-then-execute shape** (DS-3/DS-5/DS-6 precedent).
**Session opener:** [`openers/archive/ds7-intelligence-descent-opener.md`](./openers/archive/ds7-intelligence-descent-opener.md) (authored at `e5ea0c7`, archived in this close batch).
**Chronological + substantive predecessor:** [`2026-06-11_01_-_DS6-LANDED.md`](./2026-06-11_01_-_DS6-LANDED.md).

---

## DS-7 Intelligence L1->L3 derivation completes at this commit batch

Spec at `docs/platform/domain/intelligence.md` (commit `255daad`): L2 identity + boundaries + six contract families + ten §7 service-level invariants + §8 nine questions; §L3 fourteen-capability inventory across five internal areas (Dialogue & being-state / Profile accumulation / Sensing & shaping feeds / Generation / Guard railing & lifecycle) + dependency chain + external dependencies + Sources-status + Step 2 + Step 3 blocks. **ADR-U029 Whisp ownership split by face** promoted from PENDING.md (commit `1ac2147` — the parked candidate executed verbatim in substance; PENDING entry dispositioned, world-model.md promotion remark updated, README pointer repointed, same commit). Entity CLAUDE.md at `intelligence/CLAUDE.md` (commit `9972171`, registry row removed same-commit — fourth consecutive clean removal). This bridge + STATUS.md update close the batch. **With DS-7 landed, all seven Domain Services are specified; the Extension System is next, then the Phase 3 close-out.** **No push to origin** — Stefan dispositions push.

## Session arc

All five pre-flight checks passed; Check 4 shape (a) fired at session open (the root CLAUDE.md context-mode re-injection — `git restore` classifier-denied, removed via the sanctioned Edit fallback); shape (b) did not fire in-session (the parallel writer's `4019e32` "translation complete" pre-dated the session). State-read consumed at the collapsed-shape authoring pass with zero material divergence; **one corrected inheritance surfaced at authoring** (below). NO FIRST DECISION; Step 1 cold derivation, checkpoint ratified in full (14/5 slate, the four routed-seam positions, six consumer-line confirms, the L2-line altitude finding, single-session shape), single Write held uncommitted. Step 2 ran as sandboxed sweeps with every zero dual-method verified: **zero cold retractions, zero Class 2 deltas, 2 Class 3 findings**. Step 3: five sanctioned seam amendments, README + domain CLAUDE.md updates, entity CLAUDE.md, **the ADR-U029 promotion at its own ratification gate**, this bridge.

## Ratified decisions (all by Stefan, 2026-06-11, this session)

1. **The Step 1 candidate as surfaced** (14 capabilities / 5 areas / 10 invariants / 9 questions; all four routed-seam cold leans).
2. **Salience-channel shape (world-model.md §8 Q7): push event** — DS-7 calls DS-1's salience intake contract; a derived read would invert "the world never depends on the intelligence." Amendment landed (`255daad`).
3. **Generation posture (content.md §8 Q8 + journeys.md §8 Q3, joint): the same role-gated write-path with a distinct audit posture** — attributable as AI-generated, under a human's authority, never shipping unreviewed (PRINCIPLES-AI); no dedicated generation seam. Amendments landed (`255daad`).
4. **Whisp-carried messages (communication.md §8 Q5): no** — the branch is the FIM-FIM channel; dialogue is the being-face, never a transport. Amendment landed (`255daad`).
5. **Recommendation-signal supply (discovery.md §8 Q2): declared interests only at Ferd**; DS-7-accumulated signals later only under explicit bucket-level PC-2 consent, never silently. Amendment landed (`255daad`).
6. **THE WHISP-SPLIT ADR PROMOTION (scope gate ratified separately): ADR-U029** (`ADR-U029-whisp-ownership-split-by-face.md`), a new numbered ADR from `templates/adr.md`, Status Accepted, dated to the DS-1 ratification — DS-1 owns world-presence / DS-7 owns the being / DS-7 consumes DS-1, never reverse; the eighth-service alternative recorded as rejected. Nothing in the derivation contradicted the split. Landed `1ac2147`.
7. **Six sibling consumer-line re-checks: all six confirmed without edit**; narrative.md's loop-persistence "DS-3/DS-7 territory" wording tolerates the Q8 resolution (DS-3 owns runtime persistence; DS-7 receives only consented accumulation through its own family — no direct sibling write).
8. **L2-line altitude finding ratified:** the README DS-7 line gained the dissolved-assessment framing, the PRINCIPLES-AI anchor, starved-drive sensing, and the outbound shaping feeds (`255daad`); pointer repointed to ADR-U029 at the promotion commit.

## The corrected inheritance (calibration event)

The DS-6 bridge's pickup block stated that `profile_data` (ADR-U005) is "realized DS-7-adjacent substrate in the baseline." **Wrong on disk:** zero realizations anywhere — live migrations, archive, seeds, lib, app (exact-pattern + loose second method; the loose method's only hit is `lib/types/user.ts` `ProfileData`, a PC-2 profile-display interface — name collision only). The same pickup block carried "calibrate, don't inherit" — **its own instruction caught its own error** at opener authoring. ADR-U005 is an **unrealized lock** (the DS-5 ADR-U021 law-stands-unrealized shape). The realized DS-7-adjacent vocabulary is instead the **assessment step-type cluster** (DS-3/U008 territory, in canon-tension with S17's dissolved-assessments lock — classified out as DS-3 evolution debt).

## Forward-commitment classification

**ALL FOURTEEN capabilities full-forward** — zero DS-7 substrate exists (no tables in the re-verified 19-table baseline, no lib module, no Edge Functions, no AI-provider dependency; every zero dual-method verified). The purest near-zero entity of the series: DS-7's only realized adjacency is another service's vocabulary.

## Pickup lists

**Extension System (next entity; its opener must inherit):**
- **All seven DS specs now exist** — the sibling-provisional rule inverts: the Extension System derivation re-checks every service's extension-point claims (each spec's §5), including DS-7's four registries (bucket kinds, sense kinds, rail kinds, dialogue-context providers — `intelligence.md` §5, provisional).
- ADR-U008 + ADR-U018 are THE locks; the Ferd non-closure constraint is the sub-tier's own authoring discipline (domain CLAUDE.md).
- Opener-authoring notes: ADR-enumeration-by-grep nouns (extension/plugin/step.type/registr/hook/contract); expect near-zero-code with the named exception of the realized step-type substrate (`lib/types/journey.ts` `StepType` CHECK-list closure — DS-3 territory, the recorded correction target); **sweep `supabase/seeds/` alongside migrations** (the rule held at n=2 this run); template applicability check — the Extension System is "Platform Domain — extensions," not a numbered DS; STATUS.md carries it Pending.
- The DS-7 close-out slate rides through it (below) if the close-out follows immediately.

**Phase 3 close-out (the adjudication slate accumulated across the series):**
- **#4 migration-name-as-shorthand: the landing adjudication is due** — n=7 opportunities, exactly 1 decisive firing (DS-5 sprint3); promotion with that firing as evidence, or retirement.
- **Tier-CLAUDE-as-L1-boundary-authority: n=3** (1 fire at DS-5, no-fires at DS-6/DS-7) — adjudicate opener-practice vs template text.
- **Seeds-directory-in-canonical-table-reads: n=2** (fired productively at the DS-7 authoring; held at Step 2) — template §5b candidate.
- **Constitutional-docs-in-authority-chain: n=1, new** (PRINCIPLES-AI.md at DS-7) — should template §3 name constitutional ecosystem docs as a standing slot for entities whose substance touches them?
- **The retraction-rate series is complete for the seven DSs:** PC-4 7/9; DS-1 0; DS-2 0; DS-3 0; DS-4 0; DS-5 0; DS-6 0; DS-7 0.

**Privacy vertical:** U010's "AI data handling" obligations over DS-7 (`intelligence.md` §8 Q7 — portability/retention of `profile_data` buckets); bucket-level consent posture (PC-2 surface); Shadow-Whisp TTL ephemerality + atomic transcendence (U027/S46); dissolved-instrument data sensitivity (validated-instrument results stored beneath dialogue).

**Observability vertical:** dialogue / accumulation / enforcement / feed events — all content-free by design (the privacy invariants bind event payloads).

**doc-health / PC-3 channel:** `seeds/01_permissions.sql` line 39 — `complete_journey_activities` description "Complete journey activities and assessments" carries pre-canon assessment vocabulary in the canonical permission catalog (the DS-6 stale-pointer family).

**DS-3 (anchor, no action):** the realized assessment step-type cluster (StepType union + questionnaire-form seeded steps) is in canon-tension with S17 — recorded as DS-3 evolution debt; the dissolved-instrument engine (`intelligence.md` §L3) is the forward shape.

**Hub / Gimbal (product tier):** Whisp surfaces at FEAT time are shell/feature work over the dialogue, own-state, accumulation-control, and rail families; equipment-keying stays feature-grain (U025).

**Doc-health channel:** cascade additions this session: `intelligence/CLAUDE.md` created, registry row removed (`9972171`); domain README DS-7 line revised twice (altitude `255daad`; ADR pointer `1ac2147`); domain CLAUDE.md enumeration now says all seven exist (`255daad`); ADR-U029 created (`1ac2147`); no new assertion-bearing diagrams (the dependency SVG already carries Intelligence — verified consistent with the derived dependency set, including "Intelligence also reads Discovery and the Extension System").

## A-candidate ledger snapshot at DS-7 close

- **A#1, A#2, A#3, A#6, A#7** — carry forward as framings.
- **A#9: no realized object** (fifth data point: PC-3 fire, DS-4 classified-out, DS-5 fire, DS-6 fire, DS-7 no-object) — the realized RPC/Realtime surface contains nothing DS-7-shaped to mistake for a contract.
- **A#8 cumulative-forward** ran the lineage; no mid-stream retraction existed to catch at a zero-substrate entity.
- **PW-1: no object** (second consecutive — no DS-7 schema predates the partition).
- **PW-MARCH1: clean** — nothing AI/accumulation-shaped lost at D15 (the archive's only relevant vocabulary is assessment step content in retired journey seeds, lineage adjudicated at DS-3).
- **PW-5:** 19-table baseline re-verified (two methods: CREATE TABLE enumeration + RLS-enable count, both exactly 19).
- **PW-T1: no fire** — the `ProfileData` collision is name-only (PC-2 display furniture).
- **Retraction-rate series:** complete (see Phase 3 close-out pickup).
- **#4: NO FIRE** (n=7, 1 firing) — the landing adjudication is the close-out's.
- **Tier-CLAUDE watch: n=3, no-fire** (the sub-tier's "Intelligence's AI-derived data obligations under Privacy" line is cascade-placement, not boundary law).
- **Seeds rule: n=2, held** (fired at authoring; confirmed at Step 2).

## PW status at DS-7 close

PW-1: no object. PW-MARCH1: clean. PW-2: two §8 questions tagged speculative-third-shape (Q1 dialogue-runtime substrate; Q9 graduation disposition) — both deferred by design. PW-5: held (re-verified). PW-T1: no fire.

## Methodology data points (§13 capture)

**Seventh-instance framing — the fourteen cumulative template revisions held as template text.** The workhorse was again **empty-result verification**, which this run added a third catch *class*: a **tool-level false-empty** — the sandbox grep's `-o` flag silently returns empty output; the method-contrast rule (file-level + `-n` methods disagreeing with it) blocked false-zero detail claims before assertion. The rule's catch classes now span repo level (DS-6 seeds relocation), claim level (DS-4 false positives), and tool level (DS-7). **ADR-enumeration-by-grep + citation-precision** again out-performed inheritance: U005/U010/U016 entered the binding set with verified text, and **PRINCIPLES-AI.md entered the chain entirely outside the ADR mechanism** — the register row's "guard railing" foot traces to a constitutional doc no predecessor carried.

**Prompt 1 — worked well:** the opener's pre-verified routed-seam slate (five seams, exact line quotes) made the resolutions near-mechanical at Step 3; the **corrected calibration at authoring made Step 2 sizing exact** (zero surprises); the SETTLED block prevented re-litigation at the obvious temptation points (the split itself; the feed-vs-recommendation boundary when writing the signal-supply capability); the **promotion-as-separate-gate shape ran clean** — scope surfaced (shape/number/citations), ratified, executed verbatim.
**Prompt 2 — redundant/confusing:** nothing misfired. The MADR template's Considered-options/pros-cons sections required modest expansion of the PENDING entry's one-line eighth-service rejection — drafting latitude, not substance latitude; within the verbatim-in-substance discipline.
**Prompt 3 — wished was named:** a **constitutional-docs slot in template §3** — the authority chain names L1/cores/ADRs/template/carry-forward, but constitutional ecosystem docs (PRINCIPLES-AI.md here; VISION/MANIFESTO for other entities) have no standing slot; DS-7 found it via the register-row trace, not via the template. n=1; rides to the Phase 3 close-out slate.
**Prompt 4 — new substance priors for the Extension System:** the pickup block above (all-seven-specs inversion; the U008/U018 locks; the realized step-type closure as correction target; the seeds rule).
**Prompt 5 — new A/PW observations:** the tool-level empty-result catch class (above); **pickup-block claim hygiene** — a predecessor bridge's factual claim was wrong on disk and the calibrate-don't-inherit instruction it carried caught it; no new watch needed (the existing rule is the defense and it worked), but the event is the strongest confirmation yet that pickup-block claims are memory, not disk. The first PENDING-to-ADR promotion mechanics held cleanly (parked-candidate-executes-verbatim; one named gate; same-commit disposition).

## Template revision disposition

**NO REVISION LANDED; the rider slate consolidates at the Phase 3 close-out** (DS-7 was the last Domain Service — the close-out adjudicates with full-series evidence): #4 (n=7, 1 firing — promote or retire); tier-CLAUDE watch (n=3, 1 fire); seeds-directory rule (n=2, productive — template §5b candidate); constitutional-docs slot (n=1, new). Rationale: one consolidated adjudication with complete evidence beats a piecemeal landing one entity before the series closes.

## Carry-forward confirmations

CODE stayed a correction target. ASCII-only labels held. The 2026-04 Hub L3 working set was not used. OLDFEAT: not listed, not read. Sessions append-only. The settled classifications (the split — promoted, not reopened; notifications boundary; attachment seam; profile-media vs profile-data; feed-vs-recommendation) were consumed, never reopened; the anti-leaderboard guardrail was consumed as invariant and now binds DS-7's outputs (§7 invariant 6). One PENDING.md disposition (the Whisp-split entry — promoted to ADR-U029). **One new ADR (U029, the promotion); zero ADR amendments.** The cross-tier-write channel, PC-1 Finding #4 (consumed as frame for Q1 only), and the avatars-bucket routing were not touched.

## Repo state at session close

Branch `main`. Commits this session: `e5ea0c7` (opener) + `cb054ca` (STATUS In flight) + `255daad` (spec + five seam amendments + README L2-line + domain CLAUDE.md, 8 files) + `9972171` (entity CLAUDE.md + registry row) + `1ac2147` (ADR-U029 + PENDING disposition + world-model remark + README pointer) + this bridge with the opener archived + STATUS.md close. The context-mode injection was discarded at session open per the ratified disposition (Edit fallback after classifier denial); no novel-path commits intervened in-session. Tree otherwise clean. **No push to origin.**

---

*DS-7 Intelligence L1->L3 derivation completes at this commit batch. All seven Domain Services are specified.*
