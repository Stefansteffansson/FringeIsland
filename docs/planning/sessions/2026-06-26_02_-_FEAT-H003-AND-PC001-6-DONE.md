# Session bridge — FEAT-H003 + FEAT-PC001 both 6-done (Mist arrival complete, IDN-1 closed)

**Date:** 2026-06-26 (follows session `2026-06-26_01`)
**Session type:** build + finalize
**Status:** Open — the Mist **arrival half (IDN-1) is complete**: FEAT-H003 (Hub) and FEAT-PC001 (Platform Core) are **both `6-done`**, full suite + lint + build green, E2E green. **Not committed, not pushed** (Stefan gates). doc-health-check + dashboard refresh recommended before close.
**Participants:** Stefan + Claude

> Durable handoff. Picks up the prior bridge's open work (the FEAT-H003 UI + E2E increment + both 6-done finalizes) and closes it. IDN-1 (the Mist on arrival) is now whole on both tiers; IDN-2 (transcendence + the robust reaper) remains FEAT-H004 ↔ FEAT-PC002.

---

## Session summary

Built the FEAT-H003 UI + E2E increment TDD red-first (unit tier as the driver), backfilled the substrate-emergent integration coverage (labelled test-after), and ran the full pyramid green. Then finalized **both** paired features to `6-done` with honest Implementation notes, §L4 + README updates, and the project's **first CHANGELOGs**. One process decision: the CHANGELOG.

## What was decided / locked

- **CHANGELOG: follow the decided procedure, don't divert.** The `feature-development` skill ("update CHANGELOG.md for user-visible changes") + both task ACs call for a CHANGELOG; FEAT-H001/H002 finalizes had *skipped* it (a deviation, not a precedent). Created **`hub/CHANGELOG.md`** (backfilled H001 2026-06-24, H002 06-25, H003 06-26) and **`docs/platform/core/CHANGELOG.md`** (FEAT-PC001). *Locked — Stefan's steer: follow procedure unless there's a reason to divert.*
- **§L4 migration attribution corrected (build-informed).** The Hub §L4 row said FEAT-H003 "carries a net-new migration" — pre-dated the Q4 paired-spec split. FEAT-PC001 owns the migration; the row now reads "consumes the FEAT-PC001 substrate, no migration of its own." *Locked.*

## What was produced (code — uncommitted)

- **AuthContext three-state + facade** — `hub/lib/auth/AuthContext.tsx` gains `identity` (= `deriveIdentity(user)`, memoised in render, never queried in the auth listener) + a `beginMist()` facade emitting `mist.entered` / `mist.enter_failed` (V4, failures included; `reaperRealised:false` records the accumulation gap).
- **Sessionless entry** — `hub/app/page.tsx` (replaced the placeholder Home): identity-aware doors — sessionless → Sign in / Sign up / **Look around**; FIM → "Continue to your groups" (no Mist chrome); Mist → "Keep looking around".
- **Minimal Mist-presence landing** — `hub/app/mist/page.tsx`: identity-level only (real beginning + become-a-FIM CTA → `/signup`), status-gated (FIM → `/groups`, sessionless → `/`).
- **Tests:** `tests/unit/lib/auth/AuthContext.test.tsx` (5, red-first), `tests/unit/app/entry.test.tsx` (4, red-first), `tests/unit/app/mist-presence.test.tsx` (4, red-first), `tests/integration/auth/mist-continuity.test.ts` (2, **test-after, labelled**), `tests/e2e/entry.spec.ts` (3) + `cleanupAnonymousUsers` janitor in `tests/e2e/helpers/auth.ts`.

## What was produced (docs)

- **FEAT-H003** → `6-done` + Implementation notes; **FEAT-PC001** → `6-done` (from 4-ready) + Implementation notes.
- §L4 rows: Hub `SPECIFICATION.md` (maturity + migration-attribution fix) + `identity-specification.md` (maturity). Both `features/README.md` index rows → 6-done.
- New: `hub/CHANGELOG.md`, `docs/platform/core/CHANGELOG.md`.
- Task statuses → `done`: `TASK-H003-02..06`, `TASK-PC001-01` (was review), `TASK-PC001-02`.

## Verification (DoD met)

- **Full Jest 41/41 (12 suites)** · `npm run lint` clean · `npm run build` clean · **Playwright 12/12** (3 new entry-journey + 9 existing FEAT-H001/H002 — **no regression**).
- **Test pyramid upright + honest:** every story has a unit-tier criterion **demonstrated red first**; the substrate-emergent negative properties (continuity unlinkability, no-PII) are **backfill test-after, explicitly labelled** in the spec + the test header; the E2E is the outside-in composition check (green first full run because its behaviours were red-first below).
- **Run from `hub/`** — `npm test` at repo root runs the wrong project (86 suites fail, 0 tests; a pre-existing root jest setup, unrelated). Always `cd hub` (or `--prefix hub`).

## Build-informed findings

- **`mist.entered` carries `reaperRealised:false`** — the accumulation gap is now observable in the telemetry stream, not just documented. No FEAT-PC002 reaper yet; E2E mints + reaps its own anon Mists via `cleanupAnonymousUsers` (lists anon auth users, deletes journeys → proto group → auth user).
- **Observability is realised at the consuming seam, not the trigger.** FEAT-PC001 STORY-5's Mist-session-creation event is emitted by the Hub's `beginMist` (the only app code in the anon-sign-in path); the substrate is a SECURITY DEFINER trigger with no app-telemetry surface. Documented honestly in PC001 notes.

## What is still open

- **Close-ritual items (Stefan's disposition):**
  - **doc-health-check** recommended — cross-cutting this + the prior session (schema migration, ADR edits, Visitor→Mist rename, new specs, new CHANGELOGs, replaced Home route). Watch for stray `Visitor`/`Guest` and cascade integrity.
  - **`npm run dashboard`** — refresh the overview snapshot.
  - **Commit + push** — this session's work is **uncommitted**; prior commits `bf34e9e` + `adb1906` + the prior bridge are **unpushed**. All gated on Stefan.
- **Accumulation gap still LIVE** — anonymous sign-in on with no reaper; Mist rows accumulate until **FEAT-PC002**. Known, bounded, logged.
- **Stray `hub-legacy/tests/e2e/.auth/user.json`** (gitignore gap) still untracked.

## Deferred (IDN-2)

- **FEAT-H004 ↔ FEAT-PC002:** atomic Mist→FIM transcendence, the robust ephemerality/TTL reaper + GDPR erasure, consent substrate (PC-2 §8 Q8/X4). The become-a-FIM CTA routes to FEAT-H002 sign-up today; it does **not** migrate Mist data.

## For the next session

- **Read order:** this bridge → `docs/products/hub/features/FEAT-H003-mist-identity-on-arrival.md` (Implementation notes) → `docs/platform/core/features/FEAT-PC001-mist-anonymous-substrate.md` (Implementation notes) → `hub/app/page.tsx` / `hub/app/mist/page.tsx` / `hub/lib/auth/AuthContext.tsx` (the landed surface).
- **Likely next:** either (a) run the close ritual (doc-health-check + dashboard + commit/push), or (b) open the next Phase-3 area (Groups), or (c) scope FEAT-H004 ↔ FEAT-PC002 (IDN-2) when the consent + reaper decisions are ready.
- **Orientation:** anon sign-in ON; live Supabase wired (`hub/.env.local`); tests `cd hub && npm test` (all) / `npx jest --selectProjects unit|integration` / `npm run test:e2e` (needs `npm run dev`).
