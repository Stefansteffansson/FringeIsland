# Autonomous L1->L3 session-opener — `V3 Notifications`

**Template path:** `docs/templates/autonomous-l1-l3-session-opener.md`
**Instance landing path:** `docs/planning/sessions/openers/notifications-descent-opener.md`
**Instance authored:** 2026-06-12, against template baseline `c1554f2`.

> **Twelfth instance of the autonomous L1->L3 template — the FOURTH VERTICAL**, and the first authored against baseline `c1554f2` (the rider promoted at the V1 close: Step-1-realization-claims-need-disk-anchor is now STANDING §5a text — this opener carries ZERO riders; the §5b calibration block below is the sanctioned disk-anchor source the standing text names). **NO FIRST DECISION** (PENDING.md verified zero V3-relevant entries at authoring; first ratification gate is the entity-shape gate, then the Step 1 checkpoint). **THE ENTITY-SHAPE ADJUDICATION GATE FIRES** (fourth vertical firing; standing §4 text; all three prior verticals held). **The named trap for this run is `lib/email/send.ts`**: an email-service abstraction whose header comment names Resend/SendGrid and whose `sendEmail()` is a console.log simulation — abstraction realized, delivery NOT; no vendor dependency exists in `package.json`. A Step 1 that claims realized email delivery has fallen into exactly the promoted discipline's class.

---

## §1 Pre-flight checks — STOP

Run all five; hard-fail on deviation; surface and wait.

1. **Working directory:** `/d/WebDev/GitHub/FringeIsland` (or Windows-equivalent).
2. **Branch:** `main`.
3. **Tip commit:** at or after `8921958` (the Administration STATUS-close commit; pushed to origin 2026-06-12). Acceptable interveners: this opener's own commit, its STATUS In-flight commit, the cc-execute-prompt.md commit, and `docs/novel/` path commits.
4. **Working tree.** Default clean. Two named acceptable shapes: (a) the root `CLAUDE.md` context-mode re-injection — **the sentinel (`28b2fa1`) held at the V1 run (first clean tree in seven sessions)**; if it fires anyway, disposition is Edit-removal (judge the residual `M` flag by `git diff` content-emptiness; CRLF stat-noise acceptable); if it stays quiet a second time, the closing bridge records the Edit-removal disposition as retire-as-routine. (b) Concurrent `docs/novel/` activity — do not read, modify, or commit. Anything else unnamed: surface and wait.
5. **Template baseline:** most-recent-touch of `docs/templates/autonomous-l1-l3-session-opener.md` expected at `c1554f2`. Hard-fail if earlier; soft-flag and adjudicate if later.

## §2 State-read pass (ordered)

1. **`docs/planning/sessions/2026-06-12_04_-_ADMINISTRATION-LANDED.md`** — chronologically-immediate predecessor; carries THE INHERITANCE BLOCK (consume in full), the promotion-as-amendment FIRST-DECISION shape, the calibration-count lesson (counts derived from the same enumeration that produces the names), and the sentinel observation.
2. **`docs/planning/sessions/2026-06-12_03_-_OBSERVABILITY-LANDED.md`** — the V4 seams V3 consumes (trigger-completeness law; delivery failures as observability events; the no-ratified-content inversion precedent — V3 repeats that shape).
3. **`docs/planning/sessions/2026-06-12_02_-_PRIVACY-LANDED.md`** — the V2 seams (external-delivery privacy boundary; consent/preference law; sandbox tool catches).
4. **`docs/planning/sessions/2026-06-12_01_-_PHASE-3-CLOSE-OUT-LANDED.md`** — the no-fork verticals decision and confirmed routings.
5. **`docs/planning/sessions/2026-05-14_03_-_EXPERIMENT-B-COMPARISON-PHASE-COMPLETE.md`** — autonomous-discipline carry-forwards (load-bearing sections per template).

Then verify against disk:

- **The spec EXISTS** at `docs/verticals/notifications/SPECIFICATION.md` — id `V3`, name `Notifications`, status `draft`, **`last_updated: 2026-04-26` — SCAFFOLD-PURE, the V4-shape no-ratified-content inversion** (pre-dates Session B and ADR-U028; last content shaping was the 2026-04 L2/L3/L4 migration `ff40d1d`; the 2026-06 touch `814395b` was a doc-health link repoint only). Current shape: §1 one scaffold paragraph (the spam-vs-trust framing is sound substance to refine); §2 six scope bullets (**the "Mobile push (iOS + Android)" bullet pre-dates ADR-U025** — products are equipment profiles, no native apps are locked; re-derive delivery channels from the equipment frame); §3 three to-be-selected/designed entries; §4 stub; §5 two standing questions; §6 PC two bullets / DS two bullets / Surfaces two bullets (all scaffold-era, freely revisable); §7 four items. **This run populates and refines IN PLACE; nothing demands verbatim preservation.**
- **The entity CLAUDE.md EXISTS as a DELIBERATE STUB** at `docs/verticals/notifications/CLAUDE.md` (cascade Session 3; same shape as Observability's and Administration's). Verify, do not author; its parenthetical names "DM real-time subscriptions, the notification bell, channel routing" — consistent with the realized substrate below.
- The verticals tier file `docs/verticals/CLAUDE.md` and the V3 line in `docs/verticals/README.md` ("`notifications/SPECIFICATION.md` — V3 Notifications", L13 at authoring).
- ADRs per the §3 enumeration — presence + amendment state (note: **ADR-U028 now carries the 2026-06-12 root-admin amendment** landed at the V1 run; consume, don't re-adjudicate).
- **PENDING.md carries NO V3 entry** (verified zero at authoring) — re-verify; the open watch-items (DS-1/DS-7 names) are not V3's.
- No doc-health registry row for the entity CLAUDE.md.

## THE ENTITY-SHAPE ADJUDICATION GATE (pre-Step-1 — first ratification gate)

Surface this evidence + lean to Stefan; ratification fixes template shape + landing path before Step 1.

**Evidence (assembled at authoring, 2026-06-12):**
- The artifact exists and is canonical: vertical-spec skeleton; L3 is an Obligation inventory (ADR-U002); the spec's L3 header sanctions fresh derivation.
- G-03 (re-verify, don't inherit) names the work; Notifications is fourth in the confirmed order.
- **The prior-ratified-content boundary is the V4 SHAPE — no-ratified-content inversion:** `last_updated` 2026-04-26 pre-dates Session B; nothing in the file is ratified content; all sections are freely revisable (second instance of this shape — V4 precedent held cleanly).
- **Two scaffold-era claims are flagged for fresh derivation, not preservation:** the §2 mobile-push bullet (pre-U025; delivery channels follow equipment + status, not locked native apps) and the §3 provider entries (email is now partially abstracted on disk — see the trap — and the entries must say realized-abstraction/simulated-delivery honestly).
- **NO quoted-example agreement surface** (the tier file's canonical example is V4's — verified at the V1 run); the standard four-sibling-row agreement check applies.

**Authoring lean:** the three-step RUN shape applies unchanged; skeleton stays vertical-spec; in-place population with `last_updated` bumped (status `draft` -> Stefan's call at Step 3); Step 1 Edit-shaped (A#5 sub-batch-of-1; three clean precedents). The four standing deviations carry; the V4-shape inversion is the gate-surfaced boundary determination.

## §3 Authority chain for cold derivation

- **L1:** root `CLAUDE.md` + `docs/verticals/CLAUDE.md` (tier). No sub-tier at verticals.
- **Entity CLAUDE.md:** stub by design — no boundary law.
- **Tier-CLAUDE domain-noun sweep (FIRED at authoring; all four rows disk-verified 2026-06-12):** `docs/platform/CLAUDE.md` (**notifications are triggered by events at every layer, not just Communication; the platform emits notification triggers, the Communication service routes them, products surface them; a platform feature that changes FIM-visible state without emitting a notification trigger is INCOMPLETE; trigger design is part of the feature spec, not an afterthought**), `docs/products/CLAUDE.md` (state a FIM cares about is a notification trigger; product surfaces expose notification preferences and the inbox; **they don't author notification copy — that's shared**), `docs/studios/CLAUDE.md` (studios emit to creators about their content AND to consumers downstream — a retired journey's enrolled FIMs are notified through the product, not the Studio; **the Studio's job is emitting the trigger accurately; routing is owned by Communication**), `docs/design-system/CLAUDE.md` (**the design system owns the visual language of notifications — inbox, badges, toasts, banners, severity levels; copy is authored per-event, appearance is canonical; products never restyle**). Agreement check at Step 3; doc-health does not catch it.
- **Constitutional slot (fires CONSTRAINT-SIDED — the V4 pattern):** `docs/ecosystem/VISION.md` — dual-verify zero notification nouns in run. `docs/ecosystem/MANIFESTO.md` carries the CONSTRAINT: **"Lived experience over passive consumption" — "FringeIsland is not here to fill your time. We are here to catch it on fire"** — notifications must pull members back to *meaningful moments*, never farm attention or engagement (the scaffold §1's spam line is this constraint's platform face; the derivation should make the connection explicit); "Member privacy over commercial opportunity" binds outbound delivery (nothing marketing-shaped without explicit consent). `docs/ecosystem/PRINCIPLES-AI.md` — marginal at V3 (no standing AI mandate; if AI-curated digests surface as a question, the human-sign-off and anchoring rails apply — note, don't force).
- **Canonical cores:** `docs/ecosystem/universe/roles/README.md` — marginal but live at one seam: the **Shadow** (anonymous, ephemeral, no PII — ADR-U027) can hold no email address and no durable notification state; in-session delivery only. Consume; never resolve the core's open threads.
- **Sibling-vertical authority (THREE populated siblings — first run with three):** `docs/verticals/privacy/SPECIFICATION.md` (V2, active): external delivery is a privacy boundary — an email leaves the platform's RLS perimeter carrying member-referencing content; content minimisation, lawful-basis split (transactional vs anything marketing-shaped), and the sub-processor rule bind any selected vendor; notification preferences are consent-adjacent member state. `docs/verticals/observability/SPECIFICATION.md` (V4, active): delivery failures are observability events (no silent drop); notification emission is instrumentable; note the payload distinction honestly — V4's content-free rule governs *observability* events, while notification payloads deliver content TO the member; the V2-bound class is *external* delivery payloads. `docs/verticals/administration/SPECIFICATION.md` (V1, active): `admin_send_notification` is a realized admin primitive — admin notifications are admin acts (audited, human-executed per V1 §6 PC). All three consumed as authority, never amended.
- **Domain-service authority:** `docs/platform/domain/communication.md` (DS-5, landed) — **the notifications boundary is SETTLED (DS-5 run, consume never reopen): the vertical owns the obligation, DS-5 routes, products surface; the platform tier CLAUDE.md carries the law.** DS-5 characterized the realized actionable-notification slice. Consumed as authority, never amended.
- **Architectural authority (enumerated by domain-noun grep at authoring: notif|email|digest|inbox; citation-precision binds; run a second-noun sweep — push|subscription|broadcast|preference|quiet|unsubscribe — at state-read):** **ADR-U002** (the charter: "Notifications/Email — observational and outbound. Does not mutate state"; the system actor — fundamentally different from human-operated V1; "internal listener, outward deliverer"; the REJECTED alternative — notifications-as-L5-Communication — is the boundary's origin: triggers fire at every layer), **ADR-U016** (the Verticals cascade slot names "notifications triggered" — trigger design belongs in every lifecycle cascade spec), **ADR-U023** (one line: verticals unchanged — marginal). Marginal hits to re-verify membership, not inherit: **ADR-U025** (equipment profiles — the scaffold's mobile-push bullet pre-dates it; delivery channels follow equipment + status), **ADR-U027** (Shadow ephemerality bounds Shadow-facing delivery), **ADR-U021** (membership-exit display law — does an exit event notify?), **ADR-U028** (admin/Console notification surfaces — marginal).
- **Template (spec skeleton):** `docs/templates/vertical-spec.md`.
- **Predecessor carry-forward:** the Notifications pickup slate below.

**Cold-derivation discipline** holds as template: no `supabase/`, `lib/`, `app/`, `tests/` reads during Step 1. **The standing §5a disk-anchor text binds:** Step 1 cites realized substrate ONLY from the §5b calibration block — anything else is lock-only. `lib/email/send.ts` is the pre-named trap.

## Entity-specific carry-forward block — the Notifications pickup slate

1. **The DS-5 notifications-boundary resolution (SETTLED — consume, never reopen):** the vertical owns the obligation, DS-5 routes, products surface. V3's §6 must levy the obligation in exactly this shape — V3 does not own a dispatcher *service*; it obliges the platform to provide one and every service to publish through it.
2. **The V1 seam:** `admin_send_notification` is realized (one of V1's six `admin_*` primitives); admin notifications are audited human acts (V1 §6 PC). V3 consumes — the admin send-path is the realized trigger-emission slice on the operator plane.
3. **The V4 seam:** the platform tier row's trigger-completeness law is enforcement-shaped (a state change without a trigger is an incomplete feature — feature-spec detectable); delivery failure is an observability event (V4 §6 DS degradation rule).
4. **The V2 seam:** external delivery crosses the privacy perimeter — content minimisation in emails/push, vendor-as-sub-processor, transactional-vs-marketing lawful basis, unsubscribe duty. Art. 34 member breach-notification names a delivery duty (V2 §5 Q5 / V1 §5 Q5 own detection + process; if V3 owns the delivery *channel* half, land it as a seam-aware question, not an obligation grab).
5. **NOT folded, by record:** V3 is scaffold-pure (the V4-shape inversion) — nothing is ratified; the two §5 standing questions re-derive freely, and the per-user-vs-per-group preferences question gains realized substance (the live `notifications` table is **group-keyed**: `recipient_group_id` — the universal group pattern already answers part of it; see calibration).

**Ownership discipline:** obligations stay levied on the named services; this run does NOT amend V2, V4, V1, communication.md, any DS spec, or any §8 question. Class 3 findings route to pickup lists.

## §4 / §5a Step 1 — cold derivation (Edit-shaped)

Populate in place: §3 Tooling (re-derive the three entries against the calibration block — the in-app slice is REALIZED: live `notifications` table + actionable-notification functions + bell/context UI with realtime; **email is abstraction-realized, delivery-simulated — THE TRAP, name it honestly**; push, digest, preferences, quiet hours, the shared dispatcher all unrealized), §4 Failure modes (structural; candidates the substance suggests: **notification spam / attention-farming** (the constitutional failure mode — passive-consumption mechanics wearing a notification's clothes), **silent trigger gap** (FIM-visible state change without a trigger — the platform row's "incomplete feature"), **delivery failure unobserved** (V4 seam), **preference/consent bypass** (a category the member suppressed arrives anyway — V2 seam), **external-delivery content leak** (full member content in an email that leaves the perimeter), **Shadow durability leak** (notification state outliving the ephemeral session, U027)), §5 Open questions (two standing re-derived — digest batching; per-user-vs-per-group preferences against the group-keyed realized shape — plus candidates: provider selection (joint with V2 sub-processor duty), transactional-vs-marketing basis split, Shadow-facing delivery bounds, the Art. 34 delivery-channel seam), §6 Obligations per tier (PC: dispatcher + preferences storage (PC-2) + trigger-emission law + audit seam for admin sends; DS: publish-through-dispatcher-never-send-directly + category declaration + DS-5's routing role per the settled boundary + content-minimisation on externally-bound payloads; Surfaces: inbox/bell/badges as design-system primitives + preferences reachable + quiet hours + the copy-vs-appearance split per the tier rows), §7 Cross-cutting checklists (four revisable; expect ~10-12). All sections freely revisable (the inversion); the scaffold's spam-vs-trust §1 substance refines in place.

Carry-forward priors: P-O1 / D7 / X3 / X5 / Finding #4 / D3 carry as template text — expected inverted/no-object at a no-capability entity (V1/V2/V4 precedent; X3 has no known V3 object). Entity-specific priors: the system actor (U002 — notifications are the platform speaking, not a human; the V1 contrast is load-bearing), over-prescription kills verticals, attention-is-protected (MANIFESTO).

**Step 1 checkpoint** (before any Edit lands): obligation counts per tier; checklist delta (**DoD reflection expected NO-OP** — PROCESS.md:196 generic line; verify, don't re-add); open-questions delta; any proposed touch on the scaffold §1 substance (sanctioned — nothing ratified); any deviation forced; **the standing disk-anchor compliance statement** (every realization claim + its §5b anchor; `lib/email/send.ts` explicitly dispositioned). Wait for ratification. A#5 sub-batch-of-1 binds (three clean precedents).

## §5b Step 2 — stress-test (both polarities)

**Calibrated at authoring (2026-06-12, disk-verified; dual-method discipline and the §6 tool-level catch class bind on every zero):**

- **Compliance polarity — REALIZED in-app substrate (enumerate, characterize, don't re-discover):** the live `notifications` table (rebuild `20260222000000` L214: `recipient_group_id` **group-keyed** per the universal group pattern; `type TEXT` free-typed — **no category catalog, no CHECK**; `title`/`body`/`payload JSONB`; `is_read`/`read_at`); the sprint3 actionable-notification slice (`20260228125730_sprint3_smart_notifications.sql`: `handle_notification_action`, `_handle_stewardship_nomination_action`, `nominate_steward` — characterized at the DS-5 run, cite it); the UI surface (`components/notifications/NotificationBell.tsx`, `lib/notifications/NotificationContext.tsx` — 2 realtime subscriptions, `components/admin/NotifyModal.tsx`); the DM real-time slice (`lib/messaging/MessagingContext.tsx` — 2 realtime subscriptions; the stub's parenthetical); `admin_send_notification` (V1's function family — cite V1 §3/Sources-status, don't re-characterize).
- **THE TRAP (realized-abstraction / simulated-delivery):** `lib/email/send.ts` — `SendEmailOptions`/`SendEmailResult` interfaces with a `sendEmail()` whose body is console.log simulation; the header comment names Resend/SendGrid as *examples to integrate later*; `package.json` carries no email vendor. The §3 entry must say: abstraction seam realized, delivery unrealized, provider to-be-selected.
- **Absence polarity (calibrated zeros; named false positives to re-verify hit-by-hit):** email vendor (the one grep hit is send.ts's own comment line); push delivery (web-push/fcm/apns — zero); digest (Next.js `error.digest` false positives at `app/error.tsx` L13/L44, `app/global-error.tsx` L14/L46); quiet hours; unsubscribe in the notification sense (`subscription.unsubscribe()` at `lib/auth/AuthContext.tsx` L66 is the realtime-channel API — false positive); frequency caps; the shared dispatcher; `notification_preferences`; notification categories; notification permissions in `seeds/01_permissions.sql` (zero — unlike `moderate_forum`, no notification permission exists).
- **Tool catches (inherit; standing §6):** `find` is a false tool in the sandbox; glob-expansion grep false-empties; `git grep` without `-o` is the reliable method; judge every zero by output lines, never `$?`.

Cluster batch-and-report as template; checkpoint before the Step 2 block lands. **Retraction-rate series continues (thirteenth run):** prediction zero; the send.ts trap is the promoted discipline's first post-promotion test.

## §5c Step 3 — adjudication

NO FIRST DECISION. Q-resolution slate over §5 (resolve, hold, or route — the two re-derived standing questions plus what the derivation adds); the tier-CLAUDE agreement check (four sibling Notifications rows vs revised §6 — manual; no quoted-example surface); **DoD reflection: expected no-op** (confirm, don't edit); ADR amendments expectation: ZERO; entity-CLAUDE stub disposition (stays stub unless entity-specific discipline surfaces — note: a *selected* email vendor with a concrete API would be exactly the stub's named trigger shape, but selection is not this run's work); **the sentinel watch verdict** (if shape (a) stayed quiet again, record retire-as-routine in the bridge); pickup lists (receiving entity: **Transactions — LAST in G-03 order**; its opener must carry the verticals-close-out agenda assembly duty, the Extension System precedent: the final entity of a series assembles the close-out agenda in its closing bridge); closing bridge.

## §6–§9 Disciplines

All template text binds at baseline `c1554f2`. ASCII-only labels; sessions append-only; OLDFEAT blindness carries; no re-litigation of settled classifications (the Extension System confirmations + the close-out routings + the V2/V4/V1 Step 3 dispositions — the DS-5 notifications-boundary resolution is settled; the U028 root-admin amendment is landed law, consume it).

## §10 Output expectations and commit shape

Expected: (i) the in-place spec population commit (Edits held uncommitted until Step 3 ratification — single combined Steps 1+2+3 commit); (ii) consequential-edit commit(s) only as sanctioned at Step 3 (expectation: zero — no quoted-example surface, no known agreement gap); (iii) closing bridge at `docs/planning/sessions/{DATE}_NN_-_NOTIFICATIONS-LANDED.md` (with the **Transactions opener's inheritance block, including the verticals-close-out agenda-assembly duty**); (iv) STATUS.md close. NO entity-CLAUDE creation or substantiation commit. NO ADR commits expected. **No push to origin** — Stefan dispositions push.

## §11–§13

Closing-bridge required sections, scope boundaries, and the five-prompt post-run capture as template. §13 additionally answers: (i) did the PROMOTED disk-anchor text hold at its first run as standing template text — specifically at the pre-named send.ts trap? (ii) did the no-ratified-content inversion hold at its second instance (V4 precedent)? (iii) did the sentinel stay quiet a second time (if yes: record Edit-removal retire-as-routine)? Record the retraction-rate data point (thirteenth run). Pickup lists must carry the Transactions opener's inheritance block (the LAST vertical; same gate shape; agenda-assembly duty).

## §14 Start sequence

§1 pre-flight -> §2 state-read -> **the entity-shape adjudication gate (surface evidence + lean; wait for ratification)** -> §5a Step 1 -> checkpoint -> §5b -> checkpoint -> §5c -> §13 -> closing bridge.

---

*End of instance. Authored 2026-06-12 against template baseline `c1554f2`; the twelfth instance, fourth vertical, zero riders (the disk-anchor discipline is standing text), no FIRST DECISION, one named trap (`lib/email/send.ts` — abstraction realized, delivery simulated).*
