# Autonomous L1->L3 session-opener — `V1 Administration`

**Template path:** `docs/templates/autonomous-l1-l3-session-opener.md`
**Instance landing path:** `docs/planning/sessions/openers/administration-descent-opener.md`
**Instance authored:** 2026-06-12, against template baseline `ca43284`.

> **Eleventh instance of the autonomous L1->L3 template — the THIRD VERTICAL**, authored against the same baseline as Privacy and Observability (`ca43284`; no revision landed at either vertical run). **ONE RIDER**: **Step-1-realization-claims-need-disk-anchor** (now n=2 with one decisive firing — at V4 it blocked exactly its banked class, the archived-migration immutability prose; every Step 1 realization claim carries a disk anchor from this opener's calibration or is explicitly marked lock-only; **the named trap for this run is ADR-U014**: its prose says feature flags "live in Infrastructure as a simple database configuration table" — disk is ZERO, the U004-pg_cron class exactly). **THE ENTITY-SHAPE ADJUDICATION GATE FIRES** (third vertical firing; standing §4 text; both prior verticals held). **A FIRST DECISION EXISTS — the first in the verticals series**: the PENDING.md root-admin entry (`0c1902c`, ratified 2026-06-12) names this derivation as its adjudication point.

---

## §1 Pre-flight checks — STOP

Run all five; hard-fail on deviation; surface and wait.

1. **Working directory:** `/d/WebDev/GitHub/FringeIsland` (or Windows-equivalent).
2. **Branch:** `main`.
3. **Tip commit:** at or after `11a7881` (the Observability STATUS-close commit). Acceptable interveners: this opener's own commit, its STATUS In-flight commit, the cc-execute-prompt.md commit, and `docs/novel/` path commits.
4. **Working tree.** Default clean. Two named acceptable shapes: (a) the root `CLAUDE.md` context-mode re-injection — disposition: **discard via Edit-removal** (seventh instance if it recurs; `git restore` is classifier-denied; judge the residual `M` flag by `git diff` content-emptiness — CRLF stat-noise is acceptable). (b) Concurrent `docs/novel/` activity — do not read, modify, or commit. Anything else unnamed: surface and wait.
5. **Template baseline:** most-recent-touch of `docs/templates/autonomous-l1-l3-session-opener.md` expected at `ca43284`. Hard-fail if earlier; soft-flag and adjudicate if later.

## §2 State-read pass (ordered)

1. **`docs/planning/sessions/2026-06-12_03_-_OBSERVABILITY-LANDED.md`** — chronologically-immediate predecessor; carries THE INHERITANCE BLOCK (heaviest pickup source — consume in full), the authorization-check taxonomy, the rider's decisive-firing record, and the mid-run-PENDING-authoring shape.
2. **`docs/planning/sessions/2026-06-12_02_-_PRIVACY-LANDED.md`** — first-vertical precedent; the V2 seams V1 consumes (breach-response process side; erasure-cascade channel); the sandbox tool catches.
3. **`docs/planning/sessions/2026-06-12_01_-_PHASE-3-CLOSE-OUT-LANDED.md`** — the no-fork verticals decision and confirmed routings.
4. **`docs/planning/sessions/2026-06-11_03_-_EXTENSION-SYSTEM-LANDED.md`** — gate precedent + Carry-forward confirmations (settled classifications: consume, never reopen).
5. **`docs/planning/sessions/2026-05-14_03_-_EXPERIMENT-B-COMPARISON-PHASE-COMPLETE.md`** — autonomous-discipline carry-forwards (load-bearing sections per template).

Then verify against disk:

- **The spec EXISTS** at `docs/verticals/administration/SPECIFICATION.md` — id `V1`, name `Administration`, title "Administration & Moderation", status `draft`, `last_updated: 2026-06-10`, on the `docs/templates/vertical-spec.md` skeleton. **NOT scaffold-pure — the U028-touched shape (a THIRD ratified-content boundary, see the gate):** §1 Purpose and §2 Scope carry ADR-U028-ratified governance-by-scope content (the in-place-care vs Console split; Ferd routings; enterprise-plane seats); §3 carries four entries (one names the Console as planned); §4 stub; §5 three standing questions; §6 Platform Core is a PLACEHOLDER (carrying a scaffold-era "read the live code and migrations directly" instruction that **contradicts cold-derivation discipline — the derivation REPLACES the placeholder, never follows its instruction**); §6 Domain Services one thin generic line; §6 Surfaces two bullets (the second U028-ratified); §7 five items (the fifth U028-ratified). **This run populates and refines IN PLACE.**
- **The entity CLAUDE.md EXISTS as a DELIBERATE STUB** at `docs/verticals/administration/CLAUDE.md` (cascade Session 3, decision D2; same shape as Observability's). Verify, do not author; quote-sync-without-substantiation has V4 precedent if a wording dependency surfaces; genuine entity-specific discipline surfaces for adjudication, never silently substantiated.
- The verticals tier file `docs/verticals/CLAUDE.md` and the V1 line in `docs/verticals/README.md` ("V1 Administration & Moderation").
- ADRs per the §3 enumeration — presence + amendment state.
- **The PENDING.md FIRST-DECISION entry EXISTS** at `docs/architecture/decisions/PENDING.md` ("Root-admin authority is role-based", authored `0c1902c`) — verify, consume; its adjudication is §5c work.
- No doc-health registry row for the entity CLAUDE.md.

## THE ENTITY-SHAPE ADJUDICATION GATE (pre-Step-1 — first ratification gate)

Surface this evidence + lean to Stefan; ratification fixes template shape + landing path before Step 1.

**Evidence (assembled at authoring, 2026-06-12):**
- The artifact exists and is canonical: vertical-spec skeleton; L3 is an Obligation inventory (ADR-U002).
- G-03 (re-verify, don't inherit) names the work; Administration is third in the confirmed order.
- The spec's L3 header sanctions fresh derivation.
- **The prior-ratified-content boundary is a THIRD SHAPE — partial:** between V2's folded-ratified-content and V4's untouched-scaffold. The U028-derived material (`last_updated` 2026-06-10 = the U028 ratification date) in §1, §2, §3's Console entry, §6 Surfaces' scope-routing bullet, and §7's scope-routing item is **ratified content — refine-don't-re-derive binds on it** (V2 precedent). §4, §6 Platform Core, and §6 Domain Services are stub/scaffold-era — fresh derivation.
- **The §6 Platform Core placeholder carries an anti-discipline instruction** ("read the live code... for the current state") — scaffold-era text predating the L3 derivation discipline. The lean: the derivation replaces it; realized-state reads stay in Step 2 where they belong.
- **NO quoted-example agreement surface** (the tier file's canonical example is V4's — verified at authoring); the standard four-sibling-row agreement check applies, nothing extra.

**Authoring lean:** the three-step RUN shape applies unchanged; skeleton stays vertical-spec; in-place population with `last_updated` bumped (status `draft` -> Stefan's call at Step 3); Step 1 Edit-shaped (A#5 sub-batch-of-1; two clean precedents). The four standing deviations carry; the third-shape ratified-content boundary is the gate-surfaced delta.

## §3 Authority chain for cold derivation

- **L1:** root `CLAUDE.md` + `docs/verticals/CLAUDE.md` (tier). No sub-tier at verticals.
- **Entity CLAUDE.md:** stub by design — no boundary law.
- **Tier-CLAUDE domain-noun sweep (FIRED at authoring):** all four sibling tier files carry Administration rows that are real boundary law — `docs/platform/CLAUDE.md` (**every lifecycle event has a complete cascade specification before implementation, ADR-U016**; cascades document every layer; triggers + RLS do the heavy lifting; Platform Core exposes admin primitives, products wrap them), `docs/products/CLAUDE.md` (every admin action from a product surface is **auditable and reversible where feasible**; Steward and DeusEx actions surface differently; UIs never expose raw admin primitives — wrapped and named for the role), `docs/studios/CLAUDE.md` (Dreamineer self-service lifecycle; **DeusEx force-archive/takeover only in defined circumstances** — separate flows, explicit design), `docs/design-system/CLAUDE.md` (destructive variants — larger targets, clearer copy, slower defaults — owned by the design system). Agreement check at Step 3; doc-health does not catch it.
- **Constitutional slot (fires BOTH-SIDED at this entity — verified at authoring; first vertical where it contributes a boundary AND a mandate):** `docs/ecosystem/VISION.md` zero admin-noun text (dual-verify in run). `docs/ecosystem/MANIFESTO.md` carries the CONSTRAINT — mutual-respect-over-judgment ("to be seen without being sized up; to be met without being measured"): moderation and admin power must read as care, never judgment; U028's care-vs-administration scope split is the structural answer and the derivation should make the connection explicit. `docs/ecosystem/PRINCIPLES-AI.md` carries a MANDATE — load-bearing for §5 Q2 (AI-assisted moderation): AI does not act without human sign-off; guard rails are human-authored and bidirectional.
- **Canonical cores:** `docs/ecosystem/universe/roles/README.md` — the enterprise plane (L2: Universeers | the Council | DeusEx; surface: the Console) and community-plane care roles; the core's own open question (Universeers' internal structure) is consumed, never resolved. Privacy-model marginal (Stewards cannot see private developmental data — binds admin surfaces).
- **Sibling-vertical authority (TWO populated siblings — first run with both):** `docs/verticals/privacy/SPECIFICATION.md` (V2, active): the breach-response PROCESS side is V1's (V2 §5 Q5 — detection is V4's, landed); `admin_hard_delete_user` is the realized erasure-adjacent actor (the erasure-cascade design channel's evidence). `docs/verticals/observability/SPECIFICATION.md` (V4, active): the recording side is landed — every security-relevant action audits through the shared writer (admin acts are the realized slice); the Console's audit-log viewer consumes through Platform APIs, never direct storage queries; V4 §5 Q3 (on-call) seams with V1's operational ownership. Both consumed as authority, never amended.
- **Architectural authority (enumerated by domain-noun grep at authoring: administra|lifecycle|console|deusex|moderat|feature.flag; citation-precision binds):** **ADR-U028** (the charter: governance by scope; the Console; the enterprise plane; the Ferd routings — moderation in-place, audit-log viewer + feature flags to the Console, self-service exit in-experience, economy split), **ADR-U002** (Administration = human-operated lifecycle cascades; tightest-to-the-layers ordering), **ADR-U019** (DeusEx as authority of last resort — last-Steward recovery; **realized substrate exists: the fix-orphans admin page**), **ADR-U014** (feature flags as Infrastructure DB config table — **UNREALIZED; the named rider trap**), **ADR-U006/U007** (universal group pattern + three-layer permission model — the authority frame for the FIRST DECISION), **ADR-U016** (cascade-specification-before-implementation + the Verticals cascade slot), **ADR-U027** (Shadow lifecycle admin seams), **ADR-U011** (economy management on the Console; member-buying in-experience). Marginal hits to re-verify membership, not inherit: U023 (1), U029 (1), U010/U012 (audit-split context — both sides now landed).
- **Template (spec skeleton):** `docs/templates/vertical-spec.md`.
- **Predecessor carry-forward:** the Administration pickup slate below.

**Cold-derivation discipline** holds as template: no `supabase/`, `lib/`, `app/`, `tests/` reads during Step 1. **THE RIDER BINDS (n=2):** Step 1 cites realized substrate ONLY from the §5b calibration block — anything else is lock-only. U014 is the pre-named trap.

## Entity-specific carry-forward block — the Administration pickup slate

1. **THE FIRST DECISION (from the V4 run, `0c1902c`):** the PENDING.md root-admin entry — the ratified principle (platform-admin capability flows from holding the role whose permission set grants administration; DeusEx membership alone confers nothing) against the realized `is_platform_admin()` name-proxy (~20 call sites, ~9 RLS policies; divergence reachable via role removal). This derivation adjudicates the parking: promote to ADR (the U029 PENDING-to-ADR mechanics are precedented) / land as ADR-U028 clarification amendment / stay parked with a tighter trigger. The §6 Platform Core obligation lands regardless.
2. **The authorization-check taxonomy (V4 bridge):** capability checks via `has_permission()` (59 call sites, ~27 policies — the full group+role+permission walk); visibility/state checks via the bare-membership helper family; the DeusEx name proxy. V1 owns reconciling the three families against the model. X3 diagnosis note: RC7's "broken" Tier-1 policies passed a profile id where the rebuilt `has_permission()` expects an acting group id — diagnose before fixing, fix is downstream work.
3. **U012 audit split — both sides landed:** V4 records, V2 exposes; V1 consumes the Console audit-log-viewer routing (U028) and owns the operator plane around it.
4. **Seams:** breach-response process (V2 Q5 — the 72-hour duty's process half); on-call posture (V4 Q3 — V1's operational ownership).
5. **NOT folded, by record:** V1's U028 content IS ratified (the inverse of V4's untouched-scaffold note) — the refine-don't-re-derive constraint has a real object here.

**Ownership discipline:** obligations stay levied on the named services; this run does NOT amend V2, V4, any DS spec, or any §8 question. Class 3 findings route to pickup lists.

## §4 / §5a Step 1 — cold derivation (Edit-shaped)

Populate in place: §3 Tooling (verify-and-refine the four entries against the calibration block — `is_platform_admin()` exists WITH the parked deviation, cite the PENDING entry honestly; the DeusEx wiring is realized; the audit log was characterized at the V4 run — cite V4 §3, don't re-characterize; the Console is planned/unrealized; **candidate fifth entry: feature flags per U014 — unrealized, name it honestly**), §4 Failure modes (structural; candidates the substance suggests: orphaned group without Steward (U019's recovery case), un-audited admin act, **scope leak** — a universe-scoped affordance woven into the member experience (U028 + MANIFESTO violation), moderation-as-judgment (the constitutional failure mode), **admin-authority divergence** (the proxy/role split — a reachable realized risk), appeal-less remediation, compromised admin account), §5 Open questions (three standing stay unless resolved — Q2 AI-assisted-moderation is now anchored by PRINCIPLES-AI's human-sign-off mandate; the derivation may surface admin-role granularity — one all-permissions DeusEx role vs differentiated admin seats, the model question behind the role-stripped divergence and the roles core's Universeers question), §6 Obligations per tier (the PC PLACEHOLDER REPLACED with real obligations — the role-based-authority principle lands here as obligation; lifecycle cascades per U016; last-resort stewardship per U019; the DS generic line refined tier-specifically; Surfaces refined with the U028-ratified scope-routing bullet kept verbatim-in-substance), §7 Cross-cutting checklists (five revisable; the scope-routing and confirm-modal items keep; **DoD reflection expected NO-OP** — the generic vertical-checklists line at PROCESS.md §5 covers V1; verify, don't re-add). L2 §1–§2 are U028-ratified — touched only on genuine contradiction (surface first).

Carry-forward priors: P-O1 / D7 / X3 / X5 / Finding #4 / D3 carry as template text — X3 has a real object at this entity (the RC7 signature drift, pickup item 2); note applicability honestly. Entity-specific priors: human-operated cascades (U002 — administration is the vertical where a human is always the actor); over-prescription kills verticals; the care-not-judgment constitutional split.

**Step 1 checkpoint** (before any Edit lands): obligation counts per tier; checklist delta (DoD no-op — confirm); open-questions delta; any proposed L2 §1–§2 touch (expected: none — ratified content); any deviation forced; **rider compliance statement** (every realization claim + its anchor; U014 explicitly dispositioned). Wait for ratification. A#5 sub-batch-of-1 binds (two clean precedents).

## §5b Step 2 — stress-test (both polarities; the richest compliance substrate of the series)

**Calibrated at authoring (2026-06-12, disk-verified; dual-method discipline and the §6 tool-level catch class bind on every zero):**

- **Compliance polarity — REALIZED admin substrate (enumerate, characterize, don't re-discover):** seven `admin_*` SECURITY DEFINER functions in live migrations (`admin_hard_delete_user`, `admin_send_notification`, `admin_force_logout`, `admin_update_user_status`, `admin_decommission_user`, `admin_exit_user_from_platform` — across the rebuild, `fix_rc7`, and `sprint4_platform_exit`; redefinitions exist — count functions, not definition sites); user-lifecycle states `is_active`/`is_decommissioned` wired to `app/admin/page.tsx` via RPC; three admin pages (`app/admin/`, `app/admin/deusex/`, `app/admin/fix-orphans/` — the last is U019's realized substrate); the DeusEx seeds wiring (`seeds/04_system_groups.sql`: DeusEx role holding ALL permissions, `auto_assign_deusex_role_on_accept`, `prevent_last_deusex_*` guards); `moderate_forum` permission (`seeds/01_permissions.sql` L49) + `forum_update_moderate` policy (rebuild L1881–1895) — **the realized in-place community-moderation slice, U028's pattern on disk**; `admin_audit_log` (fully characterized at the V4 run — consume its §3/Sources-status, don't re-run); the 44-row permission catalog.
- **Absence polarity (calibrated zeros):** feature flags (U014 — THE TRAP), content reporting/flagging mechanism, review queues, takedown, group takeover/dissolution, appeal flows, the Console itself. All dual-method, judged by output lines.
- **Tool catches (inherit + ONE NEW INSTANCE AT AUTHORING):** the sandbox `git grep -o` false-empty FIRED during this opener's calibration — the `admin_*` function inventory via `-oE 'FUNCTION public\.admin_[a-z_]+'` returned empty where the non-`-o` form returns eight definition lines. `find` is a false tool; glob-expansion grep false-empties; `git grep` without `-o` is the reliable method; judge every zero by output lines, never `$?`.

Cluster batch-and-report as template; checkpoint before the Step 2 block lands. **Retraction-rate series continues (twelfth run):** prediction zero; the U014 trap is the rider's n=3 test — a retraction there means the rider failed; a clean hold strengthens its promotion case.

## §5c Step 3 — adjudication

**THE FIRST DECISION adjudication** (the PENDING root-admin entry: promote to ADR via the U029 mechanics / land as U028 clarification amendment / re-park with a tighter trigger — surface evidence + lean; Stefan adjudicates); Q-resolution slate over §5 (resolve, hold, or route — appeal workflow, AI-assisted moderation, regional moderation, plus what the derivation adds); the tier-CLAUDE agreement check (four sibling Administration rows vs revised §6 — manual; no quoted-example surface, verify); **DoD reflection: expected no-op** (confirm, don't edit); ADR amendments only if the FIRST DECISION lands one (otherwise expectation: zero); entity-CLAUDE stub disposition (stays stub unless entity-specific discipline surfaced); **the rider promotion adjudication** (n=2 decisive entering this run; n=3 verdict at this close — promote to standing §5a text or ride to the verticals close-out; surface with the full three-instance evidence); pickup lists (receiving entity: **Notifications — next in G-03 order**; plus any service-routed Class 3); closing bridge.

## §6–§9 Disciplines

All template text binds at baseline `ca43284`. ASCII-only labels; sessions append-only; OLDFEAT blindness carries; no re-litigation of settled classifications (the Extension System confirmations + the close-out routings + the Privacy and Observability Step 3 dispositions — V4's Q-slate is settled; V1 consumes the on-call and breach-process seams, it does not reopen the routings).

## §10 Output expectations and commit shape

Expected: (i) the in-place spec population commit (Edits held uncommitted until Step 3 ratification — single combined Steps 1+2+3 commit); (ii) a FIRST-DECISION commit if the PENDING entry promotes to an ADR or U028 amendment (the series' second PENDING promotion if it lands); (iii) consequential-edit commit(s) only as sanctioned at Step 3; (iv) closing bridge at `docs/planning/sessions/{DATE}_NN_-_ADMINISTRATION-LANDED.md` (with the Notifications opener's inheritance block); (v) STATUS.md close. NO entity-CLAUDE creation or substantiation commit. **No push to origin** — Stefan dispositions push.

## §11–§13

Closing-bridge required sections, scope boundaries, and the five-prompt post-run capture as template. §13 additionally answers: (i) the rider at n=3 — did the pre-named U014 trap hold, and what is the promotion verdict? (ii) the third-shape ratified-content boundary — did partial refine-don't-re-derive hold beside fresh derivation inside the same §6? (iii) the FIRST DECISION shape at a vertical (first since DS-3) — did mid-series PENDING adjudication mechanics hold? Record the retraction-rate data point. Pickup lists must carry the Notifications opener's inheritance block (next vertical; same gate shape).

## §14 Start sequence

§1 pre-flight -> §2 state-read -> **the entity-shape adjudication gate (surface evidence + lean; wait for ratification)** -> §5a Step 1 -> checkpoint -> §5b -> checkpoint -> §5c (including the FIRST DECISION adjudication) -> §13 -> closing bridge.

---

*End of instance. Authored 2026-06-12 against template baseline `ca43284`; the eleventh instance, third vertical, one rider (Step-1-realization-claims-need-disk-anchor, n=2 with one decisive firing), one FIRST DECISION (the PENDING root-admin entry).*
