# Autonomous L1→L3 session-opener — `V2 Privacy/GDPR`

**Template path:** `docs/templates/autonomous-l1-l3-session-opener.md`
**Instance landing path:** `docs/planning/sessions/openers/privacy-descent-opener.md`
**Instance authored:** 2026-06-12, against template baseline `ca43284`.

> **Ninth instance of the autonomous L1→L3 template — the FIRST VERTICAL, and the first instance authored against the Phase-3-close-out-consolidated baseline** (`ca43284`: all seven close-out revisions bind as template text — the §5b body-content read-set rule, the §3 tier-CLAUDE domain-noun sweep, the §5b seeds rule, the §3 constitutional-docs slot, the §13 series verdict, the §4 entity-shape adjudication gate, the §6 tool-level catch class). **ZERO RIDERS** — the rider slate consolidated and landed at the Phase 3 close-out; this is the first instance carrying no instance-rule riders. **THE ENTITY-SHAPE ADJUDICATION GATE FIRES** as the first ratification gate (now standing §4 template text; Extension System precedent): the first vertical, the first run whose Step 1 output is an in-place population of an existing canonical scaffold. **NO FIRST DECISION** (no parked naming or charter item; the spec's identity "V2: Privacy, GDPR & AI Consent" is settled).

---

## §1 Pre-flight checks — STOP

Run all five; hard-fail on deviation; surface and wait.

1. **Working directory:** `/d/WebDev/GitHub/FringeIsland` (or Windows-equivalent).
2. **Branch:** `main`.
3. **Tip commit:** at or after `e4367ee` (the Phase 3 close-out STATUS commit). Acceptable interveners: this opener's own commit, its STATUS.md In-flight commit, the cc-execute-prompt.md commit, and `docs/novel/` path commits.
4. **Working tree.** Default clean. Two named acceptable shapes: (a) the root `CLAUDE.md` context-mode re-injection — disposition: **discard via Edit-removal** (the `git restore` path is classifier-denied; Edit-removal is the precedented fallback, four instances). A stat-noise `M` flag from CRLF autocrlf may persist after the discard — judge by `git diff` content-emptiness. (b) Concurrent `docs/novel/` activity — do not read, modify, or commit. Anything else unnamed in `docs/verticals/`, `docs/platform/`, `docs/architecture/decisions/`, `docs/templates/`, `docs/planning/sessions/`: surface and wait.
5. **Template baseline:** most-recent-touch of `docs/templates/autonomous-l1-l3-session-opener.md` expected at `ca43284`. Hard-fail if earlier; soft-flag and adjudicate if later.

## §2 State-read pass (ordered)

1. **`docs/planning/sessions/2026-06-12_01_-_PHASE-3-CLOSE-OUT-LANDED.md`** — chronologically-immediate predecessor; carries the verticals decision (no fork; this gate shape) and the confirmed routings.
2. **`docs/planning/sessions/2026-06-11_03_-_EXTENSION-SYSTEM-LANDED.md`** — the gate's precedent run + Carry-forward confirmations (settled classifications: consume, never reopen).
3. **`docs/planning/sessions/2026-06-11_02_-_DS7-LANDED.md`** — the heaviest Privacy pickup source (see carry-forward block).
4. **`docs/planning/sessions/2026-05-14_03_-_EXPERIMENT-B-COMPARISON-PHASE-COMPLETE.md`** — autonomous-discipline carry-forwards (load-bearing sections per template).
5. **`docs/planning/sessions/2026-06-10_-_SESSION-B-CONFORMANCE-REGISTER.md`** — rows 41-46 + the privacy-spec row ("partial — AI consent yes; Shadow no" → Shadow content since folded): the provenance of the spec's existing §6 Shadow/S43 content.

Then verify against disk:

- **The spec EXISTS** at `docs/verticals/privacy/SPECIFICATION.md` — id `V2`, status `draft`, `last_updated: 2026-06-10`, on the `docs/templates/vertical-spec.md` skeleton with the L2 (§1–§5) / L3 (§6–§7 + Sources-status) / L4 partition. L2 §1–§2 and the §6 Shadow/S43 obligations are populated (Session B + ADR-U027 provenance); §3 tooling is stub, §4 failure modes empty, §6 is partial, §7 has seven checklist items. **This run populates and refines IN PLACE — it does not create a file.**
- **The entity CLAUDE.md EXISTS** at `docs/verticals/privacy/CLAUDE.md` — the first run where it pre-exists; verify, do not author. It carries real boundary law (see §3 below). No doc-health registry row exists for it (verify; no removal step expected).
- The verticals tier file `docs/verticals/CLAUDE.md` and `docs/verticals/README.md` (the L2 inventory line for V2 lives there — verify the line at state-read).
- ADRs per the §3 enumeration below — presence + amendment state.

## THE ENTITY-SHAPE ADJUDICATION GATE (pre-Step-1 — first ratification gate)

Surface this evidence + lean to Stefan; ratification fixes template shape + landing path before Step 1.

**Evidence (assembled at authoring, 2026-06-12):**
- The artifact exists and is canonical: `docs/verticals/privacy/SPECIFICATION.md` on the **vertical-spec skeleton** (`docs/templates/vertical-spec.md`), whose L3 is an **Obligation inventory, not a Capability inventory** — verticals levy obligations on other entities' capabilities; they own no capabilities, no tables, no APIs (ADR-U002).
- G-03 (in `docs/ecosystem/how-we-work/gaps.md` — re-verify, don't inherit) names the work: populate §3 through §6 of the vertical spec.
- The spec's own L3 header sanctions fresh derivation: "Derived fresh from L1 (Vision) and L2 whenever the vertical enters active development."
- Prior ratified substance is IN the file (L2 §1–§2; the §6 Shadow/S43 obligations, ADR-U027/Session-B provenance): the derivation refines and augments — it never cold-rewrites ratified content; contradictions surface for adjudication.

**Authoring lean:** the autonomous template's three-step RUN shape applies unchanged (cold derivation → code-informed stress-test → adjudication); the SPEC skeleton stays vertical-spec (not DS-spec); the landing path is in-place population of the existing file with `last_updated` bumped (status `draft` → Stefan's call at Step 3). Named structural deviations from the DS-shaped template: (i) L3 content type is obligations; (ii) §5b "canonical-table reads" inverts — the entity owns no tables; Step 2 stress-tests the obligations against the platform-wide realized substrate (see §5b below); (iii) L4 is often-sparse by design; (iv) Step 1 is **Edit-shaped, not Write-shaped** — the first in-place-population run of the series (A#5 sub-batch-of-1 binds for real).

## §3 Authority chain for cold derivation

- **L1:** root `CLAUDE.md` + `docs/verticals/CLAUDE.md` (tier). No sub-tier exists at verticals.
- **Entity CLAUDE.md (pre-existing, boundary law):** `docs/verticals/privacy/CLAUDE.md` — (i) cite GDPR articles where the obligation tracks a named provision, never paraphrase (Art. 6 lawful basis, Art. 15 access, Art. 17 erasure, Art. 20 portability, Art. 30 records — the 15/20 distinction binds the export pipeline); (ii) AI-derived data is user data — §6 Domain Services names DS-7 explicitly, in addition to the generic per-service obligation; (iii) the erasure-overpromise gotcha — never write unqualified "erased everywhere it appears" against AI model state; qualify scope or hold at §5.
- **Tier-CLAUDE domain-noun sweep (template §3 text; FIRED at authoring):** all five sibling tier files carry "Privacy / GDPR" obligations rows that are real boundary law — `docs/platform/CLAUDE.md` (RLS on every FIM-data table; least-privilege SECURITY DEFINER), `docs/products/CLAUDE.md` (FIM-to-FIM privacy settings; Shadows first-class per ADR-U004), `docs/studios/CLAUDE.md` (creator privacy on content metadata; previews must not leak real identities), `docs/design-system/CLAUDE.md` (components rendering FIM data respect privacy state at component level). **The verticals-tier gotcha binds: these rows and the spec's §6 must agree, and doc-health does not catch the drift — the agreement check is this run's Step 3 work** (smallest-necessary consequential edits to the tier files are sanctioned if §6 changes make their rows stale).
- **Constitutional ecosystem docs (template §3 standing slot; first full consumption):** all three bind, verified carrying privacy text at authoring — `docs/ecosystem/VISION.md` (the principle this vertical operationalises; the spec's own L2 derivation law), `docs/ecosystem/PRINCIPLES-AI.md` (AI consent, opt-out, guard-railing — the DS-7 obligation's constitutional anchor), `docs/ecosystem/MANIFESTO.md` (the trust posture §1 Purpose rests on).
- **Canonical cores:** `docs/ecosystem/universe/personal-growth/privacy-model.md` (the strongest Shadow/consent home per the Session B register; S43/S46 — per-region, per-audience, revocable home sharing; ephemerality), plus cosmology/roles cores where home-sharing or role-visibility touches them.
- **L2 inventory line:** the V2 row/line in `docs/verticals/README.md` — verify at state-read.
- **Architectural authority (enumerated by domain-noun grep at authoring: privacy|GDPR|consent|erasure|lawful|PII|anonym|personal data|opt-out; citation-precision binds — read the ADRs, verify any attribution):** **ADR-U002** (five verticals locked; the 2026-04-12 amendment), **ADR-U004** (anonymous sign-in — mechanism holds; naming + lifecycle superseded-in-part by U027 per Session B), **ADR-U010** (privacy as dedicated vertical; "AI data handling" obligations), **ADR-U021** (forum anonymisation soft-flag), **ADR-U027** (Shadow identity lifecycle — the heaviest binding: minimisation, TTL ephemerality, explicit-erase, transcendence consent-capture, mid-migration guard), **ADR-U023** (decomposition frame). Marginal hits to re-verify membership, not inherit: U012 (cross-mention only), U016 (single hit), U013 (named in the tier file's relevant-ADRs but zero privacy-noun hits — include only if its a11y/i18n text carries privacy-adjacent obligations on inspection).
- **Template (spec skeleton):** `docs/templates/vertical-spec.md`.
- **Predecessor carry-forward:** the Privacy pickup slate below.

**Cold-derivation discipline** holds as template: no `supabase/`, `lib/`, `app/`, `tests/` reads during Step 1. The obligation inventory derives from L1 + L2 + constitutional docs + cores + ADRs + carry-forward only.

## Entity-specific carry-forward block — the Privacy pickup slate (routed across DS-1..DS-7)

1. **World Model (DS-1/DS-5):** the cord-health consent surface — `world-model.md` §6 names Privacy as its consent surface (glanceable / invited / self-first); the S43 private-home obligations (default-locked; the FIM holds the only key; per-region, per-audience, revocable) landed owner-agnostic at G-3.
2. **Content (DS-4):** `content.md` §8 Q3 — capture provenance and real-world rights (bystanders, private interiors, location traces) routed to the Privacy vertical / research-spike channel.
3. **Discovery (DS-6):** the search-indexing posture over communication content (scope mirrors membership — `discovery.md` §8 Q3); the search-history / saved-search posture.
4. **Intelligence (DS-7, heaviest):** U010's AI-data-handling obligations over DS-7 (`intelligence.md` §8 Q7 — portability/retention of `profile_data` buckets); bucket-level PC-2 consent for recommendation signals (declared interests only at Ferd); Observability events content-free by design — the privacy invariants bind event payloads.
5. **Already folded (ratified, refine-don't-re-derive):** the ADR-U027 Shadow obligations + S43 home-sharing in the spec's §6 (Session B → 2026-06-10).

**Ownership discipline:** the obligations stay levied on the named services. This run writes the obligation side; it does NOT resolve service-owned §8 questions and does NOT amend DS specs (Class 3 findings route to pickup lists — no sanctioned cross-entity amendments exist for this run).

## §4 / §5a Step 1 — cold derivation (Edit-shaped)

Populate in place: §3 Tooling (name the gaps honestly — consent store partial; export pipeline and erasure cascade to-be-designed; missing shared tooling is a flag, not a failure), §4 Failure modes (structural: what breaks, how detected, how recovered — this is what makes the vertical enforceable rather than aspirational), §5 Open questions (the three standing ones stay unless resolved; add what the derivation surfaces), §6 Obligations per tier (tier-specific, never generic — "rewrite any bullet that sounds like a slogan until it sounds like a checklist"; GDPR articles cited; DS-7 named explicitly; per-service where the ask varies), §7 Cross-cutting checklists (machine-checkable; each item a DoD candidate). L2 §1–§2 are touched only if the derivation surfaces a genuine contradiction (surface first).

Carry-forward priors: P-O1 / D7 / X3 / X5 / Finding #4 / D3 carry as template text but apply weakly to an entity that owns no SQL surface — note inverted applicability rather than forcing application; X3 may fire via U027's TTL configuration if the derivation touches it. Entity-specific priors: the erasure-overpromise gotcha; the GDPR terms-of-art rule; over-prescription kills verticals (specify the rule, not the inventory).

**Step 1 checkpoint** (before any Edit lands): obligation counts per tier; checklist delta (§7 is wired to PROCESS.md §5 DoD — name any item whose addition obligates a DoD reflection); open-questions delta; any proposed L2 §1–§2 touch; any deviation the vertical's substance forced. Wait for ratification. **A#5 sub-batch-of-1 Edit cadence binds throughout** — the first Edit-shaped Step 1 of the series; the §13 capture reports cadence shape explicitly.

## §5b Step 2 — stress-test (inverted scope: obligation-bearing substrate, both polarities)

The entity owns no tables; Step 2 stress-tests the obligation inventory against the platform-wide realized substrate. Calibrated at authoring (dual-method discipline and the §6 tool-level catch class bind on every zero):

- **Absence polarity (calibrated near-zero):** `consent`, `gdpr`, `erasure`, `data_export`, `opt_out`/`opt-out` — ZERO hits in live migrations, archive, and seeds; `privacy` — zero live, one archive hit (`20260215134017_add_direct_messaging.sql` L13, the B-MSG-002 "Message Privacy (RLS)" comment). The consent store / export pipeline / erasure cascade are UNREALIZED — matching L2 §3's to-be-designed honesty. Verify, don't discover.
- **Compliance polarity:** RLS coverage across the 19-table baseline (PW-5 — re-verify the count both methods); the Shadow/anonymous-auth substrate (ADR-U004 mechanism, U027 lifecycle — what is realized vs lock-only); **`supabase/seeds/04_system_groups.sql`** — the Visitor anonymous baseline group (L56) and the **`[Deleted User]` anonymized-content sentinel (L96), an erasure-adjacent realized artifact found via the seeds rule at authoring**; `display_preference` / visibility substrate.
- Migration archeology, framework-mechanism evidence, and the createClient survey apply where the obligations touch them (auth flows, export-shaped routes — expect absence; record scope per SS-16/17).

Cluster batch-and-report as template; checkpoint before the Step 2 block lands. **Retraction-rate series continues:** first vertical data point; the series verdict predicts zero retractions against this calibrated opener — a non-zero result is signal, surface it prominently.

## §5c Step 3 — adjudication

Q-resolution slate over §5 open questions (resolve, hold, or route — the DPO question, AI-state erasure, GDPR-vs-global-standard); **the tier-CLAUDE agreement check** (the five sibling "Privacy / GDPR" rows vs the revised §6 — manual, doc-health does not catch it; smallest-necessary consequential edits sanctioned); **the PROCESS.md §5 DoD reflection** if §7 checklist items changed (the tier rule: checklists feed DoD — don't let the two drift; surface scope before editing); ADR amendments only if a Q-resolution genuinely warrants one (expectation: zero; U027's TTL configuration stays deferred-by-design); pickup lists (receiving entities: Observability — next in G-03 order — plus any service-routed Class 3); closing bridge.

## §6–§9 Disciplines

All template text binds at baseline `ca43284`, including the seven close-out landings — this run is their first instance as template text; the §13 capture adjudicates whether each held. ASCII-only labels; sessions append-only; OLDFEAT blindness carries; no re-litigation of settled classifications (the Extension System bridge's Carry-forward confirmations list + the close-out's confirmed routings).

## §10 Output expectations and commit shape

Expected: (i) the in-place spec population commit (Edits held uncommitted until Step 3 ratification — single combined Steps 1+2+3 spec commit, DS-3 cadence law applied to Edit shape); (ii) consequential-edit commit(s) only as sanctioned at Step 3 (tier-CLAUDE rows; PROCESS.md §5; smallest necessary); (iii) closing bridge at `docs/planning/sessions/{DATE}_NN_-_PRIVACY-LANDED.md`; (iv) STATUS.md close. NO entity-CLAUDE creation commit (it pre-exists). **No push to origin** — Stefan dispositions push.

## §11–§13

Closing-bridge required sections, scope boundaries, and the five-prompt post-run capture as template. §13 additionally answers: did the entity-shape gate's named deviations suffice, or did the vertical's substance bend template text the gate didn't cover (evidence for the four remaining verticals)? Record the retraction-rate data point. Pickup lists must carry the Observability opener's inheritance block (the next vertical; same gate shape).

## §14 Start sequence

§1 pre-flight → §2 state-read → **the entity-shape adjudication gate (surface evidence + lean; wait for ratification)** → §5a Step 1 → checkpoint → §5b → checkpoint → §5c → §13 → closing bridge.

---

*End of instance. Authored 2026-06-12 against template baseline `ca43284`; the ninth instance, first vertical, zero riders.*
