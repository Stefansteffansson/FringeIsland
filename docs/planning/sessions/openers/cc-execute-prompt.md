Author the DS-7 Intelligence descent session-opener, then execute it. This is
the seventh instance of the autonomous L1->L3 template, run in the collapsed
author-then-execute shape (DS-3/DS-5/DS-6 precedent). DS-7 is the last Domain
Service before the Extension System and the Phase 3 close-out.

PART 1 — AUTHOR THE OPENER

Follow docs/planning/sessions/openers/README.md Step 1. Author from
docs/templates/autonomous-l1-l3-session-opener.md at baseline 766e134
(unchanged — NO revision landed at DS-5 or DS-6). Land at
docs/planning/sessions/openers/ds7-intelligence-descent-opener.md; mark the
DS-7 row In flight in openers/STATUS.md before executing. The archived DS-6
opener (openers/archive/ds6-discovery-descent-opener.md) is the structural
precedent — carry forward its Check 4 two-shape encoding and Section 2/3
conventions.

The opener must encode:

1. Substantive predecessor: the DS-6 closing bridge
   (docs/planning/sessions/2026-06-11_01_-_DS6-LANDED.md) — inherit its "DS-7
   Intelligence (next entity; its opener must inherit)" pickup block in full,
   plus the routed seams accumulated across the chain: communication.md §8 Q5
   (Whisp-carried messages — DS-5's cold lean: no, the branch is the FIM-FIM
   channel), world-model.md §8 Q7 (the salience channel — DS-7 feeds DS-1 by
   calling DS-1's contract), content.md §8 Q8 (AI-generated content posture,
   joint with DS-3 §8 Q3), and discovery.md §8 Q2 (recommendation-signal
   sourcing — DS-7 may supply profile signals; resolve from the DS-7 side,
   PC-2 consent surface binds).
2. THE WHISP-SPLIT ADR PROMOTION FIRES AT THIS DESCENT (PENDING.md, riding
   since DS-1): the split-by-face decision (DS-1 world-presence / DS-7 being)
   promotes to a numbered ADR in this run's close batch — the first
   PENDING-to-ADR promotion of the descent series. Promotion executes the
   parked candidate; it does NOT re-litigate the split. STOP and surface if
   the derivation surfaces anything contradicting it.
3. Pre-flight: tip at or after 2930702 (the DS-6 close batch + doc-health +
   dependency-SVG fix end there; origin is pushed to it); template
   most-recent-touch at 766e134 exactly (Check 5);
   /d/WebDev/GitHub/FringeIsland on branch main (Checks 1-2).
4. Check 4 names the same two acceptable shapes: (a) the root CLAUDE.md
   context-mode re-injection (disposition: discard; Edit-removal as the
   sanctioned classifier-denial fallback); (b) concurrent docs/novel/
   activity (commits 007b9a5, 4f00fac, 4019e32 precedent — the parallel
   writer has committed mid-session at DS-5 and DS-6; Swedish translation
   chapters now exist). Anything else unnamed in docs/platform/,
   docs/architecture/decisions/, docs/planning/sessions/: hard-fail.
5. NO FIRST DECISION expected: PENDING.md's related watch-items line already
   dispositions the DS-7 name in place ("Intelligence" kept deliberately —
   renaming it "Whisp" would break the cosmology-neutral naming lock). Do
   not reopen. The first ratification gate is the Step 1 checkpoint
   (DS-4/DS-5 precedent). The register DS-7 row is the charter seed:
   "Whisp dialogue; assessments dissolved (validity question open);
   starved-drive sensing (S28); guard railing".
6. Enumerate binding ADRs by sweeping docs/architecture/decisions/ for DS-7's
   domain nouns (AI/intelligen/whisp/profile/sense/assess/mentor/accumulat/
   guard) — verify attributed text against the ADR files (citation-precision,
   template text). ADR-U005 (profile_data flexible table) is expected to be
   THE entity-specific realized lock. Also sweep the tier and sub-tier
   CLAUDE.md files for the same nouns (the n=2 watch — known hit to verify:
   the domain sub-tier file names "Intelligence's AI-derived data obligations
   under Privacy" as future entity-CLAUDE content). NEW instance rule from
   DS-6 (n=1 rider): sweep supabase/seeds/ alongside migrations everywhere
   the canonical-table-read discipline applies — seed files carry canonical
   vocabulary in this repo.
7. Step 2 expectation, stated for verification: DS-7 is NOT expected pure
   near-zero — profile_data (ADR-U005) is realized DS-7-adjacent substrate
   in the baseline, and PENDING.md's Whisp-split entry names DS-7's faces.
   Calibrate at authoring (tables, lib, surfaces, seeds), don't inherit.
   Record the retraction-rate point (series: PC-4 7/9; DS-1 0; DS-2 0;
   DS-3 0; DS-4 0; DS-5 0; DS-6 0).
8. SETTLED — do not re-litigate: the Whisp split itself (promote, don't
   reopen); the notifications boundary (vertical/DS-5/products); the
   attachment seam (DS-4 assets by ID); profile/avatar media (PC-substrate);
   the feed-vs-recommendation boundary (chronology + scope filters is DS-5,
   beyond is DS-6 — resolved at DS-6); the anti-leaderboard guardrail
   (invariant, enforced at sources).
9. Riding watches: #4 migration-name-as-shorthand (n=6 opportunities,
   1 decisive firing at DS-5 — DS-7 is the LAST entity before Phase 3
   close-out, so this run's verdict feeds the landing adjudication);
   tier-CLAUDE-as-L1-boundary-authority (n=2, apply as instance rule);
   seeds-directory sweep (n=1, item 6 above). Sandboxed sweeps for Step 2;
   Cluster S structural survey first.
10. NOT this run's work: the cross-tier-write channel (DS-3-anchored, DS-5
    anchors recorded); PC-1 Finding #4 and the avatars-bucket routing; the
    vertical obligation inventories; the Extension System derivation (next
    in the queue after DS-7).

PART 2 — EXECUTE

Proceed from the authored opener's Section 1 pre-flight checks. Trust disk
over memory; material divergence halts, non-material citation corrections
bundle into the Step 1 gate. Ratification gates come to Stefan: the Step 1 /
Step 2 / Step 3 checkpoints, plus the Whisp-split ADR promotion scope before
it lands (shape, number, what it cites). Single-session expected with split
fallback. The ratified Step 1 Write is held uncommitted until Step 3. Close
batch per the template: spec commit + the Whisp-split ADR promotion commit
(with the PENDING.md entry dispositioned) + entity CLAUDE.md with same-commit
doc-health registry-row removal (.claude/skills/doc-health-check/SKILL.md
Section 7 carries the intelligence/CLAUDE.md row) + closing bridge with
Section 13 capture and the opener archived + STATUS.md close. NO push to
origin — Stefan dispositions push.

Staleness note: the tip anchor 2930702 is the only fact here that goes stale
if commits land before the next session — Check 3's "at or after" form
tolerates novel-path and pipeline commits; anything new in docs/platform/ or
docs/architecture/decisions/ should be named to the next session.
