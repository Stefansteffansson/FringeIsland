Author the DS-6 Discovery descent session-opener, then execute it. This is the
sixth instance of the autonomous L1->L3 template, run in the collapsed
author-then-execute shape (DS-3/DS-5 precedent).

PART 1 — AUTHOR THE OPENER

Follow docs/planning/sessions/openers/README.md Step 1. Author from
docs/templates/autonomous-l1-l3-session-opener.md at baseline 766e134
(unchanged — NO revision landed at DS-5). Land at
docs/planning/sessions/openers/ds6-discovery-descent-opener.md; mark the DS-6
row In flight in openers/STATUS.md before executing. The archived DS-5 opener
(openers/archive/ds5-communication-descent-opener.md) is the structural
precedent — carry forward its Check 4 two-shape encoding and Section 2/3
conventions.

The opener must encode:

1. Substantive predecessor: the DS-5 closing bridge
   (docs/planning/sessions/2026-06-10_06_-_DS5-LANDED.md) — inherit its "DS-6
   Discovery (next entity; its opener must inherit)" pickup block in full,
   plus the DS-4 bridge's DS-6 pickup (published-registry consumer line; the
   marketplace seam) and the DS-1-bridge identity re-derivation watch it
   points to.
2. Pre-flight: tip at or after ce1b947 (Check 3; the DS-5 close batch ends
   there); template most-recent-touch at 766e134 exactly (Check 5);
   /d/WebDev/GitHub/FringeIsland on branch main (Checks 1-2).
3. Check 4 names the same two acceptable shapes: (a) the root CLAUDE.md
   context-mode re-injection (disposition: discard; Edit-removal as the
   sanctioned classifier-denial fallback); (b) concurrent docs/novel/
   activity (commits a733879, 2fe74d5, 007b9a5 precedent — the parallel
   writer committed mid-session at DS-5). Anything else unnamed in
   docs/platform/, docs/architecture/decisions/, docs/planning/sessions/:
   hard-fail.
4. FIRST DECISION candidate (first since DS-3): PENDING.md's related
   watch-items line parks the DS-6 naming collision — "Discovery" vs the
   universe-discovery log, "decide at DS-6's charter re-derivation". This
   combines with the DS-1-bridge identity re-derivation watch: the descent
   re-derives DS-6's charter, and the naming call gates the L2 identity
   section. Run the vocabulary-vetting discipline on any candidate name.
5. Enumerate binding ADRs by sweeping docs/architecture/decisions/ for DS-6's
   domain nouns (search/recommend/marketplace/discover/catalog/index/rank/
   browse) — verify attributed text against the ADR files (citation-precision,
   template text). Also sweep the tier and sub-tier CLAUDE.md files for the
   same nouns — the n=1 watch from DS-5 (the platform tier file carried the
   decisive notifications boundary text no ADR stated); apply as instance
   rule, Section 13 adjudicates.
6. Step 2 expectation, stated for verification: DS-6 is expected
   NEAR-ZERO-CODE (no search/recommendation/marketplace substrate known in
   the 19-table baseline) — verify by calibration sweep at authoring, don't
   inherit. Record the retraction-rate point (series: PC-4 7/9; DS-1 0;
   DS-2 0; DS-3 0; DS-4 0; DS-5 0).
7. Routed seams this descent owns: communication.md §8 Q4 (feed-vs-
   recommendation boundary — resolve from the DS-6 side; sanctioned amendment
   to communication.md §8 Q4 on ratification); the content.md §3
   published-registry consumer line + the marketplace seam (Transactions
   explicitly none at DS-4 — the marketplace is DS-6's surface; re-check from
   the owned side); the journeys.md published-catalog line (no counts, no
   rankings at the source — the anti-leaderboard guardrail is invariant,
   consume don't re-litigate); DS-5's provisional search-indexing consumer
   line.
8. SETTLED — do not re-litigate: the Whisp split (promotion at DS-7);
   profile/avatar media (PC-substrate); the attachment seam (DS-4 assets by
   ID, n=4 pattern); the notifications boundary (vertical/DS-5/products).
9. Riding watches: #4 migration-name-as-shorthand rides strengthened (first
   decisive firing at DS-5 — sprint3's name under-describes its body; landing
   adjudication at the next firing or Phase 3 close-out);
   tier-CLAUDE-as-L1-boundary-authority (n=1, item 5 above). Sandboxed sweeps
   for Step 2; Cluster S structural survey first.
10. NOT this run's work: the cross-tier-write channel (DS-3-anchored, new
    DS-5 anchors recorded); PC-1 Finding #4 and the avatars-bucket routing;
    the Notifications-vertical obligation inventory.

PART 2 — EXECUTE

Proceed from the authored opener's Section 1 pre-flight checks. Trust disk
over memory; material divergence halts, non-material citation corrections
bundle into the next gate. Ratification gates come to Stefan: the FIRST
DECISION (naming + charter), then the Step 1 / Step 2 / Step 3 checkpoints.
Single-session expected with split fallback. The ratified Step 1 Write is
held uncommitted until Step 3. Close batch per the template: spec commit +
entity CLAUDE.md with same-commit doc-health registry-row removal
(.claude/skills/doc-health-check/SKILL.md Section 7 carries the
discovery/CLAUDE.md row) + closing bridge with Section 13 capture and the
opener archived + STATUS.md close. NO push to origin — Stefan dispositions
push.

Staleness note: the tip anchor ce1b947 is the only fact here that goes stale
if commits land before the next session — Check 3's "at or after" form
tolerates novel-path and pipeline commits; anything new in docs/platform/ or
docs/architecture/decisions/ should be named to the next session. The opener
filename uses the PRE-rename name (ds6-discovery-...) per the template
header, since the FIRST DECISION may rename the entity.
