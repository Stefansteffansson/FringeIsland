# Session bridge — Cycle J-A built end-to-end: FEAT-PD002 + FEAT-H019 + FEAT-PC016 all 6-done, the Journeys area opens on the Hub

**Date:** 2026-07-07 (third session of the day; executes bridge `_02`'s entry point exactly — the J-A build inside the hardened gates).
**Session type:** Full build cycle — four-scout terrain verification → task files (J-D3 honoured) → red-first platform contracts through the schema gate (Stefan's "yes to all") → Hub surface API-first → E2E + the first ADR-U043 Performance-DoD rows → 6-done close-out → all merged.
**Status:** `main` clean and synced through **PR #117** (`b82e488`; merge order #115 → #118 → #117). Dev DB at **`20260707145549`** (two J-A migrations applied + repaired, both Stefan-run/nodded). Dashboard refreshed (620 files). A dev server may still be running on `localhost:3000` (left up for Stefan's manual walk).
**Participants:** Stefan (the schema-gate nod — Q1–Q5 "yes to all"; both migration applies; the merge go-ahead) + Claude.

---

## What was done (in order)

- **Task files returned (board J-D3):** TASK-JA-01..09 under `docs/planning/backlog/tasks/` (cycle-scoped ids — no numeric predecessor survives retro deletion), dependency-ordered, schema task gated; all nine `done` at close. Maturity 4-ready → 5-in-cycle → 6-done with L4 rows + all three `features/README` indexes in the same commits. **Drift find fixed en route:** FEAT-PC016 had no §L4 row in `organisation-specification.md` (the "Six FEAT-PC-*" count was stale → seven, row added to Membership lifecycle).
- **FEAT-PD002 (first DS-3 feature) + FEAT-PC016 rider — migration `20260707130821`** (schema gate: the Open Q1–Q5 board presented with evidence, **nodded "yes to all"**): six own-actor SECURITY DEFINER contracts + `get_my_pending_nominations` + the `journey_enrollments` write-narrowing. Resolutions recorded in the spec's Implementation notes — Q1 row-deletion; **Q2 the oracle's one-directional dual-enrolment semantic homed substrate-side** (B-JRN-003's "detected" was app-layer-only in hub-legacy); Q3 the `journeys_select_published` RLS mirror verbatim; **Q4 both** (policy drops + DML revoke) **plus the partial unique active-party index** the 2026-02-21 rebuild lost; Q5 in-contract durable fan-out (`group_journey_enrollment`). Red demonstrated first: 39 failed / 2 labelled greens → 41/41.
- **Build finding → gate amendment `20260707145549`:** the v1 viewer block couldn't serve H019 STORY-5's "affordance per the payload" rule — `get_journey_detail` replaced in place additively (`individual_enrollment` + per-`enrolled_via` `enrollment_id`/`status`/`can_withdraw`). 2 asserts demonstrated red against the applied v1 payload → green post-amendment (journeys suite 37/37).
- **Load-bearing verifications made at build (not assumed):** self-enrol's key resolves **Tier-1** (the FI Members baseline role carries `enroll_self_in_journey`; `handle_new_user` Step 7 binds every FIM); group visibility mirrors `get_group_detail`'s full gate **including the PC015 wields branch** (internal `_journey_party_visible()`); dev pre-checks before the gate: 0 duplicate active parties, all 4 legacy policy names verified in `pg_policies`.
- **FEAT-H019 (first Hub build under the ADR-U043 budgets):** 5 BFF routes (3 Edge+`dub1` reads / 2 Node mutations — **route-policy conformance green, zero new exceptions**), the journeys session-cache client (sign-out invalidation wired in AuthContext), `/journeys` catalogue (FIM gate, Enrolled badges, **new `SkeletonGrid` B6 primitive** — none existed), `/journeys/[id]` detail (steps overview; viewer-shaped enrolment block — self-enrol, the H018 wielding-confirm naming the group, destructive-withdraw; frozen renders held-state, no affordance; **header seeds from the cached catalogue card**), the **GRP-4 seam filled** (`get_group_enrollment_summary` composed at the group-detail BFF as an ADR-U042 `{data}|{error}` slice + `GroupJourneysSection`), nav entry. **PC016 STORY-2:** `leadership.ts` thinned to a pure relay (two unit tests re-pointed from table-read mechanics, labelled); **the 2026-07-06 audit LOW finding recorded closed** in `api-conformance-register.md` §7.
- **Performance DoD bound for the first time:** first-paint = exactly 2 reads + zero duplicate fetches across auth-event churn (asserted; the groups-page 3x-refire guard), B6 skeleton deferral asserted (labelled test-after — DoD verification rows). **The production waterfall deliberately rides the J-O3 area gate — pending Stefan's live walk.**
- **Final gates:** unit **509/509** (74 suites) · integration **341/341** (35 suites, post-amendment full sweep) · E2E journeys **2/2** + full sweep 56/57 (**the one failure = the documented `entry.spec` flake-watch item, 3/3 isolated**) · `next build` green · lint 0 errors. CHANGELOGs (root + Hub) + Implementation notes in all three specs (red→green evidence, labelled exceptions honest).

## Findings / lessons (for the J-A retro)

1. **Lint drift:** the upgraded eslint-plugin-react-hooks added `set-state-in-effect`, which fails the house load-on-mount pattern — **pre-existing on main's groups page** (main's lint was red before this session). Suppressed at the three call sites with justification comments; **rule disposition (configure vs restructure the pattern) is a retro item.** One pre-existing warning (`runAdminSql` unused in the security suite) left untouched.
2. **Stacked-PR trap:** merging #115 with `--delete-branch` made GitHub **auto-close** stacked #116 (a closed PR whose base is deleted cannot reopen). Recovery: successor PR #118 (same branch → main, zero new commits; the gate record lives in #116's thread), then **retarget-before-delete** for #117. Lesson: with stacked PRs, either merge top-down with explicit retargeting, or never `--delete-branch` until the whole stack is retargeted.
3. **`test:integration:journeys` existed before its folder** — creating `hub/tests/integration/journeys/` wired it automatically (nice prior planting).
4. Concurrent full-integration + full-E2E sweeps against the shared dev DB coexisted fine this time; the `entry.spec` flake remains sweep-only.

## Next session — entry point

**J-O3 area-gate waterfall with Stefan** (production stable domain, authenticated real path, cold + warm, ≥3 runs/scenario — plus the J-O5 first-ever-cold < 2 s scenario) **before the boundary bet is chosen** (candidates: P3b journey slice / P1-residual / P4). Then **Cycle J-B**: the ADR-U044 step-model migration (step rows + registries + step-instances; the 47 legacy seed steps migrate mechanically), progress contracts over the narrowed substrate, the player; withdraw semantics (Q1) revisited when step-instances FK to enrolments. The J-A retro rides the cycle boundary (doc-health-check due then — first Journeys boundary; the three `_01` soft flags still carry).

## Standing / parked (deltas from `_02`; otherwise carried unchanged)

- **`get_my_pending_nominations()`:** BUILT (6-done); `leadership.ts` thinned; audit LOW closed.
- **GRP-4 enrolment-summary seam:** FILLED (slice-composed at the BFF per the one-way rule).
- **J-O3 waterfall + boundary bet:** OPEN — needs Stefan's live walk; J-A's pages join the protocol.
- **New retro items:** the `set-state-in-effect` rule disposition; the stacked-PR merge lesson.
- Everything else carries: `entry.spec` + `profile.spec` flake watch, Stefan's dashboard toggles, test hygiene, platform-core CHANGELOG disposition, `test:integration:rbac` cleanup, MEM-9 → Communication gate, IDN-12 + perf T2 parked, ADR-U040 referral rebuild unscoped, logo pick, launch checklist, the 3 doc-health soft flags.
