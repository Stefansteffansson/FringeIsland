# Session bridge — 2026-07-20 (03): Cycle C-C decomposed + built — live layer complete on both halves, held at two gates

**Session span:** one continuous autonomous session, 2026-07-20 (following bridge `_02`, the C-B close). Scoped doc-health → C-C decomposition to 4-ready (paired specs, taxonomy fixed) → tasks → platform half built with demonstrated red → surface half built and verified green → **held at the gates for Stefan's nods**.
**Previous bridge:** `2026-07-20_02_-_CYCLE-C-B-BUILT-FORUM-ATTRIBUTION-LIVE.md`.
**PR trail:** #214 (C-C decomposition — FEAT-PD010 + FEAT-H027, merged) · #215 (channel-scope steering batch — §L2 §4 named list + Hub CLAUDE §4 line + DS-5 CLAUDE refresh, **held for the steering nod**) · #216 (tasks CC-01..06 + specs to 5-in-cycle, merged) · #217 (**SCHEMA GATE** — migration `20260720153000` hint triggers + receive policies, red 9/3 demonstrated, apply commands in the body, **held for the NAMED nod**) · #218 (Hub live layer — manager + tenants + reconciliation, verified unit 107/791 + lint 0 + `next build` clean, **merge after #217 + apply**).

## What stands ready, as a member would tell it (pending the gates)

When someone writes to me, my inbox, my unread badge, and the conversation I'm reading find out by themselves — the platform whispers *that something happened* (never *what*), and the page goes and fetches the truth through the same doors it always used. My group's forum does the same while I'm on the group page, without eating my half-written reply. If my connection drops, the messages surfaces quietly say they're reconnecting, keep working, catch up the moment the socket returns — and a missed whisper costs me seconds, never a message, because the tables were always the truth.

## The decomposition (merged, #214/#216)

- **Channel taxonomy fixed at decomposition** inside the ADR-U039 rails: `account:<auth_uid>:conversations` (per-member; one subscription serves inbox + open detail + badge — the exact pattern the A-NTF bell joins as `account:<auth_uid>:notifications`, named forward-looking only) and `group:<group_id>:forum` (per-group; one emit per event; membership-gated receipt — §8 Q7's law: RLS on `realtime.messages`, never a channel filter).
- **Payload walk finding:** every verify-on-signal re-fetch is already served by the realized C-A/C-B contracts (`get_my_conversations` / `get_conversation_detail` / `get_group_forum`) — **C-C's platform half is pure hint emission**; no new read contracts, no payload changes. Hints are ids-only (`{conversation_id}` / `{post_id}` as correlation).
- Precedent facts disk-verified before authoring (session-guard mechanics, the PC009 `realtime.send`/policy shapes, cache/courier paths, publication state — only `notifications` remains in `supabase_realtime`, A-NTF's disposition).

## The build in facts

- **Platform (FEAT-PD010, at the gate, #217):** `ds5_emit_hint` (one non-fatal home, **WARNING-logged** — the PC009 swallow upgraded per the tier's no-silent-failures law); `messages` AFTER INSERT fan-out per **active** participant (sender included, departed excluded; P-O1 chain in reverse; per-emit isolation); `forum_posts` INSERT + moderation-transition triggers (trigger-level `WHEN` = idempotency); two receive policies (own-conversations = the session-channel shape byte-for-byte; forum via reused `is_active_group_member` — spec Q1 resolved by reuse, PG17-ceiling-safe); **no client-send policy on any C-C topic** (spoofing impossible by construction); no publication changes, no new tables. Conformance lockstep: `ds5_emit_message_hint` → `DS_OWNED_ALLOWLIST`.
- **Red→green, honestly:** `realtime-hint-emission.test.ts` demonstrated **9 red / 3 labelled green** pre-apply (greens: session-channel regression guard, the always-refused rpc probe — the C-B precedent shape — and the Q1 helper guard). Receipt is probed via the **WebSocket subscribe path** (the sessions.test.ts precedent) because `realtime.topic()` only resolves in Realtime's join-time auth — a SQL SELECT would prove nothing; no-client-send is a structural `pg_policies` assertion. `markArrivedOnce` not used — it's an E2E-only helper; the integration comm suites use `createTestUser` (noted in the suite header).
- **Surface (FEAT-H027, #218):** `hub/lib/realtime/` — manager (one socket, `setAuth`-before-join, private-only, declarative tenants, status + rejoin surface, teardown; **A-NTF readiness proven by a synthetic-tenant test**), conversations tenant (hint → `invalidateMessagesCache` + `conversationsChanged`; badge/inbox/targeted-detail re-read through existing couriers; read-marking byte-identical), forum tenant (page-scoped; `dropGroup` + re-read; drafts preserved; tombstones live), STORY-6 reconciliation (`ReconnectingNotice` only after a subscribed channel degrades — no first-connect flash; reconcile on recovery + visibility; `COMM_POLL_MS` 60s only degraded+visible). `session-guard.ts` untouched. All red-first at the unit tier per suite (one vacuous-green caught and refitted with a real red driver). **Verified independently: unit 107 suites / 791 tests · lint 0 errors · `next build` clean.**

## Found at build

1. **Forum live re-read simplification (deliberate, in-fence):** on a hint, the section re-reads its first page; loaded-earlier pages collapse (load-earlier re-clickable, drafts preserved) — chosen over new fetch plumbing per the refetch-don't-patch fence. Record in FEAT-H027 Implementation notes at 6-done.
2. **Two `react-hooks/set-state-in-effect` hits** fixed cleanly (not suppressed): `useCommChannel` derives the indicator; `MessagesLink`'s initial badge read is an inline promise chain. Behaviour-preserving, tests green.
3. **Doc-health finding fixed in #215:** the DS-5 entity CLAUDE.md (merged post-C-B as #207) still called ADR-U021's display law unrealized — C-B realized it (`ds5_resolve_author_display`). Rewritten to post-C-B truth.

## Open at close — the two nods and the ordered tail

1. **PR #215 (steering)** awaits a merge nod — channel-scope batch (§L2 §4 + Hub CLAUDE §4 + DS-5 CLAUDE refresh).
2. **PR #217 (SCHEMA GATE)** awaits the **NAMED** nod. At the nod: apply + repair `20260720153000`, run `tests/integration/communication` (expect the 9 reds flip green, zero test edits), merge #217, then merge #218 (surface — safe only after the policies exist).
3. **Then the CC-06 tail:** author + run the E2E live journeys (two-context: B's inbox/badge move on A's send; forum post appears live; reconnect path — the sessions.spec `bringToFront` determinism trick), full sweeps (unit + integration sequential + E2E) + `next build`, CHANGELOG, **6-done transitions** for both specs (Implementation notes incl. the forum-collapse simplification + E2E-tier posture; §L4 rows in Hub SPECIFICATION.md + communication.md; both feature indexes), tasks CC-01/02 → done, the plain-English walkthrough, the close bridge. Performance disposition already recorded in-spec: no first-paint request added or rerouted → no deep-cold trigger (ADR-U043 Amendment 1 not fired).
4. **Task-board note:** CC statuses live on the held branches (CC-01/02 `review` on #217; CC-03/04/05 `done` + CC-06 `in_progress` on #218) — main shows `todo` until the merges; not drift.
5. Standing items carry (TASK-MIST-01, TASK-DOC-003/004, Vercel scale-to-one, logo, launch checklist; C-D announcements design session is next after C-C closes).
