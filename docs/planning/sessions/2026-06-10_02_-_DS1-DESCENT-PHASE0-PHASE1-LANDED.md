# DS-1 descent session — Phase 0 (PC re-check) + Phase 1 (Step 1 derivation) landed

**Date:** 2026-06-10
**Type:** Session bridge (closes the DS-1 descent session run from
`openers/archive/ds1-world-model-descent-opener.md` — a custom two-phase opener, not the
autonomous L1→L3 template).
**Inputs consumed:** Session B conformance register (Section 3 + Section 6 handoff), the
cosmology / roles / beings cores, VISION.md v1.2, ADR-U023/U025/U026/U027/U028, the four PC
specs (targeted re-walk), the domain-service template, the ecosystem-decomposition skill.

---

## What landed (three commits)

| What | Commit |
|---|---|
| Phase 0: ratified delta record + PC-1 scheduled-job substrate (pg_cron) | `5dfc313` |
| Phase 1: `world-model.md` (L2+L3 Step 1) + Whisp-split decision (PENDING.md) + domain README labels + `world-model/CLAUDE.md` + template consumers-enum fix | `cde6ffb` |
| Session close: this bridge + STATUS.md + opener archived | (this commit) |

## Ratified decisions (all by Stefan, 2026-06-10, this session)

1. **PC-2 Shadow lifecycle stays a single L3 row**; TTL config + atomic migration are invariants
   within it. **Ball-grant at transcendence is a DS-1 capability** consuming PC-2's transcendence
   event (Core ← Domain direction holds). Full record: `2026-06-10_01_-_DS1-DESCENT-PHASE0-DELTA.md`.
2. **PC-3 Dreamineer templates confirmed demand-driven** — role-template entries, no capability
   rows; re-check fires at studio decomposition.
3. **S43 home-sharing seam:** DS-1 owns home structure + share-state + read-path enforcement;
   PC-3 supplies the audience primitive; PC-2 status only; Privacy keeps the obligations.
4. **PC-1 gains the scheduled-job substrate (pg_cron)** — closes the dangling PC-2 external
   dependency. (PC-4 Console pending-marker and PC-2 §9 cross-ref were offered, NOT ratified —
   recorded in the delta record so the findings aren't lost.)
5. **The Whisp's L2 owner — split by face** (the long-flagged cascade gap): DS-1 owns
   world-presence (cord, Void distance, anchoring, severance, respawn position); DS-7 owns the
   being (dialogue, filling, senses, internalisation, guard railing); DS-7 consumes DS-1.
   ADR candidate recorded in `decisions/PENDING.md`; promote at the DS-7 descent.
   Preceded by an anatomy challenge (Stefan): verdict — keep ADR-U023's entity set; one
   watch-item (DS-6 Discovery's charter is partly contradicted by the new canon — re-derive,
   possibly shrink, at its own descent); DS-1 is the new centre of gravity.
6. **DS-1 spec ratified as landed** with seven embedded calls: §7 service-level invariants block
   (additive template deviation); branch = DS-1 world-entity (cold lean, ratification deferred to
   first branch FEAT, spec §8 Q3); respawn resolution (DS-1) vs delivery (DS-3); DS-4 assets
   referenced opaquely only; NPC body+culture layers in DS-1; Transactions explicitly none;
   Shadow cord state inherits ADR-U027 ephemerality.
7. **Template fix now** (domain-service-spec.md consumers enum: game out, world-studio in) and
   **DS-1 entity CLAUDE.md authored now** (first Domain Service entity CLAUDE).

## Where DS-1 stands

`docs/platform/domain/world-model.md` holds the **Step 1 cold derivation**: L2 identity,
boundaries, contract families, §7 invariants, §8 open questions (Q1 coordinate/privacy substrate
is a research-spike candidate), and an 18-capability §L3 inventory across six internal areas.
**Step 2 (code-informed stress-test) and Step 3 (adjudication) are pending** per the standing
pattern; forward-commitment classification defers to Step 2 (expectation: mostly full-forward).
Reader tours + L4 follow per the maturity gates.

## Handoff / open items

1. **DS-1 Step 2 + Step 3 session** — next in the DS-1 line. Stress-test against
   `supabase/migrations/`, `lib/`, `app/` (cumulative-forward read order, A#8); adjudicate;
   classify forward-commitment.
2. **Whisp-split ADR promotion** — at the DS-7 descent (second consumer of the boundary).
3. **Cascade SVG Whisp-placement gap** (`01-decomposition-cascade.svg`) — the decision now exists;
   the SVG + `how-we-work/index.html` update is a doc-health pickup (was already flagged residue
   in the Session B register §6.4).
4. **PC-4 Console pending-marker** — finding recorded, not ratified; ride a future PC-4 amendment
   or doc-health pickup.
5. **Branch formation/decay (spec §8 Q3)** — cold lean recorded (DS-1 world-entity, not a PC-3
   group); ratify when the first branch FEAT-PD is specified.
6. **Doc-health-check run** — recommended since Session B (every on-demand trigger fired there);
   this session adds CLAUDE.md authoring + template edit to the pile. Still outstanding.
7. **Root `CLAUDE.md` remains uncommitted by design** (Session B handoff item 1 — Stefan's split).
8. **DS-2..DS-7 + Extension System** — remain pending; sequencing note: the register's DS-6 row
   plus this session's anatomy-challenge verdict mark DS-6 for identity re-derivation at entry.

## Carry-forward confirmations

CODE stays set aside as a correction target. ASCII-only labels held. No new assertion-bearing
diagrams were created (nothing joins the doc-health diagrams registry). The 2026-04 Hub L3
working set was not used as derivation input. Sessions remained append-only.

---

## In-session addendum (same day, after "move on"): Steps 2-3 landed; naming decisions parked

1. **DS-1 Step 2 (code-informed stress-test) + Step 3 (adjudication) executed in-session** and
   recorded at the foot of the spec's §L3: **zero DS-1 domain artifacts exist in code** (18-table
   end-state schema, all PC-2/PC-3/PC-4/DS-3/DS-5 territory; no stale retired-model terms; noise
   classes recorded). Zero-delta adjudication — the inventory committed unchanged; **all eighteen
   capabilities full forward-commitment**. Handoff item 1 of this bridge is thereby CLOSED;
   DS-1's derivation is complete (reader tours remain a later maturity gate per G-32 practice).
2. **Naming challenge outcome (Stefan):** PC names all confirmed; **DS-3's rename
   (Experience Engine -> journey-named, plus the Engine-suffix asymmetry) parked in
   `decisions/PENDING.md` for the DS-3 descent** (commit `e86e5be`); DS-1/DS-6/DS-7 naming
   watch-items recorded there with no action.
3. STATUS.md: DS-1 row -> **Done**; DS-2 Narrative Engine -> **Next**.
