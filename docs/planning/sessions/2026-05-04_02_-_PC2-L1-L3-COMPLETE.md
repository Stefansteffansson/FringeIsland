# Session bridge: 2026-05-04 (2) — PC-2 Identity L1→L3 derivation complete; stress-test pattern at n=3, two A-candidates added

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-05-04 (second bridge of 2026-05-04)
**Session type:** L-level architectural commit. Phase 2 entity 2. PC-2 Identity L1→L3 derivation; three-step code-informed stress-test pattern executed at n=3; adjudication producing pickup lists for downstream entities. First execution of sub-batch-of-1 multi-Edit gate cadence under the new pre-emit discipline.

**Chronological predecessor:** `2026-05-04_01_-_PC1-L1-L3-COMPLETE.md` (committed `b75342f`).
**Substantive predecessor:** same — PC-1's Finding #5 pickup item ("PC-2 owns session lifecycle and authenticated-context handoff") was the load-bearing carry-forward into this session's §6 partition.

---

## Session arc

State-read at open verified disk: PC-1 spec landed (`infrastructure-specification.md`, 332 lines, commit `12897d2`), bridge file present, `lib/supabase/{client,server,middleware}.ts` extant, Finding #5 text matches as cited. Phase 2 entity 2 opened directly into the three-step shape at standing methodology (n=3 instance).

The session ran cleanly through Steps 1, 2, and 3. No process-on-process inversion this time — the discipline-stack altitude reset from PC-1 held throughout. Two distinct disciplines surfaced mid-session:

- **Print-batch-before-gate** at L-level surface drafts emerged at Step 1 when the first cold-derivation Write was caught and reviewed before commit. Memory rule authored mid-session.
- **Sub-batch-of-1 multi-Edit cadence** emerged at Step 3 Plans B+C+D when two distinct emission failures (one real undercount, one UI-sequencing false-positive decline) compounded against the pre-emit count discipline. Resolved by fallback to one Edit per gate, 7 gates total.

Both disciplines added to the print-batch-before-gate memory file as durable refinements.

---

## Load-bearing output

**PC-2 Identity L1→L3 derivation complete.** Spec at `docs/platform/core/identity-specification.md`. Three steps executed:

- **Step 1 (cold derivation, 2026-05-04)** — L1+L2 cold-derived from root `CLAUDE.md`, `docs/platform/CLAUDE.md`, `docs/platform/core/README.md` L2 line, `docs/platform/core/CLAUDE.md`, ADR-U023, template, and PC-1 Finding #5 pickup. Identity-primitives-only framing excluding Journal (L2-line-revision candidate from cold derivation alone). `user_id` contract surface named as the load-bearing platform-wide export. Substrate-vs-mechanism partition with PC-1 (PC-1 publishes role conventions; PC-2 publishes session lifecycle and authenticated-context handoff). 11 capabilities + 1 latent (Journal pending Q1).
- **Step 2 (code-informed stress-test pass, 2026-05-04)** — Run against `supabase/migrations/`, `lib/supabase/`, `app/api/`, `proxy.ts`, `next.config.ts`, `docs/platform/core/features/`. Three-class output: 5 Class 1 confirms, 4 Class 2 entity-internal deltas, 5 Class 3 cross-entity findings (including F-Q4b proxy.ts wiring uncertainty as research-spike candidate). Three phase-wide observations (PW-1, PW-2, PW-3).
- **Step 3 (adjudication, 2026-05-04)** — Sequenced per PW-3: Plan A (Q4 cluster reshape touching §3, §6, §7) committed first; Plans B+C+D (Q1/Q2/Q3/Q5 adjudication, profiles → `public.users` rename, role-name vocabulary moved to PC-3, §L3 Step 3 outputs) batched under sub-batch-of-1 cadence. README L2 line revised separately ("Identity (PC-2) — Authentication, sessions, profiles, user_id contract").

**Stress-test pattern at n=3.** Was n=2 from PC-1 (standing methodology promoted there). PC-2 is the second consecutive recurrence — pattern executes as standard L3 derivation shape with no special promotion-confirmation work; A-candidate #5 emerged as session methodology learning rather than from a stress-test finding.

---

## Two new A-candidates added (ledger now at 5)

PC-1 surfaced A-candidates #1 (latent-vs-delta), #2 (tier-shape escalation channel), #3 (database-shaped L2 framing). PC-2 adds:

4. **Schema-predates-partition** (PW-1, provisional). Existing schema entities (`users` with `personal_group_id`, `handle_new_user` trigger, PC-3 `role_templates`) appear to predate the PC-1/PC-2/PC-3/PC-4 partition; cuts don't map cleanly. Distinct from PC-1 Finding #4 (substrate-tier confusion) — PW-1 is **temporal-shape mismatch**: partition is newer than schema. Promotion expected at PC-3 given that C3-1, C3-2, C3-3, C3-4 all carry-forward there with the same temporal-shape signature; PW-1 functions as the pre-staged finding for that Phase 2 close-out promotion.
5. **Multi-Edit gate UI sequencing + emission discipline** (session methodology learning, distinct from PW-1/PW-2/PW-3). Multi-Edit parallel gates display prompts sequentially in the harness UI, creating a false-positive emission-failure signal under pre-emit count discipline. Real emission failures (PC-2 Step 3 attempts 1+2: announced 7, emitted 3 — twice) and false-positive UI-sequencing declines (attempt 3: announced 3, emitted 3, declined on first visible prompt) are distinct failure modes; both share the same countermeasure (sub-batch-of-1). Promote at PC-3 if either shape recurs.

(A-candidate #1 also took a compounding observation this session via PW-2 — the third shape "speculative" emerged at F-Q4c. Captured in spec §L3 Step 2 phase-wide observations and PC-3 pickup as promotion-watch.)

---

## Carry-forward to next sessions

Three distinct carry-forwards travelling beyond this session:

1. **PC-1 Finding #4 (secrets/credentials substrate is app-tier).** Still carrying. PC-2 cold derivation surfaced no app-tier substrate adjacent to secrets/credentials, so the carry-forward did not extend at PC-2. Phase-2 close-out adjudicates after PC-3 and PC-4 land. Three-destination escalation path (Phase-2 close-out / candidate ADR / tier-shape revision) still preserved without collapse.
2. **F-Q4b research-spike (PC-2 originated).** Does Next.js 16 auto-discover `proxy.ts` as middleware? Was the 2026-01-21 migration premise (commit `68e544a`, "Migrated middleware.ts to proxy.ts for Next.js 16 compatibility") correct? Routed to **program-level pickup** per Step 3 disposition; owner assigned at next cycle retrospective; default to PC-2 backlog if not picked up. Two-part outcome required: (a) is session-refresh wired today; (b) was the migration premise correct.
3. **PW-1 + PW-2 + A-candidate #5 promotion-watch at PC-3.** All three are provisional pending recurrence at PC-3. PC-3 derivation should record at its §L3 sources-status block whether each pattern recurs.

---

## PC-3-specific carry-forward (verbatim from spec §L3 Step 3)

*The block below is verbatim from `identity-specification.md` §L3 Step 3 → "To PC-3 Organisation author (next entity in chain)". Pasted here so PC-3's state-read finds it without reading the full PC-2 spec.*

PC-2 derivation surfaced four cross-entity findings that are Step 1 inputs for PC-3:

1. **Today's `handle_new_user` AFTER INSERT trigger on `auth.users` is a PC-2 / PC-3 seam-trigger.** It executes user-row INSERT (PC-2), then in the same transaction creates a personal group in `public.groups` (PC-3), inserts the user-group link, creates self-membership, instantiates a "Myself" role on the personal group, enrolls the user in the FringeIsland Members system group, and claims any pending invitations addressed to the email. The composed invariant "no User exists without a user-row in `public.users` + personal group + memberships + Myself role" is preserved transactionally. PC-2 accepts the seam at its spec; PC-3 should decide whether to: (a) accept the seam permanently and document the partition bilaterally; (b) factor into PC-2 emission + PC-3 cascade response per ADR-U016 (the factoring pickup); or (c) escalate as an ADR if the factoring touches the §4 dependency chain semantics.
2. **`public.users` carries a `personal_group_id UUID` column today** — a structural inversion of the strict-upward §4 chain (PC-2 schema with FK to PC-3 `public.groups`). PC-3 derivation should specify whether the factoring relocates this column (companion to finding 1), and if so, into what artifact (join table on PC-3 side, attribute on the personal group, etc.).
3. **The four FringeIsland role names (Steward, Guide, Member, Observer) live in PC-3, not PC-2.** Disk evidence: TEXT values in `role_templates` table, no PG ENUM. PC-2 had originally cold-derived role-name vocabulary as a PC-2 concept (per the sub-tier `docs/platform/core/CLAUDE.md` temporary anchor); Step 2 disk evidence settles the partition at PC-3. PC-3 derivation should §2 / §3 / §5 the role-name vocabulary as PC-3-owned, determine the canonical artifact shape, and address ADR-U007 in §6.
4. **`has_permission(user_id, group_id, permission_name)` does not exist on disk; ADR-U007 pattern is latent in PC-3.** RLS today uses `auth.uid()` directly with policy-specific logic; RBAC realized via direct triggers (e.g., `prevent_last_leader_removal()`). PC-3 derivation should §3-list `has_permission()` as a primary capability, pin its signature and location in §5, and §8-flag whether ADR-U007's permission-resolution pattern is implemented in full or whether the existing direct-RLS pattern stands as the alternative.

**Two phase-wide watches** PC-3 should record at its §L3 sources-status block:
- **PW-1 (schema-predates-partition).** PC-2 surfaced this as a recurring class of finding. PC-3's existing schema (groups, memberships, roles, permissions tables; the `prevent_last_leader_removal()` trigger; etc.) is highly likely to show the same temporal-shape mismatch. If PC-3 confirms recurrence, promote PW-1 to a named program-level pattern at Phase 2 close-out.
- **PW-2 (speculative as third shape).** PC-2 surfaced "speculative" as a candidate third shape beyond the latent-vs-delta distinction. If PC-3 cold-derivation produces hypotheses that disk doesn't currently support but are appropriate as directional future state (likely candidates: PC-3 hypothesizing API endpoints for groups/memberships), tag them as speculative and record. Promote at Phase 2 close-out if recurrence confirms.

**Sub-tier `docs/platform/core/CLAUDE.md` revision** belongs in PC-3's commit batch when its L2 spec is authored: the temporary anchor naming PC-2 as the home of the four roles + `has_permission()` should migrate to PC-3's entity-level CLAUDE.md per the cascade policy.

**L2-line discipline** — PC-3's L2 line in `docs/platform/core/README.md` is "Groups, memberships, roles, permissions." PC-2's L2 line was revised at Step 3 to drop a misplaced domain-scope item (Journal). PC-3 derivation should stress-test its own line for the same shape — are all four items domain-agnostic identity-organization primitives, or does any item read as domain-scope?

---

## Way-of-working refinements this session (durable, A-candidate material)

Two refinements emerged under real pressure during this session and were authored to the print-batch-before-gate memory file mid-session: (i) print-batch-before-gate at L-level surface drafts (caught at PC-2 Step 1); (ii) sub-batch-of-1 multi-Edit cadence (caught at PC-2 Step 3 Plans B+C+D after two failed gate attempts). Both are durable for L-level work going forward. See A-candidate #5 for the promotion criteria and the canonical statement of the rule. PC-3 should default to sub-batch-of-1; sub-batch-of-3 is an optimization to attempt only if discipline holds at PC-3 Step 1 / Step 2.

---

## Tripwires

| # | Description | Status |
|---|---|---|
| 4 | Disk-of-record verification before asserting (carry-forward AND drafted-this-session content) | ACTIVE — held cleanly throughout. State-read at session-open verified bridge claims against disk; Step 2 stress-test verified Step 1 cold-draft against disk; Step 3 pickup lists cite disk anchors per finding. F-Q4b verification (`git log` of `proxy.ts`) executed before Step 2 finding text was finalized. |
| 6 | Discipline-as-deferral | NOT FIRING — discipline-stack altitude reset from PC-1 held throughout. No process-on-process inversion this session. Tripwire remains armed. |

Tripwires #1, #2, #3, #5 — closed in prior bridges; not re-engaged this session.

---

## Repo state at session close

Branch `main`. This session produces **three commits**, with the README changes combined into one commit covering both the L2 revision and the staleness sweep, all authored locally by Stefan in this order:

1. **`identity-specification.md`** Steps 1+2+3 combined per the approved consolidated commit message.
2. **`README.md`** L2 line revision per Q1 adjudication ("Authentication, sessions, profiles, user_id contract") + staleness sweep (lines 17-18 — drop `_(to be written)_` for PC-1 and PC-2 area specs that have now landed) combined per the approved consolidated commit message.
3. **This bridge file.**

Don't conflate: spec is authored artifact; bridge is process artifact; the README commit bundles documentation hygiene with the L2 revision. The bridge is written last so this section describes the world as it actually is at session close, not prospectively.

---

## Discipline posture for next session (PC-3)

- Bouncing-partner cycle, surface-draft discipline, candidate ledger, tripwires armed.
- Discipline stack **on** for PC-3 L1→L3 derivation work itself (L-level commit). Stack stands down for sub-questions inside that derivation that don't compound.
- **Print-batch-before-gate** at L-level (durable from this session). For any message that would Write/Edit 2+ tool calls against one authoring artifact, print the draft for review first.
- **Sub-batch-of-1 multi-Edit cadence default** (durable from this session). N planned Edits → N sequential single-Edit gates with pre-emit announcement per gate. Sub-batch-of-3 acceptable as optimization only after the discipline holds at PC-3 Step 1 / Step 2.
- State-read at session-open: read this bridge **and verify against disk** (Tripwire #4). Specifically verify PC-2 spec landed as recorded, README L2 line revised as recorded, README staleness sweep landed, PC-3 carry-forward block reads as cited.
- No open adjudication carried into next session. PC-3 entry is clear.

---

## Three-day fitness function

Lives at phase-level, not as sub-question gate. Phase 2 closes when Core L1→L3 lands (after PC-3 and PC-4). At that point: (a) revisit Finding #4 with PC-2/PC-3/PC-4 evidence; (b) revisit F-Q4b research-spike status; (c) revisit PW-1 / PW-2 / A-candidate #5 for promotion to named patterns; (d) fitness-check the three-day turnaround at phase level; (e) decide on `ecosystem-decomposition` skill update for n=3 stress-test (or n=4 if confirmed at PC-3).

---

## Session-open prompt for CC (next session)

PC-3 Organisation L1→L3 derivation. Same three-step shape as PC-1 and PC-2 (cold derivation → code-informed stress-test → adjudication). Phase 2 entity 3.

State-read pass. Verify against disk:
- Branch state, tip commit
- This bridge file existence and disk-filename
- PC-2 spec at `docs/platform/core/identity-specification.md` — confirm landed as recorded
- README L2 line for PC-2 revised to "Authentication, sessions, profiles, user_id contract"
- README staleness sweep landed (lines 17-18 no longer carry `_(to be written)_` for PC-1/PC-2 specs)
- PC-3 carry-forward block in this bridge reads as cited (4 cross-entity findings, 2 phase-wide watches, sub-tier CLAUDE.md revision pickup, L2-line discipline)

Authority chain for PC-3 cold derivation:
- L1: root `CLAUDE.md` + `docs/platform/CLAUDE.md`
- Sub-tier: `docs/platform/core/CLAUDE.md` (note temporary anchor for the four roles + `has_permission()` — slated for migration to PC-3's entity-level CLAUDE.md when authored)
- L2 inventory: `docs/platform/core/README.md` ("PC-3 Organisation — Groups, memberships, roles, permissions")
- ADR-U023 (Platform Core / Domain Services decomposition)
- Template: `docs/templates/platform-core-spec.md`
- **PC-2 carry-forward block** (this bridge §"PC-3-specific carry-forward" or `identity-specification.md` §L3 Step 3 → "To PC-3 Organisation author") — read before deriving PC-3's contract surface, §5 storage, §6 auth-and-authz

Cold-derivation discipline: do **not** read `supabase/migrations/`, `lib/`, `app/`, or any existing `FEAT-PC*` during Step 1. Step 2 stress-test runs after cold draft lands.

Watch flags for PC-3:
- **L2-line altitude pre-stress-test** (per PC-2 carry-forward item 6). Stress-test "Groups, memberships, roles, permissions" at Step 1 — does any item read as domain-scope?
- **PW-1 promotion-watch** (schema-predates-partition). PC-3's existing schema is the most likely site for recurrence. Record at §L3 sources-status block whether PW-1's signature is present.
- **PW-2 promotion-watch** (speculative as third shape). If PC-3 cold-derivation produces hypotheses disk doesn't support but are appropriate as directional future state, tag as speculative and record.
- **A-candidate #5 promotion-watch** (multi-Edit gate emission discipline). If sub-batch-of-3 or any larger batch attempt fails at PC-3 Step 1 / Step 2 / Step 3, record the failure mode as confirmation. Promote at Phase 2 close-out if either shape recurs.
- **Finding #4 carry-forward** (secrets/credentials substrate is app-tier). If PC-3 surfaces app-tier substrate adjacent to secrets/credentials, route to the same Phase-2 close-out / candidate ADR / tier-shape revision channel.
- **`handle_new_user` factoring decision** (per PC-2 carry-forward item 1). PC-3 should §6 / §5 take a position: (a) accept-seam permanently with bilateral partition documentation; (b) factor into PC-2 emission + PC-3 cascade response per ADR-U016; (c) escalate as ADR.

Phase-2 close-out arrives after PC-4 lands. At that point: (a) revisit Finding #4 with PC-2/PC-3/PC-4 evidence; (b) revisit F-Q4b spike status; (c) revisit PW-1 / PW-2 / A-candidate #5 for promotion; (d) fitness-check the three-day turnaround at phase level; (e) decide on `ecosystem-decomposition` skill update for stress-test pattern at n=3 (or higher).
