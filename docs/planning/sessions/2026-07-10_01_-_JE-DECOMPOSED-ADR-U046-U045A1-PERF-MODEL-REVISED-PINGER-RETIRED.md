# Session bridge — 2026-07-10_01 — J-E decomposed (ADR-U046 + ADR-U045 Amendment 1); perf model revised to per-instance; pinger retired by experiment

**Session class:** boundary session spanning 2026-07-09 evening → 2026-07-10 (the J-D→J-E boundary work, then a live perf investigation Stefan triggered mid-session). Hub v2 Phase 3 state after this session: Journeys J-A..J-D `6-done`; **J-E decomposed to `4-ready` (unbuilt)**; J-F planned (ADR-U046); J-O3 area gate after J-F. *Date-drift note: the J-E decomposition artifacts carry 2026-07-09 as the decomposition date; the ratifications and merges landed just past midnight on 2026-07-10 — recorded here, artifacts left as-is.*

## What happened

1. **Prior bridge's open items closed** — #149/#150/#151 merges verified; keep-warm's manual dispatch verified healthy (401 + `x-proxy-timing`). **PR #152 (merged):** ADR-U043 Amendment 1 enforcement homes (hub-v2 gate tail rule + cold definition in both gate instances; idle-depth + deep-cold spot-check rows in the `feature-development` Performance DoD) **plus the eight-line homing debt cleared** (J-A/J-B/J-C retro §4 lines homed: six into `feature-development`, two into `ecosystem-decomposition` as the new "Decomposition verification walks" subsection).
2. **J-O6 design session ran → [ADR-U046](../../architecture/decisions/ADR-U046-step-response-capture.md) (PR #153, merged; plan v7).** All six board recommendations accepted: responses live on the step-instance (the ADR-U044 §4 lived record; Journal rejected — `create_journal_entry` is FIM-only while the onboarding walker is a Mist); responding optional-always (invariant 3); responses private-only in Ferd (progress-facts consent never covers content); minimal authored takeaways (inline `pending-DS-4`, seeded at J-E); DS-7 synthesis deferred; **capture + review rendering build as J-F, before the area gate**.
3. **Cycle J-E decomposed (PR #154, merged; plan v8)** — paired specs [FEAT-PD006](../../platform/domain/features/FEAT-PD006-onboarding-designation-and-arrival-contracts.md) ↔ [FEAT-H023](../../products/hub/features/FEAT-H023-onboarding-arrival-and-carry-over.md), both `4-ready`, payload-walked clean (one deliberate forward key: the takeaway seed, rendered at J-F). Board settled: designation = `is_onboarding_designated` + single-designation partial unique index; journey-level takeaway = nullable `journeys.takeaway`; first-arrival read = `get_onboarding_status()`; **[ADR-U045 Amendment 1](../../architecture/decisions/ADR-U045-onboarding-journey.md)** — Stefan's refinement: onboarding is the *unavoidable-but-unwalled front door* (voluntariness governs advancement, not entry) → **no opt-out store**; enrolment-absence is the whole first-arrival signal. Substrate audit confirmed carry-over continuity-free at `finalise_transcendence` (file:line-cited) and the designation substrate absent. The **L4 Journal retrofit** (FEAT-H011 revision, session cache + skeleton) rides the J-E build as a distinct task set.
4. **Perf model revised — provisioning is per-instance; concurrency defeats warm capacity** (analysis [§7](../hub-v2/2026-07-09-cold-load-regression-analysis.md), PRs #155/#156, merged). Trigger: Stefan's fresh-browser walk (groups fast, journeys ~5 s). Measured live: DB acquitted (`get_journey_catalog` 76 ms), route config acquitted (all Edge+`dub1`); sequential 401 probes (4 552 → 129 ms, cross-route riders fast) vs the authenticated Chrome-driven walk — **the `/journeys` 4-way concurrent boot drew three fresh ~4–5 s boots two minutes after the environment was provisioned** (656/4 846/3 643/4 876 ms; immediate reload 243–511 ms). GitHub Actions cron measured at 10 fires in ~23 h (1.5–4 h gaps vs `*/5`) — **workflow disabled 09:19 UTC**. The L2′ concurrent-pinger validation experiment **failed both ways** (pool decay across a 4.5-min gap is a coin flip; a 60-second-old pool missed the real 4-route fan-out — 3 842/5 219 ms on warm middleware; the cost sits in the function-sandbox layer). **Pinging retired as a strategy.**
5. **Industry research landed** ([`docs/research/2026-07-10-cold-start-industry-research.md`](../../research/2026-07-10-cold-start-industry-research.md), PR #157, merged): our findings are textbook (a ping warms one container; concurrency defeats it — Yan Cui et al.); **Vercel has deprecated the Edge runtime and recommends migrating to Node "for improved performance"**; Fluid's in-instance concurrency (the fan-out fix) is Node-only; scale-to-one (the base-case fix) is Pro-only. Ranked recommendation recorded: edge→Node migration interlude → fan-out reduction (L3 un-parked) → Pro if residual hurts.

## Decisions (all Stefan's, this session)

- ADR-U046 — all six J-O6 recommendations accepted; J-F cycle created before the area gate.
- J-E board: designation flag + partial unique index · `journeys.takeaway` · `get_onboarding_status()` · **ADR-U045 Amendment 1** (mandatory-entry / never-walled / no opt-out — Stefan's own refinement over the drafted opt-out question).
- Merges: #152 (skills carve-out), #153 (ADR carve-out), #154 (J-E decomposition), #155–#157 (fuller-auto docs).
- Pinger retired on the experiment's evidence; keep-warm workflow left **disabled** (removal decision open).
- **Edge→Node migration accepted in principle, scheduled as its own interlude next session** (not bolted onto this one).

## Open items / next session

- **Recommended order: the edge→Node migration interlude BEFORE the J-E build** — H023 carries a mandatory deep-cold spot check before `6-done`; migrating first means J-E is measured once, on the target runtime. The interlude's known scope: **ADR-U036 Amendment (carve-out)** superseding the addendum's "no perf-forced migration"; the `feature-development` **Route-policy DoD row flip** (currently mandates Edge+`preferredRegion` for hot reads — carve-out); the **PR #111 route-policy conformance test** updated red-first; ~15 route files; before/after deep-cold measurement under the Amendment-1 protocol (pinger already off).
- **J-E build** (feature-development skill): PD006's migration is a **schema-gate carve-out** — ship held at the gate with red tests + apply commands per the standing rule; H023 consumes API-first; the FEAT-H011 L4 retrofit rides as its own task set; JE-6 flagged that it touches a `6-done` spec (revision note required).
- **`keep-warm.yml` removal decision** — disabled, still on `main`, referenced by the ADR-U036 addendum + analysis §6/§7; retire it with a pointer when the migration interlude amends U036.
- **Cold reality until migration/L3 land:** first visit after a few minutes idle pays the wave (fan-out pages worst). The B1 morning-walk expectation from bridge `_01` is superseded — the pinger it depended on is retired. **Vercel Pro (scale-to-one) decision parked with Stefan.**
- **J-F** (ADR-U046 capture + review substance) after J-E; then the **J-O3 area gate** under the amended protocol — carry the note that the journeys page's fan-out makes it the boot-lottery-exposed surface (gate waterfall should measure it explicitly).
- Doc-health-check **not run** this session (its own trigger rule: no cycle boundary, no renames/deletions/schema/restructures — all changes additive docs). Next natural run: the J-E cycle close.

## Key numbers (for the retro)

`/journeys` 4-way concurrent boot: 656 / 4 846 / 3 643 / 4 876 ms → immediate reload 243–511 ms. Sequential probe: 4 552 → 129 ms (cross-route first hits 131/193 ms). `get_journey_catalog()` in-DB: 76 ms. Pinger experiment: tick-2 re-drew 3/4 boots after 4.5 min; a 60-second-old pool missed 2/4 of the real fan-out (3 842 / 5 219 ms). GitHub cron: 10 scheduled fires in ~23 h against `*/5`.
