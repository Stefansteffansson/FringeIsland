# Session bridge: 2026-05-13 (2) — PC-3 Organisation Step 2 landed; Step 3 deferred

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-05-13 (second bridge of 2026-05-13)
**Session type:** L-level architectural commit. Phase 2 entity 3 Step 2. PC-3 Organisation §L3 Step 2 code-informed stress-test pass executed across migrations (Clusters A-D, 19 files cumulative-forward), `lib/` (Clusters lib-1 + lib-2), `app/api/*` (Cluster api-1), and `docs/platform/core/features/` reconciliation (NULL output). Step 2.7 composition lands §L3 Step 2 block + nine sources-status amendments at commit `255219d`. Manual side of Experiment B restart — autonomous-side run on `experiments/B-pc3-full` not visible from this bridge.
**Position in PC-3 sequence:** Second of three planned PC-3 entity bridges. Predecessor `2026-05-13_01` landed Step 1; successor `YYYY-MM-DD_NN_-_PC3-STEP3-LANDED.md` will close PC-3 L1→L3 derivation.

**Chronological predecessor:** `2026-05-13_01_-_PC3-STEP1-LANDED.md` (PC-3 Step 1 landed; Steps 2+3 deferred — this session executes the deferred Step 2 in its own session per capacity-planning split).
**Substantive predecessors:** `2026-05-12_01_-_EXPERIMENT-B-ABORTED-RESTART-PLAN.md` (Experiment B restart specification — manual run continues here); `2026-05-04_03_-_EXPERIMENT-A-COMPLETE-AND-DEFERRED-WORK.md` (Experiment A priors and Step 2 watch definitions); `2026-05-04_02_-_PC2-L1-L3-COMPLETE.md` (PC-2 carry-forward block to PC-3; Q6 deferred-amendment routing).

---

## Session arc

Pre-flight checks (4/4 pass): pwd, branch `main`, tip at `5dca171` (the prior session's CC session-opener commit), working tree state with `CLAUDE.md` modified-unstaged. State-read pass: five session bridges in order (this session's predecessor `2026-05-13_01`, Experiment B restart, Experiment A, PC-2, PC-1) + disk-verification per Tripwire #4 (PC-3 spec at commit `8d880d6`; entity-level CLAUDE.md at `5592773`; sub-tier CLAUDE.md anchor removed; bridge file present; **PC-2 spec un-amended on disk** confirming Experiment B comparison-phase integrity).

Step 2.1 — migration reads in four clusters, batch-and-report cadence (methodology variant from PC-1/PC-2 single-pass):
- Cluster A (6 files, 2026-02-21 + 02-22): drop_old_schema + 3 fix_* + rebuild_universal_group_pattern + add_avatar_url_to_groups. Anomaly P1 surfaced: filename-order vs actual chronology. Rebuild migration (34K tokens) exceeded direct Read limit; structural extraction via R-b `ctx_execute_file` (methodology variant SS-14).
- Cluster B (5 files, 2026-02-23 + 02-24): protect_personal_group_id + enhanced_member_invitations + fix_test_regressions + fix_rc7_admin_user_ops + fix_hard_delete_leader_trigger_bypass. Cluster B's migration 10 reading retracted Cluster A's §8 Q5 verdict (`is_platform_admin()` EXISTS on disk; introduced for PG17 RLS compatibility).
- Step 2.2 — git log committer-date evidence on the four `handle_new_user`-touching migration filenames: D15 renumbering confirmed at smoking-gun specificity (commit `ce58227` *"consolidate 71 migrations into clean schema rebuild"*, 2026-02-22 10:45:32 +0100). P1 escalates to Class 3 finding; §8 Q1 brittleness reading softens dramatically (rebuild and mig-4 share the same commit; functional adds, not regressions).
- Cluster C (3 files, 2026-02-27): add_display_name_system + fix_personal_group_rls_visibility + seed_deleted_user_sentinel_group. P4 partial-resolution: `[Deleted User]` seeded; FI Members + DeusEx still TBD.
- Cluster D (5 files, 2026-02-28 sprint batch): sprint0_security_fixes + sprint1_foundation_schema + sprint2_leave_group_core + sprint3_smart_notifications + sprint4_platform_exit. DeusEx seed not in active migrations; §8 Q7 fully reified (`admin_exit_user_from_platform` + D-R3 admin-assisted); P11 archeology surfaces in sprint1 comment (*"The 8 predefined journeys were lost during D15 rebuild"*).
- Mop-up Greps: zero `INSERT INTO public.permissions` in rebuild; zero `INSERT INTO public.role_templates` anywhere; only `archive/20260216140506_rbac_role_management.sql` carries permissions + role_template_permissions seeds. P4 resolution: outcome (b) — system groups + permission registry not seeded by active migrations.

Step 2.3 — `lib/` (2 clusters). Cluster lib-1 (5 full reads): `constants/permissions.ts` + `supabase/{client,server,middleware}.ts` + `hooks/usePermissions.ts`. **PW-2 SHAPE SHIFT surfaced**: client hits `supabase.rpc('get_user_permissions')` directly — no Next.js API wrapper. Cluster lib-2 (7 grep+head): `admin/*` + `types/{group,admin}.ts` + `auth/AuthContext.tsx` + permissions-INSERT mop-up. **Service-role construction confirmed open-coded at `lib/admin/admin-users-query.ts`** (first identified beyond app/api/* X5 sites); P6 settled to SQL-side orchestration only (no app-tier supervised-bypass).

Step 2.4 — X5 enumeration: 4 service-role-using routes in `app/api/` + 1 in `lib/admin/*` = 5 sites / 6 createClient instances (`journeys/[id]/enroll` has 2). Site count consistent with Experiment A X5 within enumeration-methodology variance. No PC-3-owned operation routes in `app/api/v1/*` (no `v1/groups`, `v1/memberships`, `v1/roles`, `v1/permissions` routes exist) — PW-2 retraction confirmed at third disk anchor.

Step 2.5 — `app/api/` (Cluster api-1): 2 full reads (`admin/users/route.ts` + `invitations/send-email/route.ts`) + 2 grep+head (journeys). P10 sentinel-UUID + literal-NULL conventions both live at server-side admin-tier; P15 auth-flow duplication confirmed (5×); P16 three permissions gate the X5 routes (`invite_members`, `enroll_group_in_journey`, `manage_all_groups`).

Step 2.6 — `features/` reconciliation NULL within canonical scope. Mid-stream **OLDFEAT discovery**: pre-refactor feature artifacts at `docs/TMP/OLDFEAT/` surfaced by Stefan; manual run defers reconciliation per blindness invariant; SS-16 enumeration-claim-scoping methodology observation captured.

Step 2.7 — Three-turn composition discipline: structural outline → full text draft → Write. Two Edits per sub-batch-of-1 (Sources-status amendments first; Step 2 sub-section second). Both Edits landed clean with all three tightenings + optional C2-1 refinement applied. Spec amendment commit `255219d` (59 insertions, single file, no deletions).

---

## Two retractions at Step 2

Step 2 produced two distinct shape-corrections through different mechanisms, both methodology-relevant for comparison-phase analysis:

1. **§8 Q5 retraction** (Cluster A → Cluster B). Cluster A's verdict *"`is_platform_admin()` does not exist"* was a single-migration-snapshot reading of the rebuild migration. Cluster B's migration 10 (`fix_rc7_admin_user_ops`) defined the function. **Mitigation that surfaced the correction: cumulative-forward read order.** Promoted as A-candidate #8 first shape.

2. **PW-2 retraction** (Clusters lib-1 + api-1). Cold-derivation classified PC-3's public HTTP API surface as "speculative third shape." Disk evidence at three convergent sites (client-direct RPC via `lib/hooks/usePermissions.ts`; absence of `v1/groups`/`v1/memberships`/`v1/roles`/`v1/permissions` routes; the 4 existing custom routes being service-role escalation escape hatches) settles the opposite: **PostgREST RPC is the canonical, realized PC-3 public HTTP API surface today.** Mitigation: framework-provided contract mechanism awareness; promoted as A-candidate #9 promotion-watch.

Both retractions are **INDEPENDENT shape-corrections, not coupled**. Together they make a methodology data point: cold derivation's intuitions about contract surface and capability state can be systematically wrong, and the corrections come from different reading practices (cumulative-forward vs framework-mechanism-checking). Comparison-phase should note.

---

## Load-bearing output

**PC-3 Organisation §L3 Step 2 block landed.** Spec at `docs/platform/core/organisation-specification.md`, commit `255219d` (59 insertions, single file). Three-class output (5 Class 1 confirms / 6 Class 2 entity-internal deltas / 6 Class 3 cross-entity findings) + nine sources-status amendments (SS-8 through SS-16).

**Tightenings applied at composition gate** (per bouncer ratification before Write):
- SS-10 phrasing on A-candidate #8 ratification timing
- C2-2 ADR-U018 disposition with two explicit Step-3-resolution options
- C3-1 Mode 4 conditional phrasing + sequencing sentence for the four failure modes
- C2-1 split into three sentences (sentinel-UUID + literal-NULL + canonical-pair recap) for clarity

**No ADR amendments this session.** ADR-U006 (FK direction + immutability + supervised-bypass), ADR-U007 (signature + Tier-1/Tier-2 composition + sentinel-UUID + literal-NULL calling conventions), and ADR-U018 (enum-via-CHECK entity-typing-vs-state disposition) all deferred to Step 3 session per capacity-planning disposition.

**§L3 Step 3 block + §3 / §5 / §6 / §7 / §8 / §L4 spec amendments** also deferred to Step 3.

---

## Step 2 findings density observation

PC-3 Step 2 produced substantively higher findings density than PC-1/PC-2 Step 2: **8 §8 questions advanced + 16 pre-findings + 2 A-candidates ratified/promoted + 2 side-tasks**. Schema density (PW-1 monolithic-rebuild mechanism) + D15 destructiveness finding cluster (P1 + P7 + P11) + X5 anti-pattern detail at full disk-anchor specificity together explain the magnitude. Capacity-planning data point: Step 3 workload at PC-3 will be heavier than PC-1/PC-2 Step 3; expected ~5 commits at Step 3 close (1 spec amendment combining §L3-Step-3 + §3/§5/§6/§7/§8/§L4 amendments per PC-2 precedent; 3 ADR amendments as separate commits per Experiment A discipline; 1 bridge).

---

## Step 2 findings summary (compressed; spec carries detail at commit `255219d`)

**5 Class 1 confirms.** §8 Q1 accept-seam pattern (5 functional-add redefinitions, no regressions per Step 2.2 git log); §8 Q7 account lifecycle reified (`admin_exit_user_from_platform` + `admin_hard_delete_user` + `admin_decommission_user` + D-R3 admin-assisted); §8 Q8 personal_group_id FK + immutability + supervised-bypass; D7 role-name vocabulary as TEXT; P-O1 actor primitive eager-loaded at AuthContext.

**6 Class 2 entity-internal deltas.** §8 Q5 split-by-context (`is_platform_admin` for RLS; `has_permission` Tier-1-only for RPCs); §8 Q3/Q4 P3 three hardcoded enum-via-CHECK (`group_type`, `display_preference`, `status`) with two-option ADR-U018 disposition lean; permission registry growable-by-design + baseline-stable since D15 (44 lib display vs ~31 ADR-U007 cited — mop-up pending); type-vs-disk drift in `lib/types/group.ts` by omission (`display_preference` aligned; `group_type`/`status` missing); two server-side admin Tier-1 calling conventions live simultaneously (sentinel-UUID + literal-NULL); `callerUserId` naming inconsistency at `lib/admin/admin-users-query.ts` boundary.

**6 Class 3 cross-entity findings (headline detail inline):**

- **C3-1 P1 four-mode fresh-DB deployability concern.** Mode 1 (loud, mig 2-4 against non-existent tables) + Mode 2 (loud, sprint1 RAISE on missing DeusEx) + Mode 3 (silent, `handle_new_user` FI Members fallthrough — most insidious) + Mode 4 (loud, fires earliest at runtime — empty permissions table → universal `has_permission` false). DevOps-tier Phase 2 close-out pickup; phrasing: *"Fresh-DB deploy from current active migrations produces a non-functional system unless seeded from pre-D15 archive migrations OR via undocumented manual seeding workflow."*

- **C3-2 X5 service-role anti-pattern at 5 sites / 6 createClient instances; two-tier centralization gap.** Sites: `lib/admin/admin-users-query.ts` (1) + `app/api/admin/users/route.ts` (1 + delegates to lib helper) + `app/api/invitations/send-email/route.ts` (1) + `app/api/v1/journeys/enrollments/route.ts` (1) + `app/api/v1/journeys/[id]/enroll/route.ts` (2 createClient instances). All identical config (`{ autoRefreshToken: false, persistSession: false }`); all raw `process.env.SUPABASE_SERVICE_ROLE_KEY!` consumption. **Centralization gap is two-tier**: **Gap A** — service-role client construction (no centralized helper in `lib/supabase/`); **Gap B** — JWT-verify + profile-resolve auth-flow (duplicated 5× even if Gap A closed). PC-1 Finding #4 channel framing shifts from "single centralization decision" to **two-tier centralization: substrate + auth-flow plumbing**. Three permissions gate X5 routes: `invite_members`, `enroll_group_in_journey`, `manage_all_groups`.

- **C3-3 PW-2 retracts; §7 design-rule amendment language drafted.** *"PostgREST RPC is the canonical PC-3 public HTTP API surface. Custom Next.js API routes (`app/api/*`) apply selectively where business logic exceeds RLS-expressibility — specifically: cross-table mutations the route must execute as a single transaction with non-RLS-derivable authorization; external API calls that require service-role privileges; multi-step transactions composing more than one PC-3 primitive. New PC-3 routes proposed for `app/api/*` must demonstrate one of these three justifications; default answer otherwise is 'expose via PostgREST RPC.'"*

- **C3-4 Q9 DeusEx vocabulary layer-bounded.** Present in schema-tier (function names + system group name + role name) + admin-helper-tier (`lib/admin/*` helper + comments + interface name `DeusexMember` with minor casing inconsistency). **Absent from API-route layer.** Containment supports resolution (b) grandfathering at Step 3.

- **C3-5 Rebuild migration crosses PC-1/PC-3/PC-4/DS-* boundaries.** 18 tables across all four Platform Core areas + Domain Services in one 2,223-line artifact. PC-1 Finding #3 (`admin_audit_log` → PC-4 ownership) confirmed at full disk anchor specificity.

- **C3-6 PC-2/PC-3 display-name coupling.** `nickname`/`display_preference`/`show_real_name` on PC-2 territory; sync trigger to PC-3 personal-group name; `handle_new_user` propagates. Routes to deferred §8 Q6 PC-2 amendment list.

---

## A-candidate ledger snapshot at session close

Program-level A-candidate ledger now spans #1-#9:

- **#1 Latent-vs-delta distinction** (promotion-watch). Carry-forward from PC-1.
- **#2 Tier-shape escalation channel** (promotion-watch). Carry-forward from PC-1.
- **#3 Database-shaped L2 framing assumption** (promotion-watch). Carry-forward from PC-1.
- **#4 Schema-predates-partition** (promotion-watch). PW-1 disk-anchored at full specificity this session (D15 monolithic rebuild); program-level-pattern promotion fully earned and expected at Phase 2 close-out.
- **#5 Multi-Edit gate emission discipline** (promotion-watch). Sub-batch-of-1 cadence held throughout PC-3 Step 2; no multi-Edit emission failures.
- **#6 Cold-derivation-with-priors as methodology variant** (promotion-watch). Three Experiment A priors (P-O1, D7, X3) actively held through PC-3 Step 1 cold derivation; verified against disk at Step 2 (P-O1 at AuthContext + multiple disk anchors; D7 at `role_templates` table + name-convention lookup; X3 at `has_permission` disk signature L419-L475). Methodology variable shared across both Experiment B runs (per Experiment A bridge); not differentiating between runs. Promotion-watch armed for PC-4 Step 1 cold derivation.
- **#7 Tool-payload verification distinction** (promotion-watch). Structural-inventory-before-defect-assertion mitigation held this session; no rendering false-positives observed.
- **#8 Single-migration-snapshot vs cumulative-forward read order** — **RATIFIED at first promotion-watch this session**; two-shape confirmation via §8 Q5 retraction + Step 2.2 committer-date evidence on D15 renumbering. Named-program-pattern promotion at Phase 2 close-out per existing watch schedule (requires recurrence at PC-4 or Phase 3 entity Step 2s).
- **#9 Framework-provided contract mechanisms invisible to cold derivation** (promotion-watch; new this session). Mitigation: at Step 1 cold derivation of contract surface, explicitly check for framework-provided contract mechanisms. Promotion criterion: pattern recurs at DS-* Step 1s.

---

## PW status at session close

- **PW-1 (schema-predates-partition) — promotion fully earned, disk-anchored at three layers.** D15 consolidation commit `ce58227` absorbs 71 migrations into one 2,223-line artifact; predates April-2026 four-area partition + ADR-U023 + ADR-U018; the 71 prior migrations are gone from disk. Distinct sub-shape recorded: *"partition postdates the SINGLE migration that defines all of it."* Promotion to named program-level pattern at Phase 2 close-out.
- **PW-2 (speculative as third shape) — RETRACTED at promotion-watch.** Three convergent disk confirmations that PostgREST RPC is the canonical, realized HTTP API surface. Methodology residue routes to A-candidate #9.

---

## OLDFEAT discovery + deferral decision

Mid-Step-2.6, Stefan surfaced that pre-refactor feature specs exist at `docs/TMP/OLDFEAT/` (outside the canonical `docs/platform/core/features/` directory). Step 2.6 Glob scope did not catch this directory; CC's "no FEAT-* anywhere" verdict overclaimed against the three actually-searched patterns.

**Decision: manual run does NOT reconcile against OLDFEAT in this session.** Rationale: preserve scope symmetry with the parallel autonomous run on `experiments/B-pc3-full` — the autonomous run's OLDFEAT-consumption status is unknown to the manual side per the bouncing-partner blindness invariant; reconciling on one side but not the other would compromise comparison-phase attribution.

OLDFEAT reconciliation is a post-Experiment-B work item, slotted for a future session after comparison-phase analysis closes. The cost (deferred reconciliation) is small against the comparison-phase attribution integrity gain.

---

## Methodology variants this session

- **Cluster batch-and-report cadence at Step 2.1** (vs PC-1/PC-2 single-pass migration reads). Cluster A + B + C + D + targeted mop-ups; each cluster reports findings before next opens; bouncing-partner ratifies cluster-by-cluster. Justified by PW-1 wanting per-migration evidence + Cluster A filename-anomaly + re-ordering value on early signal.
- **R-b `ctx_execute_file` structural extraction at rebuild migration**. Rebuild's 34K tokens exceeded direct Read 25K limit; sandbox-mediated structural extraction produced disk-anchor specificity without context flooding. Same tool-surface mediation as PC-3 Step 1's PC-2 spec read; methodology variable continues to be exercised across Steps 1 and 2 of this manual run.
- **Enumeration-claim-scoping methodology observation (SS-16).** Step 2.6 verdict overclaim self-corrected at bouncer adjudication. Mitigation discipline incorporated going forward: state patterns searched + scope-bounded verdicts ("no hits within [patterns]" vs "no hits anywhere"). Promotion criterion: similar overclaim recurs at PC-4 or DS-* Step 2 → A-candidate.

---

## Program-level carry-forward (Phase 2 close-out routing)

**P1 four-mode fresh-DB deployability concern** routes to DevOps-tier Phase 2 close-out pickup. Compounded with P11 (D15 archeology, asymmetric recovery — 2 system groups + permissions registry + role_templates + role_template_permissions not recovered) + P5 (PG17 RLS-vs-PLPGSQL substrate concern documented in migration 10 strategy comment) + P6 (session-variable bypass for admin orchestration; locked to SQL-side).

Sub-findings within the P1 + P11 + P5 + P6 cluster: **P8** (archive convention exists at `supabase/migrations/archive/`; pre-D15 migrations preserved there including DeusEx-rename history; methodology note: archive-as-retired vs deleted-via-consolidation is asymmetric on disk); **P9** (system-group seeding distributed across active migrations — `[Deleted User]` dedicated-seeded; FringeIsland Journeys feature-migration-seeded; DeusEx + FringeIsland Members NOT seeded in active set; routes to PC-3 §5 documentation pickup + DevOps-tier restoration migration adjudication at Phase 2 close-out).

**X5 anti-pattern** routes to PC-1 Finding #4 channel with new two-tier framing (Gap A substrate + Gap B auth-flow). PC-2/PC-4 amendment scope or new platform-tier infrastructure decision at Phase 2 close-out.

**Sub-question for Phase 2 close-out pickup (P1 mode-4):** what does `supabase db reset` actually report on a fresh-DB run against migrations 1-4? Stefan-side or supabase-local-dev test; not Step 2 work.

---

## Tripwires

| # | Description | Status |
|---|---|---|
| 4 | Disk-of-record verification before asserting | ACTIVE — held cleanly throughout Step 2.1 / 2.3 / 2.5 disk reads. **One self-corrected overclaim at Step 2.6**: enumeration-claim-scope failure (verdict generalized "no FEAT-* anywhere" beyond the three patterns actually searched). Surfaced by Stefan's OLDFEAT mention; methodology observation SS-16 captured. **Tripwire #4's domain may want extension** to "enumeration scope must match assertion scope" — adjacent failure mode to disk-of-record-verification proper. Bridge-level methodology-observation framing for Step 3 discipline. |
| 6 | Discipline-as-deferral | NOT FIRING — discipline-stack altitude held throughout. Bouncing-partner cycle ratifies cluster-by-cluster; sub-batch-of-1 multi-Edit cadence preserved at composition gate. Tripwire remains armed for Step 3. |

Tripwires #1, #2, #3, #5 — closed in prior bridges; not re-engaged this session.

---

## Repo state at session close

- Branch `main`. Three commits planned at session close: spec amendment commit `255219d` (landed first — bridge body cites this SHA); this bridge commit (pending); Step 3 session-opener commit (pending).
- Branch `experiments/B-pc3-full` exists locally in the autonomous-run worktree at `D:/WebDev/FringeIsland-experiment-B/`; state determined by autonomous-run CC; not visible from manual side.
- Pre-existing root `M CLAUDE.md` modification still unstaged (carried across sessions; outside experiment scope; not committed this session).
- **No push to origin** per Experiment B comparison-phase disposition. Comparison-phase analysis runs before push.
- After all three commits land, branch will be ahead of `origin/main` by 7 commits (was 4 at session-open, +3 this session).

---

## Discipline posture for Step 3 session

- Bouncing-partner cycle, surface-draft discipline, candidate ledger, tripwires armed.
- Discipline stack **on** for Step 3 adjudication + spec amendments + ADR amendments. L-level work continues.
- **Print-batch-before-gate** at L-level surface drafts (durable).
- **Sub-batch-of-1 multi-Edit cadence default** (durable). Step 3 will produce multi-Edit work across §3 / §5 / §6 / §7 / §8 / §L4 sections + 3 ADR files; sub-batch-of-1 holds.
- **Structural-inventory-before-defect-assertion** at rendering false-positive surfaces (durable from A-candidate #7).
- **Enumeration-claim-scoping discipline** (NEW from SS-16). For any future enumeration-based verdict: state the patterns searched + report scope as "no hits within [patterns]" rather than "no hits anywhere."
- State-read at session-open: read this bridge + verify against disk (Tripwire #4). Specifically verify spec commit `255219d` landed; §L3 Step 2 block present; §L3 Step 3 block + §3/§5/§6/§7/§8/§L4 amendments NOT yet written; no ADR amendments yet; PC-2 spec STILL un-amended (Experiment B comparison-phase integrity).
- No open adjudication carried into Step 3 session. PC-3 Step 3 entry is clear.

---

## Session-open prompt for Step 3 (embedded; operational artifact at `cc-pc3-step3-manual.md`)

PC-3 Organisation Step 3 (adjudication + spec amendments + ADR amendments). Same three-step shape as PC-1 / PC-2 (which ran Steps 1+2+3 in single sessions); this is PC-3's third-of-three split-session continuation following Step 1 (this session's predecessor) and Step 2 (this session). **PC-3 Organisation L1→L3 derivation completes at Step 3 close** (entity derivation finishes; PC-3 implementation via FEAT-PC-* feature specs + code is downstream and not in scope for Phase 2; Phase 2 remains open until PC-4 lands).

State-read pass + Tripwire #4 disk verification (this session's bridge, PC-3 Step 2 spec amendment commit `255219d`, PC-2 spec un-amended for Experiment B integrity).

Work scope:
- **§8 Q1–Q9 adjudication.** Q1 (accept-seam permanently; ADR-U016 cascade-spec entry as documentation hygiene); Q2 (4-part `has_permission` amendment); Q3/Q4 P3 (ADR-U018 disposition: narrowing-to-entity-typing OR stricter); Q5 (split-by-context confirmed); Q7 (lifecycle reified; routed to FEAT-PC-* spec); Q8 (3-part ADR-U006 amendment); Q9 (DeusEx layer-bounded; resolution (b) holds, grandfather); Q6 (PC-2 amendment carry-forward deferred to post-Experiment-B).
- **§L3 Step 3 block authoring** (adjudication outputs + pickup lists).
- **§3 / §5 / §6 / §7 / §8 / §L4 spec amendments** per Step 2 findings (PostgREST RPC contract surface; `is_platform_admin` + `has_permission` split-by-context; supervised-bypass discipline; pickup-list-driven Q amendments).
- **ADR amendments** as separate commits with explicit provenance: ADR-U006 (FK direction + immutability + supervised-bypass); ADR-U007 (signature + Tier-1/Tier-2 composition + sentinel-UUID + literal-NULL conventions); ADR-U018 (entity-typing-vs-state disposition).
- **Pickup lists** for PC-4 + DS-* + possibly PC-1 / PC-2 amendment-list candidates.

Active watches at Step 3:
- ADR amendment-shape decisions (separate commits with explicit provenance per Experiment A discipline).
- §7 amendment reusability for sibling Platform Core areas (does the design rule generalize?).
- OLDFEAT post-Experiment-B work item routing.
- Permission-count 44-vs-31 mop-up (Class 2 disposition).
- C3-2 Gap A + Gap B framing shapes PC-1 Finding #4 pickup-list phrasing (two-tier centralization is the new framing this session).
- A-candidate #9 interaction with §7 amendment (cross-reference design rule + methodology lesson).

Disciplines in effect at Step 3: standing methodologies (bouncing-partner cycle, surface-draft, candidate ledger, tripwires, sub-batch-of-1 multi-Edit, print-batch-before-gate at L-level Writes, structural-inventory-before-defect-assertion) + **enumeration-claim-scoping** (new from SS-16).

Blindness boundary: autonomous-run output on `experiments/B-pc3-full` REMAINS out of scope. Comparison-phase is separate future session.

Bridge filename at Step 3 close: `2026-MM-DD_NN_-_PC3-STEP3-LANDED.md` (filename pattern parallel to STEP1-LANDED + STEP2-LANDED); bridge body explicitly notes **PC-3 Organisation L1→L3 derivation completes at this commit**. Matches PC-1 / PC-2 bridge precedent on derivation-completion phrasing.

Commit shape at Step 3 close: 5 commits expected — (i) spec amendment (combined §L3-Step-3 + §3/§5/§6/§7/§8/§L4 amendments per PC-2 precedent); (ii-iv) three ADR amendments (U006 + U007 + U018) as separate commits; (v) bridge. No push.

Phase 2 close-out arrives after PC-4 lands. At that point: (a) revisit P1 four-mode + P11 archeology cluster with PC-4 evidence; (b) revisit PW-1 / A-candidate-#4-promotion; (c) decide on A-candidates #8 / #9 named-program-pattern promotion if recurrence at PC-4; (d) revisit X5 two-tier centralization decision; (e) fitness-check three-day turnaround at phase level; (f) ecosystem-decomposition skill update.
