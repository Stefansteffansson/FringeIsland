# Session bridge — Cycle G-C decomposed (FEAT-PC012 ↔ FEAT-H015: invitations & joining, MEM-1/2/3)

**Date:** 2026-07-04
**Session type:** Decompose session (`ecosystem-decomposition`, L4) — follows the G-B close (bridge `2026-07-04_04`).
**Status:** Specs `4-ready`, ready for the G-C build session.
**Participants:** Claude (autonomous; Stefan's decision board of 2026-07-03 already settled D3/D4 — no new decisions taken, four Open Qs routed to the schema gate).

---

## Substrate audit at decomposition (disk, 2026-07-04)

The invitation lifecycle is **RLS-table-driven with no contract layer**: `memberships_insert_invite` (`status='invited'`, `added_by_group_id` = actor, `invite_members`-gated), `memberships_update_accept` (self-only `invited→active`), decline = self-delete; `pending_email_invitations` (token UUID + 30-day expiry + `UNIQUE(group_id, invited_email)`, `invite_members`-gated RLS, **no UPDATE policy** — claim flips status only via SECURITY DEFINER); `handle_new_user()` **Step 8 auto-claims** at sign-up (case-insensitive, unexpired, multi-group); `auto_assign_member_role_on_accept` + the three `notify_invitation_*` triggers are Conformant. `UNIQUE(group_id, member_group_id)` makes duplicate-invite refusals structural. **No member-search primitive exists anywhere** (verified zero + independent cross-check; legacy searched via a users-RLS policy that lives only in `migrations/archive/`). `get_member_groups()` and `get_group_detail()` both filter `status='active'` — invited memberships are invisible to every existing read, so the invitee needs dedicated contracts.

**Consequence:** the platform half is contracts-over-proven-rules — **no new table, no policy changes**; RLS stays as defense-in-depth beneath ~9 new SECURITY DEFINER contracts (+ TRUNCATE hygiene on `pending_email_invitations` and `group_memberships`, neither covered by PC010/PC011).

## What was decomposed

- **[FEAT-PC012](../../platform/core/features/FEAT-PC012-group-invitation-and-joining-contracts.md)** (`4-ready`) — `search_invitable_members()` (**D3 executed**: name-partial + **exact**-email match, cap 8, `invite_members`-gated, no emails in the payload — tagged the **DS-6 re-home seam**; PC-3 §L3 deliberately carries no search capability); `invite_member()` (P0002 no-leak for anything not an invitable FIM personal group — Mist proto-groups never invitable); `invite_by_email()` (**D4 executed**: durable row + the existing signup auto-claim, **no dispatch** — V3 seam planted; existing-FIM emails **convert server-side** to membership invitations, else the row would strand — Open Q2); `get_group_invitations()` + two cancels (predicate-based expiry rendered honestly — **no reaper exists or is built**); `get_my_invitations()` + `accept_group_invitation()` / `decline_group_invitation()` (auto-claimed invitations answer identically). **Decomposition finding, closed in-spec:** FEAT-PC002's erasure cascade misses `pending_email_invitations` — PC012 STORY-6 extends erasure to invitations addressed to the erased email (Art. 17). Open Q1–Q4 for the gate: exact-only email search, existing-FIM conversion, `invite_members`-gated pending-list read, erasure-amendment shape.
- **[FEAT-H015](../../products/hub/features/FEAT-H015-group-invitations-and-joining.md)** (`4-ready`) — the invitations panel on `/groups/[id]` (typeahead + invite-by-email with **honest undispatched copy**, pending list with Expired badge + ConfirmModal cancels; panel gated on `invite_members` from the already-fetched my-permissions read — no new flag plumbing); **my-invitations on `/groups`** (accept/decline; invitation context only, never private-group detail); the email-invited newcomer's arrival arc (sign-up → auto-claimed invitation appears → accept) as an E2E story; ~7 BFF handlers (reads Edge+`dub1`); id-only telemetry — **emails and search queries never in events**. E2E specs use dedicated spec-created FIMs (the G-B suite-isolation finding, adopted as default).
- **§L4 reconciliation in the same batch:** organisation-specification (summary refreshed to three PC-3 specs; Membership lifecycle row extended; **Pending-invitation claim row added** — contracts around the claim, the `handle_new_user` trigger untouched; without-specs list updated), hub SPECIFICATION (H015 row + coverage note; stale H013/H014 "4-ready" parentheticals corrected to 6-done), both feature indexes, Groups plan G-C row.

## Next session (build, `feature-development`)

1. TASK files for PC012 (D6: task files for every 4-ready feature) → red-first integration (invitation matrices, both births, the claim arc, erasure extension, adversarial direct paths) → one migration (~9 functions + erasure amendment + TRUNCATE revokes + grants) → **schema gate: `review`, pause for Stefan's nod** (rules on Open Q1–Q4 + the direct-caller question, incl. the direct email-INSERT residue that bypasses the conversion).
2. Then H015: TASK files, red-first route-units → panel/section units → E2E (the invitation arc across two FIMs; the email-invited newcomer sign-up arc; decline; cancel) → `next build` gate → 6-done paperwork.
3. Standing: G-36/IDN-10 parked specs by next cooldown; org-spec §5 seeding-sites doc-health finding queued; IDN-12 + perf T2 parked; P3b/P4/P1-residual parked; group-as-actor design session at the G-E → G-F boundary; `test:integration:rbac` legacy-script cleanup at cooldown.
