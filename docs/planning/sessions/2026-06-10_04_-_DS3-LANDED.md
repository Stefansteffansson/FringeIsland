# Session bridge: 2026-06-10 (4) — DS-3 Journeys L1->L3 derivation complete

**Filename convention:** `YYYY-MM-DD_NN_-_TOPIC.md`
**Date:** 2026-06-10 (fourth bridge of 2026-06-10)
**Session type:** Autonomous L1->L3 run, single-session (Steps 1+2+3). **Third instance of the autonomous L1->L3 template, first on the revised template** (`1de43ad`).
**Session opener:** [`openers/ds3-experience-engine-descent-opener.md`](./openers/ds3-experience-engine-descent-opener.md) (authored at `9b96c8c` — in the SAME session that executed it: the descent was ordered with no opener on disk; the deviation was surfaced and resolved by running the README's Step 1 first, collapsing the two-step author-then-execute workflow).
**Chronological + substantive predecessor:** [`2026-06-10_03_-_DS2-LANDED.md`](./2026-06-10_03_-_DS2-LANDED.md).

---

## DS-3 Journeys L1->L3 derivation completes at this commit batch

*(The entity descended as "DS-3 Experience Engine" and lands as **DS-3 Journeys** — the FIRST DECISION below.)* Spec at `docs/platform/domain/journeys.md` (commit `60df6ca`): L2 identity + boundaries + contract families + eight §7 service-level invariants + §8 eight open questions; §L3 fifteen-capability inventory across six internal areas (Journey structure / Authoring / Enrolment / Progress & delivery / Loop & respawn delivery / Personalisation) + dependency chain + external dependencies + Sources-status + Step 2 + Step 3 blocks. Entity CLAUDE.md at `journeys/CLAUDE.md` (commit `a827124`). Template revisions adjudicated and landed (commit `a08ab70`). This bridge + STATUS.md update close the batch. **No push to origin** — Stefan dispositions push.

## Session arc

The session began with a pipeline deviation: no DS-3 opener existed (STATUS.md row `Next`, opener column `—`; DS-2's close never triggered README Step 1). Surfaced, then resolved by authoring the opener in-session (`9b96c8c` + STATUS.md `2dfbcc3`) and executing it from Section 1. All five pre-flight checks passed; Check 4 fired its registered re-occurrence shape (root `CLAUDE.md` context-mode block) — notably, `git checkout -- CLAUDE.md` was PERMITTED this time (the DS-2 classifier denial did not recur). State-read consumed all items + four disk verifications; two binding ADRs missing from the opener's enumeration (ADR-U017, ADR-U020) were caught and added to the authority chain mid-state-read. FIRST DECISION ratified before any identity text. Step 1 cold derivation, checkpoint, single Write (held uncommitted). Step 2: the program's first non-zero-delta stress-test, run through sandboxed sweeps. Step 3: 7 fold-back Edits at sub-batch-of-1, the sanctioned cross-entity amendments, and the rename ripple (one scoped sed batch for 28 mechanical files + prose-aware Edits, DS-2 precedent).

## Ratified decisions (all by Stefan, 2026-06-10, this session)

1. **FIRST DECISION — DS-3 lands as Journeys** (`journeys.md`, slug `journeys`), the PENDING.md candidate; the Engine-suffix half was pre-decided at DS-2 and was not reopened. PENDING.md entry appended as fully resolved; ripple executed via sweep-then-enumerate (floor 7 sites, actual 28 living files; historical/append-only files untouched).
2. **DS-2 §8 Q1 resolved:** the universal calendar stays DS-2's; adaptive per-FIM pacing is DS-3's (Adaptive per-FIM pacing capability). `narrative.md` amendment landed (sanctioned cross-entity edit).
3. **DS-2 §8 Q2 resolved:** loop runtime state is DS-3's, consuming DS-2 declarations; the personal-data weight + ADR-U027 Shadow-loop ephemerality obligations land in DS-3; DS-2 stays personal-state-free. `narrative.md` amendment landed.
4. **Respawn three-way split confirmed from the DS-3 side** (DS-1 position / DS-2 structure / DS-3 delivery); neither sibling's text revised.
5. **Step 1 candidate ratified as surfaced** (15 capabilities / 6 areas / 8 questions / 8 invariants); **single-session shape with split fallback** chosen; **Step 2 findings + Step 3 scope ratified** at the Step 2 gate; **registry-row removal via sed explicitly approved** after a classifier denial (see Methodology).

## Forward-commitment classification — the program's first mixed profile

**Six capabilities partially realized:** Journey registry & route types; Step registry & step-kind system; Enrolment lifecycle (richest realization — RLS, lifecycle states, cascade semantics); Progress tracking (per-enrolment grain only); Delivery runtime; Journey authoring write-path (schema-side only). **Nine full forward-commitment:** Equipment-requirement declaration; Depth-setting registry; Season/episode attachment; Shadow enrolment & transcendence continuity; Loop runtime state; Respawn delivery; Loop-persistence application; Signature personalisation application; Adaptive per-FIM pacing. Wave-scope determination is wave-planning's, not L3's.

## Pickup lists

**DS-4 Content (next entity; its opener must inherit):**
- DS-2 §8 Q8 (beat-vs-content-block boundary) + Q3 (mythology three-way, joint with DS-1 Q5) — both routed to the DS-4 descent by DS-2's bridge; DS-3 adds **journeys.md §8 Q7** (step→content-block vs step→beat reference direction; cold lean: composition at delivery, no step→beat references). The three resolve together.
- DS-3's steps reference DS-4 blocks opaquely by ID (the standing reference-direction pattern) — DS-4's descent re-checks this boundary per the sibling-provisional rule.
- The expected-placeholder registry row for `content/CLAUDE.md` is in `.claude/skills/doc-health-check/SKILL.md` Section 7 — note the classifier-denial fallback below if removal is needed in-session.
- Opener-authoring notes: enumerate binding ADRs by sweeping the decisions index for the entity's domain nouns (DS-3's opener missed U017/U020); the empty-result verification watch (below).

**DS-7 Intelligence:** the AI-Generative route seam (journeys.md §8 Q3 — where Wayfinder authorship ends and generation intelligence begins); the personalisation input seam (DS-7 feeds through DS-3's contract, salience-channel direction pattern); the Whisp-split ADR promotion still fires at DS-7's descent (PENDING.md).

**Journey Studio (studio decomposition):** Journey Studio is DS-3's write-path (Wayfinder-gated; mandatory equipment declaration at authoring per ADR-U025); Guide/Steward in-journey authority (journeys.md §8 Q8) and group-loop visibility (Q6) shape its management surface.

**Step-type specification session (ADR-U008-mandated, pre-implementation):** now carries THREE vocabularies — ADR-U008 Tier-1 types, the narrative core's six content families, and the realized TS `StepType` three-value union (journeys.md §8 Q1). The session is the named resolution venue and precedes significant step-system implementation.

**PC-4 / Phase 3 close-out channel:** first DS-side anchors for the cross-tier-write substrate question — PC-4/PC-3-tier lifecycle functions directly UPDATE `journeys`/`journey_enrollments` (`20260223171200` admin reassignment; sprint2/3/4 freeze + DeusEx transfer). Publish-primitives-vs-direct-write remains the standing question.

**PC-1 Finding #4 channel:** the enroll route (`app/api/v1/journeys/[id]/enroll/route.ts`) open-codes service-role construction (Gap A) + JWT-verify/profile-resolve plumbing (Gap B) — a clean X5 instance; migrates when two-tier centralization lands.

**Code correction target (set aside 2026-06-07):** registry-ization of the realized CHECK-listed kind vocabularies (`journey_type`, `difficulty_level`) and sealed TS unions; the `journey_type` pre-canon→canon route-type vocabulary migration (Hybrid unrepresented).

**First-experience canon work:** journeys.md §8 Q2 (Shadow enrolment posture) is joint with the unwritten `first-experience.md` — the canon's named highest-risk gap now has a platform-side question waiting on it.

**Doc-health channel:** cascade additions this session: `journeys/CLAUDE.md` created, registry row removed (`a827124`); the rename label-edited two SVGs (registry rows unaffected); no new assertion-bearing diagrams.

## A-candidate ledger snapshot at DS-3 close

- **A#1, A#2, A#3, A#6, A#7** — carry forward; A#1 (latent-vs-delta) got its first real exercise at a code-rich DS and held as a framing (realized substrate read as latent capability floor, not delta).
- **Promoted five (A#5, A#8, A#9, PW-1, P-O1)** — all applied and all fired: A#8 cumulative-forward traced drop→rebuild→sprints; A#9 confirmed with the custom-route exception cleanly justified; P-O1 + D7 found live in RLS; A#5 per-phase (single-Write Step 1; 7 sub-batch-of-1 fold-back Edits; one sanctioned sed batch); PW-1 disk-anchored at the D15 lineage.
- **Retraction-rate series:** PC-4: 7/9 clusters; DS-1: zero; DS-2: zero; **DS-3: zero at the first code-rich entity.** Refinement: with strong carry-forward priors, code presence produced five Class 2 *deltas*, not retractions — retraction rate may track *prior coverage*, not code presence alone. The hypothesis needs a code-rich entity with weak priors to separate the variables; none remains in Phase 3 (DS-4..DS-7 all inherit rich priors), so the series likely settles at Phase 3 close-out.
- **Sweep-then-enumerate:** n=2, fired decisively, LANDED as template text (`a08ab70`).
- **New watches (n=1, riding):** *empty-result verification* — a silently-failing `find` claimed zero API routes; the term-sweep caught two; verify empty listings by a second method before claiming absence (extends the §6 listing-commands discipline). *ADR-enumeration-by-grep* at opener authoring.

## PW status at DS-3 close

PW-1: **fired and disk-anchored** (journeys/enrollments born in the D15 rebuild, predating the partition — temporal, not architectural; no ADR amendment reached for, per the discipline). PW-MARCH1: **fired in recovered form** — the 8 predefined journeys lost at D15 and re-seeded at sprint1 with the loss documented in-migration; first instance with a clean in-window recovery record. PW-2: Q1 tagged speculative-third-shape and PARTIALLY resolved by Step 2 (the realized third vocabulary narrowed the shape space) — at a code-rich entity the resolution channel does shift back toward Step 2, confirming the DS-2 observation's flip side. PW-5: nothing new (the 19-table baseline held). PW-T1: type-vs-runtime coverage symmetric for DS-3 (journey.ts mirrors the realized schema; no drift found within the patterns searched).

## Methodology data points (§13 capture)

**Third-instance framing — the `1de43ad` revisions at their first as-template-text test: all eight held cleanly.** Cluster-cadence text (composed between clusters, surfaced once); directory scope-survey (ran before deep reads); A#5 per-phase; retraction-rate line (data point recorded); §6 entity-type-agnostic note (held at the first code-rich DS); cores-in-authority-chain (held, with a wrinkle: the register row's "signature-vs-charter" term traced past the cores to universe-discovery S19 directly — the cores don't carry it yet; canon-sub-page gaps push derivation to the discovery log, which the authority chain already sanctions); Check 5 (exact baseline); Check 4 genericization (fired its named shape exactly; disposed; the DS-2 `git checkout` denial did NOT recur — classifier behaviour is non-deterministic across sessions, which strengthens the "name the fallback pattern, not the command" posture).

**Riding-candidate verdicts:** #4 migration-name-as-shorthand — still no decisive firing (n=3 opportunities); rides. #5 three-casing dual-reading — **fired twice** (`journey_type` dual reading; the "Experience engine" SVG case-variant that neither sweep pattern matched); LANDED. Sweep-then-enumerate — fired decisively (floor 7 vs actual 28); LANDED. Commit-cadence clarification — held clean (no DS-2 tension); LANDED. Filename convention — second deliberate use; LANDED.

**Prompt 1 — worked well:** the FIRST-DECISION-before-identity-section pattern at its second use (rename cost again zero — the spec was born under the ratified name); the stated-expectation Step 2 framing, which at a code-rich entity correctly predicted *that* substrate existed while leaving *what shape* to the evidence; sandboxed sweep execution for Step 2 clusters (kept raw migration/code content out of session context; only classified summaries surfaced — a context-economy pattern worth naming for code-rich entities).

**Prompt 2 — redundant/confusing:** nothing in the template misfired. Two environment frictions: the classifier denied the doc-health registry-row Edit as self-modification (resolved by explicit in-session ratification + scoped sed; openers should anticipate that ANY denial class can appear at novel sites and name the generic fallback: surface, ask, use the narrowest sanctioned alternative); the silently-failing `find` (see new watch).

**Prompt 3 — wished was named:** empty-result verification (now a riding watch); ADR-enumeration-by-grep at opener authoring (the opener's ADR list missed U017/U020 — caught at state-read, but an authoring-time grep of the decisions index for the entity's domain nouns would have caught it earlier).

**Prompt 4 — new substance priors for DS-4:** the three-question content-boundary cluster (DS-2 Q8 + Q3/DS-1 Q5 + DS-3 Q7) resolves jointly at DS-4's descent; the opaque-reference direction pattern now has three precedents (DS-1 assets, DS-2 blocks, DS-3 blocks); DS-4 should expect a NON-zero Step 2 only if content/media substrate exists — the 19-table baseline says no DS-4 tables, so zero-delta is the stated expectation there.

**Prompt 5 — new A-candidate/PW observations:** retraction-rate-may-track-prior-coverage (refines the series; see ledger); PW-2 resolution-channel flip-back at code-rich entities; the deltas-not-retractions shape as the signature of well-carried priors.

**Also captured:** the opener-missing pipeline deviation and its in-session resolution (author-then-execute collapsed into one session cleanly; the README's two-step shape remains the documented default — whether to canonise the collapsed shape is a pipeline decision, not a template one); the session ran its Step 2 sweeps through sandboxed execution as a deliberate context-economy choice at the first code-rich entity.

## Template revision disposition

**REVISIONS LANDED at commit `a08ab70`:** §5b naming-drift dual-reading (PC-4 #5, two-instance evidence); §5a rename-bearing sweep-then-enumerate (n=2); §10 single-session commit cadence (n=2); header instance-filename convention (n=2). **RIDING:** #4 migration-name-as-shorthand (no decisive firing at n=3); new n=1 watches: empty-result verification, ADR-enumeration-by-grep. Revision history table updated in the same commit.

## Carry-forward confirmations

CODE stayed a correction target (Step 2 read it as evidence only; the closure-pattern and vocabulary findings route to the correction target, not to in-session code edits). ASCII-only labels held. The 2026-04 Hub L3 working set was not used. OLDFEAT: not listed, not read. Sessions append-only (historical bridges, ADRs, V4/V1 references, and the discovery log were excluded from the rename ripple). The Whisp split was consumed, never reopened. Zero ADR amendments (the ADR-U018 narrowing already covered the discriminator-column question; PENDING.md carries the rename disposition).

## Repo state at session close

Branch `main`. Commits this session: `9b96c8c` (opener) + `2dfbcc3` (STATUS In-flight) + `60df6ca` (spec + seam amendments + PENDING.md + rename ripple, 31 files) + `a827124` (entity CLAUDE.md + registry row) + `a08ab70` (template revisions) + this bridge + STATUS.md close. Tree otherwise clean (the pre-flight CLAUDE.md injection was discarded per the registered disposition). **No push to origin.**

---

*DS-3 Journeys L1->L3 derivation completes at this commit batch.*
