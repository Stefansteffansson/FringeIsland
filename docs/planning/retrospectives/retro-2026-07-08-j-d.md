# Retrospective — Cycle — 2026-07-08 (J-D)

**Scope:** Cycle (Cycle J-D — group progress & frozen mode: FEAT-PD005 + FEAT-H022 — plus the same-day post-close stale-toggle fix, PR #146)
**Period covered:** 2026-07-08 (J-D decomposed, bridge [`_02`](../sessions/2026-07-08_02_-_CYCLE-J-D-BUILT-PD005-H022-6-DONE-FROZEN-SHARING-GROUP-PROGRESS.md)) → 2026-07-08 (J-D closed + the fix; retro committed 2026-07-08 at the J-E boundary)
**Facilitator:** Claude
**Participants:** Stefan + Claude

> Sixth committed cycle retro (previous: [`retro-2026-07-08-j-c.md`](retro-2026-07-08-j-c.md), Cycle J-C — the third retro carrying this date; suffixes disambiguate per the J-C precedent). Per the backlog L5 lifecycle, the five J-D task files (TASK-JD-01..05, all `done`) are deleted when this retro commits — this document, bridge `_02` (+ its fix addendum), the completion plan's cycle rows, and git history (PRs #141–#146) are the permanent record.

---

## 1. Three Ls

### Liked

- **The payload-walk discipline caught the cycle's structural tension before build, again (n=3):** Q9 — two of the four freeze reasons end the very membership that grounds read standing, so JRN-14's walks would have been concealed from their own travellers. Caught at derivation, decided at the gate, shipped as lived-record read standing.
- **The gate board scaled to nine questions and absorbed build-time evidence without re-litigation:** Q2 arrived assuming a new permission and left revised by the live catalog (the pair already seeded + template-wired, 97 instantiated grants — found by the seeds-directory discipline overturning a dossier claim before the gate); Q3 was refined to the ADR-U034 Amendment-1 conventions the dossier had missed. Both revisions rode the same nod.
- **The bound completion-report protocol had its first successes:** the Hub and E2E sessions reported immediately, unprompted (2/3 — the red-suite session still idled twice; the lead-pings-on-idle half carried it).
- **Stefan's felt-path walk caught a real bug within the hour of 6-done — third area running.** The stale sharing checkbox survived 656 unit tests, a green E2E arc, and every gate, because all of them revisited via full page loads.

### Learned

- **The payload walk has a blind spot the rider exposed (its first miss after three catches):** the walk covered the cycle's own payloads but not the *other* feature's payload the new surface consumes — the Progress panel keyed on `enrollment_id`, which `get_group_enrollment_summary` (FEAT-PD002) never served. One additive-key rider, separately nodded, but the class is real: **the walk must cover every payload the surface renders from, not just the cycle's own.**
- **A full-load revisit is not a client-side revisit.** The E2E arc's revisit assertions used `page.goto` — a fresh JS context with an empty module cache — and passed while Stefan's Link-navigation repro failed. History pops in one JS context are the honest revisit; the arc now carries the exact repro.
- **The "page owns optimistic state" doctrine over-applied:** sharing is server-confirmed state, not optimistic progress — its mutation must write the confirmed response through to the session cache (the completion-moment precedent generalizes to every mutation of confirmed client state).
- **The dossier single-snapshot trap recurred at delegation:** the substrate dossier's consent section read the original PC002 migration and missed Amendment 1 (the `decision` column + `consent_purposes` catalog). A#8 cumulative-forward applies to *dossier prompts*, not just first-party reads — caught at the lead's convention check before authoring.
- **Role instances take template names** ('Steward Role Template', not 'Steward') — the red suite's name-matching lookup found nothing; the fix mirrors `create_engagement_group`'s own permission-derived binding. A tier-gotcha candidate at next touch.
- **The auto-mode permission classifier blocked the *nodded* migration apply** (`apply-migration-temp.js`, chained and standalone) — resolved via the sanctioned Supabase MCP path plus the standard history repair, identical end state, surfaced to Stefan in-session.

### Lacked

- **A client-side-navigation tier in the E2E vocabulary** — every revisit in every arc was a full load until PR #146 (→ §4).
- **A permission allowlist for the house apply script** — friction at the gate's most critical moment (→ §5 action).

## 2. Metrics

- **Throughput:** 2 features (FEAT-PD005, FEAT-H022) taken 4-ready → 6-done in one session, plus the same-day stale-toggle fix; 5 tasks; 2 migrations (`20260708150000` + the rider `20260708190000` — **no new tables/columns/RLS across both**: one consent-catalog row, a reads-only standing helper, three contract re-issues, two new contracts); the consent-gated group-progress read with invariants 4+8 enforced in the contract; the per-enrolment sharing consent (append-only, latest-wins); the additive `freeze`/`progress_sharing` player blocks; Q9 lived-record read standing; the bridge-`_14` freeze re-verification discharged by demonstration; Q8 recorded working-as-intended; IDN-10/G-36 hook #4 advanced on evidence.
- **Cycle time:** one session (boundary ritual → decomposition → 6-done → fix). PRs #141–#146.
- **Final gates:** integration 27/27 new (15 red verified / 12 pins) → slice 115/115 → full sweep **419/419** (38 suites, no flake) · unit 611 → 656 at close → **660/660** post-fix (88 suites) · E2E `frozen-and-group-progress` incl. the client-side revisit repro + all four journeys/player specs **6/6 lead-verified** + clean isolated re-runs · `next build` green ×3 · lint 0 · suppressions 4/5 throughout.
- **Adaptations:** one labelled in-suite (roster uniqueness re-keyed to `member_group_id` — display names collide legitimately); one labelled paired-suite (H020/H021 frozen assertions superseded by the new posture, unit-only); zero integration-sibling adaptations.
- **Spillover:** none. Two walkthrough-routed edges parked deliberately (§3).
- **DoR / DoD compliance:** DoR held (both halves 4-ready, payload-walked — with the miss noted above); the schema gate honoured twice (the Q1–Q9 board; the rider's separate nod — held un-applied both times until Stefan's word); DoD held including the ADR-U043 performance rows and honest Implementation notes (Q-board ratified with build revisions inline; deviations labelled).

## 3. Decisions taken in this retro

- **Task-file sweep:** the five TASK-JD files are deleted in this retro's commit.
- **The two walkthrough edges are parked explicitly, not decided:** *thaw-on-rejoin* (no unfreeze path exists — right for closed/archived, an unmade choice for leave/removal+rejoin) and *sharing-survives-rejoin* (the old consent is still the latest decision) sit on the **J-O3 area-gate look list** with the J-E boundary as the latest decision point — never silently dropped.
- **Flake watch adds** `journeys.spec` JRN-1 (cold-route-compile vs a 5s visibility timeout; once, non-reproducible).
- **The report-idle protocol stands as-is** (2/3 first-time success; the ping half carries the remainder; tooling still not earned).

## 4. Process changes

- **New J-D lines (interim binding until homed):**
  1. **The decomposition payload walk covers every payload the surface renders from** — including *other* features' payloads it consumes, not just the cycle's own (the rider lesson).
  2. **E2E revisit assertions navigate client-side in one JS context** — a full-load revisit resets module state and masks cache staleness (the PR #146 lesson).
  3. **Mutations of server-confirmed client state write the confirmed response through to their session cache** — the completion-moment precedent, generalized; "the page owns optimistic state" is scoped to genuinely optimistic progress.
  4. **Delegated dossier prompts instruct cumulative-forward reads** (A#8 extended to delegation — a dossier's single-snapshot answer is a false floor).
- **Carried unhomed:** the eight lines from the J-C retro. With the four above, the homing debt is **twelve lines** — a dedicated skill-homing session is now overdue; recommended slot: immediately after the J-O6 design session, before J-E decomposition (it pauses for the nod — steering-file edits).

## 5. Action items / standing list

- [x] Sweep TASK-JD-01..05 — owner: Claude (this commit).
- [x] Doc-health-check at this boundary — findings below — owner: Claude.
- [ ] **The skill-homing session (twelve §4 lines)** — recommended before J-E decomposition — owner: Claude (pauses for Stefan's nod).
- [ ] **A settings allowlist rule for `scripts/apply-migration-temp.js`** so the classifier cannot block a nodded apply — owner: Stefan + Claude, next session.
- [ ] J-O6 design session at this boundary — owner: Stefan + Claude (next in queue).
- [ ] FEAT-H010 step-instances export section at next touch (carried).
- **Carried unchanged:** the J-O3 area-gate look list (now + thaw-on-rejoin, sharing-survives-rejoin, the copy drafts, the frozen toggle suppression, the legacy-completed review edge) · flake watch (+ JRN-1 per §3) · Stefan's dashboard toggles · test hygiene · platform-core CHANGELOG disposition · MEM-9 → Communication gate · IDN-12 + perf T2 parked · ADR-U040 referral rebuild unscoped · logo pick · launch checklist · the doc-health soft-flag set as re-confirmed in §6.

## 6. Doc-health check (cycle boundary, J-D→J-E)

Cycle-boundary doc-health for J-D→J-E is **clean** — no hard fails, no critical findings (delegated audit, lead-reviewed; the audit session reported immediately, unprompted — the protocol's third success in four delegations). The J-D change set verifies consistent end-to-end: both specs `6-done` with full Implementation notes (H022 including the PR #146 post-6-done fix note); feature README indexes match disk (hub 22/22, domain 5/5); both §L4 table rows consistent; both migrations verified as function/grant work plus one idempotent consent-catalog row — zero table/column/RLS/trigger/index DDL, matching the Implementation-notes claim; the completion plan (v6, J-D CLOSED, J-O6 slotted) internally consistent with all 26 links resolving; bridges, retros, and indexes resolve everywhere; no retired-concept leakage introduced.

Findings this boundary: **(a)** one new soft flag, **found-and-fixed in this commit**: the `journeys.md` §L4 "Capabilities without specs" prose still called FEAT-PD005's slices "4-ready" after the same-file table row went `6-done` — a prose lag at the maturity transition (the table was correct). **(b)** Awareness-only: PR-range narratives differ by intent (the plan's J-D line cites the cycle arc #143–#145; the journeys row cites the feature-bearing #143–#144) — both accurate for what they describe, not harmonized. **(c)** Carried-flag correction: the `capability-foundation.md` note names a file that no longer exists — the unbannered Three-Worlds restatement actually lives in the reference snapshot `hub-l3-input.md` (top-level non-authority disclaimer present, no cosmology-specific banner; low severity) — the carried flag is re-pointed accordingly. **(d)** One aside for a future ADR touch: ADR-U023 still calls the V4 SVG "(current visual)" — a stale-at-write descriptor, noted beside the existing V4-SVG sweep target. The remaining carried flags stand unchanged (world-studio load-order format; hub §L3 MEM-2's ADR-U040 pointer; old-cosmology/AI-Mentor residuals confined to legitimate homes; `journeys.md` §L3 pre-PD003 JSONB prose — working-as-designed under the L3-snapshot/L4-current convention; the V4-SVG unbannered-superseded sweep target; the FEAT-H021 chronology note, now shifted to :19/:29/:31 by the addendum and internally consistent).
