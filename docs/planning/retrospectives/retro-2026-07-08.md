# Retrospective — Cycle — 2026-07-08

**Scope:** Cycle (Cycle J-B — the player core: FEAT-PD003 + FEAT-H020 — plus the same-day Q1 addendum that re-grounded withdraw→re-enrol continuity)
**Period covered:** 2026-07-07 (J-B decomposed, bridge `_04` §2) → 2026-07-07 (J-B closed + Q1 addendum, bridge `_04` + addendum; retro committed 2026-07-08 at the J-C boundary)
**Facilitator:** Claude
**Participants:** Stefan + Claude

> Fourth committed cycle retro (previous: [`retro-2026-07-07.md`](retro-2026-07-07.md), Cycle J-A). Per the backlog L5 lifecycle, the six J-B task files (TASK-JB-01..06, all `done`) are deleted when this retro commits — this document, the session bridge ([`_04`](../sessions/2026-07-07_04_-_CYCLE-J-B-BUILT-PD003-H020-6-DONE-PLAYER-LIVE.md)), the completion plan's cycle rows ([`phase-3-journeys-completion-plan.md`](../hub-v2/phase-3-journeys-completion-plan.md)), and git history (PRs #126–#131) are the permanent record.

---

## 1. Three Ls

### Liked

- **The delegated-build shape worked at its first real scale.** Five sub-agent sessions (red suite, three Hub blocks, E2E) with the lead verifying every claim first-hand — re-running suites, re-checking counts — before committing. The heaviest cycle of the area landed in one day without a quality incident traceable to delegation.
- **The J-A payload-walk lesson paid out immediately:** both J-B specs were decomposed with the consuming stories walked against the proposed `get_player_state` payload (first application of the J-A retro §4 process change) — and unlike J-A, the Hub build needed **zero** contract amendments.
- **The gate ritual scaled to seven questions.** Q1–Q7 arrived with the dev pre-check evidence attached (live seed shape, legacy-enrolment count) and got "yes to all — apply and continue" in one pass; the conversion function returned exactly 47 with parity guards silent.
- **`next build` fired at the last gate exactly as designed** — it caught a real type-seam defect (`PlayerStep` re-export) that 578 unit tests could not see. Standing confirmation of the "next build is the type gate" rule.

### Learned

- **A plain-English walkthrough of "what did we build" is a review tier of its own.** Stefan's read-through of the merged bridge caught that withdraw→re-enrol *restarted* the journey — surviving step-instances were keyed to the old enrolment row. 65 integration tests, the E2E arc, and the seven-question gate board all passed without asking the cross-enrolment continuity question. The Q1 addendum (PR #131, gate-nodded: re-enrolment reactivates the most recent withdrawn row; instances carry; `enrolled_at` preserved, `enrolled_by` = reactivator) fixed it same-day, red-first.
- **A nodded schema-semantic change ripples into sibling suites by design.** The Q1 terminal-`withdrawn` decision invalidated four J-A assertions (row-deletion withdraw, JSONB step_count, legacy kind strings) plus two suite bugs surfaced (one of them the P0001 gating *working*). All eight post-apply failures diagnosed test-side; adaptations labelled, none weakened. Budget the paired-suite adaptation into any gate PR that changes shipped semantics.
- **Sub-agent delegation has two named frictions:** agents idling without sending their final report (a ping recovered it, 3/5 times), and one agent initially fixturing from the wrong seed file (`seeds/05` vs the live sprint1 set — a divergence the dossier had explicitly flagged). Verification-at-the-join caught both.
- **Dev-DB 522s under long sweeps are a named flake class now:** a Supabase-side Cloudflare 522 during fixture creation broke one sweep run (green 24/24 isolated). supabase.co fronts all hosted traffic with Cloudflare; a 522 is their origin timing out, not our code (explanation in-thread on PR #128 after Stefan challenged it).

### Lacked

- **A formal "what did we build, in plain English" tier at cycle close.** The continuity gap survived every existing gate because no gate asks the narrative question; Stefan's ad-hoc read-through is what caught it (→ §4).
- **A reliable completion signal from sub-agent sessions** — the report-idle friction cost three pings; worth watching before it earns tooling.

## 2. Metrics

- **Throughput:** 2 features (FEAT-PD003, FEAT-H020) taken 4-ready → 6-done in one day, plus the same-day Q1 addendum; 6 tasks; 2 migrations (`20260707190000` substrate + contracts, `20260707213500` re-enrol reactivation); the ADR-U044 substrate realized (`content_families` ×6, `step_kinds` ×7 presets, `journey_steps`, `journey_step_instances` with the open-instance partial unique index); 47 legacy steps converted (parity-guarded, reusable, wired into `seeds/05`); 3 player contracts (`get_player_state`, `enter_journey_step`, `complete_journey_step`); 3 conformant BFF routes; the player page (`/journeys/[id]/play`) with one-read boot, resume, optimistic advance (B5) + background auto-save with rollback/retry, `StepRail`, `PlayerSkeleton`, and the JRN-18 renderer registry (7 kinds + mandatory fallback).
- **Cycle time:** 1 day (decomposition → 6-done → addendum). PRs #126–#131.
- **Final gates:** integration 61/61 → **65/65** post-addendum · unit 578/578 (81 suites) · E2E player arc 1/1 + `journeys.spec` 2/2, withdraw→re-enrol→resume green, clean isolated re-runs · full sweep 364/365 (the documented Cloudflare-522 fixture flake, green isolated) · `next build` green (after catching the `PlayerStep` seam) · lint 0 errors.
- **Spillover:** none in-cycle. Standing list carries (§5).
- **DoR / DoD compliance:** DoR held — both halves `4-ready` with task files and the payload walk done at decomposition; the schema gate honoured twice (Q1–Q7 board; the addendum's own nod). DoD held including the ADR-U043 Performance rows (player is the area's B5-critical surface: optimistic advance + skeleton-over-spinner asserted) and honest Implementation notes recording Q1–Q7 and red→green.

## 3. Decisions taken in this retro

- **The dev-DB 522 class joins the flake watch** (with `entry.spec` + `profile.spec`). Disposition: watch item, not action; if it recurs, the fix is a retry wrapper in the seed helpers — not test deletion, not sweep-splitting.
- **Task-file sweep:** the six TASK-JB files are deleted in this retro's commit (parents both `6-done`; this retro + the bridge + plan rows are the permanent record).
- **The Q1 addendum's canon ground is recorded as standing:** the grammar's minimum change is a traveller-fact — it does not un-happen at withdrawal; deliberate fresh-start stays loops/respawn forward shape (ADR-U044 forward sections). Future enrolment-lifecycle work starts from this, not from row semantics.

## 4. Process changes

- **Plain-English walkthrough at cycle close:** before (or with) the session bridge, the lead writes a short "what did we build, as a user would tell it" narrative and walks it against the shipped behaviour — explicitly asking the continuity/lifecycle questions the test tiers don't. Candidate home: `feature-development` skill close-out section; until homed, it binds as a retro decision.
- **Paired-suite adaptation budget:** any schema-gate PR that changes shipped semantics budgets sibling-suite adaptation as in-scope work, recorded as labelled adaptations (never silent weakening). Same candidate home (gate section); interim binding.
- **Carried from J-A (still unhomed):** dependency-upgrade PRs walk the feature gates; stacked-PR retarget-before-delete; the decomposition payload walk (now n=2, proven at J-B) — home all five lines at the next skill-touching session.

## 5. Action items / standing list

- [x] Sweep TASK-JB-01..06 — owner: Claude (this commit).
- [ ] Home the five §4 lines (three J-A + two J-B) in their skills at the next skill-touching session — owner: Claude.
- [ ] Doc-health-check run at this boundary — findings recorded below — owner: Claude.
- [ ] FEAT-H010 (Download my data) owes a step-instances section at its next touch (flagged in the journeys §L4 row) — owner: whoever next touches H010.
- **Carried unchanged:** flake watch (`entry.spec`, `profile.spec`, + the 522 class per §3) · Stefan's two dashboard toggles · test hygiene (`test:integration:rbac` cleanup) · platform-core CHANGELOG disposition · MEM-9 → Communication gate · IDN-12 + perf T2 parked · ADR-U040 referral rebuild unscoped · logo pick · launch checklist · the three `_01` doc-health soft flags + the V1/V4 unbannered-superseded sweep-target note.

## 6. Doc-health check (cycle boundary, J-B→J-C)

Cycle-boundary doc-health for J-B→J-C is clean — no hard fails, no critical findings (delegated audit, lead-reviewed). The J-B change set (FEAT-PD003 + FEAT-H020 to 6-done, 2 migrations) verifies consistent end-to-end: both §L4 feature-inventory summaries carry the features at 6-done; both feature READMEs match disk (hub 20/20, domain 3/3); both Implementation-notes sections are filled (PD003 including the same-day Q1 re-enrolment-reactivation addendum). Schema is documented per the owning-spec convention — the four new tables carry literal DDL in FEAT-PD003 with RLS enabled on all four in `20260707190000`; `journeys.md` documents them at L4 + the ADR-U044 Q1 resolution. ADR links resolve (U043/U044/U045); the CLAUDE.md cascade is intact (all entity files present, load-order pointers resolve); no active→archived reference leak introduced by J-B. The same three soft flags carry forward unchanged (world-studio load-order line format; hub §L3 MEM-2's deferred ADR-U040 pointer; old-cosmology/AI-Mentor residuals confined to legitimate homes).

Two new low-priority notes for the next boundary: (a) `docs/planning/reference/2026-04_hub-l3-working-set/capability-foundation.md` is a third unbannered restatement of the retired Three Worlds cosmology (historical reference snapshot; candidate for an inline banner if a cooldown sweep touches that folder); (b) `journeys.md`'s L3 "Realized substrate (Step 2)" prose still describes the pre-PD003 JSONB steps in present tense — working-as-designed under the L3-snapshot/L4-current convention (the prose self-defers grain to L4), but a mid-file landing could misread it; noted for retro judgement, not a fix.
