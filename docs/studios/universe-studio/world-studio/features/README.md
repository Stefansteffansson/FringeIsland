# World Studio — features

Feature specs for World Studio (prefix **WS**), per the Model A shape (embedded stories; see
`docs/templates/feature-spec.md` — every spec carries `maturity:` and `requires-equipment:`).

No features specified yet. Known seeds from the discovery and ADR-U026:

- Personal-scope home furnishing (the every-FIM slice; `requires-equipment: none` or canvas-leaning)
- Sensors capture-foot (scan real objects/places via the Gimbal; `requires-equipment: sensors`)
- Shared-world terrain and culture authoring (Dreamineer-gated; `requires-equipment: comfortable-canvas`)
