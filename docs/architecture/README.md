# Architecture

The technical anatomy of the FringeIsland ecosystem and the decisions that shape it.

## Architecture Decision Records (ADRs)

ADRs `ADR-U001` through `ADR-U022` currently live in `../old_universe/decisions/` pending migration to this directory. See [`decisions/README.md`](./decisions/README.md) for the full list.

**Do not** assume ADRs are already here — they are not yet migrated.

## Anatomy diagrams

The current anatomy diagrams live in `../planning/reference/`:

- `../planning/reference/ECOSYSTEM_ANATOMY_V3.svg` — main anatomy diagram
- `../planning/reference/DOMAIN_SERVICE_DEPENDENCIES.svg` — dependency flow diagram

Text companions to these diagrams (e.g., `ARCHITECTURE_ANATOMY.md`, `DOMAIN_ENTITIES.md`, `DATABASE_SCHEMA.md`, `AUTHORIZATION.md`) are pending migration from `../old_universe/architecture/`.
