'use client';

import { AdminModerationQueue } from '@/components/admin/AdminModerationQueue';

// FEAT-H037: the content-moderation queue (ADM-10).
export default function AdminModerationPage() {
  return <AdminModerationQueue />;
}
